# Criteria

Guardrails / checks the agent must satisfy before finishing (currency checks,
must-cite rules, separation-of-duties). Optional but recommended for any skill that
proposes writes or legal conclusions.

- (replace) e.g. "Before citing an SFS, confirm it is still in force via `get_document_details`."
- (replace) e.g. "Never assert a kravpunkt is fulfilled without linked bevis."
