# Procedure

The step-by-step playbook the agent follows for this skill. **English** (per the
bilingual rule — principle #5). Be concrete and ordered; reference the exact tools
from the SKILL.md `tools` whitelist.

1. (replace) e.g. "Call `get_company_context` to load the company profile."
2. (replace) e.g. "Search relevant law with `search_laws`."
3. (replace) …
