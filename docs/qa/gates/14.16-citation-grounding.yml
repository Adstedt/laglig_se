schema: 1
story: '14.16'
story_title: 'Citation Grounding — Agent Must Only Cite Retrieved Sources'
gate: PASS
status_reason: 'All 8 ACs met. Structural citationKey enforcement in tool results, two-tier citation model in system prompt, source map alignment — clean implementation with 38 passing tests.'
reviewer: 'Quinn (Test Architect)'
updated: '2026-03-26T18:37:00Z'

top_issues: []
waiver: { active: false }

quality_score: 95
expires: '2026-04-09T18:37:00Z'

evidence:
  tests_reviewed: 38
  risks_identified: 0
  trace:
    ac_covered: [1, 2, 3, 4, 5, 6, 7, 8]
    ac_gaps: []

nfr_validation:
  security:
    status: PASS
    notes: 'No auth/data/input changes. Additive fields in read-only tool responses only.'
  performance:
    status: PASS
    notes: 'deriveCitationKeys is O(sections) — negligible for typical Swedish laws (<200 sections). citationKey computation is a single regex match per search result.'
  reliability:
    status: PASS
    notes: 'Robust null handling throughout. Graceful fallbacks for null documentNumber, null json_content, and non-standard paths. Backward-compatible with existing path-based resolution.'
  maintainability:
    status: PASS
    notes: 'Clean separation of concerns. deriveCitationKeys is a focused helper. chunkCitationKey reuse ensures format consistency. System prompt changes are well-structured and documented.'

recommendations:
  immediate: []
  future:
    - action: 'Add test for search_laws with documentNumber: null edge case'
      refs: ['tests/unit/lib/agent/tools/search-laws.test.ts']
    - action: 'Add test for deriveCitationKeys with malformed json_content'
      refs: ['tests/unit/lib/agent/tools/get-document-details.test.ts']
    - action: 'Clean up unused truncate() function in citations.ts (pre-existing)'
      refs: ['lib/ai/citations.ts:368']
