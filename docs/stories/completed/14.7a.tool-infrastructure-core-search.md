# Story 14.7a: Tool Infrastructure & Core Search Tools

## Status

Done

## Story

**As a** compliance partner agent,
**I want** shared tool infrastructure and core search tools to query the legal database,
**so that** I can search laws, retrieve document details, and inspect change events on behalf of the user.

## Context & Dependencies

This is the first of three stories split from the original 14.7 (Agent Tool Definitions). It establishes the shared infrastructure all tools depend on AND delivers the three tools that form the core "ask about the law" agent loop. Once this lands, the agent can answer legal questions — company context and write actions come in 14.7b and 14.7c.

Following Anthropic's "Building Effective Agents" guide, tools are the agent's interface to the app. Each tool needs comprehensive descriptions (same rigor as prompts), clear Zod parameter schemas, and high-signal responses.

**Key existing infrastructure:**

- **AI SDK 6.0** (`ai: ^6.0.50`): The `tool()` function from the `ai` package defines tools with Zod schemas. Already used in `app/api/chat/route.ts` via `streamText()` — currently with NO tools. This story adds them. [Source: package.json, app/api/chat/route.ts]
- **Chat route** (Story 3.3): `app/api/chat/route.ts` uses `streamText()` with `@ai-sdk/openai` and `@ai-sdk/anthropic`. Has rate limiting, auth, and mock RAG. The `tools` parameter needs to be added to `streamText()`. [Source: app/api/chat/route.ts]
- **Chat UI** (Story 14.11 — Done): `lib/hooks/use-chat-interface.ts` uses `useChat()` from `@ai-sdk/react` with `UIMessage` parts format, transport-based API. Supports `conversation_id` grouping. Client-side does NOT need changes — AI SDK tool results are handled transparently in the streaming protocol. [Source: lib/hooks/use-chat-interface.ts]
- **ContentChunk table** (Story 14.2 — Done): 228,778 chunks with pgvector HNSW index, workspace scoping. [Source: prisma/schema.prisma]
- **Retrieval pipeline** (Story 14.8): `retrieveContext(query, workspaceId, options)` in `lib/agent/retrieval.ts` — provides the backend for `search_laws`. Returns chunks with relevance scores after two-stage vector+rerank pipeline. [Source: docs/stories/14.8.rag-retrieval-pipeline.md]

**Depends on:** 14.2 (ContentChunk for search), 14.8 (retrieval pipeline for search tools)

**Depended on by:** 14.7b (company context tools use shared infrastructure), 14.7c (write tools use shared infrastructure), 14.9 (system prompt references tool behaviors), 14.10 (assessment flow uses tools), 14.11 (chat UI surfaces tool results)

## Acceptance Criteria

### Tool Infrastructure

1. All tools defined using Vercel AI SDK `tool()` function with Zod parameter schemas [Source: ai ^6.0.50]
2. Tool descriptions written as comprehensive docstrings: purpose, when to use, what it returns, edge cases — because the LLM reads these to decide which tool to call
3. Tool responses use semantic identifiers (document title + number, not just UUID)
4. Tool responses include `_meta: { tool: string, executionTimeMs: number, resultCount: number }` for transparency
5. Error responses include specific, actionable guidance in Swedish (e.g., "Dokumentet hittades inte. Kontrollera att dokumentnumret är korrekt, t.ex. 'SFS 1977:1160'.")
6. Factory function pattern `createAgentTools(workspaceId)` — returns tools with workspaceId captured in closure, avoiding SDK-specific API dependency

### Core Search Tools

7. `search_laws` — semantic search across legal document chunks. Parameters: `query` (string, required), `contentType` (optional: `SFS_LAW`, `AGENCY_REGULATION`, etc.), `limit` (optional, default 5). Calls `retrieveContext()` from Story 14.8. Returns: relevant passages with `contextualHeader`, `documentNumber`, `relevanceScore`, `path`
8. `get_document_details` — full content for a specific legal document. Parameters: `documentId` OR `documentNumber` (one required). Returns: `title`, `documentNumber`, `contentType`, `status`, `summary`, `kommentar`, `markdown_content` (truncated to ~4000 tokens if needed)
9. `get_change_details` — ChangeEvent details with amendment context. Parameters: `changeEventId` (string, required). Returns: `changeType`, `amendmentSfs`, `aiSummary`, `detectedAt`, affected sections (from SectionChange join), base law title and document number

### Wiring

10. Tool registry barrel export at `lib/agent/tools/index.ts` — exports `createAgentTools(workspaceId)` factory
11. Chat route updated: add `tools` to `streamText()`, add `maxSteps: 5` for multi-step tool use, remove mock RAG response function, extract `workspaceId` from session and pass to factory
12. At least 8 unit tests covering: tool parameter validation, response shape with `_meta`, error handling with Swedish guidance, `retrieveContext` integration mock, Prisma query mocks, not-found error paths

## Tasks / Subtasks

- [x] **Task 1: Shared types and utilities** (AC: 1-6)
  - [x] Create `lib/agent/tools/types.ts` with shared types:
    - `ToolMeta` — `{ tool: string, executionTimeMs: number, resultCount: number }`
    - `ToolResponse<T>` — `{ data: T, _meta: ToolMeta }`
    - `WriteToolResponse<T>` — `{ confirmation_required: true, action: string, params: Record<string, unknown>, preview: string, _meta: ToolMeta }` (used by 14.7c, defined here for shared typing)
    - `ToolError` — `{ error: true, message: string, guidance: string, _meta: ToolMeta }`
  - [x] Create `lib/agent/tools/utils.ts` with shared helpers:
    - `wrapToolResponse(tool, data, startTime)` — builds `ToolResponse` with `_meta` timing
    - `wrapWriteToolResponse(tool, action, params, preview, startTime)` — builds write proposal
    - `wrapToolError(tool, message, guidance, startTime)` — builds error response
    - `truncateMarkdown(content, maxTokens)` — truncate markdown to approximate token budget (chars/4). Default budget ~4000 tokens (~16K chars) — leaves room for other tool results and conversation within the model's context window.

- [x] **Task 2: Core search tools** (AC: 7-9)
  - [x] Create `lib/agent/tools/search-laws.ts`
    - Import `retrieveContext` from `lib/agent/retrieval.ts` (Story 14.8)
    - Call `retrieveContext(query, workspaceId, { sourceType: 'LEGAL_DOCUMENT', contentType, topK: limit })`
    - Map results to response shape: `{ contextualHeader, documentNumber, relevanceScore, path, snippet }`
    - Tool description must include example queries in Swedish ("arbetsgivarens skyldigheter för skyddsutrustning", "regler för kemikaliehantering", "GDPR-krav för personuppgifter")
  - [x] Create `lib/agent/tools/get-document-details.ts`
    - Query `prisma.legalDocument.findFirst()` by `id` or `document_number`
    - Return title, documentNumber, contentType, status, summary, kommentar, markdown_content (truncated via `truncateMarkdown`)
    - Error if not found: guidance to check document number format
  - [x] Create `lib/agent/tools/get-change-details.ts`
    - Query `prisma.changeEvent.findUnique()` with `include: { document: true }`
    - Join SectionChange via indirect path: `changeEvent.amendment_sfs` → `prisma.amendmentDocument.findFirst({ where: { sfs_number: amendmentSfs } })` → `prisma.sectionChange.findMany({ where: { amendment_id: amendmentDoc.id } })`
    - Return change type, amendment SFS, ai_summary, detected_at, affected sections, base law info

- [x] **Task 3: Factory function, registry, and chat route wiring** (AC: 6, 10-11)
  - [x] Create `lib/agent/tools/index.ts`:
    - `createAgentTools(workspaceId)` factory function that returns tools object with workspaceId in closure
    - Initially exports only the 3 core search tools (14.7b and 14.7c will add more)
    - Barrel re-export of types for other consumers
  - [x] Modify `app/api/chat/route.ts`:
    - Import `createAgentTools` from `@/lib/agent/tools`
    - Extract `workspaceId` using the existing pattern (line 48-49): `const workspaceId = (session as { workspaceId?: string }).workspaceId ?? 'default'`
    - Create tools: `const tools = createAgentTools(workspaceId)`
    - Add `tools` and `stopWhen: stepCountIs(5)` to the `streamText()` call (AI SDK 6.0 pattern)
    - Remove mock RAG response function (replaced by real tools)

- [x] **Task 4: Tests** (AC: 12)
  - [x] Create `tests/unit/agent/tools/search-laws.test.ts` — mock `retrieveContext`, verify response shape and `_meta` fields
  - [x] Create `tests/unit/agent/tools/get-document-details.test.ts` — mock Prisma, test ID and documentNumber lookup, test not-found error with Swedish guidance
  - [x] Create `tests/unit/agent/tools/get-change-details.test.ts` — mock Prisma, test SectionChange indirect join
  - [x] Verify `npx tsc --noEmit` passes
  - [x] Verify all existing tests still pass (3245 passed, 16 pre-existing failures in integration/perf)

## Dev Notes

### Deferred from Epic: `concise` vs `detailed` Response Modes

The epic mentions "Implement `concise` vs `detailed` response modes" as a tool response design goal. This is NOT in scope for any of the 14.7a-c stories (it was also absent in the original 14.7). If needed, it can be added as a follow-up enhancement to `lib/agent/tools/utils.ts` — e.g., a `responseMode` parameter on wrapper functions that controls field verbosity. Track separately if required.

### AI SDK 6.0 Tool Pattern

```typescript
import { tool } from 'ai'
import { z } from 'zod'

export const searchLawsTool = tool({
  description: `Search Swedish legal documents by semantic similarity.
Use this tool when the user asks about a legal topic, regulation, or compliance requirement.
Returns the most relevant passages from laws (SFS), agency regulations (AFS, BFS, etc.), and EU legislation.
The search understands Swedish legal terminology and natural language queries.

Example queries:
- "arbetsgivarens skyldigheter för skyddsutrustning"
- "regler för kemikaliehantering"
- "GDPR-krav för personuppgifter"

Returns up to {limit} results ranked by relevance.`,
  parameters: z.object({
    query: z.string().describe('The search query in Swedish'),
    contentType: z.enum(['SFS_LAW', 'AGENCY_REGULATION', 'EU_REGULATION', 'EU_DIRECTIVE'])
      .optional()
      .describe('Filter to a specific document type'),
    limit: z.number().min(1).max(20).default(5)
      .describe('Number of results to return'),
  }),
  execute: async ({ query, contentType, limit }) => {
    // implementation calls retrieveContext()
  },
})
```

[Source: Vercel AI SDK docs — `ai ^6.0.50`]

### Chat Route Modification

The existing `app/api/chat/route.ts` needs these changes:

```typescript
// BEFORE (Story 3.3 — mock RAG):
const result = streamText({
  model,
  system: systemPrompt,
  messages: messages.map(m => ({ ... })),
})

// AFTER (Story 14.7a — real tools):
import { createAgentTools } from '@/lib/agent/tools'

// WorkspaceId extraction — existing pattern at line 48-49 of route.ts:
const workspaceId =
  (session as { workspaceId?: string }).workspaceId ?? 'default'
const tools = createAgentTools(workspaceId)

const result = streamText({
  model,
  system: systemPrompt,  // Story 14.9 will enrich this
  messages,              // Pass UIMessage[] directly (AI SDK 6.0 handles parts)
  tools,
  maxSteps: 5,           // Allow multi-step tool use
})
```

[Source: app/api/chat/route.ts, lines 48-49]

### Tool Description Quality

Tool descriptions are critical — the LLM reads them to decide which tool to call. Write them like comprehensive docstrings:

- **What it does** (first line, Swedish context)
- **When to use it** (trigger conditions)
- **What it returns** (field descriptions)
- **Edge cases** (what happens on error, no results)
- **Example queries** (for search tools)

Bad: `"Search laws"` — too vague, LLM won't know when to use it
Good: `"Search Swedish legal documents by semantic similarity. Use when the user asks about regulations, compliance requirements, or legal obligations..."` — clear, contextual

### Previous Story Insights

**From Story 14.3 (Embedding Generation Pipeline — Done):**

- **Prisma `Unsupported` type:** Cannot use Prisma ORM to read/write `embedding` or query by it. Must use `$queryRaw` for vector operations. However, search tools in this story delegate to `retrieveContext()` (14.8) which handles this internally — tools don't do raw SQL themselves. [Source: Story 14.3 dev notes]
- **Database state:** 228,778 chunks fully embedded, HNSW index active. [Source: Story 14.3 operational notes]

**From Story 14.11 (Chat-First Dashboard — Done):**

- Chat route at `app/api/chat/route.ts` uses `streamText()` with model selection (OpenAI/Anthropic) and rate limiting. Currently has mock RAG — this story replaces it with real tools.
- Client-side `useChatInterface()` hook handles `UIMessage` parts format, conversation persistence, and `conversation_id` grouping. The hook does NOT need changes — AI SDK tool results are handled transparently in the streaming protocol.

**From Story 14.8 (RAG Retrieval Pipeline — Draft):**

- `retrieveContext(query, workspaceId, options)` returns `RetrievalResult[]` with fields: `id, content, contextualHeader, contextPrefix, path, sourceType, sourceId, documentNumber, similarity, relevanceScore, metadata`
- Options include: `topK`, `sourceType`, `contentType`, `overFetchMultiplier`, `minRelevanceScore`
- Handles vector search + Cohere Rerank v4 + workspace scoping internally
- Search tools in this story are thin wrappers — they call `retrieveContext()` and map the response to the tool's output shape

### Key Prisma Models (Verified)

**LegalDocument** — `content_type` (ContentType enum), `document_number` (unique), `title`, `slug`, `summary`, `kommentar`, `markdown_content`, `status` (DocumentStatus), `metadata` [Source: prisma/schema.prisma]

**ChangeEvent** — `document_id`, `content_type`, `change_type`, `amendment_sfs`, `ai_summary`, `detected_at`, `changed_sections` (JSON) [Source: prisma/schema.prisma]

**SectionChange** — `amendment_id`, `chapter`, `section`, `change_type`, `old_text`, `new_text`, `description` [Source: prisma/schema.prisma]

**ContentType enum:** `SFS_LAW`, `SFS_AMENDMENT`, `COURT_CASE_*`, `EU_REGULATION`, `EU_DIRECTIVE`, `AGENCY_REGULATION` [Source: prisma/schema.prisma]

### Source Tree

```
lib/agent/tools/
  ├── types.ts                  — NEW — ToolResponse, WriteToolResponse, ToolError, ToolMeta
  ├── utils.ts                  — NEW — wrapToolResponse, wrapWriteToolResponse, wrapToolError, truncateMarkdown
  ├── search-laws.ts            — NEW — searchLawsTool (calls retrieveContext)
  ├── get-document-details.ts   — NEW — getDocumentDetailsTool (Prisma query)
  ├── get-change-details.ts     — NEW — getChangeDetailsTool (Prisma query + SectionChange join)
  └── index.ts                  — NEW — createAgentTools factory + barrel exports

lib/agent/
  ├── retrieval.ts              — FROM Story 14.8 — retrieveContext() used by search tools
  ├── context-assembly.ts       — FROM Story 14.8
  └── legal-ref-detector.ts     — FROM Story 14.8

app/api/chat/
  └── route.ts                  — MODIFIED — add tools to streamText(), remove mock RAG

tests/unit/agent/tools/
  ├── search-laws.test.ts       — NEW
  ├── get-document-details.test.ts — NEW
  └── get-change-details.test.ts — NEW
```

[Source: architecture/12-unified-project-structure.md — lib/ for business logic, tests/unit/ mirroring lib/]

### Testing

**Test location:** `tests/unit/agent/tools/` [mirroring `lib/agent/tools/` source structure] [Source: architecture/12-unified-project-structure.md]

**Test framework:** Vitest with `vi.mock()`. [Source: architecture/3-tech-stack.md — Vitest 1.4+]

**Mocking strategy:**

```typescript
// Mock retrieval pipeline (for search tools)
vi.mock('@/lib/agent/retrieval', () => ({
  retrieveContext: vi.fn(),
}))

// Mock Prisma (for data tools)
vi.mock('@/lib/prisma', () => ({
  prisma: {
    legalDocument: { findFirst: vi.fn(), findUnique: vi.fn() },
    changeEvent: { findUnique: vi.fn() },
    sectionChange: { findMany: vi.fn() },
  },
}))
```

**Key test scenarios:**

- Search tools: verify `retrieveContext` called with correct sourceType filter, verify response shape matches `ToolResponse<T>`, verify `_meta` fields present
- Data tools: verify Prisma called with correct query, verify not-found returns `ToolError` with Swedish guidance
- All tools: verify Zod parameter validation rejects invalid input

## Change Log

| Date       | Version | Description                     | Author       |
| ---------- | ------- | ------------------------------- | ------------ |
| 2026-03-03 | 1.0     | Split from 14.7 — infrastructure + core search tools (search_laws, get_document_details, get_change_details), factory pattern, chat route wiring | Sarah (PO) |
| 2026-03-03 | 1.0-sm  | SM review: Story draft checklist PASS (5/5). All sections complete, self-contained, architecture-aligned. Ready for approval. | Bob (SM) |
| 2026-03-03 | 1.1     | PO validation: Fixed workspaceId extraction pattern to match actual route.ts (line 48-49). Added note about deferred `concise` vs `detailed` response modes from epic. GO — score 9.5/10. | Sarah (PO) |

## Dev Agent Record

### Agent Model Used
claude-opus-4-6

### Debug Log References
- AI SDK 6.0 uses `inputSchema` (not `parameters`), `zodSchema()` wrapper, and `zod/v4` import
- Multi-step tool use: `stopWhen: stepCountIs(5)` replaces deprecated `maxSteps`
- `exactOptionalPropertyTypes` requires `| undefined` on optional interface props when passing spread objects

### Integration Testing Notes (2026-03-09)

Two bugs found in `lib/agent/retrieval.ts` (Story 14.8) during live integration testing of 14.7a tools against the real database:

1. **`SET LOCAL` cannot use parameterized values** — `$executeRaw\`SET LOCAL hnsw.ef_search = ${N}\`` generates `SET LOCAL hnsw.ef_search = $1` which PostgreSQL rejects. Fix: use `$executeRawUnsafe()` (safe because the value is a hardcoded constant).
2. **Prisma enum type mismatch in raw SQL** — `source_type` (`"SourceType"` enum) and `content_type` (`"ContentType"` enum) compared to parameterized `text` values fails with `operator does not exist: "SourceType" = text`. Fix: add explicit casts `::"SourceType"` and `::"ContentType"` on the parameter references.

Both fixes applied directly to `lib/agent/retrieval.ts`. These would have also broken the live chat route.

**Data linkage note:** `ChangeEvent` records are created by sync crons for *newly detected* changes (forward-looking). `AmendmentDocument` + `SectionChange` records were bulk-imported historically. For laws like SFS 1977:1160, there are 34 AmendmentDocuments but 0 ChangeEvents. The `get_change_details` tool correctly bridges both via the indirect join, but only returns `affectedSections` when both sides have data for the same `amendment_sfs`.

**Full agent simulation verified:** End-to-end test with `generateText()` + Claude Sonnet 4 + real tools against live DB confirmed the agent autonomously calls `search_laws` (multiple angles), synthesizes results from AFS and SFS sources, and produces structured Swedish-language compliance guidance with proper citations.

### Completion Notes List
- All 4 tasks complete, 12 tests passing, 0 regressions
- Story uses AI SDK 6.0 best practices: `tool()` + `zodSchema()` + `inputSchema` + `stopWhen: stepCountIs()`
- Mock RAG removed from chat route, replaced with real tool infrastructure
- SectionChange join uses AmendmentDocument (not LegalDocument) as per schema — story task description corrected

### File List
- `lib/agent/tools/types.ts` — NEW — ToolResponse, WriteToolResponse, ToolError, ToolMeta
- `lib/agent/tools/utils.ts` — NEW — wrapToolResponse, wrapWriteToolResponse, wrapToolError, truncateMarkdown
- `lib/agent/tools/search-laws.ts` — NEW — search_laws tool (calls retrieveContext)
- `lib/agent/tools/get-document-details.ts` — NEW — get_document_details tool (Prisma query)
- `lib/agent/tools/get-change-details.ts` — NEW — get_change_details tool (Prisma + SectionChange join)
- `lib/agent/tools/index.ts` — NEW — createAgentTools factory + barrel exports
- `app/api/chat/route.ts` — MODIFIED — add tools to streamText(), remove mock RAG, add stopWhen
- `tests/unit/agent/tools/search-laws.test.ts` — NEW — 4 tests
- `tests/unit/agent/tools/get-document-details.test.ts` — NEW — 5 tests
- `tests/unit/agent/tools/get-change-details.test.ts` — NEW — 3 tests

### Change Log
| Date | Version | Description | Author |
| --- | --- | --- | --- |
| 2026-03-09 | dev-1.0 | All tasks implemented. AI SDK 6.0 best practices applied. | James (Dev) |

## QA Results

### Review Date: 2026-03-09

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

**Overall: Strong.** Clean implementation with good separation of concerns. The tool infrastructure follows AI SDK 6.0 best practices (`inputSchema` + `zodSchema()` + `zod/v4`), shared utilities are well-factored, and the factory pattern keeps callers decoupled from SDK internals. Error messages are consistently in Swedish with actionable guidance. Workspace scoping via closure is correct.

**Strengths:**
- Consistent error-handling pattern across all three tools (try/catch → `wrapToolError` with Swedish guidance)
- Tool descriptions are comprehensive docstrings following Anthropic's agent guide — includes purpose, when to use, returns, edge cases, and examples
- `truncateMarkdown` correctly cuts at paragraph boundaries and uses a sensible 4000-token default
- `_meta` envelope with timing and result count on every response gives great observability
- Clean indirect join in `get_change_details` (ChangeEvent → AmendmentDocument → SectionChange) with proper null-safety on `amendment_sfs`
- Test suite covers success, error, not-found, and edge cases with appropriate mocking depth

**No significant issues found.** One minor JSDoc fix applied (see below).

### Refactoring Performed

- **File**: `lib/agent/tools/index.ts`
  - **Change**: Updated JSDoc usage example from `maxSteps: 5` to `stopWhen: stepCountIs(5)`
  - **Why**: The comment referenced the deprecated AI SDK 5.x API. Since downstream stories (14.7b, 14.7c) will reference this factory, a misleading code example in the JSDoc could propagate the wrong pattern.
  - **How**: Single line change in the JSDoc `@example` block. No functional change.

### Compliance Check

- Coding Standards: ✓ — Strict TS (noEmit clean), explicit types, Zod validation, typed errors, no `any`
- Project Structure: ✓ — `lib/agent/tools/` for business logic, `tests/unit/agent/tools/` mirrors source, barrel export at `index.ts`
- Testing Strategy: ✓ — 12 Vitest unit tests with `vi.mock()`, mocking retrieval and Prisma at appropriate boundaries
- All ACs Met: ✓ — See traceability below

### AC Traceability

| AC | Validated By | Status |
|----|-------------|--------|
| 1. AI SDK `tool()` with Zod schemas | All 3 tool files use `tool()` + `zodSchema()` + `zod/v4` | ✓ |
| 2. Comprehensive tool descriptions | Verified: each has purpose, triggers, returns, edge cases, examples | ✓ |
| 3. Semantic identifiers in responses | search_laws returns `contextualHeader` + `documentNumber`; get_document_details returns `title` + `documentNumber` | ✓ |
| 4. `_meta` on all responses | `wrapToolResponse`/`wrapToolError` always include `_meta`; tests assert `_meta.tool`, `_meta.resultCount`, `_meta.executionTimeMs` | ✓ |
| 5. Swedish error guidance | All error paths use `wrapToolError` with Swedish `message` and `guidance`; tests verify Swedish text content | ✓ |
| 6. Factory function pattern | `createAgentTools(workspaceId)` captures workspace in closure | ✓ |
| 7. `search_laws` tool | Calls `retrieveContext()`, maps results, has query/contentType/limit params, Swedish examples in description | ✓ |
| 8. `get_document_details` tool | Prisma findFirst by id or document_number, truncates markdown, not-found guidance | ✓ |
| 9. `get_change_details` tool | ChangeEvent + indirect SectionChange join via AmendmentDocument, returns base law info | ✓ |
| 10. Barrel export at index.ts | Exports `createAgentTools` + type re-exports | ✓ |
| 11. Chat route wired | `tools` + `stopWhen: stepCountIs(5)` added to `streamText()`, mock RAG removed | ✓ |
| 12. ≥8 unit tests | 12 tests: 4 (search) + 5 (document) + 3 (change) | ✓ |

### Improvements Checklist

- [x] Fixed stale JSDoc referencing deprecated `maxSteps` API (`lib/agent/tools/index.ts`)
- [ ] **Future (not blocking):** Consider adding a unit test for `truncateMarkdown` directly in a `utils.test.ts` — currently only tested indirectly via `get-document-details.test.ts`
- [ ] **Future (not blocking):** The `search_laws` `contentType` enum doesn't include `SFS_AMENDMENT` or `COURT_CASE_*` — this is correct per AC 7 but may need expansion in 14.7b if court case search is needed
- [ ] **Future (Story 14.9):** The `buildSystemPrompt` in `route.ts` is a placeholder — Story 14.9 will replace it with a comprehensive prompt

### Security Review

No security concerns. Workspace scoping is enforced via closure (not URL params). Tool inputs are validated through Zod schemas. No raw SQL — Prisma parameterized queries and `retrieveContext()` handle query construction. Rate limiting on the chat route is preserved from Story 3.3.

### Performance Considerations

No concerns for this story. `get_change_details` makes up to 3 sequential Prisma queries (ChangeEvent → AmendmentDocument → SectionChange) which could be optimized with a raw join if latency becomes an issue, but for a tool called on-demand this is fine. The `retrieveContext()` pipeline already handles its own performance (HNSW index, Cohere rerank).

### Files Modified During Review

- `lib/agent/tools/index.ts` — JSDoc fix only (1 line)

Note: Dev should update File List if needed (change is cosmetic only).

### Gate Status

Gate: **PASS** → `docs/qa/gates/14.7a-tool-infrastructure-core-search.yml`

### Recommended Status

✓ Ready for Done
