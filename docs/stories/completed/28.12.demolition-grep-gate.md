# Story 28.12: Demolition + Grep-Gate

**Epic:** 28 — Unified DataTable Core (`docs/prd/epic-28-unified-datatable-core.md`)
**Status:** Done (2026-07-08)

## Demolition audit (checklist zeroed)

Verified by grep — everything on the epic's cleanup checklist was already
deleted in its owning story:

- `shared/virtual-table-body.tsx` (28.1), `document-list-card.tsx`,
  `document-list-grid.tsx`, `grouped-document-list.tsx` (28.10) — gone;
  zero imports remain.
- Local `SortableHeader` copies — zero remain; `ui/sortable-header.tsx` is
  the single shared primitive (consumed by core consumers and Tier-0).
- Clamp modules: core `column-sizing.ts` + personalregister's
  `employee-column-state.ts` (kept VERBATIM per 28.7's localStorage
  contract) are the only `COLUMN_SIZE_BOUNDS` holders.
- `_columnOrderKey` exists only in the core's fixed row comparator.

**Kept deliberately** (plan's checklist assumed they'd be orphaned; they are
not): `ui/draggable-column-header.tsx` (now used BY the core),
`ui/column-settings.tsx` shell (personalregister/laglistor settings UIs),
`ui/sortable-header.tsx` (shared header affordance, both tiers).

## Grep-gate

`scripts/check-table-conventions.sh`, wired into CI's lint-and-typecheck
job. Fails on:

1. `useReactTable` anywhere outside `components/ui/data-table/` — one
   whitelisted exception: `landing-v3/marketing-document-table.tsx`, the
   permanently frozen presentational copy from Story 28.4.
2. Imports of any Epic-28-deleted module (virtual-table-body,
   document-list-card/grid, grouped-document-list).

## Bookkeeping

- `docs/prd/epic-list.md` — Epic 28 marked Done with deviation summary.
- `docs/prd/epic-28-unified-datatable-core.md` — status header added.

## Verification

- Gate script passes on the current tree; verified it FAILS when a
  disallowed `useReactTable` usage is introduced (manual smoke).
- tsc, eslint, unit + epic-28 e2e suites green (run in 28.11, tree
  unchanged since except docs/CI).

## PO Implementation Record

**Validated by:** Sarah (PO) — 2026-07-08
**Disposition:** Accepted — Done

- **Scope vs. epic:** Matches Story 28.12: the cleanup checklist is grep-audited to zero (virtual-table-body, legacy card components, all local SortableHeader copies, clamp modules, `_columnOrderKey` only in the fixed core comparator — each deleted in its owning story), the CI grep-gate ships as `scripts/check-table-conventions.sh` in the lint-and-typecheck job, and epic bookkeeping landed. Core deliverable: the epic's success criterion enforced permanently — zero `useReactTable` outside `components/ui/data-table/` (one whitelisted exception: the permanently frozen `landing-v3/marketing-document-table.tsx` from 28.4).
- **Deviations:** Three primitives on the plan's absorb/delete checklist are kept deliberately because they are NOT orphaned: `ui/draggable-column-header.tsx` (now used BY the core), `ui/column-settings.tsx` (personalregister/laglistor settings UIs), `ui/sortable-header.tsx` (the single shared header affordance for both tiers). Accepted as-is with rationale in the story body.
- **Verification evidence:** Gate script passes on the current tree and was smoke-verified to FAIL when a disallowed `useReactTable` usage is introduced; tsc, eslint, unit + epic-28 e2e suites green (run in 28.11; tree unchanged since except docs/CI).
- **Follow-ups / residuals:** None from this story. (Epic-level residuals live where they belong: grouped-wrapper dedup in 28.9's record, admin manual pass in 28.5's.)
- **Documentation integrity:** This story itself performed the closure bookkeeping — `docs/prd/epic-28-unified-datatable-core.md` status header (Done 2026-07-08) and the `docs/prd/epic-list.md` Epic 28 Done entry with deviation summary — both verified present and consistent with the per-story records; `docs/architecture/table-conventions.md` is the surviving conventions artefact the gate encodes. File moved to `docs/stories/completed/` on 2026-07-08.
