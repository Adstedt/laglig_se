# Story 25.0: First-run path-choice gate (token-saver MVP)

## Status

Done

## Story

**As a** new user who just finished the workspace-creation wizard,
**I want** Laglig to ask me how I'd like to start my law list — pick a template, generate one with AI, import an existing list, or skip and build manually — instead of silently running an LLM generation in the background while I land on a chat-first dashboard with no orientation,
**so that** Laglig doesn't burn $0.50–$1.50 of tokens generating a list I never asked for, I'm not bewildered by the empty dashboard, and I have a real choice about how to get my first list on the page.

This is the **pre-MVP wedge for Epic 25** (the "B.0" path-choice gate per the epic PRD). It ships independently of Epic 24 with a minimal modal — three path cards + tertiary skip — and the full schema/action/state-derivation substrate that the rest of the epic (B.1–B.6) builds on. The import card is visibly disabled in B.0 until Epic 24.4's review surface lands; B.1 un-disables it. No tabs, no kickoff confirm, no done states, no FAB — those are B.1+.

## Acceptance Criteria

### Schema additions

1. **Prisma migration `add_first_run_modal_columns`.** Adds three nullable columns to `Workspace` (no existing column dropped or renamed):
   - `first_run_dismissed_at` — `DateTime?` — timestamp set when user closes/minimises the first-run modal. `null` = never closed → modal eligible to auto-open on dashboard visit (subject to other guards). Not-null = modal does not auto-open again.
   - `tutorial_fab_dismissed_at` — `DateTime?` — placeholder for B.6's corner FAB dismissal. Added in B.0 so we don't churn the Workspace model twice. Stays `null` after B.0.
   - `first_run_tabs_viewed` — `Json?` with `@default("[]")` — placeholder for B.6's tab-viewed tracking. Added in B.0 for the same reason. Stays `[]` after B.0.

2. **Schema default fix on `law_list_generation_status`.** The existing schema declares `law_list_generation_status String? @default("pending")` (see `prisma/schema.prisma:172`). The Epic 25 architecture's `getOnboardingState()` function (arch §6.4) checks `=== null` to decide whether to open the modal. Both cannot be true. **The same migration drops the `@default("pending")`** so new workspaces created after the auto-fire is removed (AC 14) land at `null`. Existing workspaces with `pending`/`in_progress`/`completed`/`failed`/`skipped` values are unaffected (defaults only apply to new rows).

3. **New `OnboardingEvent` model.** Workspace-scoped event log, polymorphic JSON payload, no schema-level enum on `event_type` (free-form string per the architecture's §5.2 fail-safe / additive philosophy — same approach as `ChatUsageEvent`):
   ```prisma
   model OnboardingEvent {
     id           String   @id @default(cuid())
     workspace_id String
     user_id      String
     event_type   String   // 'modal_opened' | 'path_chosen' | 'tab_viewed' | 'modal_dismissed' | 'fab_dismissed' | 'feedback_submitted' | ...
     payload      Json?    // event-specific data
     created_at   DateTime @default(now())

     workspace    Workspace @relation(fields: [workspace_id], references: [id], onDelete: Cascade)
     user         User      @relation(fields: [user_id], references: [id], onDelete: Cascade)

     @@index([workspace_id, created_at])
     @@index([event_type])
   }
   ```
   - PRIMARY KEY uses `@default(uuid())` — matches the Epic 24 ratified convention for new tables and the existing `ChatUsageEvent.id` precedent at `prisma/schema.prisma:1470`.
   - Add the inverse relations on `Workspace` and `User` models: `onboarding_events OnboardingEvent[]`.
   - **No RLS policy block in the migration** (Epic 24 convention — RLS is `ENABLE` only via the existing global migration; per-table policies are handled by the Workspace cascade and the `workspace_id` filter on every query).

### Onboarding-state derivation

4. **New module `lib/onboarding/get-onboarding-state.ts`** exporting:
   ```ts
   export type OnboardingState = {
     firstRunOpen: boolean      // true → auto-open modal at path_choice
     fabVisible: boolean        // false in B.0 (FAB ships in B.6)
     fabState: 'working' | 'done' | 'idle'  // 'idle' in B.0
   }
   export async function getOnboardingState(workspaceId: string): Promise<OnboardingState>
   ```
   Three guards combined with `&&`:
   - `FRESH` — `Date.now() - workspace.created_at.getTime() <= 24 * 60 * 60 * 1000` (24h cap, matches arch §6.4 rationale)
   - `first_run_dismissed_at === null`
   - `law_list_generation_status === null` (works because AC 2 drops the `pending` default)

   Returns `{ firstRunOpen: false, fabVisible: false, fabState: 'idle' }` for any workspace not found, on DB error, or when any guard fails. **`fabVisible` is hardcoded `false` in B.0** — the full derivation (arch §6.4) ships in B.6 alongside the FAB component.

5. **`getOnboardingState` is pure.** Only reads from Prisma; no writes, no telemetry events, no side effects. Safe to call on every dashboard render.

### Server actions

6. **New file `app/actions/onboarding-modal.ts`** exporting **five** server actions, each `'use server'`-marked and wrapped in `withWorkspace(cb)` from `lib/auth/workspace-context.ts` (no permission scope — workspace-self-write only). Action signatures:
   ```ts
   export async function minimiseFirstRunModal(): Promise<{ ok: true } | { ok: false; error: string }>
   export async function skipLawListGeneration(): Promise<{ ok: true } | { ok: false; error: string }>
   export async function dismissOnboardingFab(): Promise<{ ok: true } | { ok: false; error: string }>
   export async function recordTabViewed(tabId: string): Promise<{ ok: true } | { ok: false; error: string }>
   export async function recordOnboardingEvent(
     eventType: string,
     payload?: Record<string, unknown>
   ): Promise<{ ok: true } | { ok: false; error: string }>
   ```

7. **`minimiseFirstRunModal`** — sets `Workspace.first_run_dismissed_at = NOW()`. **Also writes an `OnboardingEvent` row** with `event_type='modal_dismissed'` and `payload={from_state: 'path_choice'}` (only state available in B.0). Returns `{ok:true}` on success.

8. **`skipLawListGeneration`** — sets both `law_list_generation_status='skipped'` AND `first_run_dismissed_at=NOW()` in a single update. Writes an `OnboardingEvent` row with `event_type='path_chosen'` and `payload={path:'skipped'}`. Returns `{ok:true}` on success.

9. **`dismissOnboardingFab`** — sets `tutorial_fab_dismissed_at=NOW()`. Writes an `OnboardingEvent` row with `event_type='fab_dismissed'` and `payload={from_state:'visible_idle'}`. **Stubbed but exposed in B.0** — there's no FAB to dismiss yet, but the action ships now so the FAB component in B.6 calls a stable contract. Add a unit test exercising the action; do not wire any UI to it in B.0.

10. **`recordTabViewed`** — appends `tabId` to `Workspace.first_run_tabs_viewed` if not already present (idempotent). Writes an `OnboardingEvent` row with `event_type='tab_viewed'` and `payload={tab_id: tabId}`. **Stubbed but exposed in B.0** — same rationale as AC 9. The tutorial tabs ship in B.3.

11. **`recordOnboardingEvent`** — generic fire-and-forget event writer. Used by the modal for `modal_opened` (fired on mount in `<FirstRunModal>`) and `path_chosen={path:'generate'|'import'|'template'}` events from the three card click handlers. The action wraps a single `prisma.onboardingEvent.create({...})` call in try/catch and **logs `[ONBOARDING_EVENT_WRITE_FAIL]` via `console.error` on failure** (mirrors Story 14.27's `[CHAT_USAGE_EVENT_WRITE_FAIL]` pattern; the same fail-safe is also applied in `app/actions/workspace.ts` per `[ENTERPRISE_INQUIRY_EMAIL_FAIL]`). **Never re-throws.** Returns `{ok:true}` even on internal failure — telemetry writes are best-effort.

12. **Fail-safe pattern applies to the other four actions too** for the OnboardingEvent row write only (the primary `workspace.update` call is allowed to throw — that one matters for correctness). Pattern: `prisma.workspace.update({...})` runs first and any failure returns `{ok:false, error}`; the `prisma.onboardingEvent.create({...})` runs after in a try/catch with `[ONBOARDING_EVENT_WRITE_FAIL]` console.error.

### Wizard rewire — remove auto-fire

13. **Auto-fire location is `app/actions/workspace.ts:347–364`**, NOT `app/onboarding/_components/confirm-step.tsx` as the Epic 25 architecture doc (§2.1, §8 source-tree) claims. The `createWorkspace` server action contains a block labelled `// Story 16.4: Trigger law list generation (fire-and-forget)` that fetches `POST /api/workspace/generate-law-list` via raw `fetch()` after workspace insert + transaction commit. The architecture-doc mistake should be flagged in the Change Log; the architecture is otherwise correct. Do not rewrite `confirm-step.tsx` — it only calls `onSubmit` (a prop) and has no knowledge of generation.

14. **Remove the auto-fire block.** Delete lines ~347–364 of `app/actions/workspace.ts` (the entire `// Story 16.4: Trigger law list generation` block including the `try/catch` wrapper, the `headers()` call, the `baseUrl` construction, and the `fetch().catch()`). Remove unused imports if any are left dangling (`headers` is still used elsewhere in the same file — verify before removing). The workspace creation flow now returns `{success:true, workspaceId}` with `law_list_generation_status` defaulting to `null` (per AC 2).

15. **Update wizard auto-fire test.** Test location is `tests/unit/actions/workspace.test.ts`, **NOT** `tests/unit/components/onboarding/confirm-step.test.tsx` as the architecture doc claims (that file covers Story 5.12 tier-aware copy and has nothing about generation). Locate the assertion that verifies the fetch call is fired (search for `generate-law-list` in the file). Update it to assert the call is **NOT** made post-`createWorkspace`. If no such assertion exists today, **add one** so we have regression coverage on the no-auto-fire contract.

16. **Behavioural smoke contract.** After AC 14 ships, the post-signup flow is: wizard submits → `createWorkspace` writes the row with `law_list_generation_status=null` and `first_run_dismissed_at=null` → `revalidatePath('/')` → client navigation to `/dashboard` → `getOnboardingState` returns `{firstRunOpen: true, fabVisible: false, fabState: 'idle'}` → modal renders.

### Minimal modal — path-choice step only

17. **New folder `components/features/onboarding-modal/`** with two new files in B.0 (rest of the folder ships across B.1–B.6 per arch §6.1):
    - `first-run-modal.tsx` — top-level Dialog wrapper. `'use client'`. Receives `{open: boolean}` prop; renders shadcn `<Dialog>` from `components/ui/dialog`. On mount, fires `recordOnboardingEvent('modal_opened', {trigger: 'first_run'})`. Owns local state for which sub-step is active (in B.0, only `path_choice` exists — but the state machine is wired so B.1+ can plug additional steps in without rewriting the shell).
    - `path-choice-step.tsx` — `'use client'`. Renders the three-card grid + tertiary skip text link. No props beyond callbacks for the four affordances.

18. **Three cards in a `grid-cols-1 sm:grid-cols-3` grid.** Cards mirror the in-app `<CreateListChooser>` pattern at `components/features/document-list/create-list-chooser.tsx` (Story 12.10b + 24.6) — same icon-above-label-above-meta-line layout, same hover-lift, same recommended-chip treatment. **B.0 uses plain card chrome — full brand-chrome upgrade (Safiro brand bar, dim/blur backdrop, modal shadow) ships in B.1 per the PRD.**

    | Card | Icon (lucide) | Label | Meta line | Click handler |
    |---|---|---|---|---|
    | Börja från mall (recommended-chip) | `LayoutTemplate` | "Börja från mall" | "Branschstartpunkt · 80–120 lagar →" | See AC 19 — template sub-step |
    | Generera laglista nu | `WandSparkles` | "Generera laglista nu" | "Använd er profil — AI bygger →" | See AC 20 |
    | Importera befintlig (disabled in B.0) | `FileUp` | "Importera befintlig" | "Kommer snart" | See AC 21 |

19. **Mall card click handler** (AC 18 row 1):
    - Fires `recordOnboardingEvent('path_chosen', {path: 'template'})` (best-effort).
    - Calls `minimiseFirstRunModal()` to dismiss the modal.
    - Navigates client-side to the existing apply-template flow. **B.0 routing target:** `/laglistor/mallar` (the template catalog index). Rationale: the inline template-pick sub-step described in the PRD (PRD Story B.0 bullet 4, line 100) requires fetching `getPublishedTemplates()` server-side and embedding `<TemplateOptionCard>` inside the modal — that lift adds ~half a day and is not required to deliver the token-saver wedge. The catalog index page already lets the user pick a template and apply it. **The inline picker is moved to B.1 as part of the brand-chrome upgrade** — flagged in Change Log. AC unchanged: any path that records `path:'template'` and does not fire generation satisfies the token-saver intent.
    - **No `POST /api/workspace/generate-law-list` call. No LLM tokens consumed.**

20. **Generera card click handler** (AC 18 row 2):
    - Fires `POST /api/workspace/generate-law-list` via raw `fetch()` from the client. **This is the same call previously fired by `createWorkspace`** (per AC 13) — the user now triggers it intentionally instead of having it auto-fire.
    - Fires `recordOnboardingEvent('path_chosen', {path: 'generate'})`.
    - Calls `minimiseFirstRunModal()` to close the modal. The existing `<LawListGenerationProgress>` banner (`components/features/dashboard/hem-page.tsx:74`) takes over as the in-page progress surface.
    - On `fetch` failure: show a `toast.error(...)` from `sonner` ("Något gick fel. Försök igen.") and keep the modal open. Do NOT minimise. Do NOT record `path_chosen`.

21. **Importera card** (AC 18 row 3) — **visibly disabled in B.0**. Render the card with `opacity-60`, `cursor-not-allowed`, an inline "Kommer snart" badge (use `<Badge tone="neutral" variant="soft">` — valid tones in `lib/ui/badge-tones.ts` are `neutral | info | success | warning | danger`; no `muted` tone), and a `disabled` interactive state (no hover-lift, no click handler firing the path). On click (still capture for telemetry):
    - Fires `recordOnboardingEvent('path_chosen', {path: 'import'})` so we measure latent demand for the import path.
    - Shows a `toast.info(...)` from `sonner`: "Vi mejlar dig när importen är klar att använda."
    - Calls `minimiseFirstRunModal()`.

    **B.1 un-disables this card** once Epic 24.4 ships the review surface. The card remains disabled in B.0 even if `EPIC_24_IMPORT_ENABLED` (or any feature flag) is on — B.0 ships before Epic 24.4 lands.

22. **Tertiary "Hoppa över — bygg manuellt" text link** below the card grid. Plain text-button styling (`variant="link"` with `text-muted-foreground` and small font size — not a card, low visual weight per the PRD). Click handler:
    - Calls `skipLawListGeneration()` (AC 8).
    - Modal closes.
    - No toast.
    - **Modal does not re-open** on subsequent dashboard visits because both `law_list_generation_status='skipped'` AND `first_run_dismissed_at=NOW()` are set — both guards in `getOnboardingState` (AC 4) reject.

23. **No Minimera affordance other than the four card/link handlers.** No X button in the dialog header in B.0 (B.2 adds the X + Minimera button when the tutorial step lands). Closing the modal in B.0 is explicit and tied to choosing a path.

### Dashboard wiring

24. **Modify `app/(workspace)/dashboard/page.tsx`** to call `getOnboardingState(context.workspaceId)` in parallel with the existing `Promise.all` over `getDashboardData` + `getUnacknowledgedChangeCount` + `searchParams`. Pass the result through to `<HemPage>` as a new prop:
    ```ts
    const onboardingState = await getOnboardingState(context.workspaceId)
    // ...
    return (
      <HemPage
        {...existingProps}
        onboardingState={onboardingState}
      />
    )
    ```
    Wrap the call in try/catch and default to `{firstRunOpen: false, fabVisible: false, fabState: 'idle'}` on error (consistent with the existing pattern of falling back gracefully on data-fetch failures — see `dashboard/page.tsx:46–48`).

25. **Modify `components/features/dashboard/hem-page.tsx`** to accept the new prop and mount the modal:
    ```ts
    interface HemPageProps {
      // ...existing props
      onboardingState: OnboardingState
    }
    // inside return, after the LawListGenerationProgress block:
    {onboardingState.firstRunOpen && <FirstRunModal open={true} />}
    ```
    The `<LawListGenerationProgress>` banner block (lines 74–77) stays **unchanged** — when the modal is open, it visually covers the banner area; when the modal closes, the banner shows its existing states per generation status. No double-fetching; both surfaces read the same SWR key when the modal grows progress UI in B.2.

26. **No regression in dashboard load time.** The new `getOnboardingState` call is one indexed lookup on `Workspace` by ID (selecting four columns). Add it to the existing `Promise.all` so it runs in parallel with the existing reads. The dashboard's p95 load time must not regress measurably after merge.

### Telemetry & observability

27. **`OnboardingEvent` event types emitted in B.0** (event_type, payload):
    - `modal_opened` — `{trigger: 'first_run'}` — fired on `<FirstRunModal>` mount.
    - `path_chosen` — `{path: 'template' | 'generate' | 'import' | 'skipped'}` — fired from each card/link's click handler (and from `skipLawListGeneration`).
    - `modal_dismissed` — `{from_state: 'path_choice'}` — fired by `minimiseFirstRunModal` (path_choice is the only sub-state in B.0).
    - `fab_dismissed` and `tab_viewed` — stubbed actions write these but no UI fires them in B.0.

28. **No activity-log entries.** Onboarding events are product telemetry, not user-facing audit-log entries (arch §5.2 + §12). Do not add anything to `lib/activity/action-constants.ts`.

29. **Admin dashboard surfacing is out of scope for B.0.** The `OnboardingEvent` table is queryable manually until the full modal ships. Wiring an admin tile is deferred.

### Feature flag

30. **Wrap the modal mount in `hem-page.tsx` behind an env-driven feature flag `FIRST_RUN_MODAL_V0`** (read once at module load, per the existing project convention; see how `LAWLIST_IMPORT_GROUPINGS_ENABLED` is handled in `lib/import/grouping-llm.ts` from Story 24.7). **Default-on semantics:**
    - **Unset** (env var missing): treated as **on** — modal renders. Rationale: unset → safe default → ship-and-rollback model.
    - `'false'` or `'0'`: treated as **off** — modal does not render.
    - Any other value (including `'true'`, `'1'`, empty string, typos): treated as **on**.
    - Production / preview / dev all default to **on** unless `FIRST_RUN_MODAL_V0=false` is explicitly set. The flag is the **one-line emergency-disable** path per the epic PRD's Risk Mitigation section.

    When the flag resolves to **off**:
    - The modal does not render even if `onboardingState.firstRunOpen === true`.
    - The auto-fire removal (AC 14) is NOT reverted by the flag — that's a permanent code change. If the flag is flipped off in an incident, dashboard reverts to its pre-Epic-25 state (banner if status non-null, plain chat-first if null), with no first-run modal AND no auto-fire of generation. Users in that state can still trigger generation manually via the in-app `<CreateListChooser>` / `<ManageListModal>` (Story 12.10b + 24.6). This is the documented incident posture per arch §13.

### Tests

31. **Unit tests for `getOnboardingState`** in `tests/unit/lib/onboarding/get-onboarding-state.test.ts` (new file). Cover all four guard branches:
    - Fresh workspace + no dismissal + null status → `firstRunOpen: true`
    - Fresh workspace + dismissed → `firstRunOpen: false`
    - Fresh workspace + null dismissal + status set (`pending` / `in_progress` / `completed` / `failed` / `skipped` — five sub-cases) → `firstRunOpen: false`
    - Stale workspace (>24h old) + null dismissal + null status → `firstRunOpen: false`
    - DB error or workspace not found → `{firstRunOpen: false, fabVisible: false, fabState: 'idle'}`
    - Every branch returns `fabVisible: false` and `fabState: 'idle'` in B.0

32. **Unit tests for `app/actions/onboarding-modal.ts`** in `tests/unit/app/actions/onboarding-modal.test.ts` (new file). For each of the five actions:
    - Happy path — column write + event row written
    - Permission gate — `withWorkspace` rejects unauthenticated call
    - `OnboardingEvent.create` failure — primary write still succeeds, action returns `{ok:true}`, error logged
    - `skipLawListGeneration` specifically asserts BOTH columns are updated in one call
    - `recordTabViewed` is idempotent — calling twice with the same `tabId` does not duplicate the array

33. **Component test** for `<PathChoiceStep>` in `tests/unit/components/features/onboarding-modal/path-choice-step.test.tsx` (new file):
    - Renders all three cards + the Hoppa över link
    - Mall card click → calls the template handler + records `path_chosen={path:'template'}`
    - Generera card click → fires `POST /api/workspace/generate-law-list` (mock fetch) + records `path_chosen={path:'generate'}` + closes modal
    - Generera fetch failure → toast shown, modal stays open, no `path_chosen` event
    - Importera card is rendered with disabled visual state + "Kommer snart" badge
    - Importera card click → records `path_chosen={path:'import'}` + toast + closes
    - Hoppa över link click → calls `skipLawListGeneration` + closes
    - Importera card does NOT navigate or fire generation

34. **Integration test** for `createWorkspace` in `tests/unit/actions/workspace.test.ts` (extend existing):
    - Assert `fetch` is NOT called with `/api/workspace/generate-law-list` after `createWorkspace` resolves successfully (regression guard for AC 14).
    - Assert the returned workspace has `law_list_generation_status: null` (verifies AC 2's default drop).
    - All existing tests in the file pass unchanged.

35. **Smoke test on staging** (manual, documented in Completion Notes once shipped):
    - New signup → land on `/dashboard` → modal opens with three cards + Hoppa över link
    - Click Generera → modal closes → banner appears within ~3s with `pending` then `in_progress` state
    - Reload → modal does NOT re-open (because `law_list_generation_status` is now set)
    - Fresh signup → click Hoppa över → modal closes → reload → modal does NOT re-open → banner does NOT appear (status is `skipped`)
    - Fresh signup → click Importera → toast appears → modal closes → reload → modal does NOT re-open (because `first_run_dismissed_at` is set, even though status is still null)
    - Query `OnboardingEvent` table — confirm event rows for `modal_opened` + `path_chosen` are present per session

36. **Post-merge production verification** (within 24h of merge to main; documented in Completion Notes):
    - Query production `OnboardingEvent` aggregated by `event_type` and `payload->>'path'` — confirm a non-zero count of `path_chosen` rows across all four paths (`template`, `generate`, `import`, `skipped`) means real users are reaching the modal and exercising every card. If `path:'generate'` is the only non-zero bucket, B.0 has effectively reproduced the old auto-fire behaviour and we should investigate (modal copy / card hierarchy / template-route accessibility).
    - Spot-check `ChatUsageEvent` aggregated by day for the 7 days post-merge vs the 7 days pre-merge — token cost per signup on the import/template/skip slice should drop measurably (this is the B.0 success criterion per the epic PRD). Compare workspaces created during each window.
    - Confirm no Sentry errors with prefix `[ONBOARDING_EVENT_WRITE_FAIL]` or unhandled exceptions from `app/actions/onboarding-modal.ts` — telemetry writes are best-effort but a sustained error rate indicates a bug.
    - Confirm `/dashboard` p95 load time on Vercel Analytics is within the pre-merge envelope.
    - If any of the above fails: flip `FIRST_RUN_MODAL_V0=false` in Vercel env vars (one-line emergency-disable per AC 30), open a follow-up ticket, then redeploy.

### Compatibility & out-of-scope guards

37. **`<LawListGenerationProgress>` banner unchanged.** No edits to `components/features/dashboard/law-list-generation-progress.tsx`. All existing tests for that component pass unchanged. The banner's polling, completion-dismiss, and failed-state behaviour are out of scope for B.0.

38. **In-app `<ManageListModal>` + `<CreateListChooser>` unchanged.** No edits to `components/features/document-list/manage-list-modal.tsx` or `create-list-chooser.tsx`. The first-run modal **mirrors** the three-card pattern visually but is a separate component (different mount surface, different copy, different state machine). Do not extract a shared component in B.0 — premature abstraction.

39. **Existing `Workspace` model relationships unchanged.** Only additive column changes + a new `onboarding_events` inverse relation. No existing index touched.

40. **No new HTTP routes.** All new server-side surface area is in server actions. `POST /api/workspace/generate-law-list` is unchanged and re-used by the Generera card handler (AC 20).

41. **No B.1+ scope leaks into B.0.** Explicitly out of scope: brand-chrome Dialog upgrade (Safiro bar, dim/blur, modal shadow), inline template-pick sub-step inside the modal, generate-kickoff confirm step, import-upload step, tutorial step + tab framework, six tab content panels, done states, FAB component, Hjälp sidebar entry, URL-driven deep-link, `tutorial_only` mode, feedback form. Each lands in its own B.1–B.6 story per the epic PRD.

## Tasks / Subtasks

- [x] **Task 1 — Prisma migration `add_first_run_modal_columns` (AC 1, 2, 3)**
  - [x] Add `first_run_dismissed_at DateTime?`, `tutorial_fab_dismissed_at DateTime?`, `first_run_tabs_viewed Json? @default("[]")` to `Workspace` in `prisma/schema.prisma`
  - [x] Drop `@default("pending")` from `law_list_generation_status` (keep type `String?`)
  - [x] Add `OnboardingEvent` model per AC 3 + add inverse relations on `Workspace` (`onboarding_events OnboardingEvent[]`) and `User` (`onboarding_events OnboardingEvent[]`)
  - [x] Migration SQL hand-written at `prisma/migrations/20260514000000_add_first_run_modal_columns/migration.sql` (user applies manually in Supabase — `prisma migrate dev` intentionally NOT run per user instruction). SQL is additive except the one required `DROP DEFAULT` on `law_list_generation_status` (AC 2); no DROP COLUMN, no RENAME.
  - [x] Ran `npx prisma generate` to regenerate the typed client (reads schema only — no DB connection)
- [x] **Task 2 — `getOnboardingState` pure function (AC 4, 5)**
  - [x] Create `lib/onboarding/get-onboarding-state.ts` with the `OnboardingState` type + `getOnboardingState(workspaceId)` function
  - [x] Read `Workspace` columns: `created_at`, `first_run_dismissed_at`, `tutorial_fab_dismissed_at`, `law_list_generation_status`
  - [x] All three guards `&&`'d together for `firstRunOpen`
  - [x] `fabVisible: false`, `fabState: 'idle'` hardcoded for B.0
  - [x] Workspace-not-found / DB-error → default safe state (try/catch returns `DEFAULT_STATE`)
- [x] **Task 3 — Server actions in `app/actions/onboarding-modal.ts` (AC 6–12)**
  - [x] Create the new file with `'use server'` at the top
  - [x] Import `withWorkspace` from `@/lib/auth/workspace-context`
  - [x] Implement `minimiseFirstRunModal` (AC 7) — workspace column write + event row write in fail-safe wrapper
  - [x] Implement `skipLawListGeneration` (AC 8) — both columns in single update + event row
  - [x] Implement `dismissOnboardingFab` (AC 9) — stubbed for B.6
  - [x] Implement `recordTabViewed` (AC 10) — stubbed for B.3 + idempotent array append
  - [x] Implement `recordOnboardingEvent` (AC 11) — generic event writer with fail-safe `[ONBOARDING_EVENT_WRITE_FAIL]` logging
  - [x] All five actions return discriminated `{ok:true} | {ok:false, error}` per AC 6 (shared `ActionResult` type; auth failure propagates from `withWorkspace` per AC 32)
- [x] **Task 4 — Modal shell + path-choice step (AC 17–23)**
  - [x] Create folder `components/features/onboarding-modal/`
  - [x] `first-run-modal.tsx` — `'use client'`; wraps `<Dialog>` from `@/components/ui/dialog`; owns local open state (snappy close) + sub-step shell; fires `recordOnboardingEvent('modal_opened', {trigger:'first_run'})` on mount via `useEffect`. No X button / no Esc / no outside-click dismissal in B.0 (AC 23) — `[&>button]:hidden` + `onInteractOutside`/`onEscapeKeyDown` preventDefault.
  - [x] `path-choice-step.tsx` — `'use client'`; renders three cards via `grid grid-cols-1 sm:grid-cols-3` + tertiary `Hoppa över` link below; owns the four affordance handlers (testable via the component test), takes a single `onClose` prop
  - [x] Mall card → `path_chosen={path:'template'}` event + `minimiseFirstRunModal` + client-side navigation to `/laglistor/mallar` via `useRouter().push`
  - [x] Generera card → `fetch('/api/workspace/generate-law-list', {method:'POST'})` + on success `path_chosen={path:'generate'}` event + `minimiseFirstRunModal`; on failure `toast.error` + modal stays open (early `return` before recording)
  - [x] Importera card — visibly disabled (`opacity-60`, `cursor-not-allowed`, `aria-disabled`, `<Badge tone="neutral" variant="soft">Kommer snart</Badge>`); on click → `path_chosen={path:'import'}` + `toast.info` + `minimiseFirstRunModal`
  - [x] Hoppa över link → `skipLawListGeneration` + close
  - [x] Recommended-chip on Mall card (`<Badge tone="info" variant="soft">Rekommenderas</Badge>` + ring/border treatment mirroring `<CreateListChooser>`)
  - [x] Icons: `LayoutTemplate`, `WandSparkles`, `FileUp` from `lucide-react`
- [x] **Task 5 — Dashboard wiring (AC 24, 25, 26, 30)**
  - [x] Modify `app/(workspace)/dashboard/page.tsx` to call `getOnboardingState(context.workspaceId)` in parallel (via `Promise.all` alongside the existing generation-status lookup, both wrapped in try/catch)
  - [x] Pass `onboardingState` prop to `<HemPage>`
  - [x] Modify `components/features/dashboard/hem-page.tsx` to accept the new prop + mount `<FirstRunModal open={true} />` when `onboardingState.firstRunOpen === true`
  - [x] Wrap the mount behind `FIRST_RUN_MODAL_V0` env flag (AC 30) — **DEVIATION:** env read is in `dashboard/page.tsx` (server, module-load) not `hem-page.tsx`, because hem-page.tsx is a client component and cannot read non-`NEXT_PUBLIC_` env vars; the flag value is passed as the `firstRunModalEnabled` prop and the mount guard (`firstRunModalEnabled && onboardingState.firstRunOpen`) stays in hem-page.tsx as AC 30 intends
  - [x] Keep `<LawListGenerationProgress>` block unchanged
- [x] **Task 6 — Remove auto-fire from `createWorkspace` (AC 13, 14, 15, 16)**
  - [x] Delete the auto-fire block at `app/actions/workspace.ts` (was lines 347–364) — replaced with a Story 25.0 explainer comment; `revalidatePath('/')` preserved
  - [x] Verify `headers` import is still used elsewhere — it IS (line ~594 in `regenerateLawList`), so the import was kept
  - [x] **Also removed** the explicit `law_list_generation_status: 'pending'` from `createWorkspace`'s `tx.workspace.create` (was line 205) — the test caught that `createWorkspace` set it explicitly rather than relying on the DB default; per AC 2/14 new workspaces must land NULL, so the explicit set had to go too. `regenerateLawList`'s own `'pending'` set is untouched (out of scope).
  - [x] Added regression block `Story 25.0: no generation auto-fire` to `tests/unit/actions/workspace.test.ts` — asserts (1) `fetch` is never called with `/api/workspace/generate-law-list`, (2) `createWorkspace` does not write `law_list_generation_status`
  - [x] Ran `tests/unit/actions/workspace.test.ts` — 18/18 pass (16 pre-existing + 2 new)
- [x] **Task 7 — Unit + component tests (AC 31–34)**
  - [x] `tests/unit/lib/onboarding/get-onboarding-state.test.ts` — 11 tests, all guard branches per AC 31 (incl. 5-case `it.each` for non-null status)
  - [x] `tests/unit/app/actions/onboarding-modal.test.ts` — 14 tests, all five actions per AC 32 (happy path, auth gate, fail-safe event write, primary-write failure, skip-both-columns, recordTabViewed idempotency)
  - [x] `tests/unit/components/features/onboarding-modal/path-choice-step.test.tsx` — 7 tests, all click handlers per AC 33
  - [x] Extended `tests/unit/actions/workspace.test.ts` per AC 34 (done in Task 6)
  - [x] Validation: 32 new tests pass; targeted regression of all 4 "tests to maintain" files (confirm-step, create-list-chooser, manage-list-modal, template-option-card) + workspace = 94/94 pass; `tsc --noEmit` clean for all Story 25.0 files; eslint clean
- [x] **Task 8 — Manual smoke + post-merge verification + completion notes (AC 35, 36)**
  - [x] Smoke scenarios (AC 35) + post-merge verification (AC 36) — **deferred to staging/production**; cannot run from the dev environment. Full procedure documented in Completion Notes for the next pass (mirrors the Story 24.7 deferred-smoke precedent).
  - [x] Completion Notes recorded below
- [x] **Task 9 — Verify out-of-scope guards (AC 37–41)**
  - [x] `<LawListGenerationProgress>` — not in `git diff --name-only`; untouched ✓
  - [x] `<ManageListModal>` + `<CreateListChooser>` — not in `git diff --name-only`; untouched ✓ (regression tests still 30/30 green)
  - [x] No new HTTP routes — `git status` shows no `app/api/` additions ✓
  - [x] Migration SQL — zero `DROP COLUMN` / `RENAME`; only the one intended `DROP DEFAULT` on `law_list_generation_status` (AC 2) ✓

## Dev Notes

### ⚠ Architecture-vs-code discrepancies flagged during drafting

The architecture doc `docs/architecture/first-run-onboarding-modal.md` contains three statements that conflict with the actual codebase. The dev should rely on this story's ACs (which are reconciled against the code) and not the architecture's claims for these three points:

1. **Auto-fire location.** Architecture §2.1 + §8 source-tree state the auto-fire of `POST /api/workspace/generate-law-list` lives in `app/onboarding/_components/confirm-step.tsx`. **It does not.** The actual location is `app/actions/workspace.ts:347–364`, inside the `createWorkspace` server action, in a block labelled `// Story 16.4: Trigger law list generation (fire-and-forget)`. `confirm-step.tsx` only calls `onSubmit()` and has no knowledge of generation. See AC 13 + AC 14 for the actual remove-here-instead instruction.

2. **Schema state for new workspaces.** Architecture §6.4 + §5.1 imply `law_list_generation_status` is `null` for fresh workspaces. **It defaults to `"pending"`** per `prisma/schema.prisma:172` (`law_list_generation_status String? @default("pending")`). Without dropping that default in the same migration, `getOnboardingState`'s `=== null` guard would never fire for new workspaces and the modal would never open. AC 2 mandates the default drop in the migration.

3. **Wizard test location.** Architecture §11 + §16 (Developer Handoff) say "Update existing test (`tests/unit/components/onboarding/confirm-step.test.tsx`) to assert the call is NOT made." **That test file does not contain any auto-fire assertion** — it covers Story 5.12 tier-aware copy. The auto-fire assertion (if it exists) is in `tests/unit/actions/workspace.test.ts`. See AC 15.

These are documentation bugs in the architecture doc — not story-level scope changes. The intent of every claim is preserved; only the file paths are wrong. Story 25.0's Change Log records the reconciliation.

### Source artefacts

- Epic PRD: `docs/prd/epic-25-first-run-onboarding-modal.md` — Story B.0 scope at lines 90–119; the PRD is the authoritative scope source.
- Architecture: `docs/architecture/first-run-onboarding-modal.md` — schema (§5), state machine (§6.2), server actions (§7.1), sequencing (§9). Authoritative for design intent; **see flags above for three specific file-path corrections.**
- PO assessment (2026-05-13): Sarah promoted the feedback form from B.6-future to B.5-in-scope-MVP; re-numbered re-entry layers to B.6. Does not affect B.0 scope.

### Existing patterns to follow

- **`withWorkspace(cb)` server-action wrapper** at `lib/auth/workspace-context.ts`. Same pattern as Story 24.5's `createCatalogIngestRequest` and Story 24.7's `proposeGroupings`. No permission scope needed (workspace-self-write only).
- **Fail-safe telemetry pattern** — see Story 14.27 (`[CHAT_USAGE_EVENT_WRITE_FAIL]`) and Story 5.12 (`[ENTERPRISE_INQUIRY_EMAIL_FAIL]`). Both wrap the secondary write (email or event row) in try/catch + `console.error` with a distinct prefix; never re-throw. Apply the same pattern with prefix `[ONBOARDING_EVENT_WRITE_FAIL]`.
- **Dialog from `components/ui/dialog`** — shadcn primitive, no bespoke modal chrome. B.0 uses plain Dialog defaults; B.1 layers Safiro brand bar + dim/blur backdrop + modal shadow on top.
- **`<CreateListChooser>` three-card pattern** at `components/features/document-list/create-list-chooser.tsx` — visual reference for the path-choice grid. Same icon-above-label-above-meta-line layout, same recommended-chip on Mall, same `grid-cols-1 sm:grid-cols-3`. **Do not extract a shared component** in B.0 (different surface, different copy, different state machine).
- **Toast via `sonner`** — `toast.error(...)` and `toast.info(...)` calls. Already used throughout the app; no new dependency.
- **Feature flag pattern** — read `process.env.FIRST_RUN_MODAL_V0` once at module load; treat `'false'` or `'0'` as off, anything else as on. Mirrors `LAWLIST_IMPORT_GROUPINGS_ENABLED` from Story 24.7 (see `lib/import/grouping-llm.ts` for the implementation precedent).

### Key decisions baked into this story

- **Mall path navigates to `/laglistor/mallar` instead of an inline template picker.** PRD Story B.0 bullet 4 mentions an inline template-pick sub-step using `getPublishedTemplates()` + `<TemplateOptionCard>`. That adds ~half a day of wiring and is not required to deliver the token-saver wedge. The catalog index already lets the user pick and apply a template. **The inline picker moves to B.1** as part of the brand-chrome upgrade. AC 19 preserves the token-saver outcome (no LLM call on the template path) which is what B.0 is for. Token-cost telemetry will validate this in production.
- **Importera card stays disabled in B.0 even if Epic 24 is partially live.** B.0 ships before Epic 24.4's review surface. Un-disabling the card requires that review surface to exist (so the import flow has somewhere to go on success). B.1 owns the un-disable.
- **No FAB, no tab framework, no done states in B.0.** The PRD explicitly defers all of these to B.2–B.6. B.0 is the minimum substrate to stop the token bleed; richer UI ships in subsequent stories.
- **Stubbed actions `dismissOnboardingFab` and `recordTabViewed` ship in B.0** even though no UI calls them. Rationale: their contracts are well-defined now (arch §7.1) and shipping them in B.0 means B.3/B.6 don't have to ship action + UI in the same PR. Unit tests cover the actions; no integration cost.
- **Schema default drop is non-trivially in-scope.** It's the most subtle part of B.0 — easy to miss, breaks the entire trigger logic if missed. AC 2 + Dev Note flag #2 above call this out twice deliberately.
- **`OnboardingEvent.id` uses `@default(uuid())`.** Matches Epic 24's ratified `@default(uuid())` convention for new tables and the existing `ChatUsageEvent.id` precedent (verified at `prisma/schema.prisma:1470`). An earlier draft of this story claimed cuid was the precedent — that was incorrect; uuid is consistent across all recent models.

### Out of scope explicitly

- Brand-chrome Dialog upgrade — Safiro bar, dim/blur backdrop, modal shadow (B.1)
- Inline template-pick sub-step inside the modal (B.1; B.0 navigates to `/laglistor/mallar` instead)
- Un-disabling the Importera card (B.1, after Epic 24.4 lands)
- Generate-kickoff confirm step (B.1)
- Import-upload step (B.1)
- Tutorial step + progress strip + tab framework (B.2)
- Six tutorial tab content panels (B.3)
- Done states for any of the three paths (B.4)
- Feedback form (B.5)
- Corner FAB + Hjälp sidebar entry + tutorial-only mode + URL deep-link (B.6)
- Admin dashboard tile for `OnboardingEvent` (deferred indefinitely)
- Activity-log entries for any onboarding event (intentionally separate from `Activity` — see arch §5.2)

### Project structure notes

All new file paths align with `docs/architecture/12-unified-project-structure.md`:
- `lib/onboarding/get-onboarding-state.ts` — fits the `lib/{feature}/` convention (kebab-case file under feature folder)
- `app/actions/onboarding-modal.ts` — fits the existing `app/actions/{feature}.ts` convention (see `app/actions/change-events.ts`, `documents.ts`, etc.)
- `components/features/onboarding-modal/` — fits the `components/features/{feature}/` convention (matches `components/features/onboarding/` which holds the wizard, but is a distinct folder for the post-signup modal)
- `tests/unit/{matching path}` — mirrors source structure per the testing convention

**Naming caveat:** the unified-project-structure doc shows `PascalCase` for some component files (e.g., `KanbanBoard.tsx`) but **the actual repo uses `kebab-case` for component files** (e.g., `law-list-generation-progress.tsx`, `change-assessment-view.tsx`, `import-review-page.tsx`). Use `kebab-case` for new component files — it matches the rest of the feature components and is what every Epic 24 story used.

### Testing

**Test locations (all new in this story unless noted):**
- `tests/unit/lib/onboarding/get-onboarding-state.test.ts` — pure function unit tests
- `tests/unit/app/actions/onboarding-modal.test.ts` — server action unit tests
- `tests/unit/components/features/onboarding-modal/path-choice-step.test.tsx` — component test
- `tests/unit/actions/workspace.test.ts` — extend existing (auto-fire regression guard)

**Frameworks:** Vitest (unit + integration), Vitest + React Testing Library (component). Mock Prisma via the existing test setup pattern; mock `withWorkspace` via the same pattern Story 24.7's tests use (see `tests/integration/app/actions/law-list-import.test.ts` for the `mockCtxHasPermission` toggle pattern).

**Coverage targets:**
- `getOnboardingState`: 100% on the four guard branches + error path
- Server actions: 95%+ on the five actions; assert column write + event row + fail-safe path for each
- `<PathChoiceStep>` component: 90%+ on the four click handlers + disabled-card visual + fetch-failure branch
- `createWorkspace` regression: one new assertion (no-auto-fire) + one new assertion (`status: null`)

**Existing tests to maintain (no edits beyond AC 15):**
- All Story 16.4 tests for `<LawListGenerationProgress>` pass unchanged
- All Story 5.12 tests in `confirm-step.test.tsx` pass unchanged (this story does NOT touch `confirm-step.tsx`)
- All Story 12.10b tests for `<CreateListChooser>` + `<TemplateOptionCard>` pass unchanged
- All Story 24.6 tests for `<ManageListModal>` pass unchanged

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-05-13 | 0.1 | Initial draft from Epic 25 PRD (Story B.0 scope). Three architecture-vs-code discrepancies flagged in Dev Notes: (1) auto-fire location is `app/actions/workspace.ts:347–364` not `confirm-step.tsx`; (2) `law_list_generation_status @default("pending")` needs dropping in the same migration so `getOnboardingState`'s null check fires; (3) wizard auto-fire test location is `tests/unit/actions/workspace.test.ts` not `tests/unit/components/onboarding/confirm-step.test.tsx`. Mall path scope simplified to navigate to `/laglistor/mallar` index instead of an inline template-pick sub-step — picker moves to B.1 as part of the brand-chrome upgrade (saves ~½ day in B.0; token-saver outcome preserved). Stubbed `dismissOnboardingFab` + `recordTabViewed` actions ship in B.0 so B.3/B.6 don't need parallel action+UI PRs. Feature flag `FIRST_RUN_MODAL_V0` wraps the mount for emergency-disable. 40 ACs across 7 subsystems; 9 implementation tasks; 4 test files. | Bob (SM) |
| 2026-05-13 | 0.2 | PO validation pass (`validate-next-story`). Two anti-hallucination findings fixed in place: (HALL-001) `<Badge tone="muted">` corrected to `<Badge tone="neutral" variant="soft">` — valid tones in `lib/ui/badge-tones.ts` are `neutral \| info \| success \| warning \| danger`; no `muted` exists. Fixed in AC 21 + Task 4 subtask. (HALL-002) `OnboardingEvent.id @default(cuid())` corrected to `@default(uuid())` — `ChatUsageEvent.id` actually uses uuid (verified at `prisma/schema.prisma:1470`), not cuid as v0.1 incorrectly claimed; the uuid convention is consistent across all recent models. Fixed in AC 3 + Dev Notes "Key decisions". Validation report: GO; implementation readiness 9/10; no blocking issues. | Sarah (PO) |
| 2026-05-13 | 0.3 | PO polish pass — applied should-fix + nice-to-have items from the v0.2 validation report. (1) Architecture doc `docs/architecture/first-run-onboarding-modal.md` cleaned up: §2.1 wizard-handoff row, §5.1 schema-default-drop note, §6.3 source-tree, §11 testing-strategy, §16 dev-handoff all corrected to point at `app/actions/workspace.ts` instead of `confirm-step.tsx`; §8 + §11 also harmonised to `get-onboarding-state.ts` (was inconsistent with §6.4). (2) AC 30 made the `FIRST_RUN_MODAL_V0` unset-default behaviour explicit (unset → on; `'false'` or `'0'` → off; any other value → on). (3) New AC 36 added: post-merge production verification (within 24h of merge) — `OnboardingEvent` path-distribution sanity check, `ChatUsageEvent` week-over-week token-cost delta, Sentry scan for `[ONBOARDING_EVENT_WRITE_FAIL]`, dashboard p95 load-time check, and the emergency-disable instruction if any check fails. Compatibility section renumbered AC 36→37, 37→38, 38→39, 39→40, 40→41. Task 8 + Task 9 updated to reflect the renumber + new AC. | Sarah (PO) |
| 2026-05-13 | 0.4 | Status flipped from Draft → **Approved**. Re-validation (v0.3) returned GO with no Critical or Should-Fix items remaining. Story is implementation-ready and unblocked for Dev (James). | Sarah (PO) |
| 2026-05-14 | 0.5 | Implementation complete — status → **Ready for Review**. All 9 tasks done; 34 new/extended tests (11 + 14 + 7 + 2), all green; full `tests/unit` suite re-run 4641/4641 pass (zero regressions); `tsc` + eslint clean for all Story 25.0 files. Schema migration hand-written (not `prisma migrate dev`) per user instruction and applied manually in Supabase by the user. Three implementation deviations recorded in Debug Log References: (1) the auto-fire removal also required deleting an explicit `law_list_generation_status: 'pending'` set in `createWorkspace` that the story assumed was a DB default — caught by the new regression test; (2) `FIRST_RUN_MODAL_V0` env read placed in `dashboard/page.tsx` (server) not `hem-page.tsx` (client component can't read non-`NEXT_PUBLIC_` env), flag value passed as a prop with the mount guard staying in hem-page.tsx; (3) `exactOptionalPropertyTypes` required conditionally spreading the Prisma Json `payload` key. AC 35 (staging smoke) + AC 36 (post-merge verification) deferred to staging/production — full procedure documented in Completion Notes. Full-suite note: 36 integration-test failures observed in the all-tests run are pre-existing Prisma connection-pool exhaustion in `tests/integration/**`, unrelated to this story (zero failures reference any Story 25.0 file; `tests/unit` fully green). | James (Dev) |
| 2026-05-14 | 0.6 | QA-fix pass — applied 3 of 6 items from the CONCERNS gate (`docs/qa/gates/25.0-path-choice-gate.yml`). **TEST-001 (medium):** extracted the `FIRST_RUN_MODAL_V0` predicate to `lib/onboarding/first-run-modal-flag.ts` (`isFirstRunModalEnabled`, behaviour-preserving) + new `first-run-modal-flag.test.ts` (8-case matrix); `dashboard/page.tsx` wired to the helper. **ROBUST-002 (low):** `useRef` guard in `first-run-modal.tsx` so `modal_opened` fires once under dev StrictMode. **TEST-002 (low):** new `first-run-modal.test.tsx` (4 tests) — shell render + mount telemetry + StrictMode fires-once. **ROBUST-001 / A11Y-001 / DEBT-001 (all low):** accepted deferrals — QA assigned them to B.1/B.3; documented with rationale in Completion Notes (changing them now is scope creep into B.1's surface rework / B.3's tab wiring). Validation: `tsc` + eslint clean; full `tests/unit` suite 353 files / 4653 tests pass (+12, zero regressions). Story test total 46 (was 34). Status → **Ready for Review** (per apply-qa-fixes Status Rule: prior gate was CONCERNS → QA re-review requested). | James (Dev) |
| 2026-05-14 | 1.0 | QA re-review lifted the gate **CONCERNS → PASS** (quality score 95). Story owner accepted the recommendation and flipped Status → **Done**. The three remaining low-severity items (ROBUST-001, A11Y-001, DEBT-001) carry forward as documented B.1/B.3 follow-ups; AC 35 staging smoke + AC 36 post-merge verification remain to be run on staging/production per the Completion Notes procedure. | Alexander (owner) |

## Dev Agent Record

### Agent Model Used

claude-opus-4-7 (1M context) — James (BMad Dev agent persona)

### Debug Log References

- **Migration applied manually.** Per user instruction, `prisma migrate dev` was NOT run. Migration SQL was hand-written at `prisma/migrations/20260514000000_add_first_run_modal_columns/migration.sql` matching the project's Prisma-output style (verified against `20260507120000_add_law_list_import`). User confirmed the migration ran successfully in Supabase mid-implementation. `npx prisma generate` was run locally to refresh the typed client (reads schema only, no DB connection).
- **`createWorkspace` set `law_list_generation_status: 'pending'` explicitly.** The story (AC 2/14) assumed new workspaces relied on the DB `@default("pending")`. They did not — `createWorkspace`'s `tx.workspace.create` set the column explicitly (was line 205). The first run of `workspace.test.ts`'s new regression test caught this (`expected 'pending' to be undefined`). Fixed by removing the explicit set; `regenerateLawList`'s own `'pending'` set is untouched (out of scope).
- **Feature-flag env read moved to the server.** AC 30 says "wrap the modal mount in `hem-page.tsx` behind an env-driven flag". `hem-page.tsx` is a client component and cannot read non-`NEXT_PUBLIC_` env vars (Next.js inlines them as `undefined` client-side), so `FIRST_RUN_MODAL_V0=false` would never take effect there. The env read was placed in `dashboard/page.tsx` (server, module-load) and passed down as the `firstRunModalEnabled` prop; the mount *guard* (`firstRunModalEnabled && onboardingState.firstRunOpen`) stays in `hem-page.tsx` as AC 30 intends.
- **`exactOptionalPropertyTypes` + Prisma Json.** `onboarding-modal.ts` initially failed `tsc` on `payload: payload ?? undefined` (TS2412 — `undefined` not assignable to Prisma's Json input type). Fixed by conditionally spreading the key (`...(payload !== undefined && { payload: payload as Prisma.InputJsonValue })`).

### Completion Notes List

**Implementation summary**

Story 25.0 ships the Epic 25 "B.0" path-choice gate — the token-saver wedge. New users finishing the workspace wizard now land on `/dashboard` with `law_list_generation_status = null`, which `getOnboardingState` keys on to auto-open a three-card modal (Börja från mall / Generera laglista nu / Importera befintlig — disabled in B.0) plus a tertiary "Hoppa över" link. The silent generation auto-fire was removed from `createWorkspace`; generation now only fires when the user intentionally clicks "Generera". Five `OnboardingEvent`-backed server actions (two stubbed for B.3/B.6) provide the substrate the rest of the epic builds on.

**Test totals — 34 new/extended tests, all green**

- Unit (`tests/unit/lib/onboarding/get-onboarding-state.test.ts`): **11 tests** — all four guard branches + 5-case status `it.each` + not-found + DB-error + B.0 fab defaults.
- Unit (`tests/unit/app/actions/onboarding-modal.test.ts`): **14 tests** — all five actions: happy path, auth-gate propagation, fail-safe event write, primary-write failure, skip-writes-both-columns, recordTabViewed idempotency.
- Component (`tests/unit/components/features/onboarding-modal/path-choice-step.test.tsx`): **7 tests** — three-card render, disabled Importera card, all four affordance handlers, Generera fetch-failure branch.
- Extended (`tests/unit/actions/workspace.test.ts`): **+2 tests** — no-auto-fire regression guard + no `law_list_generation_status` write. 18/18 in-file pass.
- Targeted regression: all 4 "tests to maintain" suites green (confirm-step 6, create-list-chooser 15, manage-list-modal 15, template-option-card 8). Full unit suite re-run to confirm zero regressions (see below).
- `tsc --noEmit` clean for all Story 25.0 files; eslint clean.

**Full-suite note:** the complete `vitest run` (5560 tests) showed 36 failures across 13 files — all in `tests/integration/**`, failing on `Timed out fetching a new connection from the connection pool` (10-connection Prisma pool exhausted by running the entire integration suite in one parallel batch). These are pre-existing environmental flakiness, unrelated to this story — zero failures reference any Story 25.0 file, and the `tests/unit` suite is fully green. Recommend running `tests/integration` in smaller batches or with a larger pool in CI.

**Manual smoke + post-merge verification — DEFERRED (cannot run from dev env)**

AC 35 (staging smoke) and AC 36 (post-merge production verification) cannot run from the development environment. Procedure for the next pass:

_AC 35 — staging smoke:_
1. New signup → land on `/dashboard` → modal opens with three cards + Hoppa över link.
2. Click Generera → modal closes → `<LawListGenerationProgress>` banner appears within ~3s (`pending` → `in_progress`).
3. Reload → modal does NOT re-open (`law_list_generation_status` now set).
4. Fresh signup → Hoppa över → modal closes → reload → no modal, no banner (status `skipped`).
5. Fresh signup → Importera → toast appears → modal closes → reload → no modal (`first_run_dismissed_at` set, status still null).
6. Query `onboarding_events` — confirm `modal_opened` + `path_chosen` rows per session.

_AC 36 — within 24h of production merge:_
- `onboarding_events` aggregated by `event_type` + `payload->>'path'` — confirm non-zero counts across all four paths.
- `ChatUsageEvent` 7-day pre/post token-cost delta on the import/template/skip slice.
- Sentry scan for `[ONBOARDING_EVENT_WRITE_FAIL]` + unhandled exceptions from `app/actions/onboarding-modal.ts`.
- `/dashboard` p95 load time within the pre-merge envelope.
- If any check fails: set `FIRST_RUN_MODAL_V0=false` in Vercel env, open a follow-up ticket, redeploy.

**Migration handoff**

`prisma/migrations/20260514000000_add_first_run_modal_columns/migration.sql` was applied manually in Supabase by the user (confirmed mid-implementation). If the `_prisma_migrations` history table needs syncing for future `prisma migrate` runs, run `npx prisma migrate resolve --applied 20260514000000_add_first_run_modal_columns` against the environment.

**QA-fix pass (2026-05-14, post-CONCERNS gate)**

Applied 3 of the 6 gate items; the other 3 are accepted deferrals (QA itself assigned them to B.1/B.3):

- **TEST-001 (medium) — FIXED.** Extracted the `FIRST_RUN_MODAL_V0` flag predicate to `lib/onboarding/first-run-modal-flag.ts` as the pure, behaviour-preserving `isFirstRunModalEnabled(envValue)`. `dashboard/page.tsx` now imports it. New `first-run-modal-flag.test.ts` covers the full matrix (unset / empty / `'false'` / `'0'` / casing / `'true'` / `'1'` / garbage) — 8 tests. The emergency-disable lever now has regression coverage.
- **ROBUST-002 (low) — FIXED.** Added a `useRef` guard in `first-run-modal.tsx` so the `modal_opened` telemetry fires exactly once even under React 18 dev StrictMode's double-invoked mount effect.
- **TEST-002 (low) — FIXED.** New `first-run-modal.test.tsx` (4 tests) covers the shell: renders title/description + path-choice step, fires `modal_opened` on mount, fires it exactly once under `<StrictMode>` (directly validates the ROBUST-002 guard), and renders nothing when `open=false`.
- **ROBUST-001 (low) — DEFERRED to B.1.** Fire-and-forget on skip/template/import is the intentional B.0 snappy-close design; QA assigned the `await` + error-toast rework to B.1's path-choice surface pass. Not changed now (would be scope creep into B.1).
- **A11Y-001 (low) — DEFERRED to B.1.** The disabled Importera card's `aria-disabled` treatment is per AC 21's explicit design; the card un-disables entirely in B.1, where the a11y treatment is revisited.
- **DEBT-001 (low) — DEFERRED to B.3.** `recordTabViewed`'s read-then-write TOCTOU is in a B.3 stub with zero B.0 traffic; the atomic-append fix lands when B.3 wires the tutorial tabs.

Validation after fixes: `tsc` clean for all Story 25.0 files, eslint clean, full `tests/unit` suite re-run **353 files / 4653 tests pass** (+12 from the two new test files, zero regressions). Story test total: 46 (was 34).

### File List

**New files**
- `prisma/migrations/20260514000000_add_first_run_modal_columns/migration.sql` — hand-written migration (user applies in Supabase)

- `lib/onboarding/get-onboarding-state.ts` — `OnboardingState` type + pure `getOnboardingState(workspaceId)` derivation
- `lib/onboarding/first-run-modal-flag.ts` — `isFirstRunModalEnabled()` pure predicate for the `FIRST_RUN_MODAL_V0` flag (QA TEST-001)
- `app/actions/onboarding-modal.ts` — 5 server actions (`minimiseFirstRunModal`, `skipLawListGeneration`, `dismissOnboardingFab`, `recordTabViewed`, `recordOnboardingEvent`) + shared `writeOnboardingEvent` fail-safe helper
- `components/features/onboarding-modal/first-run-modal.tsx` — Dialog shell, modal-open telemetry (StrictMode-guarded), no-dismiss chrome
- `components/features/onboarding-modal/path-choice-step.tsx` — three-card grid + Hoppa över link + the four affordance handlers

**Modified files**
- `prisma/schema.prisma` — Workspace: +3 first-run columns, dropped `@default("pending")` on `law_list_generation_status`, +`onboarding_events` relation; User: +`onboarding_events` relation; new `OnboardingEvent` model
- `app/(workspace)/dashboard/page.tsx` — parallel `getOnboardingState` call, `FIRST_RUN_MODAL_V0_ENABLED` now via the extracted `isFirstRunModalEnabled` helper, passes `onboardingState` + `firstRunModalEnabled` props
- `components/features/dashboard/hem-page.tsx` — accepts the two new props, mounts `<FirstRunModal>` behind the flag + `firstRunOpen` guard
- `app/actions/workspace.ts` — removed the Story 16.4 generation auto-fire block + the explicit `law_list_generation_status: 'pending'` set from `createWorkspace`
- `components/features/onboarding-modal/first-run-modal.tsx` — QA ROBUST-002: added `useRef` guard so `modal_opened` telemetry fires exactly once under dev StrictMode
- `tests/unit/actions/workspace.test.ts` — +`Story 25.0: no generation auto-fire` describe block (2 regression tests)

**New test files**
- `tests/unit/lib/onboarding/get-onboarding-state.test.ts` — 11 tests
- `tests/unit/lib/onboarding/first-run-modal-flag.test.ts` — 8 tests (QA TEST-001)
- `tests/unit/app/actions/onboarding-modal.test.ts` — 14 tests
- `tests/unit/components/features/onboarding-modal/path-choice-step.test.tsx` — 7 tests
- `tests/unit/components/features/onboarding-modal/first-run-modal.test.tsx` — 4 tests (QA TEST-002)

## QA Results

### Review Date: 2026-05-14

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Strong, disciplined implementation that delivers the B.0 token-saver wedge cleanly. The risk-escalation triggers fired (41 ACs, `app/actions/workspace.ts` auth-adjacent surface touched) so this was a deep review — and the code holds up well. Every new file carries a header docblock that scopes B.0 vs B.1+, inline comments consistently explain the *why* (schema-default drop, server-side env read, `exactOptionalPropertyTypes` workaround, `[&>button]:hidden`), and the fail-safe telemetry pattern from Story 14.27 is applied correctly across all five server actions. The three architecture-vs-code discrepancies were caught during drafting and reconciled — and the dev caught a *fourth* during implementation (the explicit `law_list_generation_status: 'pending'` set in `createWorkspace`) via the new regression test, which is exactly the feedback loop you want.

34 new/extended tests, all green; full `tests/unit` suite re-run 4641/4641. The full-suite integration failures the dev flagged are confirmed pre-existing Prisma connection-pool exhaustion in `tests/integration/**` — zero reference any Story 25.0 file.

One genuine coverage gap (the emergency-disable flag) and a few low-severity robustness/a11y notes for follow-up — none blocking.

### Refactoring Performed

None. The code is clean and the one high-value improvement (extracting + testing the `FIRST_RUN_MODAL_V0` flag logic) is a structural change to a server-component module that is better owned by Dev — see Improvements Checklist. No QA-side edits to source files this round.

### Compliance Check

- Coding Standards: ✓ — TS strict, no `any` (the one `as Prisma.InputJsonValue` cast is justified + commented), `withWorkspace` on every action, fail-safe telemetry pattern, shadcn primitives, kebab-case files.
- Project Structure: ✓ — `lib/onboarding/`, `app/actions/`, `components/features/onboarding-modal/`, tests mirror source. Matches `docs/architecture/12-unified-project-structure.md`.
- Testing Strategy: ✓ — correct test levels (pure→unit, actions→unit w/ mocked Prisma+withWorkspace, component→RTL). Mock patterns match the `documents-save.test.ts` precedent. Coverage targets from the story met for the files that have tests.
- All ACs Met: ✓ (with notes) — 41/41 implemented. AC 35/36 (smoke + post-merge) are inherently manual and explicitly deferred with a documented procedure. AC 30 (feature flag) is implemented and correct-by-inspection but has **no automated test** — see TEST-001.

### Improvements Checklist

- [ ] **(med) TEST-001 — `FIRST_RUN_MODAL_V0` flag logic has no test.** The emergency-disable lever (`FIRST_RUN_MODAL_V0_ENABLED` in `app/(workspace)/dashboard/page.tsx:29`) is the production safety mechanism per AC 30 + AC 36, yet the unset / `'false'` / `'0'` / other-value / casing matrix is verified by inspection only. The logic is simple and correct, but a regression here silently breaks the incident response. Suggested fix: extract the predicate to a tiny pure helper (e.g. `lib/onboarding/first-run-modal-flag.ts` exporting `isFirstRunModalEnabled(envValue: string | undefined)`) and unit-test the matrix; `page.tsx` imports it. Owner: dev.
- [ ] **(low) ROBUST-001 — fire-and-forget server actions on skip/template/import.** `handleSkip` / `handleTemplate` / `handleImport` in `path-choice-step.tsx` `void` the server action and immediately `onClose()`. If `skipLawListGeneration()` fails server-side, `law_list_generation_status` + `first_run_dismissed_at` are never set and the modal re-opens on the next dashboard load — the AC 22 "does not re-open" guarantee degrades silently. Benign (user re-picks; failure is logged server-side) and acceptable for B.0's minimum-substrate scope, but worth an `await` + error toast when B.1 reworks this surface. Owner: dev (B.1).
- [ ] **(low) A11Y-001 — disabled Importera card sends a mixed signal.** The card has `aria-disabled="true"` but is fully interactive (`tabIndex={0}`, `onClick`, keyboard handler all active — per AC 21's "disabled interactive state … still capture telemetry"). Screen-reader users are told "disabled" yet activation does something. Per-spec for B.0, and the card un-disables in B.1 anyway — but consider dropping `aria-disabled` in favour of an `aria-describedby` "Kommer snart" hint if the disabled state lingers. Owner: dev (B.1).
- [ ] **(low) TEST-002 — `<FirstRunModal>` shell is untested.** The `modal_opened` mount telemetry and the no-dismiss chrome (`[&>button]:hidden`, `onInteractOutside`/`onEscapeKeyDown` preventDefault — AC 17 + AC 23) are inspection-verified only. The story's AC 33 scoped the component test to `<PathChoiceStep>`, so this is per-spec, but a thin render test would close AC 23. Owner: dev (optional).
- [ ] **(low) ROBUST-002 — `modal_opened` may double-fire in React StrictMode (dev only).** `first-run-modal.tsx`'s mount `useEffect` will run twice under dev StrictMode → two `modal_opened` rows. Production is unaffected. A `useRef` guard would make dev telemetry clean. Owner: dev (optional).
- [ ] **(low) DEBT-001 — `recordTabViewed` read-then-write is not atomic.** Findunique-then-update on `first_run_tabs_viewed` has a TOCTOU window; concurrent tab-view records could drop one. It's a B.3 stub with zero B.0 traffic — fold an atomic jsonb append (or a `$transaction`) into B.3 when the tutorial tabs actually wire it. Owner: dev (B.3).

### Security Review

**PASS.** All five server actions are wrapped in `withWorkspace(cb)` — authentication + workspace scoping enforced; no permission scope is correct (workspace-self-write only, AC 6). Auth failure propagates as a throw rather than being swallowed into `{ok:false}` — verified by the auth-gate tests. No PII in `OnboardingEvent.payload` (path / tab-id / trigger / from_state enums only). No hardcoded secrets. The client-side `fetch('/api/workspace/generate-law-list', {method:'POST'})` is same-origin so the session cookie is sent automatically — auth is intact, and it's actually simpler than the old server-side `headers: { cookie }` plumbing. `onboarding_events` ships with `ENABLE ROW LEVEL SECURITY` (no policy) per the project pattern. No new attack surface; the auto-fire removal *reduces* surface (one fewer internal `fetch`).

### Performance Considerations

**PASS.** `getOnboardingState` is a single PK-indexed `findUnique` selecting four columns, run in parallel via `Promise.all` with the existing generation-status lookup (AC 26) — net dashboard cost is one indexed read, negligible against p95. No N+1. The fail-safe `OnboardingEvent` insert is one row after the primary write. `recordTabViewed`'s two-query read-then-write is a B.3 stub with no B.0 traffic. Migration adds two indexes on a new low-write table — fine.

### Files Modified During Review

None. Story File List is accurate and complete as written — no Dev update needed.

### Gate Status

Gate: **CONCERNS** → docs/qa/gates/25.0-path-choice-gate.yml
Quality score: **90/100**

### Recommended Status

**✓ Ready for Done** — the CONCERNS gate reflects one medium coverage gap (TEST-001, the emergency-disable flag) plus four low-severity follow-up notes, none of which block shipping. The implementation is correct, well-tested where it counts, and the flag logic is correct-by-inspection. Story owner decides: either fold TEST-001 in now (≈20 min: extract helper + matrix test) or accept it as tracked debt and ship. The four low items are legitimately B.1/B.3 follow-ups. Final call is the owner's.

---

### Re-Review Date: 2026-05-14 (post v0.6 QA-fix pass)

### Reviewed By: Quinn (Test Architect)

### Re-Review Scope

Targeted re-review of the three fixes the dev applied against the v0.6 CONCERNS gate, plus regression confirmation. Not a full re-review — the v0.5 deep review stands for everything untouched.

### Fix Verification

- **TEST-001 (medium) — ✅ CLOSED.** The `FIRST_RUN_MODAL_V0` predicate is extracted to `lib/onboarding/first-run-modal-flag.ts` as `isFirstRunModalEnabled(envValue)`. Verified **behaviour-preserving** — `!['false','0'].includes((envValue ?? '').toLowerCase())` is byte-identical to the original inline expression; `dashboard/page.tsx:30` now calls the helper. New `first-run-modal-flag.test.ts` covers the full matrix (8 `it` blocks / 13 assertions: unset, empty, `'false'`, `'0'`, casing, `'true'`, `'1'`, garbage). The production emergency-disable lever now has regression coverage. This was the only above-LOW item on the gate — it is fully resolved.
- **ROBUST-002 (low) — ✅ CLOSED.** `useRef` guard in `first-run-modal.tsx` (`openEventFired`) — checked-then-set before the telemetry call, correctly defeats StrictMode's double-invoke. The empty-deps `eslint-disable` remains appropriate.
- **TEST-002 (low) — ✅ CLOSED.** New `first-run-modal.test.tsx` (4 tests) — render + title/description + child mount, `modal_opened` on mount, **fires-once under `<StrictMode>`** (directly exercises the ROBUST-002 guard, not just asserts it), and `open=false` renders nothing. `PathChoiceStep` cleanly stubbed so this stays a focused shell test.
- **ROBUST-001 / A11Y-001 / DEBT-001 (all low) — ✅ ACCEPTED DEFERRALS.** Documented in Completion Notes with rationale. These match the ownership I assigned in the v0.5 review (ROBUST-001 + A11Y-001 → B.1's path-choice surface rework; DEBT-001 → B.3's tab wiring). Not changing them in B.0 is the correct call — they are future-story scope, not B.0 debt.

### Regression Check

- `tsc --noEmit` clean for all Story 25.0 files.
- Story 25.0 suites: 8 files / 91 tests green.
- Dev-reported full `tests/unit` run: 353 files / 4653 tests pass (+12 from the two new test files, zero regressions) — consistent with the +2 files / +12 tests delta.

### Updated Compliance Check

- Coding Standards: ✓ — the extraction follows the `lib/onboarding/` convention; pure helper, well-documented.
- Project Structure: ✓
- Testing Strategy: ✓ — the StrictMode test is a notably good pattern (exercises the guard rather than asserting around it).
- All ACs Met: ✓ — AC 30 (feature flag) now has dedicated automated coverage; the only remaining inspection-only ACs are 35/36 (inherently-manual smoke, explicitly deferred).

### Files Modified During Review

None. No QA-side source edits.

### Gate Status

Gate: **PASS** → docs/qa/gates/25.0-path-choice-gate.yml (updated)
Quality score: **95/100**

### Recommended Status

**✓ Ready for Done.** The medium-severity coverage gap is closed, two low items are closed, and the three remaining low items are correctly scoped as B.1/B.3 follow-ups with documented ownership. Gate lifts CONCERNS → PASS. Story owner makes the final call; nothing blocks Done. Reminder: the deferred AC 35 staging smoke + AC 36 post-merge verification still need to run per the documented procedure once this reaches staging/production.
