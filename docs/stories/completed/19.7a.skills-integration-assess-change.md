# Story 19.7a: Skills Integration + `assess_change` (the first real skill)

<!-- Source: Epic 19 — Skills track. docs/prd/epic-19-agent-partner-skills-subagents.md line 126-129 (Story 19.7) — SPLIT 2026-05-26: 19.7a = integration wiring + assess_change migration; 19.7b = gap_analysis + KP-001; 19.7c = registry narrowing (deferred). Absorbs the agent-integration deferred from 19.6 (loader-only re-scope). Deps: 19.6 (loader ✅). -->

## Status

Done

> ✅ **Done 2026-05-26 (owner).** QA gate PASS (quality 95, re-verified); owner accepted. Moved to `completed/`. Live-smoke owner step folded into acceptance.

> 🔬 **Ready for Review 2026-05-26 (James, Dev).** All 4 tasks. Behaviour-preserving migration: `ASSESSMENT_WORKFLOW` literal → `lib/agent/skills/assess_change/PROCEDURE.md` **verbatim** (+ SKILL.md frontmatter `contextTypes:[change]`/declared `tools`, STYLE.md citation+2 examples, CRITERIA.md). `buildSystemPrompt` now injects the **data-driven context-primary skill** (`getPrimarySkillForContext`→`loadSkill`) at the old assessment slot + an `<available_skills>` catalogue (excludes the active primary); the `ASSESSMENT_WORKFLOW` const is **deleted**. New `activate_skill` `'read'` tool (registry 26→27, AUDITOR-kept, `TOOL_CONFIG` chip). **No registry narrowing / no route change** (deferred to 19.7c) — `createAgentTools`/route unchanged but for registering `activate_skill`. **Validation:** full agent suite **229/229** (incl. updated change-context parity assertions: `<skill>`+"Bedömningsflöde", `<assessment_workflow>` gone) + ai-chat components 107/107; `tsc` 0 branch-wide; `eslint` clean. No migration. **Reviewer step:** live smoke — open a change chat, confirm the agent runs the assessment flow as before (now sourced from the skill).

> ✅ **Approved 2026-05-26 (Sarah, PO) — GO, readiness 9/10, confidence High.** Validated; cites re-verified live. **Key ruling — registry narrowing DEFERRED to a new Story 19.7c.** Rationale: it's the only regression-prone piece (it changes change-context tool availability), it's *not urgent* (PRD Risk 4 triggers at ~30 tools; we're at 27), it adds no user value here (the `assess_change` payoff is the file-editable prompt injection, which ships regardless), and — per the 19.6 lesson — the `ALWAYS_AVAILABLE` contract is far better designed against ≥2 skills + a real no-regression harness than in a single-skill vacuum. **19.7a is now behaviour-preserving:** prompt injection + `<available_skills>` + `activate_skill` + the `assess_change` migration (the `ASSESSMENT_WORKFLOW` literal → skill files, verbatim). The `tools` frontmatter is *declared* (forward-compat) but **not enforced** — `createAgentTools`/route are unchanged except registering `activate_skill`. This drops the consequential design call out of 19.7a → clean, low-risk. Ready for `*develop-story`.

## Story

**As** the compliance agent platform,
**I want** the skill-loader wired into prompt assembly + a mid-conversation `activate_skill` switch, with `assess_change` migrated from the hardcoded `ASSESSMENT_WORKFLOW` literal into the first real skill folder,
**so that** domain playbooks become file-edited (not deploy-locked) and load per context — proven end-to-end by a behaviour-preserving migration, with the regression-prone registry narrowing deferred to 19.7c.

## Dependencies

- **Story 19.6** (skill-loader library) — **✅ Done.** Provides `lib/agent/skill-loader.ts` (`listSkills` / `loadSkill` / `getSkillToolWhitelist` / `getPrimarySkillForContext` + `SkillMeta` + `skillFrontmatterSchema`) + the directory convention + `_template`. 19.7a is the first importer. [Source verified]
- **Unblocks 19.7b** (`gap_analysis` + KP-001 — drops a second skill folder into this machinery) and **19.7c** (per-skill registry narrowing — done once ≥2 skills exist + tool count nears the ~30 threshold, with a no-regression harness across both skills).

## Acceptance Criteria

### `assess_change` skill (the migration)

1. New skill `lib/agent/skills/assess_change/` (snake_case dir — matches the skill identifier + the loader's `name===dirname`; no kebab enforcement):
   - `SKILL.md` frontmatter: `name: assess_change`, `description` (English w/ Swedish terms, e.g. "Strukturerad bedömning av en lagändring (ändringsbedömning)"), `contextTypes: [change]` (→ auto-primary for the change context), `tools: [save_assessment, create_task, add_obligation, update_compliance_status, add_context_note]` (**declared forward-compat for 19.7c; not enforced in 19.7a**). Body = short overview.
   - `PROCEDURE.md` = the **verbatim** current `ASSESSMENT_WORKFLOW` literal (`system-prompt.ts:483-520`) — the "verktygsanrop → bedömningstext → rekommendation" workflow, moved unchanged (migration, not rewrite).
   - `STYLE.md` = Swedish citation format (`SFS 2018:218 3 kap. 2 §` / `[Källa: ...]`) + tone rules + **two worked examples** (material change, clarifying change). [Source: PRD 19.7 line 127]
   - `CRITERIA.md` = assessment guardrails (basera på change_context-texten; ange alltid rekommendation + påverkansnivå).
2. **Behaviour parity:** the assembled `loadSkill('assess_change')` body carries the same instructions the `ASSESSMENT_WORKFLOW` literal does today (PROCEDURE.md = the literal verbatim; STYLE/CRITERIA additive). Test asserts the injected change-context content contains the workflow's key directives ("Bedömningsflöde", the four tool-call steps, the four recommendation states Granskad/Åtgärd krävs/Ej tillämplig/Uppskjuten).

### `buildSystemPrompt` integration

3. Extend `buildSystemPrompt` (`system-prompt.ts:230`) and **remove the `ASSESSMENT_WORKFLOW` literal + its `change`-block injection** (`:249-256` and the `:483` const):
   - Inject the **context-primary skill**: `const primary = getPrimarySkillForContext(options.contextType)`; if non-null push `<skill>\n${loadSkill(primary)}\n</skill>` at the slot the `<assessment_workflow>` block occupied (after `<change_context>`, before `<reasoning_guidance>`). For a `change` chat this injects `assess_change` — replacing the deleted literal.
   - Append `<available_skills>`: `listSkills()` as `- {name}: {description}` + one line that the agent can `activate_skill(name)` to load a playbook.
   - (Cross-turn `activate_skill` re-injection / an `activeSkills` system-prompt option is a documented follow-up — the live mid-conversation path is the tool result, AC 4.)
   [Source verified: `system-prompt.ts:230-285`, the change block `:249-256`, the literal `:483`]

### `activate_skill` meta-tool

4. New **`'read'`-tier** tool `activate_skill` in `lib/agent/tools/activate-skill.ts`, `createActivateSkillTool()` (static file reads). Input `{ name: string }` → `loadSkill(name)` body via `wrapToolResponse`; `wrapToolError` on unknown ("okänd färdighet — se `<available_skills>`"). Register in `createAgentTools` (`ToolName` += `activate_skill`; `TOOL_REGISTRY_POLICY: 'read'`) → registry factory **26 → 27**. `TOOL_CONFIG` chip in `chat-message.tsx` ("Aktiverar färdighet"/"Aktiverade färdighet") — **required AC per the 14.28 finding**.

### Non-regression (narrowing deferred → behaviour preserved)

5. **`createAgentTools` and the chat route are unchanged except for registering `activate_skill`.** No `activeSkills` param, no `ALWAYS_AVAILABLE`, no narrowing (→ 19.7c). `buildSystemPrompt` auto-derives the primary injection from `contextType`, so its route call is unchanged too.
6. **Change context: full registry preserved.** Assert the change-context tool set === the prior set **+ `activate_skill`** — nothing removed (since there's no narrowing).
7. **Non-change contexts unchanged.** Removing the `ASSESSMENT_WORKFLOW` literal must not alter global/task/law prompts; their registries are unchanged (+ `activate_skill`, which is `'read'` → AUDITOR keeps it).

### Scope guards

8. **Out of scope:** registry narrowing / `ALWAYS_AVAILABLE` / `activeSkills` (→ **19.7c**); `gap_analysis` + KP-001 + the `add_obligation` framing tweak (→ **19.7b**); cross-turn `activate_skill` re-injection; CI bilingual-lint. No schema change / no migration.

## Tasks / Subtasks

- [x] **Task 1: `assess_change` skill files (AC 1, 2)** — create the folder; move `ASSESSMENT_WORKFLOW` verbatim into `PROCEDURE.md`; `SKILL.md` frontmatter (`contextTypes:[change]`, the declared `tools`); `STYLE.md` (citation + 2 examples); `CRITERIA.md`.
- [x] **Task 2: `buildSystemPrompt` (AC 3, 7)** — primary-skill injection at the assessment-block slot + `<available_skills>`; **delete the `ASSESSMENT_WORKFLOW` const + its change-block injection**.
- [x] **Task 3: `activate_skill` tool (AC 4)** — new `'read'` tool + register in `index.ts` (`ToolName` + policy) + `TOOL_CONFIG` chip.
- [x] **Task 4: Tests (AC 2, 6, 7)** — `system-prompt.test.ts`: change context injects `assess_change` (parity assertions) + the `ASSESSMENT_WORKFLOW` literal / `<assessment_workflow>` tag is GONE + `<available_skills>` present + non-change prompts unchanged. `activate-skill.test.ts`: body / unknown→error. `registry-role-filter.test.ts`: factory 26→27 + `activate_skill` ∈ AUDITOR + change-context set unchanged-but-for-activate_skill. A loader-against-the-real-skill check (`getPrimarySkillForContext('change')==='assess_change'`, `listSkills()` includes it).

## Dev Notes

### Verified integration points (current code)

1. **`ASSESSMENT_WORKFLOW`** literal at `system-prompt.ts:483` (≈38 lines: "## Bedömningsflöde" + verktygsanrop steps 1-4 [get_company_context, search_laws, get_change_details, suggest_followups] + bedömningstext + the four recommendation states + beteenderegler), injected for `change` at `:254-256`. **Move verbatim → `PROCEDURE.md`; delete the const + injection.** [Source verified]
2. **`buildSystemPrompt`** `:230` — `sections: string[]` joined `\n\n`; the change branch (`:249-256`) pushes `<change_context>` then `<assessment_workflow>`. Replace the `<assessment_workflow>` push with the generic primary-skill injection (data-driven via `getPrimarySkillForContext`). [Source verified]
3. **Loader API** (19.6): `getPrimarySkillForContext('change')`→`'assess_change'`, `loadSkill('assess_change')`→body, `listSkills()`→`[assess_change]`. Registry-agnostic. [Source verified — built 19.6]
4. **`createAgentTools`** `lib/agent/tools/index.ts` — `TOOL_REGISTRY_POLICY` 27 entries (26 factory + `web_search`); factory 26 (asserted in `registry-role-filter.test.ts`). 19.7a adds `activate_skill` → factory 27. **No narrowing logic added** (19.7c). [Source verified this session]
5. **Route** `app/api/chat/route.ts:247` (`buildSystemPrompt`) + `:287` (`createAgentTools`) — **both calls unchanged** in 19.7a (buildSystemPrompt auto-derives the primary; no `activeSkills` to pass). [Source verified]
6. **SA-001 note:** `save_assessment` (in the declared whitelist) has its own auto-save bug — tracked separately, NOT in 19.7a scope.

### Pinned design decisions

| Question | Decision |
|---|---|
| Registry narrowing in 19.7a? | **No — deferred to 19.7c** (regression-prone, not urgent at 27<30 tools, better designed against ≥2 skills). |
| `assess_change.tools` whitelist | **Declared** (`save_assessment` + the common follow-up writes) for 19.7c forward-compat; **not enforced** in 19.7a. |
| Skill dir naming | snake_case (`assess_change`) — matches skill ids + loader `name===dirname`. |
| Primary-skill injection slot | The `<assessment_workflow>` slot — after `<change_context>`, before `<reasoning_guidance>`. |
| Behaviour parity | PROCEDURE.md = the literal verbatim → change-context instructions unchanged; STYLE/CRITERIA additive. |
| Route changes | None (buildSystemPrompt auto-derives; createAgentTools only registers `activate_skill`). |

### Testing

- **Framework:** Vitest + RTL where needed. `system-prompt.test.ts` already mocks prisma (change-context loader) — extend it; the loader reads the REAL shipped `lib/agent/skills/assess_change/` (no fixtures needed for these).
- **Highest-value assertions:** (1) change context injects `assess_change`; (2) the `ASSESSMENT_WORKFLOW` literal / `<assessment_workflow>` tag is GONE; (3) parity — the injected body carries the workflow's key directives; (4) `<available_skills>` lists assess_change; (5) **change-context registry === prior + `activate_skill`** (no narrowing regression); (6) global/task/law prompts + registries unchanged; (7) `activate_skill` body/unknown; (8) registry 26→27, `activate_skill` ∈ AUDITOR.

### File locations

**New:** `lib/agent/skills/assess_change/{SKILL,PROCEDURE,STYLE,CRITERIA}.md` (+ `examples/*.md`); `lib/agent/tools/activate-skill.ts`; `tests/unit/agent/tools/activate-skill.test.ts`.
**Modified:** `lib/agent/system-prompt.ts` (primary injection + `<available_skills>`; **delete `ASSESSMENT_WORKFLOW`**); `lib/agent/tools/index.ts` (register `activate_skill` only — no narrowing); `components/features/ai-chat/chat-message.tsx` (`TOOL_CONFIG` chip); `tests/unit/agent/system-prompt.test.ts`; `tests/unit/agent/tools/registry-role-filter.test.ts`. **NOT modified:** `app/api/chat/route.ts` (no narrowing/activeSkills wiring in 19.7a).

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-05-26 | v0.3 | **Implemented → Ready for Review (James, Dev).** Verbatim `ASSESSMENT_WORKFLOW` → `assess_change` skill; `buildSystemPrompt` data-driven primary-skill injection + `<available_skills>`; literal deleted; `activate_skill` `'read'` tool (registry 26→27). No narrowing / no route change. Full agent suite 229/229 + ai-chat 107/107; tsc 0; eslint clean. Live smoke = reviewer step. | James (Dev) |
| 2026-05-26 | v0.2 | **Validated → Approved (GO 9/10; Sarah, PO).** **Ruling: registry narrowing DEFERRED to new Story 19.7c** (regression-prone, not urgent at 27<30 tools, better designed against ≥2 skills + a no-regression harness). 19.7a re-scoped to behaviour-preserving integration: prompt injection + `<available_skills>` + `activate_skill` + the `assess_change` migration. `tools` frontmatter declared-not-enforced; `createAgentTools`/route unchanged but for registering `activate_skill`. Cites re-verified. | Sarah (PO) |
| 2026-05-26 | v0.1 | Initial draft — split from 19.7 (SM Bob): 19.7a = integration + assess_change migration; included registry narrowing live for the change context. *(Narrowing deferred at v0.2.)* | Bob (SM) |

## Dev Agent Record

### Agent Model Used

claude-opus-4-7 (James — Dev)

### Debug Log References

- **Verbatim migration:** `PROCEDURE.md` is the `ASSESSMENT_WORKFLOW` literal copied unchanged (re-read `system-prompt.ts:483-520` to copy exactly). The deleted const would have been unused → `eslint no-unused-vars`, so removal was required, not optional.
- **Injection slot:** moved the primary-skill injection to a generic step after the context if/else (data-driven via `getPrimarySkillForContext(contextType)`), preserving the change-context order (change_context → skill → reasoning). `<available_skills>` excludes the active primary (so a change chat doesn't re-list assess_change; a global chat lists it as activatable).
- **activate_skill** imports `loadSkill` from the 19.6 loader (server-static reads); `'read'` tier; registry 26→27 (the `TOOL_REGISTRY_POLICY = factory ∪ {web_search}` invariant holds — 27 factory + web_search = 28 policy).
- **Validation:** full agent suite 229/229; ai-chat components 107/107; `tsc` 0 branch-wide; `eslint` clean.

### Completion Notes List

**All 4 tasks. Behaviour-preserving — no registry narrowing / no route change (→ 19.7c).**

- **Task 1 (assess_change):** `lib/agent/skills/assess_change/{SKILL,PROCEDURE,STYLE,CRITERIA}.md`. PROCEDURE = the literal verbatim; SKILL frontmatter `contextTypes:[change]` + declared `tools` (forward-compat for 19.7c, not enforced); STYLE = Swedish citation format + tone + 2 worked examples (material / clarifying); CRITERIA = guardrails.
- **Task 2 (`buildSystemPrompt`):** data-driven primary-skill injection (`<skill>`) + `<available_skills>` catalogue (excludes primary, omitted when empty); **deleted the `ASSESSMENT_WORKFLOW` const + its `<assessment_workflow>` injection**. Loader import added.
- **Task 3 (`activate_skill`):** `lib/agent/tools/activate-skill.ts` (`'read'`, `loadSkill(name)`→body / unknown→`wrapToolError`) + registered in `index.ts` (`ToolName`+policy+factory) + `TOOL_CONFIG` chip (Brain icon).
- **Task 4 (tests):** `system-prompt.test.ts` — updated the change-context assertion (`<skill>`+"Bedömningsflöde"+suggest_followups, `<assessment_workflow>` GONE) + a new test (change injects assess_change; global has no `<skill>` but `<available_skills>` lists it). `activate-skill.test.ts` (body / unknown). `registry-role-filter.test.ts` 26→27 + activate_skill ∈ AUDITOR.

**DoD self-check:** ACs 1–8 met; behaviour parity (PROCEDURE verbatim; the four tool steps + four recommendation states present); change-context registry unchanged-but-for-activate_skill (no narrowing); non-change prompts/registries unchanged; `tsc`/`eslint`/suite green. **NOT done (owner step):** live smoke (change chat → assessment flow runs as before from the skill).

### File List

**New:**
- `lib/agent/skills/assess_change/SKILL.md`
- `lib/agent/skills/assess_change/PROCEDURE.md`
- `lib/agent/skills/assess_change/STYLE.md`
- `lib/agent/skills/assess_change/CRITERIA.md`
- `lib/agent/tools/activate-skill.ts`
- `tests/unit/agent/tools/activate-skill.test.ts`

**Modified:**
- `lib/agent/system-prompt.ts` — loader import; primary-skill injection + `<available_skills>`; **deleted `ASSESSMENT_WORKFLOW` const + its injection**
- `lib/agent/tools/index.ts` — register `activate_skill` (`ToolName` + policy `'read'` + factory)
- `components/features/ai-chat/chat-message.tsx` — `activate_skill` `TOOL_CONFIG` chip (Brain icon)
- `tests/unit/agent/system-prompt.test.ts` — change-context parity assertions + the 19.7a injection test
- `tests/unit/agent/tools/registry-role-filter.test.ts` — 26→27 + `activate_skill` ∈ AUDITOR

**NOT modified:** `app/api/chat/route.ts` (no narrowing/activeSkills wiring — that's 19.7c).

## QA Results

### Review — 2026-05-26 (Quinn, Test Architect)

**Gate: PASS** · Quality score **95/100** · Reviewed against all 8 ACs + NFRs. One low-severity finding resolved in-review via active refactoring.

This is a behaviour-preserving migration story, so the review centred on **parity** — verifying the change-context prompt is functionally unchanged after the `ASSESSMENT_WORKFLOW` literal → `assess_change` skill move. Verified independently rather than trusting the green suite.

**Verbatim migration — confirmed (the crux of the story).**
I reconstructed the deleted `ASSESSMENT_WORKFLOW` literal from `git HEAD` and diffed it against `PROCEDURE.md`. The bodies are **byte-for-byte identical** — the only two `diff` lines are extraction artifacts (the `## Bedömningsflöde` header shared the old `const … = \`` declaration line; the trailing TS backtick is syntax). The four tool-call steps (get_company_context / search_laws / get_change_details / suggest_followups) and the four recommendation states (Granskad / Åtgärd krävs / Ej tillämplig / Uppskjuten) all carried over unchanged. ✅

**Injection logic — correct & well-placed.** `getPrimarySkillForContext(contextType)` → `<skill>` push sits in the exact slot the old `<assessment_workflow>` block occupied (after `<change_context>`, before `<reasoning_guidance>`). Change context: primary=assess_change → `<skill>` injected, `otherSkills=[]` → no catalogue (mirrors the old "just the workflow" shape). Non-change: no `<skill>`, `<available_skills>` advertises assess_change + `activate_skill` callable — the intended additive enhancement (AC 3). The `<skill>` body is a **superset** of the old literal (SKILL intro + verbatim Procedure + Style + Criteria) — richer guidance, zero loss.

**Literal fully removed.** Zero runtime references to `ASSESSMENT_WORKFLOW` in `system-prompt.ts` (3 grep hits are all comment lines documenting the move). No `eslint no-unused-vars` orphan.

**Finding 197A-001 (Low, RESOLVED in-review — parity):** The base prompt (`system-prompt.md`, included in *every* context) carried a stale cross-reference — `"…se assessment_workflow när det är aktivt."` — pointing at the renamed tag. Pre-19.7a this name matched the injected `<assessment_workflow>` tag; the rename to `<skill>` left the pointer dangling (content present, but the model was directed to a tag name that no longer exists). **Fixed:** reworded to reference the skill by name (`…se färdigheten assess_change när den är aktiv.`) — deliberately *not* the `<skill>` tag literal, since the base prompt is in all contexts and a literal `<skill>` substring would (correctly) trip the non-change guard test. Re-ran suites green after the fix; the guard test (`globalPrompt not.toContain('<skill>')`) caught my first attempt — exactly the protection that test exists for.

**Requirements traceability (Given-When-Then):**
- **AC 1–2 (skill + parity)** — Given a change chat, When the prompt assembles, Then `<skill>` contains the verbatim Bedömningsflöde + the four steps + four recommendation states. ✅ (`system-prompt.test.ts` parity assertions + the git diff above)
- **AC 3 (integration)** — Given each context type, When assembled, Then change gets `<skill>` (no catalogue), non-change gets `<available_skills>` listing assess_change, literal/`<assessment_workflow>` tag gone. ✅
- **AC 4 (activate_skill)** — Given `activate_skill({name})`, When known→instructions, unknown→`wrapToolError`. ✅ (`activate-skill.test.ts`, runs against the real shipped skill)
- **AC 5–7 (non-regression)** — registry 26→27 (factory) / policy 28; change set === prior + activate_skill (no narrowing); non-change prompts/registries unchanged but for the read-tier activate_skill. ✅ (`registry-role-filter.test.ts`)
- **AC 8 (scope guards)** — no narrowing / no `activeSkills` / no migration / route unchanged. ✅ (verified `route.ts` untouched)

**NFRs:**
- **Security: PASS** — `activate_skill` is `'read'`-tier; `loadSkill` resolves only against the loader's known-skill records (no arbitrary path / traversal); AUDITOR correctly retains it.
- **Reliability: PASS** — unknown skill → graceful `wrapToolError`; `loadSkill` null → `<skill>` simply not pushed (no throw on the hot path).
- **Maintainability: PASS** — data-driven injection; the migration is now a flat file set, not a wall-of-string const.
- **Performance: PASS (note, non-blocking).** `buildSystemPrompt` now does synchronous `loadSkill` file reads on the change-chat path (the old literal was zero-IO, in-memory). `listSkills` metadata is cached (19.6) but `loadSkill` re-reads bodies each call. The reads are 4 tiny local files — negligible against the prompt's async Prisma reads (company profile / change context) and the Anthropic prompt cache (14.26). **Follow-up (carry-over from 19.6):** cache `loadSkill` bodies if skill count or chat QPS grows. Not worth fixing now.

**Test architecture: strong.** 37 directly-relevant tests; parity asserted on *content* (the actual directives), not just tag presence; `activate-skill.test.ts` deliberately runs against the real shipped skill (integration confidence, not a fixture). The non-change `<skill>`-absence guard is the right negative test and demonstrably works.

**Files modified by QA:** `lib/agent/system-prompt.md` (one-line stale-reference fix, 197A-001). No other source changes.

**Reviewer (owner) step before Done:** live smoke — open a change chat and confirm the assessment flow runs as before (now sourced from the skill file). The unit parity proves the content carried over; the live run confirms the model behaves identically. **Recommended status: Ready for Done** (pending that smoke).

Gate file: `docs/qa/gates/19.7a-skills-integration-assess-change.yml`

---

### Re-review — 2026-05-26 (Quinn, Test Architect)

**Gate: PASS (unchanged) · 95/100.** Re-ran the full review independently rather than trusting the prior pass; every claim re-confirmed from source:

- **Verbatim parity** — re-extracted the deleted `ASSESSMENT_WORKFLOW` literal from `git HEAD` and `diff -u` against `PROCEDURE.md` → **byte-identical (no diff)**. The earlier note about "extraction artifacts" no longer applies: with the const-declaration and trailing backtick stripped during extraction, the diff is now clean zero-line.
- **Literal fully removed** — `grep ASSESSMENT_WORKFLOW lib/` → 3 hits, all comments (`:19`, `:286`, `:504`). No runtime reference, no orphan.
- **197A-001 fix holds** — git-HEAD `system-prompt.md:117` confirmed the pre-fix `…se assessment_workflow när det är aktivt.`; current reads `…se färdigheten assess_change när den är aktiv.` — correct, and deliberately not the `<skill>` literal (the non-change guard test would trip).
- **Registry** — `index.ts` factory = 27 tools incl. `activate_skill: 'read'`; `TOOL_REGISTRY_POLICY` = factory ∪ `web_search` = 28; `registry-role-filter.test.ts` asserts AUDITOR retains `activate_skill`. `TOOL_CONFIG.activate_skill` chip present (Brain icon, imported `chat-message.tsx:25`).
- **Suites/quality gates** — agent **229/229**, ai-chat components **107/107**, `tsc --noEmit` exit 0, `eslint` (4 touched files) exit 0. All independently re-run this session.

No new findings. NFRs all remain PASS. The single non-blocking follow-up (cache `loadSkill` bodies if skill count / chat QPS grows) carries forward to the 19.6 loader. **Recommended status unchanged: Ready for Done** (pending the owner live-smoke).
