# Story 9.8: Skolverket (SKOLFS) Amendment Monitoring (Phase 2)

## Status

Done

## Story

**As a** kommun (municipal) compliance owner,
**I want** Skolverket's SKOLFS författningar monitored for amendments, repeals, new in-force laws, **and announced-but-not-yet-effective changes** — the same way SFS laws are,
**so that** when a SKOLFS document on my laglista changes (or is *about* to change on a known date) I get an AI-bedömd notification and can assess its impact — closing the loop that Story 9.7 (ingestion) opened.

## Context

Story 9.7 ingested the **~1,400 gällande** SKOLFS documents as `AGENCY_REGULATION` rows (API enumeration → PDF intake → attribution → chunks/embeddings → linkify) **and captured a per-doc baseline snapshot** in `metadata` (`contentHash`, `validity`, `latestChangeBySkolfsNo`, `amendmentChain[]`, `upcoming[]`, dates). This story adds **forward monitoring**. Per the 9.6 investigation, the downstream half of the pipeline (`ChangeEvent` → notify → `ChangeAssessment` → email/in-app) is **source-agnostic**; the work here is a SKOLFS-specific **detector** (a state-diff over the API) that emits `ChangeEvent`s, plus the small enrich-decoupling 9.6 identified. **SKOLFS detection is structurally easier than SFS or Socialstyrelsen**: the API *hands us* the amendment graph + `validity` flips, so the detector is a JSON state-diff, not a scraper. Per-version historik remains out of scope (deferred). [Source: Story 9.7 Dev Notes → "Monitoring fit"; Story 9.6 source-agnosticism map]

## Acceptance Criteria

1. **Scheduled detector cron.** `app/api/cron/discover-skolfs-changes` runs daily (e.g. `0 6 * * *` UTC) via `vercel.json`, makes **one** `GET /api/statute?size=20000` call for the full current state, loads the per-doc baseline from `metadata`, and classifies each diff. Daily cadence is justified (SKOLFS changes are low-frequency and batch-published; the poll is a single cheap request). The existing `notify-amendment-changes` cron (07:00 UTC) is **reused unchanged**. **The endpoint MUST mirror the existing cron-auth guard** — `CRON_SECRET` env + `authorization: Bearer ${CRON_SECRET}` → 401 on mismatch (do not ship an unauthenticated cron). [Source: vercel.json SFS crons; app/api/cron/discover-sfs-amendments:55,92-94 (CRON_SECRET/Bearer guard)]
2. **Four detection signals**, each producing the right outcome:
   - **NEW_LAW** — a base appears `VALID` that we don't have (brand-new, or `UPCOMING → VALID`): ingest its PDF (Story 9.7 ingester, `--force`/`--filter`) and emit `ChangeEvent(NEW_LAW)`. **PO decision (MVP): NEW_LAW is silent-ingest, NOT notified** — `NEW_LAW` is deliberately absent from `notify-amendment-changes`'s `NOTIFIABLE_CHANGE_TYPES = [AMENDMENT, REPEAL, NEW_RULING]` (route lines 216-220), so emitting it requires **no notify change** and produces no email (matches current SFS behavior). [Source: app/api/cron/notify-amendment-changes route:216-220]
   - **AMENDMENT** — an in-force change: `latestChangeBySkolfsNo` advanced, OR a chain amendment flipped `UPCOMING → VALID`, OR a new `VALID SENASTE_LYDELSE` appeared for the base. Re-ingest the (preferred consolidated) PDF, diff prior `full_text` vs new, emit `ChangeEvent(AMENDMENT)` with `amendment_sfs = <amending SKOLFS no.>`, `changed_sections` (from the API `change` string, e.g. `"ändr. 5, 6 §§"`), and `ai_summary` from the diff. **AMENDMENT and REPEAL are already in `NOTIFIABLE_CHANGE_TYPES`** → they notify with no notify change.
   - **REPEAL** — a base flips `VALID → EXPIRED`: set `status = REPEALED`, emit `ChangeEvent(REPEAL)`.
   - **UPCOMING (kommande ändring)** — a `VALID` base has an `UPCOMING` amendment in its chain that we have **not yet emitted an event for**: emit a "coming change" event carrying the amendment's effective date (in `ai_summary`, per AC 2a); **no re-ingest yet**. When that date passes and the amendment flips `VALID`, the next run picks it up as an AMENDMENT signal. **First-run backfill (important):** Story 9.7 captures `skolfs.upcoming[]` into the baseline but emits **no** `ChangeEvent`s (9.7 AC 13), so **9.8's first detector run must emit UPCOMING events for the pending amendments already in the baseline** (e.g. the ~18 July-2026 ämnesplan amendments) — otherwise the initial cohort gets no proactive notice and jumps straight to AMENDMENT when effective. Dedup (AC 5) makes this one-time, not per-run. **⚠️ This 4th signal needs schema + notify work (see AC 2a)** — there is no UPCOMING `ChangeType` today and the effective date has no column to flow through. [Source: 9.7 AC 9 (baseline `upcoming[]`) + AC 13 (no events emitted); verified API `validity`/`relatedDocumentMetadata` semantics; ChangeType enum below]
2a. **UPCOMING representation (PO decision — CONFIRMED by Alexander 2026-06-25: keep UPCOMING in 9.8, accept the migration — requires a migration):** the verified `ChangeType` enum is `{ NEW_LAW, AMENDMENT, REPEAL, METADATA_UPDATE, NEW_RULING }` (schema lines 695-701) — there is **no UPCOMING value**, so the "ChangeType already supported"/"no migration" framing of the earlier draft was wrong. **Decision:** add a new `ChangeType.UPCOMING_AMENDMENT` value via a **hand-off migration** (migrations are run manually — hand the user the command, never apply it), and add it to `NOTIFIABLE_CHANGE_TYPES` so kommande-ändringar notify. The **effective date is carried inside `ai_summary` text** (e.g. `"Ändring (SKOLFS 2026:23) träder i kraft 2026-07-01"`) because `ChangeEvent` has **no `effective_date` column** — this avoids a second column migration. [Source: prisma/schema.prisma:695-701 (ChangeType); user memory — migrations manual]
3. **Pending changes are detected via the chain, not `latestChangeBySkolfsNo`.** The detector scans `relatedDocumentMetadata` for `validity == "UPCOMING"` entries (the base's `latestChangeBySkolfsNo` reflects only the last *in-force* change and ignores pending ones — verified on the 20 amended-but-unconsolidated bases). [Source: Story 9.7 AC9; verified 2026-06-25]
4. **Content-hash guard before any re-ingest.** Even when the API signals an AMENDMENT, re-fetch the PDF and `computeContentHash`; only re-ingest (pay for Claude) if the hash changed vs the stored baseline. No hash change → metadata-only snapshot update. [Source: lib/agency/agency-pdf-registry.ts `computeContentHash`; Story 9.7 AC9 baseline]
5. **ChangeEvent dedup / idempotency.** Events are keyed on `(document_id, amendment_sfs)` — for UPCOMING, `amendment_sfs` = the **pending amendment's** SKOLFS number (there is no `effective_date` column to key on) — so a doc that sits in a changed state across runs (including the first-run UPCOMING backfill) emits at most one event per amendment; the snapshot in `metadata` advances to the new "last seen" state each run. [Source: Story 8.21 change-event-deduplication; app/api/cron/discover-sfs-amendments idempotency]
6. **Notification + assessment reuse — core path unchanged, with one known limitation.** `notify-amendment-changes` picks up the new `ChangeEvent`s (no content_type filter; recipients via `document_id → LawListItem → workspace`; title/`ai_summary`) and per-workspace `ChangeAssessment` works as-is — that path is genuinely source-agnostic (route lines 223-237, 334). **Limitation (verified):** the email's structured `effectiveDate` / `pdfUrl` / section-table fields are **enriched from `AmendmentDocument` via `sfs_number = ce.amendment_sfs`** (route lines 282-308); a SKOLFS event has no `AmendmentDocument` row, so `findUnique` returns null and those fields stay empty. **Mitigation:** carry the human-readable effective date + changed sections inside `ai_summary` (per AC 2a), which DOES render in both the email and the Ändringar tab. A structured SKOLFS enrichment branch is a future enhancement, not MVP. **Task 4 verifies** e2e: seed a workspace with a SKOLFS doc on its laglista, fire a synthetic ChangeEvent, confirm notification + assessment. [Source: app/api/cron/notify-amendment-changes route:216-237,282-308,334; lib/notifications/recipient-resolution.ts]
7. **Enrich decoupling (small).** `enrich-amendment-summaries` selects via `amendment_documents.sfs_number = ce.amendment_sfs`; a SKOLFS ChangeEvent has no such row. Set `ChangeEvent.ai_summary` **directly in the detector** (the diff is in hand) — preferred over generalizing the SFS-coupled join. [Source: app/api/cron/enrich-amendment-summaries — SFS-coupled join; Story 9.6 AC5]
8. **Re-ingest preserves attribution.** Re-ingestion on `--force` must not clobber `regulatory_body` / `agency_prefix` / `metadata.issuedBy` (esp. the Regeringen-issued läroplaner). Carry issuer through or re-derive from the API. [Source: Story 9.7 AC8; Story 9.6 AC6]
9. **Vercel runtime resilience.** `maxDuration = 300`; the diff is fast but a large same-day batch (e.g. the verified 2026-07-01 cluster of ~18 ämnesplan amendments) may exceed one slot for re-ingest. Process until a timeout buffer, then stop — the snapshot diff is idempotent so the next run drains the rest (same pattern as `discover-sfs-amendments`). Log each run to `CronJobRun` (`items_processed`/`items_failed`). [Source: app/api/cron/discover-sfs-amendments timeout handling; prisma/schema.prisma#CronJobRun]
10. **No regression:** lint/typecheck/build green; full unit suite green — including the new `classifySkolfsDiff` unit tests (AC 2 / Task 1).

## Tasks / Subtasks

- [x] **Task 1: Detector cron + state diff** (AC 1, 2, 3, 4) — `app/api/cron/discover-skolfs-changes`: `CRON_SECRET`/`Bearer` auth guard; one `/api/statute` poll → per-doc baseline load → classify into {NEW_LAW, AMENDMENT, REPEAL, UPCOMING}; pending via chain `UPCOMING`; `vercel.json` entry (`0 6 * * *`). Content-hash guard before re-ingest = Task 2. **Done 2026-06-26.**
  - [x] **Extracted the signal-classification into a pure, testable function** `classifySkolfsDiff(baseline, current, {firstRun}) → SkolfsSignal[]` in `lib/agency/skolfs-change-detection.ts` and **unit-tested** it (18 tests) with fixtures covering every transition: new VALID base (NEW_LAW), `latestChangeBySkolfsNo` advance / new VALID `SENASTE_LYDELSE` / brand-new immediately-VALID amendment / UPCOMING→VALID flip (AMENDMENT), `VALID→EXPIRED` (REPEAL), VALID base gains chain `UPCOMING` not in baseline + first-run baseline backfill (UPCOMING), and **no-change** (no event) + cross-run stability. Pure logic, NOT in the route handler. (AC 2, 3)
  - [x] Shared API client + pure projection `lib/agency/skolfs-api.ts` (`fetchSkolfsStatute` + `buildCurrentSnapshots`) — reuses the 9.7 enumerator's corpus rule (VALID GRUNDFORFATTNING + ALLMANNA_RAD_OVRIGT, consolidated-by-base) so the detector doesn't fork the pipeline; 3 projection unit tests.
- [x] **Task 2: Re-ingest + diff + emit ChangeEvent** (AC 2, 4, 7, 8) — **Done 2026-06-27.** Architecture decision (Alexander, "which is best?"): **design A — flag + out-of-band re-ingest**, because a Vercel cron caps at `maxDuration=300s` and the large läroplaner re-ingest needs ~15 min (9.7 Task 5) → inline normalize is impossible for the highest-value docs. So:
  - [x] `lib/agency/skolfs-events.ts` — `emitSkolfsChangeEvent(docId, signal)`: dedup'd `ChangeEvent` (key `(document_id, amendment_sfs)`, AC 5) with **`ai_summary` set in-detector** (AC 7 — bypasses the `AmendmentDocument`-coupled `enrich-amendment-summaries` join) + `changed_sections` from the API `change` string; advances `last_change_*`. Plays the SFS **crawler** role; the out-of-band re-ingest's `detectChanges` later **enriches** this same event with the text `diff_summary` (reuses `change-detection.ts:217-250`). 7 unit tests.
  - [x] Per-candidate enrichment `fetchAmendmentDetails` (`skolfs-api.ts`) — one `GET /api/document/{TYPE}/{nr}` per *signalled* base (few/day) to fill `change`/`effectiveDate`; `enrichSignal` (pure) rebuilds the Swedish `ai_summary`. Route wires enrich→emit for AMENDMENT with a 30s timeout buffer (drains over runs, AC 9).
  - [x] **AC 4 (hash guard):** chosen **Option 2 — normalize-then-dedup on the HTML `contentHash`**, which lives in the out-of-band re-ingest (`--force` upsert). In-cron emission is gated on the API `ANDRINGSFORFATTNING` (the authoritative change-of-record) + dedup; the hash guard suppresses a redundant write/version at re-ingest. **Deviation from AC-4's literal "before re-ingest" wording is intentional** — `computeContentHash` hashes post-Claude HTML, so it cannot gate Claude pre-normalize; documented.
  - [x] **AC 8 (attribution):** the 9.7 ingester re-derives `regulatory_body` via `resolveRegulatoryBody(documentNumber, issuedBy)` on every `--force` upsert (verified `scripts/ingest-skolverket-foreskrifter.ts:456,501`) → attribution preserved by the existing re-ingest path; no clobber.
  - [x] **NEW_LAW** is silent-ingest (AC 2): no `LegalDocument` row exists yet (FK) so no in-cron `ChangeEvent`; counted + logged for out-of-band ingest, not notified.
- [x] **Task 3: REPEAL + UPCOMING handling** (AC 2, 2a) — **Done 2026-06-27. Migration applied by Alexander.**
  - [x] **Migration `20260627120000_add_upcoming_amendment_change_type`** — `ALTER TYPE "ChangeType" ADD VALUE 'UPCOMING_AMENDMENT'` + schema.prisma enum value. Hand-off (manual-migrations rule); `prisma generate` synced the client. **User confirmed `migrate` ran successfully.**
  - [x] `UPCOMING_AMENDMENT` added to `NOTIFIABLE_CHANGE_TYPES` (notify route) → kommande-ändringar notify. `skolfsChangeType` now maps it; `changeTypeToNotificationType` → `AMENDMENT_REMINDER`; `notificationBodyForChangeType` + 6 UI `Record<ChangeType,…>` label/variant maps + `reader-utils` extended (exhaustive-map fan-out from the new enum value).
  - [x] **REPEAL:** route flips `status → REPEALED` then emits `ChangeEvent(REPEAL)`.
  - [x] **UPCOMING:** emit coming-change event (effective date + sections carried in `ai_summary`, no `effective_date` column), **no re-ingest**. First-run backfill works through the normal classify path — the statute poll surfaces each base's chain `UPCOMING` entries, and `firstRun=true` (zero prior SUCCESS `CronJobRun`) emits them; dedup on `(document_id, amendment_sfs)` makes it one-time.
- [x] **Task 4: Verify notify + assess reuse** (AC 6) — **Done 2026-06-27 via a guarded live first-run against the real DB** (Alexander-approved). Extracted the detector core to `lib/agency/skolfs-detector.ts` (shared by the cron route + `scripts/run-skolfs-detector-once.ts`, which defaults to dry-run, `--commit` writes). **Dry-run preview:** firstRun=true, polled 2557 bases, 1398 baselines, **45 UPCOMING** (= 9.7's "45 with UPCOMING" exactly), 0 AMENDMENT, 0 REPEAL, 2 NEW_LAW (the 2 un-ingested corpus docs — silent). **Commit:** 45 events created, 0 failed. **Idempotency:** re-run → 0 created / 45 dup (AC 5 proven live). **Notify reuse verified:** all 45 are `AGENCY_REGULATION`/`notification_sent=false` and matched by the `NOTIFIABLE_CHANGE_TYPES` query → the notify cron will process them; sample `ai_summary` = "Kommande ändring av SKOLFS 2024:616 genom 2024:617 träder i kraft 2027-07-01 (upph. 8 §)" (enriched effective date + sections, in-detector — AC 6/7 confirmed).
- [x] **Task 5: Resilience + logging** (AC 9) — **Done 2026-06-27.** `maxDuration=300`; 30s `TIMEOUT_BUFFER_MS` → stops emitting before the slot ends, idempotent diff drains the rest next run. Per-signal `try/catch` (fail-soft — one bad doc never aborts the run). `CronJobRun` start/complete/fail via `job-logger`, with `items_processed`/`items_failed` + the full stats breakdown in `metadata`. Dedup proven across runs by the `emitSkolfsChangeEvent` unit tests + the idempotent classifier.
- [~] **Task 6: Regression** (AC 10) — **lint 0 errors** (29 pre-existing warnings in unrelated perf/redis files); **typecheck clean** across all source (only stale `.next/dev/types` from a running dev server); **SKOLFS unit tests 32/32**; unit suites for every touched file green (notifications/sync 147/147). **18 full-suite failures are all pre-existing & unrelated to 9.8:** 3 stale model-version assertions (9.7 changed `AGENCY_DEFAULT_MODEL`→`claude-sonnet-4-6`, tests not updated) + 15 DB/Redis-backed integration tests that need live services. `pnpm build` deferred (a dev server holds `.next`; per the technical constraint). New `classifySkolfsDiff` unit tests present + green.

## Dev Notes

### Source-agnosticism map (from 9.6 — the key reuse fact)

| Stage | Verdict |
|---|---|
| `ChangeEvent` model | **Source-agnostic** — `ContentType.AGENCY_REGULATION` exists; global, keyed on `document_id` |
| `notify-amendment-changes` | **Source-agnostic** — no content_type filter; `document_id → LawListItem → workspace` |
| `ChangeAssessment` | **Source-agnostic** — hangs off any ChangeEvent |
| Discovery | **Source-specific** — build the SKOLFS detector (this story) |
| `enrich-amendment-summaries` | **SFS-coupled join** — bypass by setting `ai_summary` in detector |
| version-history / historik | **SFS-coupled** — out of scope (deferred) |

[Source: docs/stories/9.6.socialstyrelsen-amendment-monitoring.md]

### Why SKOLFS detection is the robust kind (vs SFS / Socialstyrelsen)

SFS/Socialstyrelsen detection *reconstructs* "what changed" by crawling an index/HTML and classifying. SKOLFS exposes the answer: `validity` flips and `latestChangeBySkolfsNo` advancing **are** the signal, and `relatedDocumentMetadata` carries the full amendment graph with per-amendment `validity`/`effectiveDate`/`change`. The detector is a **state-diff over one JSON document** the cron already fetches — no scraping, no LLM classification of document type. [Source: verified API shape 2026-06-25; Story 9.7 Dev Notes]

### What the detector must produce to reuse the rest

A global `ChangeEvent` against the base `AGENCY_REGULATION` document, `change_type ∈ {AMENDMENT, REPEAL, NEW_LAW}` (+ an UPCOMING variant per PO), `notification_sent: false`, with `ai_summary` populated (the old-vs-new consolidated-text diff, or a templated "ändring träder i kraft <date>" for UPCOMING). That alone makes notify + assess work unchanged.

### The 2026-07-01 worked example (verified data)

18 ämnesplaner are `VALID` now, each with one `UPCOMING` amendment effective `2026-07-01`. Expected behavior: 9.7 ingest captures these into each base's `skolfs.upcoming[]` baseline but emits **no** events; **9.8's first detector run backfills the kommande-ändring (`UPCOMING_AMENDMENT`) events** (deduped per AC 5, so one-time). On 2026-07-01 the cron sees `UPCOMING → VALID`, Skolverket publishes each `SENASTE_LYDELSE`, the detector re-ingests (hash changed) + emits AMENDMENT events; `notify-amendment-changes` mails affected workspaces. The batch may span >1 cron slot → drains over successive runs (AC 9). [Source: verified amendment-chain inspection 2026-06-25; 9.7 AC 9/13]

### Constraints

- Reuse the 9.7 enumerator/ingester; **don't fork the pipeline.**
- Cron on Vercel (`maxDuration` 300s) — batch/limit per run; large läroplaner re-ingests are slow (carry the 9.5/9.7 large-doc + pool considerations). [Source: Story 9.6 AC7 pool note; Story 9.7 AC6a]
- **One migration IS required** (corrected from draft 0.1): the `ChangeType.UPCOMING_AMENDMENT` enum value for the 4th signal (AC 2a). `ChangeEvent`/`ContentType.AGENCY_REGULATION`/`CronJobRun` all already exist — only the enum value is new. Migrations run **manually** → hand the user the command, never apply it. UPCOMING is confirmed in scope (AC 2a), so the migration is on the critical path. [Source: prisma/schema.prisma:695-701; user memory — migrations manual]

### Testing requirements

- **`classifySkolfsDiff` unit tests** (Task 1) — the pure signal-classifier is the core logic and must be tested in isolation, not buried in the route handler. Fixtures: new `VALID` base → NEW_LAW; `latestChangeBySkolfsNo` advance / new `VALID SENASTE_LYDELSE` → AMENDMENT; `VALID → EXPIRED` → REPEAL; `VALID` base gains a chain `UPCOMING` not in baseline → UPCOMING; **baseline `upcoming[]` present on first run → UPCOMING backfill**; identical baseline vs. current → **no event**. Plus a dedup test: same diff across two runs → exactly one event.
- **Notify/assess integration** (Task 4) — seed a workspace with a SKOLFS doc on a laglista, fire a synthetic `ChangeEvent`, assert in-app notification + email + `ChangeAssessment`; assert the `ai_summary`-carried effective date renders and that the `AmendmentDocument`-enriched email fields are (expectedly) empty for SKOLFS.
- No prose snapshot tests. Full regression per AC 10. [Source: 9.5/9.7 testing approach]

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-25 | 0.1 | Initial draft — Phase 2 (forward monitoring) split from Story 9.7. Grounded in the verified SKOLFS API semantics (validity flips, `latestChangeBySkolfsNo`, `relatedDocumentMetadata` UPCOMING chain) and the 9.6 source-agnosticism map (notify/assess reuse; detector + enrich-decoupling are the work). Adds a 4th signal — **UPCOMING / kommande ändring** — unique to SKOLFS's forward-dated API. Content-hash guard + ChangeEvent dedup + incremental resilience carried from 9.5/9.6/8.21 findings. | Sarah (PO) |
| 2026-06-25 | 0.2 | **SM draft-rigor pass — corrected two schema/notify inaccuracies.** (1) The UPCOMING signal had no `ChangeType` value: corrected the false "no migration / ChangeType already supported" claim → added **AC 2a** (PO decision: new `ChangeType.UPCOMING_AMENDMENT` via hand-off migration + add to `NOTIFIABLE_CHANGE_TYPES`; effective date carried in `ai_summary` since there's no `effective_date` column) and fixed Constraints. (2) "Notification reuse unchanged" overstated: the email's `effectiveDate`/`pdfUrl`/section-table enrichment is SFS-coupled (`AmendmentDocument` join, route 282-308) and won't populate for SKOLFS → AC 6 now states the limitation + `ai_summary` mitigation; "Verified e2e" softened to Task-4 verification. NEW_LAW confirmed silent-ingest (not in `NOTIFIABLE_CHANGE_TYPES`) — no notify change. All other citations verified accurate by SM. | Sarah (PO) / Bob (SM) |
| 2026-06-25 | 0.3 | **PO `*validate-story-draft` pass — GO (with should-fix applied).** Independently re-verified every load-bearing citation against disk: `ChangeType` enum (695-701, no UPCOMING — confirms AC 2a migration), `NOTIFIABLE_CHANGE_TYPES=[AMENDMENT,REPEAL,NEW_RULING]` (216-220), notify `AmendmentDocument` enrich join (283-300), `enrich-amendment-summaries` join (101) + `WHERE ai_summary IS NULL` (103), `change-detection.ts` helpers `detectChanges`/`createNewLawEvent`/`createRepealEvent` (150/279/314, re-exported in lib/sync/index.ts), `CronJobRun` (1787-1795), discover-sfs `maxDuration=300`/`TIMEOUT_BUFFER_MS`/drain (53/59/262). **Anti-hallucination: clean.** Applied two should-fixes: cron-auth guard requirement (AC 1/Task 1, mirroring discover-sfs:55,92-94) and a mandatory `classifySkolfsDiff` unit-test for the signal classifier (Task 1, AC 10). Readiness 8/10. Open product decision (non-blocking): confirm the UPCOMING signal stays in 9.8 (adds the enum migration) vs. split to a 9.9 follow-up. | Sarah (PO) |
| 2026-06-25 | 0.4 | **Cross-story consistency fix + UPCOMING decision ratified.** Resolved a contradiction with 9.7: the draft worked-example claimed "9.7 emits the kommande-ändring events" but 9.7 AC 13 emits none. Fixed → **9.8's first detector run backfills UPCOMING events from each base's baseline `skolfs.upcoming[]`** (AC 2, Task 3, worked example) so the initial ~18-doc July-2026 cohort gets proactive notice instead of jumping straight to AMENDMENT. AC 5 dedup key corrected for UPCOMING → `(document_id, pending amendment_sfs)` (no `effective_date` column to key on). Added a **Testing requirements** subsection (classifier fixtures incl. backfill + no-change; notify/assess integration). UPCOMING-in-9.8 + migration **confirmed by Alexander** (AC 2a). | Sarah (PO) |

## Dev Agent Record

### Agent Model Used

claude-opus-4-8[1m] (James / BMad dev agent)

### Debug Log References

- **QA fixes (2026-06-27):** `pnpm vitest run` over the 5 touched suites (skolfs-change-detection, skolfs-api, skolfs-events, skolfs-detector, amendment-notifications) → **76/76 pass**. `pnpm typecheck` → clean (excl. stale `.next/dev/types` from the live dev server). `pnpm lint` → 0 errors on touched files. `pnpm build` not run — `ps aux` confirms a `next dev --turbopack` (pid 18135) holds `.next`; deferred to avoid corrupting the running server.
- **Task 1 (2026-06-26):** Built the pure classifier `classifySkolfsDiff` + the shared API projection, then the detector route scaffold. Design decision: the detector works primarily off the single `/api/statute` poll (AC 1), which carries `validity`/`documentType`/`baseSkolfsNumber` for all ~5,400 docs but **not** `latestChangeBySkolfsNo` or per-amendment `change`/`effectiveDate` (those need per-doc fetches, deferred to Task 2 enrichment of confirmed candidates). To stay correct from statute-only data, added a 4th AMENDMENT trigger to the classifier — *any VALID amendment now in the chain that was absent from the baseline chain* — which generalizes the AC-stated triggers (latestChange advance / UPCOMING→VALID flip) and catches a brand-new immediately-in-force amendment even when `latestChangeBySkolfsNo` is unpopulated. First-run detection = zero prior SUCCESS `CronJobRun` rows for `discover-skolfs-changes` → triggers the one-time UPCOMING baseline backfill (deduped downstream per AC 5). `pnpm vitest` 21/21 pass (18 classifier + 3 projection); typecheck clean for all new files (the only `tsc` errors are stale `.next/dev/types/*` generated by a running dev server, unrelated).

### Completion Notes List

- **QA fixes applied (2026-06-27, gate CONCERNS → re-review):** Closed the 3 testable gate findings. **TEST-001** — added 3 cross-module chain-consistency tests (`skolfs-change-detection.test.ts`) proving the 9.7 baseline `amendmentChain` and the live statute projection share a consistent number-set: no false AMENDMENT when they match, a positive control (a genuinely new VALID amendment → AMENDMENT), and REPEAL on a VALID→EXPIRED flip. **TEST-002** — added 5 tests to `amendment-notifications.test.ts` proving an `AGENCY_REGULATION`/`UPCOMING_AMENDMENT` event flows end-to-end through `createChangeNotifications` → `AMENDMENT_REMINDER` notifications per recipient, with the `amendment_reminder_enabled` preference gate honored (and confirmed the preference field is mapped in `NOTIFICATION_TYPE_TO_PREFERENCE`). **TEST-003** — added `skolfs-detector.test.ts` (3 tests, mocked prisma + injected fetch): dry-run writes nothing, commit emits a dedup'd enriched AMENDMENT, and idempotency. **BUILD-001** — `pnpm build` deferred: a `next dev --turbopack` process is actively holding `.next`; running build would corrupt the running dev server (per the technical constraint). Typecheck is clean across all source (the strongest available proxy); run `pnpm build` once the dev server is stopped. Validation: 76/76 across the 5 touched suites, typecheck + lint clean. Status → Ready for Review (QA re-review requested for the gate).
- **Task 4 DONE (2026-06-27):** Verified live (Alexander-approved guarded first-run). 45 UPCOMING events written, idempotent on re-run (0 created / 45 dup), all matched by the notify pipeline's `NOTIFIABLE_CHANGE_TYPES` query, `ai_summary` correctly enriched in-detector. The detector core was extracted to `lib/agency/skolfs-detector.ts` (shared by route + runner script) during this task. **All 6 tasks complete.** Note: the 2 NEW_LAW (`SKOLFS 2004:1`, `2005:1`) are the corpus docs not present in the 1,398 ingested baselines → out-of-band ingest backlog, not a detector defect.
- **Tasks 3 + 5 + 6 DONE (2026-06-27):** REPEAL (status flip + event) and UPCOMING_AMENDMENT (notifying, migration-gated, first-run backfill via the normal classify path) wired into the detector; `ChangeType.UPCOMING_AMENDMENT` migration authored + **applied by Alexander**; `NOTIFIABLE_CHANGE_TYPES` + the exhaustive `Record<ChangeType,…>` maps across 6 UI files + `reader-utils` + `amendment-notifications` (→ `AMENDMENT_REMINDER`) extended for the new enum value. Resilience: timeout-buffer drain + per-signal fail-soft + `CronJobRun` logging. Regression: lint/typecheck clean, 32/32 SKOLFS unit tests, no new failures (the 18 full-suite failures are pre-existing env/model-version issues).
- **Task 2 DONE (2026-06-27):** AMENDMENT detection → dedup'd `ChangeEvent` with in-detector `ai_summary`, via the **flag + out-of-band re-ingest** design (the only viable shape under the 300s cron cap given ~15-min läroplaner re-ingests). `emitSkolfsChangeEvent` is the crawler half; the existing `detectChanges` enriches it with the text diff at re-ingest. Per-candidate enrichment fetch fills the API `change`/`effectiveDate` the cheap statute poll omits. Hash guard = Option 2 (post-normalize HTML hash at re-ingest). NEW_LAW silent. REPEAL + UPCOMING (migration-gated) = Task 3. 32/32 SKOLFS unit tests pass; typecheck clean for all new files. **Out-of-band re-ingest runbook:** the SKOLFS AMENDMENT `ChangeEvent`s (those with `diff_summary` null) are the work queue → re-enumerate the changed bases + `pnpm tsx scripts/ingest-skolverket-foreskrifter.ts --force --filter "SKOLFS <nr>"`, which re-derives attribution, applies the hash guard, and enriches the event via `detectChanges`.
- **Task 1 DONE (2026-06-26):** Detector cron + state-diff classifier. The classifier and statute→snapshot projection are pure and unit-tested in isolation (the AC mandate). The route (`app/api/cron/discover-skolfs-changes`) does auth guard (mirrors `discover-sfs-amendments`), one statute poll, projection, baseline load from `metadata.skolfs`, classification, and `CronJobRun` logging of the signal breakdown. Re-ingest + content-hash guard + `ChangeEvent` emission are wired in Tasks 2/3 (marked with TODOs in the route). `vercel.json` daily cron added at `0 6 * * *`. **No migration in Task 1** — the `ChangeType.UPCOMING_AMENDMENT` value (AC 2a) is authored as a hand-off migration in Task 3.

### File List

**New:**
- `lib/agency/skolfs-change-detection.ts` (pure `classifySkolfsDiff` + `enrichSignal` + `snapshotFromBaselineMetadata` + types)
- `lib/agency/skolfs-api.ts` (`fetchSkolfsStatute` + pure `buildCurrentSnapshots` + `fetchAmendmentDetails` enrichment)
- `lib/agency/skolfs-events.ts` (`emitSkolfsChangeEvent` dedup'd emitter + `skolfsChangeType`/`skolfsAmendmentSfs`)
- `lib/agency/skolfs-detector.ts` (`runSkolfsDetector` core — shared by the cron route + runner script; `commit` dry-run flag)
- `app/api/cron/discover-skolfs-changes/route.ts` (detector cron — auth + `CronJobRun` logging, delegates to `runSkolfsDetector`)
- `scripts/run-skolfs-detector-once.ts` (one-off runner — dry-run default, `--commit` writes; used for the Task 4 live verification)
- `tests/unit/lib/agency/skolfs-change-detection.test.ts` (25 tests — incl. QA TEST-001 chain-consistency)
- `tests/unit/lib/agency/skolfs-api.test.ts` (3 tests)
- `tests/unit/lib/agency/skolfs-events.test.ts` (7 tests)
- `tests/unit/lib/agency/skolfs-detector.test.ts` (QA TEST-003 — 3 tests, mocked prisma + injected fetch)
- `prisma/migrations/20260627120000_add_upcoming_amendment_change_type/migration.sql` (`ChangeType.UPCOMING_AMENDMENT`)

**Modified:**
- `vercel.json` (+`discover-skolfs-changes` daily cron `0 6 * * *`)
- `prisma/schema.prisma` (+`ChangeType.UPCOMING_AMENDMENT` enum value)
- `app/api/cron/notify-amendment-changes/route.ts` (+`UPCOMING_AMENDMENT` in `NOTIFIABLE_CHANGE_TYPES`)
- `lib/notifications/amendment-notifications.ts` (`UPCOMING_AMENDMENT` → `AMENDMENT_REMINDER` + body copy)
- `lib/agent/tools/reader-utils.ts`, `components/features/changes/change-row.tsx` (+badge variant), `components/features/changes/change-assessment-modal.tsx`, `components/features/ai-chat/details/assessment-detail.tsx`, `components/features/dashboard/change-assessment-view.tsx`, `components/features/dashboard/change-picker.tsx` (exhaustive `ChangeType` label/variant maps + `UPCOMING_AMENDMENT`)
- `tests/unit/lib/notifications/amendment-notifications.test.ts` (QA TEST-002 — +5 UPCOMING_AMENDMENT notify cases incl. end-to-end + preference gate)

### Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-26 | 0.5 | Task 1 implemented: pure `classifySkolfsDiff` classifier (5 transitions + statute-only-robust 4th AMENDMENT trigger) with 18 unit tests; shared `skolfs-api` poll + projection (reuses 9.7 corpus rule) with 3 tests; detector cron route (auth/poll/classify/log) + `vercel.json` daily entry. Re-ingest/emit deferred to Tasks 2/3. Status → InProgress. | James (dev) |
| 2026-06-27 | 0.6 | Task 2 implemented: `emitSkolfsChangeEvent` (dedup'd, in-detector `ai_summary`, crawler→enrich reuse) + `fetchAmendmentDetails`/`enrichSignal` enrichment + route enrich/emit-AMENDMENT wiring. Architecture: flag + out-of-band re-ingest (300s cron cap vs ~15-min läroplaner). Hash guard = Option 2 (post-normalize, at re-ingest). 32/32 SKOLFS unit tests; typecheck clean. | James (dev) |
| 2026-06-27 | 0.7 | Tasks 3+5+6: `ChangeType.UPCOMING_AMENDMENT` migration (applied) + enum fan-out across notify/UI/agent maps; REPEAL status-flip+emit; UPCOMING emit + first-run backfill; timeout-buffer drain + per-signal fail-soft + `CronJobRun` logging. Regression: lint/typecheck clean, SKOLFS 32/32, no new failures. Task 4 (integration verify) + first live run pending. | James (dev) |
| 2026-06-27 | 1.0 | Task 4 + detector-core extraction (`runSkolfsDetector`, shared by route + runner). Guarded live first-run committed 45 UPCOMING events (idempotent on re-run; notify-query pickup + enriched ai_summary verified). **All 6 tasks complete → Ready for Review.** | James (dev) |
| 2026-06-27 | 1.1 | QA fixes (gate CONCERNS): closed TEST-001 (chain-consistency, 3 tests), TEST-002 (UPCOMING notify end-to-end + preference gate, 5 tests), TEST-003 (`runSkolfsDetector` orchestration, 3 tests). BUILD-001 deferred (dev server holds `.next`; typecheck-clean proxy). 76/76 across touched suites; typecheck + lint clean. QA re-review requested. | James (dev) |

## QA Results

### Review Date: 2026-06-27

### Reviewed By: Quinn (Test Architect)

### Gate: **CONCERNS** → `docs/qa/gates/9.8-skolverket-skolfs-amendment-monitoring.yml` · Quality score **80**

**Verdict:** High-quality, well-architected implementation. The pure classifier / statute projection / emitter are cleanly separated and unit-covered (32/32), and the no-change + UPCOMING-backfill + dedup paths are **live-verified against the real DB** (45 events written, idempotent on re-run, notify-query pickup + enriched `ai_summary` confirmed). CONCERNS (advisory, not blocking) for three verification gaps, below.

### Code Quality Assessment

Excellent structural discipline: detection logic is a pure, fixture-tested function (`classifySkolfsDiff`) rather than buried in the route; the statute→snapshot projection reuses the 9.7 corpus rule rather than forking it; the emitter plays the SFS crawler role so the existing `detectChanges` enriches it at re-ingest. The out-of-band re-ingest design is the correct call under the 300s cron cap (vs ~15-min läroplaner normalize). Migration handled as a proper hand-off; enum fan-out across notify/UI/agent maps is complete and runtime-safe (`generate-summaries` excludes our events via `ai_summary:null`).

### Requirements Traceability (AC → coverage)

- **Full:** AC1 (cron+auth+poll), AC2a (UPCOMING migration+notify), AC3 (chain-based pending), AC5 (dedup — live-proven), AC7 (in-detector ai_summary), AC8 (attribution via re-ingest), AC9 (resilience).
- **Partial:** AC2 (AMENDMENT positive path fixture-only — `TEST-001`), AC4 (hash-guard deviation, documented — `ARCH-001`), AC6 (notify *eligibility* verified, but Notification/ChangeAssessment record creation not asserted — `TEST-002`), AC10 (lint/typecheck clean + 32/32, but `pnpm build` not run — `BUILD-001`).

### Top Issues

| ID | Sev | Finding | Action |
|----|-----|---------|--------|
| TEST-001 | medium | AMENDMENT positive-emit path is fixture-only; never observed against a real Skolverket change. Trigger (4) assumes baseline(9.7 metadata)↔statute-projected chain number-set consistency — drift could miss/false-fire. Live run = 0 AMENDMENT validates only the no-false-positive case. | Add a baseline↔statute chain-consistency test; monitor the first real amendment. Re-ingest hash guard is a partial backstop. |
| TEST-002 | medium | AC6 proved the 45 events *qualify* for the notify query + carry enriched `ai_summary`, but did not assert actual Notification + ChangeAssessment creation for a workspace listing one of the docs. Relies on the 9.6 source-agnosticism claim. | Add the seed-workspace integration test, or run the notify cron once against a listing workspace. |
| TEST-003 | low | No automated test for `runSkolfsDetector` or the route branches (REPEAL status-flip, enrichment, timeout drain) — covered only by the live run. | Mocked unit test (dry-run counts + commit/REPEAL paths). |
| BUILD-001 | low | `pnpm build` (AC10) not run — dev server held `.next`. Typecheck clean covers most. | Run `pnpm build` before merge. |
| ARCH-001 | low | AC4 deviation (accepted): in-cron AMENDMENT emit is not hash-gated (hash hashes post-Claude HTML). Spurious API signal → spurious event/notify possible. | Accept — ANDRINGSFORFATTNING is authoritative; re-ingest hash guard backstops. Revisit if false notifications appear. |
| OPS-001 | low | Live first-run used the runner script (no SUCCESS `CronJobRun` logged) → the first real cron run recomputes `firstRun=true` and re-processes 45 (all deduped). Harmless but wasteful. Also: cron auth bypassed if `CRON_SECRET` unset (same as SFS cron). | Optional: log a CronJobRun from the script; confirm `CRON_SECRET` set in prod. |

### NFR Validation

- **Security: PASS** — auth guard mirrors SFS; Prisma parameterized. Caveat: ensure `CRON_SECRET` set in prod (guard no-ops when unset).
- **Performance: PASS** — single poll + bounded per-candidate enrichment + idempotent drain; well within 300s.
- **Reliability: PASS** — per-signal fail-soft, idempotent dedup, `CronJobRun` logging, drain-over-runs.
- **Maintainability: PASS** — strong separation of concerns; reuses the existing pipeline.

### Test Architecture

32 SKOLFS unit tests added (classifier 22 incl. all 5 transitions + statute-robust trigger + enrichSignal; projection 3; emitter 7 incl. dedup/created/duplicate). Live integration verification covers the no-change/UPCOMING/dedup paths. Gaps: AMENDMENT positive path (live + chain-consistency), notify→assess record creation, orchestration-core regression test.

### Security Review

No new injection surface (Prisma). Cron endpoint protected by the standard `CRON_SECRET`/Bearer guard. Note the env-var-unset bypass is inherited from the existing SFS cron pattern — acceptable but ensure the secret is configured.

### Files / Operational Note

45 `UPCOMING_AMENDMENT` ChangeEvents are already committed to this DB (owner-approved). The next `notify-amendment-changes` cron will notify any workspace with one of these SKOLFS docs on a laglista — expected behavior; confirm the DB target is intended.

### Recommended Status

**✓ Ready for Done with advisory follow-ups** — no blocking issues. The dev-recorded status stands; the CONCERNS items (TEST-001/002, BUILD-001) are advisory quality improvements, not gate failures. The story owner decides the final bar. (Story owner, not QA, sets status — I do not modify it.)

---

### Re-Review Date: 2026-06-27 (post-fix)

### Reviewed By: Quinn (Test Architect)

### Gate: **CONCERNS** (unchanged status, materially improved) → quality score **80 → 88** · `docs/qa/gates/9.8-skolverket-skolfs-amendment-monitoring.yml`

Dev applied the QA fixes; I independently re-ran and **verified the new tests are substantive, not hollow** (spot-checked the assertions, not just pass/fail). **76/76 across the 5 touched suites · typecheck clean · lint 0 errors.**

**Resolved this round:**
- **TEST-001 (medium) → RESOLVED.** 3 cross-module chain-consistency tests drive the *real* `buildCurrentSnapshots` projection vs a 9.7-style baseline: matching chains → no false AMENDMENT; a genuinely new VALID amendment → `AMENDMENT(2026:99)` (positive control); VALID→EXPIRED → REPEAL. The AMENDMENT positive path is now automated.
- **TEST-002 (medium) → RESOLVED.** End-to-end assertion that an `AGENCY_REGULATION`/`UPCOMING_AMENDMENT` event flows through `createChangeNotifications` → `AMENDMENT_REMINDER` per recipient (3 recipients/2 workspaces) + the `amendment_reminder_enabled` preference gate. AC6 record-creation is now asserted (was inference-only). Confirmed the preference field is wired in `NOTIFICATION_TYPE_TO_PREFERENCE`.
- **TEST-003 (low) → RESOLVED.** `runSkolfsDetector` mocked test: dry-run writes nothing, commit emits one enriched AMENDMENT, idempotent.

**Why still CONCERNS (one low item):**
- **BUILD-001 (low) — `pnpm build` (AC10) remains un-run.** The dev server (`next dev --turbopack`, pid 18135) still holds `.next`; running build would corrupt it. Typecheck is clean, but a Turbopack production build can fail where `tsc` passes, so AC10 is genuinely **unverified** — I won't assert what I haven't run. **This is the sole item between the story and PASS.**

**Updated AC coverage:** AC2 (AMENDMENT) and AC6 (notify) are now **full** (were partial). AC4 remains a documented/accepted deviation (ARCH-001). AC10 partial pending the build.

### Recommended Status (re-review)

**Gate → PASS the moment a green `pnpm build` is recorded.** Stop the dev server, run `pnpm build`; if green, this flips to PASS with no further code changes. Everything else is closed. (Story owner sets final status — I do not modify it.)

---

### Final Gate: **PASS** (2026-06-27) · quality score **95**

Owner confirmed `pnpm build` is **green** → **BUILD-001 resolved, AC10 full**. All gate findings are now closed:
- TEST-001 (chain-consistency) · TEST-002 (end-to-end notify) · TEST-003 (detector core) — resolved with substantive, independently-verified tests (76/76).
- BUILD-001 — build green (owner-confirmed); combined with clean typecheck + lint + unit suites, the no-regression bar (AC10) is met.

Remaining notes are **accepted-low or monitoring**, not blockers: ARCH-001 (AC4 hash-guard-at-re-ingest, documented + accepted), OPS-001 (ensure `CRON_SECRET` set in prod; first cron run re-dedups the 45), MON-001 (observe the first real-world Skolverket amendment). Optional future: a real-DB seed integration test for notify+assess, and backfilling the 2 un-ingested corpus docs (SKOLFS 2004:1, 2005:1).

**✓ Ready for Done.** No blocking issues; all 10 ACs covered. Story owner sets final status.
