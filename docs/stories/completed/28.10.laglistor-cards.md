# Story 28.10: Laglistor Cards — One Card Implementation

**Epic:** 28 — Unified DataTable Core (`docs/prd/epic-28-unified-datatable-core.md`)
**Status:** Done (2026-07-08)

## What changed

- viewMode `'card'` now renders the SAME core-backed tables forced into the
  card renderer (`forceCardView` → `view: { force: 'card' }`), threaded
  through the flat tables and the grouped wrapper/sections. Grouped card
  mode keeps its collapsible sections with card lists inside.
- Product decisions from the epic recorded and implemented: **cards show
  `complianceStatus`** (same column defs as the table — the hand-made
  card's legacy-`status` divergence is retired), and the container-width
  rule only affects table-configured modes (force wins).
- Deleted: `document-list-card.tsx`, `document-list-grid.tsx`,
  `grouped-document-list.tsx` (+ barrel exports) — **the product now has
  exactly one card implementation** (the core's).
- Card mode also gains what the legacy cards never had: inline editors
  (status/priority interactive badges, editor meta rows), selection, and
  load-more — because it's the same DataTable.

## Verification

- 5 laglistor authed specs green post-swap (incl. narrow-viewport cards);
  29 unit tests green; tsc + eslint clean; no importers of the deleted
  files remain.

## PO Implementation Record

**Validated by:** Sarah (PO) — 2026-07-08
**Disposition:** Accepted — Done

- **Scope vs. epic:** Matches Story 28.10: viewMode `'card'` renders through the core (`view: { force: 'card' }`) across flat and grouped modes; both epic product decisions recorded and implemented — cards show `complianceStatus` (retiring the legacy-`status` divergence) and force wins over the container-width rule (narrow forces cards, wide respects the user's choice); `document-list-card.tsx`, `document-list-grid.tsx`, `grouped-document-list.tsx` deleted. Core deliverable: exactly one card implementation in the product.
- **Deviations:** None. Card mode gaining inline editors, selection and load-more is a free consequence of being the same DataTable, not a scope change. (The 28.8 flag deletion named in the epic scope was moot — no flag was ever added, per 28.8's documented deviation.)
- **Verification evidence:** 5 laglistor authed Playwright specs green post-swap (incl. narrow-viewport cards); 29 unit tests green; tsc + eslint clean; grep-verified zero importers of the deleted files remain.
- **Follow-ups / residuals:** None.
- **Documentation integrity:** The `status` → `complianceStatus` fix is listed in the epic doc's "fixes shipped en route" and the deleted card files in 28.12's demolition audit + grep-gate deny-list; `docs/architecture/table-conventions.md` and `docs/prd/epic-list.md` consistent. File moved to `docs/stories/completed/` on 2026-07-08.
