# Story 19.14: Adaptive Thinking + Reasoning for Global Chat

## Status

Done

> Dependency bump required and applied (user-approved 2026-06-23): the original
> `@ai-sdk/anthropic@3.0.23` could not express adaptive thinking. See Dev Agent
> Record → Completion Notes for the full investigation, the live verification,
> and the AI-SDK family alignment that the bump entailed.

> 🏃 **SM sign-off (2026-06-23, Bob):** story-draft checklist passed 5/5 (READY, clarity 10/10). **Task 0 closed — effort values locked:** `change → high`, `task → medium`, `law → medium`, `global → medium`. `max` deliberately avoided everywhere to respect the 90 s `maxDuration` (per the official cost-control note, `high`/`max` raise the risk of `stop_reason: "max_tokens"` on tool-heavy `change` turns; AC 9 forbids raising `maxDuration`). Cleared for dev.

> 📌 **Origin (2026-06-21, PO):** raised during a pre-distribution review of the agent's reasoning capabilities. Two findings drove this story: (1) the chat route configures **fixed per-context thinking budgets** via the now-deprecated `thinking: { type: 'enabled', budgetTokens }` path — on Sonnet 4.6 the modern, non-deprecated equivalent is **adaptive thinking** (`thinking: { type: 'adaptive' }` + `effort`), where the model self-regulates how much to think per turn; (2) the **`global` (Hem dashboard) chat has reasoning fully disabled** — `THINKING_BUDGET` has no `global` key, so it resolves to `0`, leaving the most open-ended surface (open applicability questions) with zero reasoning. This story migrates the thinking configuration to adaptive + per-context `effort` and, in doing so, enables reasoning for `global`. **Scope discipline:** this is a *configuration* change on the existing Anthropic thinking seam — **not** a model-tier change. Upgrading the assessment path Sonnet→Opus is a separate, deliberate decision and is explicitly out of scope here. Pairs naturally with **19.13** (grounding eval) so the quality delta can be measured rather than assumed.

## Story

**As a** product owner,
**I want** the agent's chat to use Anthropic adaptive thinking with a per-context effort level (including a sensible default for the previously-unreasoned global chat),
**so that** the agent reasons appropriately on hard applicability/threshold questions across *all* chat surfaces, on the non-deprecated API path, without hand-tuning fixed token budgets.

## Story Context

**Existing System Integration:**

- **Chat route:** `app/api/chat/route.ts` is the single seam. It currently holds:
  - `THINKING_BUDGET: Record<string, number>` — `{ change: 8_000, task: 3_000, law: 2_000 }`. **No `global` key** → `global` falls through to `0`.
  - `const thinkingBudget = contextType ? (THINKING_BUDGET[contextType] ?? 0) : 0` — resolves the budget; `0` disables thinking.
  - `contextType` typed `'global' | 'task' | 'law' | 'change'`.
  - `providerOptions` built **only when `thinkingBudget > 0 && modelProvider === 'anthropic'`**, as `{ anthropic: { thinking: { type: 'enabled', budgetTokens: thinkingBudget } } }`.
  - Model is `claude-sonnet-4-6` on the Anthropic path (`modelProvider = process.env.AI_CHAT_MODEL ?? 'openai'`); the OpenAI path (`gpt-4-turbo`) has no thinking.
  - `thinkingEnabled: thinkingBudget > 0` is written to telemetry (the `onFinish` / decision-log path).
  - `reasoningTokens = totalUsage.reasoningTokens ?? 0` is already captured.
  - `export const maxDuration = 90` — comment notes the 90 s budget exists to accommodate thinking on tool-heavy assessments.
- **System prompt:** `lib/agent/system-prompt.ts` injects `REASONING_GUIDANCE` ("Resonemangsvägledning") **when thinking is enabled**. That gating must track the new "is reasoning on for this context?" signal, not the removed `budgetTokens > 0`.
- **AI SDK:** the project uses the Vercel AI SDK (`streamText`, `anthropic()` provider, `providerOptions.anthropic`) — **not** the raw Anthropic SDK. Adaptive thinking and effort are expressed through `providerOptions.anthropic` (see Dev Notes for the verified shape).

**Out of scope:**

- **Model-tier change** (Sonnet → Opus) on any path — separate decision.
- The OpenAI fallback path — it stays thinking-less; adaptive applies to the Anthropic path only.
- Per-turn dynamic effort selection beyond the static per-context map.

## Acceptance Criteria

### Configuration
1. The fixed `THINKING_BUDGET` numeric map is replaced by a per-context **effort** map (e.g. `THINKING_EFFORT: Record<contextType, 'low'|'medium'|'high'|'max'>`), covering all four context types **including `global`**.
2. On the Anthropic path, thinking is configured as **adaptive** (`thinking: { type: 'adaptive' }`) with the context's `effort`, replacing the deprecated `{ type: 'enabled', budgetTokens }` shape. The exact `providerOptions.anthropic` nesting for `effort` is confirmed against the installed `@ai-sdk/anthropic` version before merge (AC 11).
3. Per-context effort is **locked** (SM sign-off 2026-06-23): **`change → high`, `task → medium`, `law → medium`, `global → medium`**. `global` is thereby adaptive-enabled (was disabled). `max` is **not** used on any context (90 s `maxDuration` guardrail — see AC 9).
4. The OpenAI path (`modelProvider !== 'anthropic'`) is unchanged — no thinking config emitted.

### Wiring & telemetry
5. The `thinkingEnabled` telemetry flag (and any other `thinkingBudget > 0` consumer) is re-derived from the new signal (adaptive-on for the context) — no dangling reference to the removed `budgetTokens` variable.
6. `REASONING_GUIDANCE` injection in `lib/agent/system-prompt.ts` is gated on the new "reasoning on for this context" signal (now true for `global` too), not on `budgetTokens > 0`.
7. `reasoningTokens` continues to be captured in telemetry (no regression to the existing usage/decision-log write).
8. **Reasoning-block UI continuity** (Epic-19 DoD, `epic-19-…:191` — "existing chat streaming, reasoning blocks, tool-call collapse, citation pills continue to work"): the streamed reasoning/thinking blocks still render in `chat-message.tsx` under adaptive thinking. Manually verify a `change` turn still shows the thinking display, and that a `global` turn (newly reasoning-enabled) renders reasoning blocks without breaking the message layout. Adaptive emits reasoning only when the model elects to think, so a no-think turn must degrade cleanly (no empty block / no layout shift).

### Safety & guardrails
9. The `maxDuration = 90` interaction is reviewed: chosen effort levels must not routinely exceed the 90 s streaming budget on tool-heavy `change` assessments. **Measure** via the existing per-turn duration telemetry (the `onFinish` usage/decision-log write that already records token + timing data) on a representative tool-heavy `change` turn. If `high`/`max` risks timeouts, dial back the default and note it (a comment + Change Log entry); do not raise `maxDuration` in this story.
10. No new env var is *required*; if effort is made env-overridable, it must default safely (missing/invalid env → the static map value, never a crash).

### Quality
11. `pnpm typecheck` + `pnpm eslint` clean. `contextType` typing stays exhaustive over the four contexts (a missing context key is a compile error, not a silent `0`/undefined).
12. **Docs-verified:** a one-line note in Dev Notes/PR confirming the chosen reasoning-config shape was checked against (a) the installed `@ai-sdk/anthropic` types and (b) the Anthropic reference for Sonnet 4.6 adaptive-thinking support. The installed stack is **`ai@^6.0.50` + `@ai-sdk/anthropic@^3.0.23`**, so two valid forms exist on this version — the provider-options form (`providerOptions.anthropic.thinking` + `effort`) **and** the AI SDK v6 portable top-level `reasoning: 'low'|'medium'|'high'` param. Task 4 evaluates both and records which was chosen and why; the public docs also show `effort` both as a sibling of `thinking` and nested within it across SDK versions, so the exact nesting must be confirmed if the provider-options form is kept.

## Tasks / Subtasks

- [x] **Task 0: Lock effort values** (AC 3) — ✅ **Done (SM, 2026-06-23):** `change → high`, `task → medium`, `law → medium`, `global → medium`; `max` excluded (90 s guardrail). Use these exact values in the effort map.
- [x] **Task 1: Effort map + adaptive config** (AC 1–4) — replaced `THINKING_BUDGET` with `THINKING_EFFORT: Record<ChatContextType, 'low'|'medium'|'high'>` covering `global`; adaptive config = effort-only `providerOptions.anthropic.effort` on the Anthropic path; OpenAI path returns `undefined` (untouched). Logic lives in `lib/agent/thinking-effort.ts`, wired into the single seam `app/api/chat/route.ts`.
- [x] **Task 2: Re-wire the "reasoning on" signal** (AC 5–7) — `thinkingEnabled` telemetry re-derived from `isAnthropic` (the provider signal; true for all four contexts incl. `global`); `REASONING_GUIDANCE` gating in `system-prompt.ts` already keys off `thinkingEnabled`, so it tracks the new signal with no change there; `reasoningTokens` capture untouched (`onFinish` line unchanged).
- [x] **Task 3: Reasoning-block UI + latency review** (AC 8–10) — verified statically: `ReasoningBlock` mounts only on `isReasoningUIPart(part)` (`chat-message.tsx:918`), so an adaptive no-think turn renders no empty block / no layout shift; `sendReasoning: true` unchanged; Sonnet 4.6 `display` defaults to `"summarized"` so summaries still stream. Effort kept at the `high` ceiling for `change` (no `max`), `maxDuration` unchanged at 90s. Effort is NOT made env-overridable (AC 10 trivially satisfied — static map, no crash surface). **Manual confirmation pending (see Completion Notes):** live render of a `change` + `global` turn, and the per-turn duration measurement on a tool-heavy `change` turn.
- [x] **Task 4: Verify against docs & choose form** (AC 12) — **Chosen: provider-options `effort` form (effort-only).** Forced by the installed stack: `ai@6.0.50` `CallSettings` has NO portable top-level `reasoning` param, and `@ai-sdk/anthropic@3.0.23` `thinking.type` accepts only `enabled|disabled` (NO `adaptive`). In that version `effort` is a SIBLING of `thinking` (`low|medium|high`, NO `max`) and maps to the raw API `output_config.effort` + the `effort-2025-11-24` beta — adaptive thinking, confirmed in `node_modules/@ai-sdk/anthropic/dist/index.js`.
- [x] **Task 5: Tests & checks** (AC 11) — `tests/unit/lib/agent/thinking-effort.test.ts` (9 tests): per-context resolution, `global` adaptive-on, absent-context default, no-`max` guardrail, OpenAI emits no config, Anthropic effort-only shape (no `thinking`/`budgetTokens`). `pnpm typecheck` clean; `pnpm eslint` clean. Chat regression (4 files / 59 tests) green.

## Dev Notes

- **Installed stack:** `ai@^6.0.50`, `@ai-sdk/anthropic@^3.0.23` (`package.json`). Both reasoning-config forms below are available on this version.
- **Verified docs (2026-06-21):**
  - **AI SDK (context7, `/vercel/ai`):** adaptive thinking is `providerOptions.anthropic.thinking: { type: 'adaptive' }`, returning `reasoningText` / `reasoning`. Effort is shown as `providerOptions.anthropic.effort: 'low'|'medium'|'high'|'max'`. ⚠️ **Shape caveat:** some doc/SDK versions show `effort` as a **sibling** of `thinking` (`{ thinking: { type:'adaptive' }, effort:'high' }`), others **nested** (`{ thinking: { type:'adaptive', effort:'high' } }`). Confirm against the installed `@ai-sdk/anthropic` if this form is chosen (AC 12). The deprecated budget form (`{ type:'enabled', budgetTokens }`) is what we're replacing.
  - **AI SDK v6 portable param:** `ai@6` also exposes a provider-agnostic top-level `reasoning: 'low'|'medium'|'high'` on `streamText`, which maps to the Anthropic adaptive/effort config under the hood. Cleaner and version-stable; the trade-off is it's one step removed from the raw provider knobs. Task 4 picks between this and the provider-options form.
  - **Anthropic reference + official docs (`platform.claude.com/docs/en/build-with-claude/adaptive-thinking`, verified 2026-06-23):** Sonnet 4.6 **supports adaptive thinking**; `thinking: {type: "adaptive"}` is the exact shape; `budget_tokens` (`type: "enabled"`) is **deprecated on Sonnet 4.6** (still functional → why the current code works); omitting `thinking` = disabled (today's `global` behaviour). Confirmed point-by-point against the official page.
- **Effort — raw API shape & Sonnet ceiling (official docs):** in the **raw Anthropic API**, effort lives at **`output_config: { effort }`** (a top-level sibling of `thinking`, *not* inside it). The AI SDK maps its `providerOptions.anthropic` (or the v6 portable `reasoning`) onto that — which is exactly why AC 12 requires confirming the SDK's surface shape. **Valid effort on Sonnet 4.6 = `low | medium | high(default) | max`.** `xhigh` exists but is **Opus/Fable-only — do not use it on Sonnet 4.6.** Default effort is `high` (Claude almost always thinks); lower levels let it skip thinking on trivial turns — the mechanism that makes enabling `global` safe.
- **⚠️ `display` default is model-dependent — latent trap for the future Opus upgrade (AC 8 + out-of-scope note):** on **Sonnet 4.6, `thinking.display` defaults to `"summarized"`**, so reasoning summaries keep rendering exactly as today — **AC 8 is safe on the current model with no `display` change**. But on **Opus 4.8/4.7 the default flips to `"omitted"`** (empty `thinking` field) — so if the separately-scoped Sonnet→Opus model bump ever happens, reasoning blocks would silently go blank in `chat-message.tsx` unless `display: "summarized"` is set explicitly. Flag this in whatever story owns the model upgrade; it is **not** handled here.
- **Why adaptive over bumping budgets:** the original ask was "should we raise the per-context budgets?" Adaptive answers that structurally — a trivial `global` lookup thinks little, a hard `change` applicability question thinks more, within the effort ceiling — so we stop hand-tuning fixed token counts. This was the explicit recommendation that spawned the story.
- **Single seam.** All thinking config lives in `app/api/chat/route.ts`. Keep the change localized there + the one gating line in `lib/agent/system-prompt.ts`. Do not scatter effort logic.
- **Don't break the OpenAI fallback.** `providerOptions` must remain absent (or thinking-less) when `modelProvider !== 'anthropic'`. Today that's enforced by the `modelProvider === 'anthropic'` guard — preserve it.
- **Telemetry continuity.** `thinkingEnabled` and `reasoningTokens` feed existing dashboards/decision logs (14.27 / 19.5 lineage). Re-derive `thinkingEnabled` from the new signal; don't drop the field.
- **Cost is not the driver.** Per the originating analysis, thinking tokens (billed as output tokens) are a rounding error at this product's B2B volume; `effort` is the cost/latency lever if ever needed. Do not add cost-gating machinery in this story.
- **Latency is the real constraint.** `maxDuration = 90`. Favor `medium` broadly and reserve `high` for `change`; only consider `max` if a measured assessment turn stays comfortably under 90 s.

### Testing

Vitest + existing chat-route test patterns. Unit-test the effort resolver: each of the four contexts maps to its expected effort; `global` is adaptive-on (not `0`/disabled); `modelProvider = 'openai'` yields no thinking config. Keep tests pure (no live model calls). No new eval harness here — if quantitative quality comparison is wanted, route it through **19.13**'s grounding eval rather than duplicating it.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-21 | 0.1 | Created from pre-distribution agent-reasoning review. Migrate chat thinking from deprecated fixed `budgetTokens` to adaptive thinking + per-context `effort`; enable reasoning for the previously-disabled `global` chat. Docs verified via context7 (`/vercel/ai`) + Anthropic Sonnet 4.6 reference; effort-shape caveat flagged for dev. Model-tier change out of scope. Status Draft pending SM/PO effort-value sign-off. | Sarah (PO) |
| 2026-06-23 | 0.2 | Validation pass (`*validate-story-draft`, GO 9/10) fixes folded in: added AC 8 (reasoning-block UI continuity, per Epic-19 DoD line 191); AC 9 now specifies measuring latency via existing duration telemetry; added Task 0 (lock effort values pre-dev); brought the AI SDK v6 portable `reasoning` param in-scope as a Task 4 alternative to the provider-options form (installed stack confirmed `ai@^6.0.50` + `@ai-sdk/anthropic@^3.0.23`). AC renumbered (now 12). | Sarah (PO) |
| 2026-06-23 | 0.3 | Verified against official Anthropic adaptive-thinking docs — story confirmed aligned. Added to Dev Notes: raw-API effort shape (`output_config.effort`); Sonnet 4.6 effort ceiling is `max` (`xhigh` is Opus/Fable-only); and the `display`-default trap — Sonnet 4.6 defaults to `"summarized"` (AC 8 safe today), but Opus 4.8/4.7 default to `"omitted"`, a risk to flag in the future model-upgrade story. | Sarah (PO) |
| 2026-06-23 | 1.0 | Story-draft checklist passed 5/5 (READY, 10/10). Task 0 closed — effort values locked (`change → high`; `task`/`law`/`global → medium`; no `max`). **Status → Approved**, cleared for dev. | Bob (SM) |
| 2026-06-23 | 1.1 | First impl (effort-only, no dep bump) — later found to DISABLE thinking on Sonnet 4.6 (live-verified). Superseded by 1.2. | James (dev) |
| 2026-06-23 | 1.2 | **Implemented correctly (Tasks 1–5).** Live testing + official Anthropic docs showed effort-only = thinking OFF on Sonnet 4.6, and `@ai-sdk/anthropic@3.0.23` rejects `thinking:{type:'adaptive'}`. **User-approved dep bump:** `@ai-sdk/anthropic ^3.0.85` (adds adaptive+display+effort); aligned the AI-SDK family (`ai ^6.0.209`, `@ai-sdk/openai ^3.0.74`, `@ai-sdk/react ^3.0.211`) to clear a `@ai-sdk/provider` version-skew `Tool`-type clash. Final shape: `{ thinking:{type:'adaptive',display:'summarized'}, effort }`. Live-confirmed reasoning for `change`→high & `global`→medium (`global` now reasons). Also fixed reasoning-token telemetry to read `outputTokenDetails.reasoningTokens` (moved in ai@6.0.209). typecheck + eslint clean; 9 unit + chat regression green; full unit suite 6615 pass (17 pre-existing infra/other-work failures, none AI-related). **Status → Ready for Review.** Manual UI render (AC 8) + 90s latency check (AC 9) pending dev-server restart on bumped deps. | James (dev) |

## Dev Agent Record

### Agent Model Used

claude-opus-4-8[1m] (James / dev agent)

### Debug Log References

- `pnpm typecheck` → clean (after dep-family alignment + `interface`→`type` fix for `ProviderOptions` assignability).
- `pnpm eslint` (changed files + smoke scripts) → clean.
- `pnpm vitest run tests/unit/lib/agent/thinking-effort.test.ts` → 9/9 pass.
- Chat regression (`chat-usage-events` + `ai-chat` + `thinking-effort`) → 52 pass.
- Full unit suite `pnpm test` → 6615 pass / 17 fail (all pre-existing: live-infra integration tests — Supabase auth "Email not confirmed", redis prod data, DB seed — + one component test tied to unrelated uncommitted `cycle-detail-page.tsx` work; none AI/chat).
- `pnpm tsx scripts/smoke-19.14-effort-payload.ts` → request body carries `thinking:adaptive/summarized` + `output_config.effort` per context; OpenAI emits none.
- `pnpm tsx scripts/smoke-19.14-live-reasoning.ts` → LIVE: production builder `change`→1141–1382 reasoning chars, `global`→716–820; effort-only control → 0. Confirms adaptive thinking on (incl. `global`). Both usage reasoning-token fields undefined on Anthropic (provider doesn't report the count).

### Completion Notes List

- **First attempt was WRONG — documented for the record.** I initially implemented the effort-only shape (`providerOptions.anthropic.effort`, no `thinking` block), reasoning from `@ai-sdk/anthropic@3.0.23`'s types + dist (where `thinking.type` ∈ `enabled|disabled` only and `effort` maps to `output_config.effort`). A LIVE Sonnet 4.6 call disproved it: **effort-only → 0 reasoning chars (thinking OFF)**; deprecated `budgetTokens` → 1008 chars; `thinking:{type:'adaptive'}` → *"invalid anthropic provider options"* (rejected by 3.0.23). The official Anthropic adaptive-thinking docs confirm: adaptive **requires** `thinking:{type:"adaptive"}`, and on Sonnet 4.6 *omitting* the thinking block = **disabled**. So the effort-only impl was a regression (it would have turned OFF the thinking that `change/task/law` had via `budgetTokens`).
- **Resolution (Task 4 / AC 2 / AC 12): bump the SDK + use the real adaptive shape.** User approved bumping `@ai-sdk/anthropic` `3.0.23 → ^3.0.85`, which adds the discriminated `thinking:{type:'adaptive', display:'summarized'|'omitted'}` form + `effort` (`low|medium|high|xhigh|max`). Final shape: `providerOptions.anthropic = { thinking: { type: 'adaptive', display: 'summarized' }, effort }`. **Live-verified after the bump:** production builder produces reasoning for `change`→high (1141–1382 chars) AND `global`→medium (716–820 chars) — `global` now genuinely reasons. `ai@6.0.50` has no portable top-level `reasoning` param, so the provider-options form is the form used.
- **Dep-family alignment (consequence of the bump).** `@ai-sdk/anthropic@3.0.85` pins `@ai-sdk/provider@3.0.10`/`provider-utils@4.0.30`, but `ai@6.0.50` pinned `@3.0.5`/`@4.0.9` → a duplicate nested `@ai-sdk/provider` copy whose structural `Tool`/`ToolSet` types clashed at the `streamText({tools})` call under `exactOptionalPropertyTypes`. Fixed by aligning the whole family within their existing majors: `ai ^6.0.50→^6.0.209`, `@ai-sdk/openai ^3.0.19→^3.0.74`, `@ai-sdk/react ^3.0.52→^3.0.211`. Single provider copy after reinstall; typecheck clean. (All within-major; no breaking major bumps.)
- **`display: 'summarized'` set explicitly.** It's the Sonnet 4.6 default today (summaries render as before — AC 8), AND it pre-empts the Opus 4.7/4.8 `display:'omitted'` trap the story flagged — so the separately-scoped Sonnet→Opus bump won't silently blank the reasoning UI.
- **`max`/`xhigh` avoided (AC 3 / AC 9).** Locked values verbatim: `change→high`, `task/law/global→medium`. `ChatThinkingEffort` is narrowed to `low|medium|high` even though 3.0.85 also offers `xhigh|max` — the 90s `maxDuration` guardrail.
- **`global` enabled (AC 1/AC 3).** Old `THINKING_BUDGET` had no `global` key → `0` (disabled). Now `global→medium` adaptive; live-confirmed reasoning. Adaptivity keeps it safe (trivial lookups skip thinking).
- **Single signal (Task 2 / AC 5–6).** `thinkingEnabled` telemetry re-derived from `isAnthropic`. `REASONING_GUIDANCE` in `system-prompt.ts` already gates on `options?.thinkingEnabled`, so it tracks the new signal with **no change** there.
- **AC 7 — reasoning-token capture, fixed post-bump.** `ai@6.0.209` moved reasoning tokens to `outputTokenDetails.reasoningTokens` and deprecated the top-level `totalUsage.reasoningTokens` the route read. Updated `onFinish` to read the new field first, falling back to the deprecated one. NB: the Anthropic provider currently populates **neither** (verified live — both undefined even with summarized thinking emitted), so the counter stays `0` on the Anthropic path exactly as pre-bump (no regression; this was always 0 in prod data). The reliable "did it think" signal is the streamed reasoning block, not this counter.
- **AC 8 (UI continuity).** Statically verified: `ReasoningBlock` renders only when `isReasoningUIPart(part)` (`chat-message.tsx:918`) → an adaptive no-think turn renders nothing extra (no empty block / layout shift). `sendReasoning: true` unchanged. Live API confirms reasoning summaries are emitted (so the stream carries reasoning parts).
- **AC 10.** Effort is a static map (not env-overridable) → no invalid-env crash surface. `resolveChatEffort` is the single hook if env-overriding is wanted later.
- **⚠️ Remaining manual verification (needs the dev server restarted on the bumped deps):**
  1. AC 8 — eyeball a `change` and a `global` turn in the UI for the "Tänkte klart" reasoning breadcrumb (model-level reasoning already proven live). A turn run before restarting the dev server post-`pnpm install` will still be on the old code.
  2. AC 9 — confirm a tool-heavy `change` turn (effort `high`) streams under the 90s `maxDuration`. If it risks timeouts, dial `change`→`medium` and note it (don't raise `maxDuration`).
  3. Smoke scripts `scripts/smoke-19.14-*.ts` are committed as repeatable checks — remove if you prefer not to keep them.

### File List

- **Added:** `lib/agent/thinking-effort.ts` — `ChatContextType`, `ChatThinkingEffort`, `THINKING_EFFORT` map, `resolveChatEffort()`, `buildChatThinkingProviderOptions()` (returns the adaptive `{ thinking:{type:'adaptive',display:'summarized'}, effort }` shape).
- **Added:** `tests/unit/lib/agent/thinking-effort.test.ts` — 9 unit tests.
- **Added:** `scripts/smoke-19.14-effort-payload.ts` — offline request-payload assertion (no API key).
- **Added:** `scripts/smoke-19.14-live-reasoning.ts` — live diagnostic proving adaptive emits reasoning (3 API calls).
- **Modified:** `app/api/chat/route.ts` — removed `THINKING_BUDGET` + `thinkingBudget`; import + use `buildChatThinkingProviderOptions`; `contextType` typed `ChatContextType`; `thinkingEnabled` re-derived from `isAnthropic`; reasoning-token capture reads `outputTokenDetails.reasoningTokens`; `maxDuration` + providerOptions comments updated.
- **Modified:** `package.json` — `@ai-sdk/anthropic ^3.0.85`, `ai ^6.0.209`, `@ai-sdk/openai ^3.0.74`, `@ai-sdk/react ^3.0.211` (family alignment) + `pnpm-lock.yaml`.

## QA Results

### Review Date: 2026-06-23

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Strong, well-disciplined implementation with an exemplary investigation trail. The thinking config is correctly localized to a single pure module (`lib/agent/thinking-effort.ts`) with thin route wiring, the `contextType` typing is exhaustive (a missing context is a compile error, not a silent disable), and the OpenAI fallback is cleanly preserved. The standout is the rigour: the dev caught their **own** wrong first implementation (effort-only, which silently *disables* thinking on Sonnet 4.6) via a live model call rather than shipping it, verified the correct shape against both the installed SDK and the official Anthropic docs, and proved the fix live (`global` now streams reasoning — the core story goal). This is precisely the failure mode (assuming the deprecated→adaptive migration "just works") that the story was written to avoid, and it was handled correctly.

The cost was a **wider-than-scoped dependency bump** — the whole `@ai-sdk/*` + `ai` family — which is necessary (provider version-skew caused a `Tool`-type clash) and verified green against the full unit suite, but is a broad surface to ship alongside a small config feature. That, plus two story-mandated **live** checks that are not yet done (AC8 in-app render, AC9 90s latency), is what holds this at CONCERNS rather than PASS.

### Refactoring Performed

- **File**: `lib/agent/thinking-effort.ts`
  - **Change**: Rewrote the stale module header docstring (it still described the abandoned effort-only approach as "adaptive thinking … with NO `thinking` block").
  - **Why**: It directly contradicted the corrected docs block + the actual implementation, and re-asserted the exact misconception that caused the original regression — a real trap for the next maintainer.
  - **How**: Header now states the real shape (`thinking:{type:'adaptive',display:'summarized'} + effort`) and an explicit IMPORTANT note that bare `effort` = thinking OFF on Sonnet 4.6.
- **File**: `tests/unit/lib/agent/thinking-effort.test.ts`
  - **Change**: Renamed the `max`-guardrail test (was "also unsupported on @ai-sdk/anthropic@3.0.23").
  - **Why**: Stale post-bump — `3.0.85` *does* support `max`/`xhigh`; we intentionally don't use them. The old wording implied a capability limit that no longer exists.
  - **How**: Renamed to "available on @ai-sdk/anthropic@3.0.85 but intentionally unused". Re-ran: 9/9 pass, typecheck + eslint clean.

### Compliance Check

- Coding Standards: ✓ (TS strict/exhaustive typing, no `any`, single-responsibility module, clear comments)
- Project Structure: ✓ (logic in `lib/agent/`, route stays the seam, tests under `tests/unit/lib/agent/`)
- Testing Strategy: ✓ unit (pure resolver/builder) + ✓ 3 committed smoke scripts (offline payload, live generate, live stream). ⚠ no automated coverage of AC6/AC8 (UI/system-prompt) — appropriately manual, but see gaps.
- All ACs Met: ✗ (10/12 met; AC8 and AC9 require live verification still pending — see below)

### Requirements Traceability (Given-When-Then)

- **AC1 ✓** Given the four contexts, when effort resolves, then `THINKING_EFFORT` covers all incl. `global`. → unit "locks effort per context".
- **AC2 ✓** Given the Anthropic path, when building options, then `{thinking:{type:'adaptive',display:'summarized'},effort}` (deprecated budget gone). → unit + offline-payload smoke + live smoke; docs-verified vs 3.0.85.
- **AC3 ✓** Given each context, then change→high, task/law/global→medium, no max. → unit "locks effort"/"never uses max".
- **AC4 ✓** Given a non-Anthropic provider, then no thinking config. → unit "OpenAI path emits no thinking config".
- **AC5 ✓** Given the new signal, then `thinkingEnabled` re-derived from `isAnthropic`; no dangling `thinkingBudget` (only a comment reference remains).
- **AC6 ◐** Given `global` on Anthropic, then REASONING_GUIDANCE injected (gating keys off `thinkingEnabled`). Correct by derivation, **no test** asserts the global prompt contains the block (TEST-001, low).
- **AC7 ◐** Reasoning-token capture updated to `outputTokenDetails.reasoningTokens` (field moved in ai@6.0.209). Code captures it; the Anthropic provider populates neither field, so it stays 0 — no regression, but unobservable (TEL-001, low).
- **AC8 ✗(pending)** Reasoning-block continuity verified statically (`chat-message.tsx:918` mounts only on `isReasoningUIPart`) and at the API level (reasoning parts stream), but **not eyeballed in the running app** (VER-001, medium).
- **AC9 ✗(pending)** maxDuration interaction reviewed in principle (no `max`, ceiling `high`, 90s unchanged) but the **mandated measurement** of a tool-heavy CHANGE turn is not done (OPS-001, medium).
- **AC10 ✓** No env var required; static map, no crash surface.
- **AC11 ✓** typecheck + eslint clean; `Record<ChatContextType>` exhaustive.
- **AC12 ✓** Docs-verified against installed SDK + Anthropic reference, and live. Exemplary.

### Improvements Checklist

- [x] Fixed stale module header that re-asserted the regression-causing misconception (`thinking-effort.ts`)
- [x] Fixed stale `max`-guardrail test name (`thinking-effort.test.ts`)
- [x] Re-ran typecheck / eslint / unit tests after refactor (clean, 9/9)
- [ ] **AC9**: measure a tool-heavy CHANGE turn (effort=high) vs the 90s `maxDuration`; dial change→medium if it risks timeout (do NOT raise maxDuration)
- [ ] **AC8**: restart dev server on bumped deps and confirm reasoning renders for CHANGE + hard GLOBAL turns, and degrades cleanly on a trivial GLOBAL turn
- [ ] **DEP-001**: smoke chat (both providers) + prompt caching + citations on a Vercel preview deploy before prod merge
- [ ] **AC6** (optional): add a `buildSystemPrompt` test asserting REASONING_GUIDANCE present when `thinkingEnabled=true`

### Security Review

No concerns. The change does not touch auth, rate limiting, quota, RBAC, or data access; provider gating and the AUDITOR `web_search` restriction are preserved. Provider options are runtime-validated by the SDK's zod schema.

### Performance Considerations

The open risk is AC9: `effort=high` on tool-heavy CHANGE assessments is the most plausible path to `stop_reason:"max_tokens"` / a 90s streaming timeout — and it is exactly the case the story said to measure. Adaptive (vs a fixed budget) is structurally safer for latency on simple turns, and `max`/`xhigh` are correctly avoided, but the high-effort tool-heavy case remains unmeasured.

### Files Modified During Review

- `lib/agent/thinking-effort.ts` (header docstring only)
- `tests/unit/lib/agent/thinking-effort.test.ts` (one test name)

→ **Dev: please add these to the story File List** (the substance was already listed; this notes the review-time touch).

### Gate Status

Gate: CONCERNS → docs/qa/gates/19.14-adaptive-thinking-and-global-chat-reasoning.yml
Quality score: 80

### Recommended Status

✗ **Changes Required** — not code changes, but the two story-mandated **live verifications** (AC8 render, AC9 latency) must be completed before Done, and the AI-SDK family bump should be smoke-tested on a preview deploy. The implementation itself is sound and API-level-verified; once AC8/AC9 are confirmed (and assuming they pass), this is a straightforward move to Done. (Story owner decides final status.)

---

### Addendum — 2026-06-23 (Quinn): AC8 confirmed in the real app

Story owner supplied a full `/api/chat` SSE stream from a tool-heavy **GLOBAL** turn (the 14-employee skyddsombud/skyddskommitté/SAM question). Decoded:

- `reasoning-start` → 9 `reasoning-delta` → `reasoning-end`, with a `signature` (encrypted thinking) — a genuine reasoning block streamed **before** the first tool call.
- **3× `search_laws`** then a **second step with another full reasoning block** = **interleaved thinking across tool steps**. Adaptive mode auto-enables interleaved thinking on Sonnet 4.6 (manual `budgetTokens` mode needs a separate beta) — positive proof the **adaptive path is genuinely active**.
- `text-start`/`text-delta` Swedish answer with correct citations; English reasoning correctly masked (no leak).

**AC8 → satisfied** (reasoning parts stream on a GLOBAL turn → `isReasoningUIPart` drives the breadcrumb; the core Story 19.14 win is real-app-proven). **VER-001 downgraded to low** (optional: eyeball CHANGE breadcrumb + trivial-GLOBAL clean-degrade).

**Gate remains CONCERNS.** Still open: **AC9** (end-to-end wall-clock of a tool-heavy CHANGE turn vs the 90s `maxDuration` — the supplied stream shows per-tool DB latencies of ~2.0/4.0/3.4s but not the turn total) and **DEP-001** (preview-deploy smoke of the AI-SDK family bump). Provide a CHANGE-turn duration and this likely flips to PASS.

---

### Addendum — 2026-06-24 (Quinn): AC9 functionally demonstrated + new UX finding

Story owner ran a heavy **CHANGE** turn ("fullständig efterlevnadsbedömning av senaste ändringen i inkomstskattelagen"). It **completed end-to-end** — skill activation → multiple `Sökte/Läste/Hämtade` tool groups → `Hämtade ändringsdetaljer (3)` / `Hämtade dokument (3)` → full assessment (correctly identified SFS 2026:464 + 2026:465, company-aware for the 22-employee hotel, action-plan table with deadlines + recommendation), then a follow-up that drafted a complete routine with an approval card. No hang, no timeout, no truncation; citations correct.

- **AC9 → functionally satisfied.** OPS-001 downgraded medium→low. Only the exact wall-clock vs the prod 90s `maxDuration` remains unmeasured (localhost has no cutoff; this was an unusually heavy turn). Capture the chat-request Duration once, or add an `onFinish` duration log for ongoing prod observability.
- **NEW — UX-001 (low, out of 19.14 scope).** Adaptive auto-enables **interleaved thinking**, so a heavy turn now emits one reasoning block per tool step → a ladder of repeated "Tänkte klart" breadcrumbs (7+ observed). `chat-message.tsx` renders each reasoning part as its own breadcrumb; 19.14 (config-only) merely exposed the existing design to many more reasoning events. **Recommend a follow-up story:** coalesce consecutive reasoning + tool steps into one collapsible "Arbetsförlopp" progress group (one live row while streaming; one "Tänkte/sökte i N steg" summary when done). This does NOT block 19.14.
