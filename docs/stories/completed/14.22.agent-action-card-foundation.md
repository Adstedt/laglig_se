# Story 14.22: AgentActionCard Foundation + Task Approval (Pilot)

<!-- Source: Epic 14, Phase 5 — Agent Approval Cards (see docs/epics/epic-14-compliance-partner-agent.md "Phase 5") -->
<!-- Context: Pilot implementation of a uniform inline approval card that replaces the sidebar write-preview pattern from Story 14.15b. Ships task-creation only; other approval types follow in 14.23/14.24. -->

## Status

Done

> ✅ **Done 2026-05-21.** Implemented (Tasks 1–8), 37 unit tests + regression green, `tsc` + `eslint` clean. **QA gate PASS (95)** — `docs/qa/gates/14.22-agent-action-card-foundation.yml`. Task 8.4 manually verified by the story owner (flag ON), incl. the markdown→HTML description fix. Committed `120d9c4`, pushed `feat/ai-agent`. One non-blocking follow-up: **SEC-001** (approve/reject role-permission gate) — deferred to a PO decision in 14.23.

## Dependencies

- **Story 14.7c** (Write Action Tools) — Done. Provides the `execute: false/true` two-phase tool pattern and `wrapWriteToolResponse` envelope used by `create_task`. See [`lib/agent/tools/create-task.ts`](../../lib/agent/tools/create-task.ts).
- **Story 14.11** (Chat-First Dashboard "Hem") — Done. Provides the `conversation_id` grouping on `ChatMessage` used for scoping pending actions to an active conversation.
- **Story 14.15a / 14.15b** (Chat Detail Sidebar / Interactive Agent Surfaces) — Done. Current home of the sidebar `write-preview-task.tsx` that this story begins to replace (full replacement lands in 14.23).
- **Story 14.18** (Collapsed Tool Call Display) — Done. Same render surface in `chat-message.tsx` — the new inline card branch must coexist with `CollapsedToolGroup` and `ToolCallRow`.

## Story

**As a** user proposing a task via the compliance partner,
**I want** the proposal to render inline in chat with editable fields and persist if I close the chat,
**so that** I can review and approve it at my own pace without losing context.

## Context

Today when the user asks the agent to create a task, the agent calls `create_task(execute: false)` which returns a `WriteToolResponse` envelope. That envelope opens `write-preview-task.tsx` in the **right sidebar** (`components/features/ai-chat/details/write-preview-task.tsx`). If the user closes the chat modal without deciding, the proposal is lost — the envelope is ephemeral, tied to the tool result's in-memory SDK state.

Phase 5 replaces that ephemeral sidebar flow with a persistent, inline card. This first story ships the foundation (DB model, server actions, card primitive) and migrates the single `create_task` pathway end-to-end behind a feature flag so the existing sidebar flow remains available as fallback. Stories 14.23 and 14.24 extend to additional approval types, batching, and full migration.

**Key constraint:** Existing `write-preview-task.tsx` must keep working. Rollout is gated behind `FEATURE_INLINE_AGENT_APPROVALS`. Story 14.23 removes the flag and deletes the sidebar component.

### Lifecycle flow (reference)

```
User message
  │
  ▼
app/api/chat/route.ts
  • Mints assistantMessageId + writes a STUB assistant ChatMessage BEFORE streamText() (AC 22a)
  • Invokes streamText() with tool loop
  │
  ▼  Agent decides to propose a task
lib/agent/tools/create-task.ts  (execute: false)
  • Creates PendingAgentAction row (status=PENDING, chat_message_id=preallocated)
  • Returns WriteToolResponse envelope with { data: { pendingActionId } }
  │
  ▼  Stream finishes
app/api/chat/route.ts  (onFinish)
  • Updates the stub assistant ChatMessage with final content + metadata (same $transaction as usage writes)
  │
  ▼  Client re-renders
components/features/ai-chat/chat-message.tsx
  • Detects pendingActionId in tool-part output
  • Renders <AgentActionCard pendingActionId={...} />
  │
  ▼  User edits + approves
components/features/ai-chat/agent-action-card.tsx
  • approvePendingAction(id) server action
  • Dispatches to createTask(params), writes result_ref, status=APPROVED
  │
  ▼  Next user turn
app/api/chat/route.ts
  • buildPendingActionsContext injects <pending_agent_actions> system block
  • Agent sees completed_since_last_turn and confirms verbally
```

### System-prompt block — lift-verbatim format

The context assembly must inject this exact shape (caps: 5 entries per bucket, newest first):

```xml
<pending_agent_actions>
Väntar på beslut:
  [ { "id": "uuid", "action_type": "CREATE_TASK", "created_at": "2026-04-17T10:15:00Z", "summary": "Skapa uppgift: \"Uppdatera brandskyddsrutiner\" (prioritet Hög)" } ]
Godkända sedan senaste svar:
  [ { "id": "uuid", "action_type": "CREATE_TASK", "result_ref": { "taskId": "..." }, "decided_at": "2026-04-17T10:20:00Z", "summary": "Skapa uppgift: \"...\"" } ]
Avvisade sedan senaste svar:
  [ ]
</pending_agent_actions>
```

## Acceptance Criteria

### Persistence Model

1. A new Prisma model `PendingAgentAction` is added with fields: `id` (uuid), `workspace_id`, `user_id`, `conversation_id` (nullable — null = active conversation), `chat_message_id` (FK to `ChatMessage`), `context_type` (`ChatContextType`, default `GLOBAL`), `context_id` (nullable), `action_type` (`PendingAgentActionType` enum, initial value `CREATE_TASK`), `status` (`PendingAgentActionStatus` enum with values `PENDING | APPROVED | REJECTED | EXPIRED`, default `PENDING`), `params` (JSON, editable snapshot of proposal parameters), `result_ref` (JSON nullable, e.g., `{ taskId }` after approve), `created_at`, `decided_at` (nullable), `expires_at` (default now + 7 days).
2. Indexes: `(workspace_id, user_id, conversation_id, status)`, `(chat_message_id)`, `(expires_at)`. Table mapped to `@@map("pending_agent_actions")`.
3. Relations: `Workspace` (onDelete Cascade), `User` (onDelete Cascade), `ChatMessage` (onDelete Cascade). The matching inverse relations are added on `Workspace`, `User`, `ChatMessage`. **`chat_message_id` stays non-null with no deferral** (per ADR-14.22-A — the FK target is guaranteed by the pre-loop stub in AC 22a; do NOT make it nullable to "work around" timing).
4. Migration is **additive** (new table, new enums; no existing model modified).

### Server Actions

5. `approvePendingAction(id)` — loads the row, verifies ownership (`workspace_id` matches active workspace context, `user_id` matches session user), dispatches to the concrete server action for the `action_type` (for `CREATE_TASK`: calls `createTask` with `params`), sets `status = APPROVED`, writes `result_ref = { taskId }`, sets `decided_at`, and `revalidatePath` on relevant routes. Returns `{ success, data?: { resultRef }, error? }`.
6. `rejectPendingAction(id)` — verifies ownership, sets `status = REJECTED`, sets `decided_at`. No side effects beyond the status update.
7. `updatePendingActionParams(id, params)` — verifies ownership and `status === 'PENDING'`, merges/replaces `params`. Rejected/approved rows cannot be updated.
8. `expirePendingActions()` — batch helper: finds all rows where `status = PENDING AND expires_at < NOW()` and sets them to `EXPIRED`. Invoked from the new cron endpoint (see AC 8a).
8a. Cron endpoint: `app/api/cron/expire-pending-actions/route.ts`. Schedule: **`0 3 * * *` (daily 03:00 UTC)** — added to `vercel.json` `crons[]` array. Rationale: off-peak window, doesn't collide with existing `cleanup-invitations` (02:00), `cleanup-workspaces` (06:00), or notification crons (07:00+). Follows the same auth-header + `maxDuration` pattern as existing cron handlers (see `app/api/cron/cleanup-invitations/route.ts` for the minimal template).
9. Read helper: `getPendingAgentAction(id)` — loads a single row, verifies ownership, returns `{ success, data?: PendingAgentAction, error? }`. Used as the SWR fetcher from `AgentActionCard`.
9a. All actions live in `app/actions/pending-agent-actions.ts`. All actions are workspace-scoped — cross-workspace access returns `{ success: false, error: 'Forbidden' }`.

### `AgentActionCard` Primitive

10. New component `components/features/ai-chat/agent-action-card.tsx` — single-action layout.
11. Props: `{ pendingActionId: string }`. The component is SWR-backed; the SWR key is `pending-action:${pendingActionId}`. Fetcher calls a new `getPendingAgentAction(id)` server action (or route — implementer chooses the consistent pattern per the codebase).
12. The card **routes to a per-type renderer** via `actionType`. For 14.22, only `CREATE_TASK` is supported; other types render a non-intrusive fallback: "Den här typen av förslag stöds inte ännu".
13. Card states: `PENDING` (editable + approve/reject), `APPROVED` (read-only summary + link to created entity), `REJECTED` (read-only summary with muted styling), `EXPIRED` (read-only, "Förslaget har gått ut" message).
14. Approve/reject/edit optimistic updates via SWR mutate. On server error, the optimistic state is rolled back and an error toast is shown (reuse the existing toast pattern — `sonner` is already wired).

### `TaskApprovalRenderer`

15. Component `components/features/ai-chat/agent-action-renderers/task-approval-renderer.tsx` — identical field surface to today's `write-preview-task.tsx` (title, description, priority, related document) so UX is unchanged from the user's perspective.
16. In `PENDING` state: editable fields + "Godkänn" (primary) and "Avvisa" (secondary ghost) buttons. Edits debounce 500ms and call `updatePendingActionParams`.
17. In `APPROVED` state: read-only summary with a link to the created task (uses `result_ref.taskId`). Styling matches the existing "done" state in `write-preview-task.tsx` for visual continuity.
18. Copy is Swedish throughout, following the existing `write-preview-task.tsx` vocabulary.

### Chat Message Integration

19. `components/features/ai-chat/chat-message.tsx` gets a new render branch: when a `tool-*` part has `output-available` state AND the output shape indicates a proposal with a `pendingActionId` (see AC 22), render an `AgentActionCard` **inline** after the tool call row (or in place of the `ToolCallRow`'s detail area — implementer chooses based on visual fit).
20. The card is rendered at the same nesting level as existing `ToolCallRow` / `CollapsedToolGroup` output — it participates in collapsing logic from 14.18 (running tool parts still flush a group; the inline card renders beneath a collapsed group when the group is expanded, or after the group if the proposal tool is the only one in the contiguous block).
21. Feature flag `FEATURE_INLINE_AGENT_APPROVALS` (read from `process.env.NEXT_PUBLIC_FEATURE_INLINE_AGENT_APPROVALS === 'true'`) gates the new branch. When `false`, `chat-message.tsx` falls back to the existing `sidebarHint === 'open'` → open `write-preview-task` sidebar flow. Both paths must coexist cleanly during rollout.

### Tool Rewiring

22. `lib/agent/tools/create-task.ts` updated: on `execute: false`, in addition to returning the `WriteToolResponse`, the tool's `execute` handler creates a `PendingAgentAction` row with `action_type: CREATE_TASK` and `params: { title, description, relatedDocumentId, priority }`. The row's `chat_message_id` **MUST** reference the `assistantMessageId` of the **stub `ChatMessage` row written before the tool loop** (per AC 22a). The row is guaranteed to exist at insert time, so the FK is always valid. The returned envelope includes `data: { pendingActionId }` so the chat renderer can resolve it.
22a. **[Revised per ADR-14.22-A, 2026-05-21]** `app/api/chat/route.ts` generates `assistantMessageId = randomUUID()` and **inserts a stub `ChatMessage` row** (that id, `content: ''`, current `conversation_id` / `context_type` / `context_id` scope) **BEFORE invoking `streamText()`**, so the FK target exists for the entire tool loop. The id is threaded into `createAgentTools(workspaceId, userId, { assistantMessageId })` via closure and emitted as the stream's assistant message id so the client reconciles its SDK message id. In `onFinish`, the stub is **updated** (not created) with final content + metadata, in the same `$transaction` as the existing `ChatUsageEvent` + `WorkspaceUsage` writes. The client's assistant-message `saveChatMessage` call is **removed** (`route.ts` becomes the sole writer of the assistant row). Skip the stub for the `'default'` workspace fallback (FK to a non-existent workspace would fail), mirroring the existing `onFinish` usage-write guard. Rationale + alternatives: [`docs/architecture/adr-14.22-a-chat-message-fk-timing.md`](../architecture/adr-14.22-a-chat-message-fk-timing.md).
23. On `execute: true`, the tool continues to behave as today (direct `createTask` call, no pending row created). Rationale: `execute: true` is only called by the agent when re-confirming after an out-of-band sidebar approval; the inline flow never calls `execute: true` directly — approval goes through `approvePendingAction`. This parallel path is retained during rollout and removed in 14.23.

### Agent Feedback Loop

24. `app/api/chat/route.ts` injects a new system-message block on each turn listing pending / completed-since-last-turn / rejected-since-last-turn actions for the current `workspace_id + user_id + (conversation_id | context_type + context_id)` scope.
25. Context shape (Swedish prompt content, stable JSON structure):
    ```
    <pending_agent_actions>
    Väntar på beslut: [ { id, action_type, created_at, summary } ]
    Godkända sedan senaste svar: [ { id, action_type, result_ref, decided_at, summary } ]
    Avvisade sedan senaste svar: [ { id, action_type, decided_at } ]
    </pending_agent_actions>
    ```
26. "Since-last-turn" is computed as `decided_at > (previous assistant message created_at)` OR `decided_at > session_start_time` when no prior assistant message exists. Caps: last 5 entries per bucket, newest first.
27. The block is injected **after** the company-context section and **before** any context-specific context (law/task/change), so the agent treats it as workflow state rather than subject content.

### Swedish Copy

28. All user-facing strings use Swedish (`"Godkänn"`, `"Avvisa"`, `"Godkänt — uppgift skapad"`, `"Förslaget har gått ut"`, etc.). Maintain tone consistency with existing write-preview components.

### Non-Regression

29. Existing chat streaming, reasoning blocks (Story 14.20), tool-call collapse (14.18), and citation pills continue to render correctly.
30. When `FEATURE_INLINE_AGENT_APPROVALS === 'false'`, every existing sidebar write-preview flow works unchanged (visual + behavioral parity verified).
31. `expirePendingActions` has no effect on rows that are not in `PENDING` state.

## Tasks / Subtasks

### Task 1: Prisma schema + migration (AC: 1–4)

- [x] 1.1: Add `PendingAgentAction` model, `PendingAgentActionType` enum (initial value `CREATE_TASK` only), `PendingAgentActionStatus` enum to `prisma/schema.prisma`. Follow snake_case field + `@@map` conventions already in the schema.
- [x] 1.2: Add inverse relations on `Workspace`, `User`, `ChatMessage` (field names: `pending_agent_actions`).
- [x] 1.3: Migration written by hand at `prisma/migrations/20260521120000_add_pending_agent_actions/migration.sql` (NOT auto-applied — per user instruction the migration is handed over for manual application). Additive-only verified by inspection. Composite index given explicit `map: "pending_agent_actions_scope_idx"` to avoid 63-char truncation drift. RLS-enable appended per project pattern.
- [x] 1.4: Ran `pnpm prisma generate` — succeeded (Prisma Client v6.19.0), schema valid, typegen confirms `PendingAgentAction` types.

### Task 2: Server actions (AC: 5–9a)

- [x] 2.1: Create `app/actions/pending-agent-actions.ts`. Export `approvePendingAction`, `rejectPendingAction`, `updatePendingActionParams`, `expirePendingActions`, `getPendingAgentAction` (read helper for SWR per AC 9).
- [x] 2.2: Each mutating action: wrap in `withWorkspace(async ({ workspaceId, userId }) => ...)` — the prevailing pattern in `app/actions/tasks.ts` (it is itself a thin wrapper over `getWorkspaceContext()`; do NOT use `getServerSession` — tasks.ts never does). Then verify `action.workspace_id === workspaceId && action.user_id === userId`, else return `{ success: false, error: 'Forbidden' }`.
- [x] 2.3: `approvePendingAction` dispatch table: `switch (action.action_type)` with `CREATE_TASK` → call `createTask(params)`. Wrap dispatch in try/catch; on dispatch failure do NOT mark the row APPROVED — return `{ success: false, error }`.
- [x] 2.4: After successful dispatch, update row in the same transaction as the write (`prisma.$transaction([...])` if the domain server action is DB-based; otherwise sequential with compensating update on failure).
- [x] 2.5: `revalidatePath` targets: `/laglistor`, `/tasks` (the task board — `/uppgifter` does not exist; see Task 4.4) on approve for `CREATE_TASK`.
- [x] 2.6: `expirePendingActions` — batch `updateMany` with the `expires_at < NOW() AND status = 'PENDING'` filter.
- [x] 2.7: Create `app/api/cron/expire-pending-actions/route.ts` that calls `expirePendingActions`. Add the entry to `vercel.json` `crons[]`: `{ "path": "/api/cron/expire-pending-actions", "schedule": "0 3 * * *" }` (daily 03:00 UTC per AC 8a). Copy the auth-header + `maxDuration` pattern from `app/api/cron/cleanup-invitations/route.ts`.

### Task 3: `AgentActionCard` primitive + SWR plumbing (AC: 10–14)

- [x] 3.1: Create `components/features/ai-chat/agent-action-card.tsx`. Use `useSWR('pending-action:${id}', () => getPendingAgentAction(id))`.
- [x] 3.2: Implement state-based render split: `PENDING` → renderer (editable), `APPROVED/REJECTED/EXPIRED` → renderer (read-only). Fallback for unsupported `action_type` as per AC 12.
- [x] 3.3: Card container styling: reuse the `Card` primitive from `components/ui/card` to match the visual weight of the existing sidebar write-previews. Add a small Swedish badge showing `action_type` label (e.g., "Förslag: Uppgift").
- [x] 3.4: Approve / reject click handlers — SWR mutate optimistic update, call server action, on error rollback + toast.

### Task 4: `TaskApprovalRenderer` (AC: 15–18, 28)

- [x] 4.1: Create `components/features/ai-chat/agent-action-renderers/task-approval-renderer.tsx`.
- [x] 4.2: Port field layout from `components/features/ai-chat/details/write-preview-task.tsx` (title `Input`, description `Textarea`, priority `Select`, related-document resolver). Do NOT copy — re-implement so we can iterate independently.
- [x] 4.3: 500ms debounced edit → `updatePendingActionParams`. Optimistic update of local SWR cache.
- [x] 4.4: Approved state: show title + priority badge + link to task at `/tasks?task=${taskId}` (the task board is `/tasks` — `/uppgifter` does not exist; deep-link confirmed via `searchParams.get('task')` in `components/features/tasks/task-workspace/index.tsx`).
- [x] 4.5: Rejected state: read-only summary with muted styling + small "Avvisat" badge. Expired state: read-only summary with "Förslaget har gått ut" message and a disabled re-propose note.

### Task 5: `chat-message.tsx` integration (AC: 19–21, 29–30)

- [x] 5.1: In `components/features/ai-chat/chat-message.tsx`, add a helper `extractPendingActionId(output: unknown): string | null` that reads `output.data?.pendingActionId` from the tool output shape.
- [x] 5.2: Add feature-flag check `const INLINE_APPROVALS_ENABLED = process.env.NEXT_PUBLIC_FEATURE_INLINE_AGENT_APPROVALS === 'true'`.
- [x] 5.3: When rendering a completed `tool-*` part whose output returns a non-null `pendingActionId` AND the flag is enabled: render the `ToolCallRow` as today **plus** an `<AgentActionCard pendingActionId={...}>` block beneath it.
- [x] 5.4: When the flag is disabled: behavior is unchanged (sidebar `write-preview-task` opens via existing `sidebarHint === 'open'` auto-open from 14.18).
- [x] 5.5: Ensure the card is NOT rendered inside `CollapsedToolGroup`'s collapsed summary (only inside the expanded detail area) — cards should always be visible even when the tool-call group is collapsed. Implementation: when a proposal tool is part of a contiguous group, the group flushes before the proposal (treat proposal tools as "non-collapsible" in `groupCompletedToolParts`) OR the card renders after the group. Document the choice in a code comment with rationale.

### Task 6: `create_task` tool rewiring (AC: 22, 22a, 23)

- [x] 6.1: In `app/api/chat/route.ts`, `const assistantMessageId = randomUUID()`, then **insert the stub `ChatMessage` (`id: assistantMessageId`, `content: ''`, current conversation/context scope) BEFORE invoking `streamText()`** (skip for the `'default'` workspace fallback). Pass `{ assistantMessageId }` into `createAgentTools(workspaceId, userId, { assistantMessageId })`. Set the stream's assistant message id to `assistantMessageId` (via `toUIMessageStreamResponse` message-id generation) so the client reconciles.
- [x] 6.2: In `app/api/chat/route.ts` `onFinish`, **`prisma.chatMessage.update`** the stub (NOT `create`) to set final `content` + `metadata`, inside the existing `ChatUsageEvent` + `WorkspaceUsage` `$transaction` so a turn either fully persists or fully rolls back.
- [x] 6.3: Extend `createAgentTools` signature in `lib/agent/tools/index.ts` to accept the third `context` parameter and pass `assistantMessageId` into each tool factory via closure.
- [x] 6.4: Update `lib/agent/tools/create-task.ts` `execute: false` path to:
  - Read `assistantMessageId` from closure.
  - Create the `PendingAgentAction` row via Prisma with that `chat_message_id`.
  - Include `{ pendingActionId }` in the response envelope's `data` field after calling `wrapWriteToolResponse`.
- [x] 6.5: Do NOT change the `execute: true` branch — retained for the sidebar flow (parity during rollout, removed in 14.23).
- [x] 6.6: Unit-test the tool's new pending-row creation path with a mocked Prisma client.
- [x] 6.7: **[ADR-14.22-A]** Remove the assistant-message `saveChatMessage` call from `lib/hooks/use-chat-interface.ts` `onFinish` (the assistant row is now server-written). Verify the SDK reconciles `message.id` to the server start-chunk id so the rendered card's `pendingActionId` resolves against the same message. Leave the **user**-message save path untouched.
- [x] 6.8: **[ADR-14.22-A]** Add a `content != ''` (non-finalized stub) filter to `getChatHistory` in `app/actions/ai-chat.ts` so aborted-stream stubs don't render as empty bubbles.

### Task 7: Agent feedback loop — system-prompt context injection (AC: 24–27)

- [x] 7.1: Add a helper `buildPendingActionsContext({ workspaceId, userId, conversationId, contextType, contextId, previousAssistantMessageAt })` to `lib/agent/context-assembly.ts` (matches existing context-assembly pattern).
- [x] 7.2: Query `PendingAgentAction` filtered by scope; bucket into pending / completed-since / rejected-since based on the cutoff timestamp.
- [x] 7.3: In `app/api/chat/route.ts`, compute `previousAssistantMessageAt` from the most recent assistant message in the incoming `messages` array (fall back to the last `chatMessage` persisted for this conversation).
- [x] 7.4: Inject the block into `buildSystemPrompt` (`lib/agent/system-prompt.ts:223`) **after the `companyContext` push (lines 229-233) and before the task/law/change branch (line 235+)** — a new `sections.push(...)` inserted between them. This keeps it as workflow state, not subject content (AC 27).
- [x] 7.5: Cap each bucket to the last 5 entries. Each entry's `summary` field is a short Swedish phrase derived from `action_type` + key `params` (e.g., for `CREATE_TASK`: `"Skapa uppgift: \"${title}\" (prioritet ${priority})"`).

### Task 8: Testing (AC: all)

- [x] 8.1: Unit: server actions in `tests/unit/actions/pending-agent-actions.test.ts` — cover ownership checks, approve-dispatch success + failure, reject, update, expire.
- [x] 8.2: Unit: `TaskApprovalRenderer` in `tests/unit/components/features/ai-chat/task-approval-renderer.test.tsx` — editable state, approve click triggers optimistic update, error rollback.
- [ ] 8.3: Integration — no chat integration harness exists for driving a live LLM turn; subsumed by the 8.4 manual playbook (a real `create_task` turn can't be exercised by unit tests).
- [x] 8.4: **Manual verification — DONE (user, 2026-05-21, flag ON):** propose → close/reopen (card persists) → approve → task created → next-turn confirmation; smooth post-tool streaming; correct priority pills. One defect found + fixed: the agent's markdown description landed unstructured in the task's HTML rich-text field → fixed via `markdownToHtml` on the create path (see Completion Notes).

## Dev Notes

### Previous Story Insights

- **Story 14.7c** (Write Action Tools, Done) established the `execute: false/true` two-phase tool pattern. `execute: false` returns a `WriteToolResponse` envelope via `wrapWriteToolResponse`; `execute: true` performs the real side-effect. This story extends (not replaces) the `execute: false` path to additionally persist a `PendingAgentAction` row. [Source: `lib/agent/tools/create-task.ts:56-74` — the `execute:false` handler; `wrapWriteToolResponse` return at `:66-73`. `lib/agent/tools/utils.ts:49-63`]
- **Rejected alternative for assistant-message-id timing**: an earlier draft considered returning `pendingActionId: null` from the tool and reconciling `chat_message_id` in `onFinish`. Rejected because it leaves rows temporarily invalid and complicates the batch query in 14.23. **Note (ADR-14.22-A, 2026-05-21):** the accepted design writes a **stub `ChatMessage` before the loop** — `chat_message_id` is never null/invalid; only the stub's *content* column is filled later. This is distinct from (and the opposite of) the rejected null-FK-then-fixup approach. The original AC 22a "preallocate + write in `onFinish`" was itself unworkable because tools run *before* `onFinish` — see [`docs/architecture/adr-14.22-a-chat-message-fk-timing.md`](../architecture/adr-14.22-a-chat-message-fk-timing.md).
- **Story 14.11** (Chat-First Dashboard "Hem", Done) introduced `conversation_id` grouping on `ChatMessage`. `null` = active conversation; archived conversations get a `crypto.randomUUID()` via `archiveConversation()`. Pending-action scoping follows the same key. [Source: `prisma/schema.prisma:1444-1464` (model moved since this story was first drafted — was 1290-1310, now `WorkspaceFile`); `app/actions/ai-chat.ts:296`]
- **Story 14.15b** (Interactive Agent Surfaces, Done) set the visual language (Card header + field form + primary/ghost button pair) that the new inline renderer should preserve for continuity. Re-implement, don't copy. [Source: `components/features/ai-chat/details/write-preview-task.tsx`]
- **Story 14.18** (Collapsed Tool Calls, Done) added the `groupCompletedToolParts` render pass. Proposal-carrying tool parts must NOT be grouped with non-proposal peers — document the choice inline in the grouper. [Source: `components/features/ai-chat/chat-message.tsx`]

### Data Models

- **New model `PendingAgentAction`** — additive migration, workspace-scoped, cascade deletes on all three FKs per the workspace-isolation pattern. [Source: architecture/17-coding-standards.md#17.4 Database Standards — "Workspace isolation" + "Transaction for related operations"]
- **Reuses existing `ChatMessage`** model and `ChatContextType` enum. Do NOT introduce a parallel enum. The chat-relevant values are `GLOBAL | TASK | LAW | CHANGE`; note the enum has since grown to also include `IMPORT_MATCHING` + `IMPORT_GROUPING` (Stories 24.3 / 24.7, unrelated import-telemetry contexts) — `PendingAgentAction` only ever uses the chat-relevant ones. [Source: `prisma/schema.prisma:1444-1482` (`ChatMessage` model + `ChatContextType` enum); architecture/4-data-models.md#4.16 AIChatMessage]
- **Dispatches to `Task`** for `CREATE_TASK` — calls existing `createTask` from `app/actions/tasks.ts`. [Source: architecture/4-data-models.md#4.30 Task — "Epic 6 First-Class Task Entity"]
- **Indexes**:
  - `(workspace_id, user_id, conversation_id, status)` — primary "list pending for this conversation" query
  - `(chat_message_id)` — batch grouping in 14.23
  - `(expires_at)` — cron expiration sweep
  [Source: architecture/17-coding-standards.md#17.4 — workspace-filtered queries]

### API Specifications

- **Server actions** live in `app/actions/pending-agent-actions.ts` (new). Standard return shape `{ success: boolean, data?: T, error?: string }` — never throw. Typed errors via `AppError` where needed. [Source: architecture/17-coding-standards.md#17.5 Error Handling Standards]
- **No REST endpoint needed** — `getPendingAgentAction(id)` server action is invoked directly from SWR's fetcher. Pattern matches the kravpunkter fetch (Story 17.16).
- **Cron endpoint**: `app/api/cron/expire-pending-actions/route.ts` — follow the existing cron pattern from `app/api/cron/sync-sfs/route.ts` (`export const maxDuration`, auth header check, return JSON).

### Component Specifications

- **`AgentActionCard`** — new component. Must use shadcn `Card`, `Button`, `Badge`, `Input`, `Textarea`, `Select` — no bespoke styled components. [Source: architecture/17-coding-standards.md#17.3 React/Next.js Standards — "UI Components: shadcn/ui"]
- **`TaskApprovalRenderer`** — mirror the field layout of the current `write-preview-task.tsx` (Input for title, Textarea for description, Select for priority, relatedDocumentId picker). 500ms debounced edits → `updatePendingActionParams`.
- **Chat integration point**: `components/features/ai-chat/chat-message.tsx` — this is the AI Chat Interface Component tree. [Source: architecture/6-components.md#6.3.5 AI Chat Interface Component]
- **SWR key**: `pending-action:${pendingActionId}` per-card, and `pending-actions:${conversationId}` for list queries on system-prompt assembly.

### File Locations

Per project structure [Source: architecture/12-unified-project-structure.md#12.2 Complete Project Structure; #12.4 File Naming Conventions — kebab-case filenames, PascalCase exported components]:

**New files:**
- `app/actions/pending-agent-actions.ts`
- `app/api/cron/expire-pending-actions/route.ts`
- `components/features/ai-chat/agent-action-card.tsx`
- `components/features/ai-chat/agent-action-renderers/task-approval-renderer.tsx`
- `tests/unit/actions/pending-agent-actions.test.ts`
- `tests/unit/components/features/ai-chat/agent-action-card.test.tsx`
- `tests/unit/components/features/ai-chat/task-approval-renderer.test.tsx`

**Modified files:**
- `prisma/schema.prisma` — new model + 2 enums + 3 inverse relations on `Workspace`, `User`, `ChatMessage`. Place in the AI-CHAT section near the `ChatMessage` model (now ~line 1464+, after `ChatMessage` / `ChatUsageEvent`) with banner `// AGENT APPROVAL CARDS — Story 14.22`. Snake_case fields + `@@map("pending_agent_actions")`.
- `lib/agent/tools/create-task.ts` — pending-row creation on `execute: false`
- `lib/agent/tools/index.ts` — may extend `createAgentTools` signature (see Task 6.1)
- `lib/agent/context-assembly.ts` — new `buildPendingActionsContext` helper
- `lib/agent/system-prompt.ts` — inject `<pending_agent_actions>` block
- `app/api/chat/route.ts` — preallocate assistant message id, pass pending-actions context
- `components/features/ai-chat/chat-message.tsx` — new render branch + feature flag check
- `.env.example` — document `NEXT_PUBLIC_FEATURE_INLINE_AGENT_APPROVALS`

### Testing Requirements

- **Framework**: Vitest 1.4+ for unit + integration, React Testing Library for component tests. [Source: architecture/3-tech-stack.md#3.1 — Frontend Testing, Component Testing, Backend Testing]
- **Locations**: `tests/unit/` mirrors source tree. [Source: architecture/2-high-level-architecture.md line 177–178 — `tests/unit/` + `tests/integration/`]
- **Coverage target**: 60–70%. [Source: architecture/3-tech-stack.md#3.1 Frontend Testing rationale]
- **Server actions** — test happy path, cross-workspace ownership rejection, dispatch-error (row stays `PENDING`).
- **Renderer** — test editable state, approve click optimistic + server success, optimistic rollback on error, all read-only variants (APPROVED/REJECTED/EXPIRED).
- **Integration** — chat turn triggers `create_task` with `execute: false` → row created → envelope carries `pendingActionId`. If no existing chat integration harness, covered by the manual playbook in Task 8.4.

### Technical Constraints

- **Prisma** 6.x — use `$transaction` for atomic approve-dispatch. [Source: architecture/3-tech-stack.md#3.1]
- **Feature flag prefix** — must be `NEXT_PUBLIC_FEATURE_INLINE_AGENT_APPROVALS` (client-side read in `chat-message.tsx`).
- **Null `conversation_id` queries** — use Prisma `{ equals: null }` explicitly, not `undefined`. [Source: architecture/17-coding-standards.md#17.4]
- **Swedish UI copy** — all user-facing strings in Swedish; match vocabulary from existing write-preview components.
- **No invented schema** — `ChatContextType`, `ChatMessage` already exist; reuse.

### Project Structure Notes

All planned locations align with `docs/architecture/12-unified-project-structure.md#12.2`:
- Server actions under `app/actions/` ✓
- Cron handlers under `app/api/cron/` ✓
- Feature components under `components/features/ai-chat/` ✓ (existing feature folder)
- Subcomponents under a new nested subfolder `components/features/ai-chat/agent-action-renderers/` — consistent with the existing `ai-chat/details/` precedent.
- Unit tests under `tests/unit/{source-tree-mirror}` ✓

**No structural conflicts identified.**

## Change Log

| Date       | Version | Description        | Author    |
|------------|---------|--------------------|-----------|
| 2026-04-17 | v0.1    | Initial draft      | Sarah (PO) |
| 2026-04-17 | v0.2    | SM rigor pass + PO validation polish; status → Approved | Bob (SM) / Sarah (PO) |
| 2026-05-21 | v0.3    | Freshness re-validation (Sarah/PO). **Critical drift resolved via ADR-14.22-A (Winston/Architect):** assistant-message persistence moved client-side, breaking the AC-22a preallocation invariant → rewrote AC 22/22a + Task 6.1/6.2 to the stub-`ChatMessage`-before-loop design, added Task 6.7 (drop client assistant-save + set stream id) + 6.8 (`getChatHistory` stub filter). Should-fixes: ownership pattern → `withWorkspace`, refreshed stale `ChatMessage` line anchors (1444-1464), noted grown `ChatContextType`, `/uppgifter`→`/tasks`, `archiveConversation` id is `randomUUID`. Nice-to-haves: tightened `create-task.ts` citation (`:56-74`), pinned the `buildSystemPrompt` insertion point (`:223`, between lines 233 and 235). Status remains Approved. | Bob (SM) / Sarah (PO) / Winston (Arch) |
| 2026-05-21 | v0.4    | PO re-validation → **GO, 9/10**. Closed the two residuals it caught: Task 2.5 revalidate target `/uppgifter`→`/tasks` (the point the PO docked), and the lifecycle-diagram verbs (preallocate/"writes row" → stub-before-loop / "updates stub") for parity with the rewritten AC 22a. No new contradictions; FK-timing invariant consistent end-to-end. | Bob (SM) / Sarah (PO) |
| 2026-05-21 | v1.0    | **Implemented (Tasks 1–8).** Migration applied manually by user. 21 unit tests added (server actions / renderer / tool), all green; `tsc` + `eslint` clean; 283-test regression passes. Status → Ready for Review. Outstanding: Task 8.4 manual live-chat verification (flag ON). | James (Dev) |
| 2026-05-21 | v1.1    | Post-review: fixed card-disappears-on-reload (persist `pendingActionIds` in metadata) + choppy post-tool streaming (render card on `!isActive`); UI polish (AI-proposal header, auto-grow description, canonical `getPriorityBadgeProps` priority pill, "Föreslog uppgift" label, select-trigger pill fix). Post-QA: added `buildPendingActionsContext` + `agent-action-card` tests (closes TEST-002/003) → 32 unit tests, regression 121 green. | James (Dev) / Quinn (QA) |
| 2026-05-21 | v1.2    | **Task 8.4 verified by user (flag ON)** → TEST-001 closed. Fixed the one defect found: agent markdown description landed unstructured in the task's HTML field → added `markdownToHtml` (`remark`+`remark-html`) on the agent create paths. 37 unit tests now (5 new); tsc + eslint clean. Gate → **PASS** (SEC-001 low remains as a PO follow-up). | James (Dev) |

## Dev Agent Record

### Agent Model Used

claude-opus-4-7 (James / Dev agent)

### Debug Log References

- `pnpm prisma generate` → ✔ Generated Prisma Client (v6.19.0); schema valid, no errors.

### Completion Notes List

**Task 1 — Prisma schema + migration (DONE; migration pending manual application by user):**
- Added `PendingAgentAction` model + `PendingAgentActionType` (CREATE_TASK only) + `PendingAgentActionStatus` (PENDING/APPROVED/REJECTED/EXPIRED) enums in the AI-CHAT section of `prisma/schema.prisma`, after `ChatUsageEvent`.
- `chat_message_id` is non-null `onDelete: Cascade` per ADR-14.22-A. `expires_at` is non-null with no DB default — application-set to now()+7d at creation (Prisma can't express `now()+interval` defaults; documented in the model comment).
- Added inverse `pending_agent_actions PendingAgentAction[]` relations on `User`, `Workspace`, and `ChatMessage`.
- Composite index `(workspace_id, user_id, conversation_id, status)` given explicit name `map: "pending_agent_actions_scope_idx"` — its auto-generated name (69 chars) exceeds PostgreSQL's 63-char limit; pinning the name avoids non-deterministic Prisma truncation and future `migrate dev` drift.
- **Migration handed to user for manual application** (per instruction — NOT auto-run). Hand-written to match Prisma's generated format + the project RLS-enable convention. Additive-only (verified): 2 CREATE TYPE, 1 CREATE TABLE, 3 CREATE INDEX, 3 FK, 1 ENABLE RLS; zero DROP/ALTER/RENAME on existing objects.
- Migration applied manually by the user (confirmed 2026-05-21).

**Task 2 — Server actions + cron (implementation DONE; unit tests pending in Task 8):**
- `app/actions/pending-agent-actions.ts` — `getPendingAgentAction`, `approvePendingAction` (dispatch switch → `createTask` for CREATE_TASK; sequential row-update after confirmed write per Task 2.4), `rejectPendingAction`, `updatePendingActionParams` (PENDING-only), `expirePendingActions` (global sweep). All per-row actions `withWorkspace` + ownership-checked → `'Forbidden'` on mismatch.
- `app/api/cron/expire-pending-actions/route.ts` — mirrors `cleanup-invitations` (GET + Bearer cron secret + job-logger + `maxDuration=60`).
- `vercel.json` — added `{ "/api/cron/expire-pending-actions", "0 3 * * *" }` (verified collision-free slot).

**Task 6 — create_task rewiring + route.ts surgery (ADR-14.22-A):**
- `lib/agent/tools/index.ts` — `createAgentTools(workspaceId, userId, context?)`; threads `{ userId, assistantMessageId, contextType, contextId }` into `create_task`.
- `lib/agent/tools/create-task.ts` — on `execute:false` creates the `PendingAgentAction` row (chat_message_id = stub id) and returns `data: { pendingActionId }`; non-fatal fallback if the write throws; `execute:true` unchanged (6.5).
- `app/api/chat/route.ts` — mints `assistantMessageId`, writes the stub `ChatMessage` before `streamText()` (skipped for `'default'` ws), threads it to `createAgentTools`, `generateMessageId: () => assistantMessageId`, and in `onFinish` **updates** the stub (content + citation metadata derived from `steps`+`sources`) inside the existing usage `$transaction`.
- `lib/hooks/use-chat-interface.ts` (6.7) — removed the client-side assistant `saveChatMessage` (server is now sole writer); user-message save untouched.
- `app/actions/ai-chat.ts` (6.8) — `getChatHistory` now filters `content != ''` so aborted-stream stubs don't render.

**Tasks 3/4 — components:**
- `agent-action-card.tsx` — SWR-backed (`pending-action:${id}`), optimistic approve/reject/edit via `mutate` + rollback + `sonner` toast, per-type renderer registry (CREATE_TASK only; fallback for others).
- `agent-action-renderers/task-approval-renderer.tsx` — PENDING editable (title/description/priority, 500ms debounced `updatePendingActionParams`); APPROVED/REJECTED/EXPIRED read-only; `/tasks?task=` link.

**Task 5 — chat-message.tsx:** `extractPendingActionId` helper + `INLINE_APPROVALS_ENABLED` flag; proposal-carrying tools are kept out of `CollapsedToolGroup`; renders `<AgentActionCard>` beneath the row with `autoOpen={false}` (suppresses the sidebar) when the flag is on. Flag off = unchanged sidebar fallback.

**Task 7 — feedback loop:** `buildPendingActionsContext` in `context-assembly.ts` (buckets pending / approved-since / rejected-since vs. previous assistant message; 5-cap; Swedish summaries); `pendingActionsBlock` option on `buildSystemPrompt` injected after company-context; `route.ts` builds + passes it (skipped for `'default'`).

**Task 8 — tests (21 new, all green):** server actions (12) — ownership/Forbidden, approve-dispatch success+failure, reject/update PENDING-guards, expire filter; renderer (6) — all states + approve/reject + disabled-when-empty; tool (3) — pending-row creation, bare-envelope fallback, write-throw fallback.

**Validation:** `tsc --noEmit` clean on all touched files; `eslint` clean; regression `vitest run` on actions + ai-chat + agent suites = **283 passed (30 files)**, no regressions.

**Post-review fixes (live testing, 2026-05-21):**
- **Card vanished on chat exit/reenter** — the card's render trigger was the *tool part's* `pendingActionId` (ephemeral SDK state); `getChatHistory`→`toUIMessages` rebuilds messages with text-only parts, so on reload nothing carried the id. Fix: `onFinish` now persists `pendingActionIds` (extracted from `steps`) into the assistant message `metadata`; `chat-message.tsx` renders cards from the union of live tool-part ids ∪ `metadata.pendingActionIds`, so the card survives reload.
- **Choppy streaming after the tool fired** — the `AgentActionCard` was mounting + firing its SWR fetch mid-stream, re-rendering the message subtree against `smoothStream`. Fix: cards now render **once at message level, gated on `!isActive`** (after the turn finishes), instead of inline mid-stream. Sidebar auto-open for proposal tools is still suppressed during the live turn.

**⚠️ Live verification still required (Task 8.4):** the route.ts streaming surgery (stub write, onFinish fill, client-save removal, citation parity, no double-write) cannot be exercised by unit tests / tsc — needs a real chat run with `NEXT_PUBLIC_FEATURE_INLINE_AGENT_APPROVALS="true"`. Re-verify the two fixes above: propose a task, leave + reenter the chat (card should persist), and confirm post-tool text streams smoothly.

**Post-QA test additions (2026-05-21, closes QA gate TEST-002/003):** added unit coverage QA flagged as in-scope gaps — `buildPendingActionsContext` (AC 24–27: bucketing, cutoff, 5-cap, summaries, null-when-empty) + `agent-action-card.tsx` (AC 10–14: SWR states, renderer routing, unsupported-type fallback, optimistic approve/reject). **32 unit tests total now** (was 21); full affected-suite regression: 121 passed. Remaining QA-gate items are TEST-001 (Task 8.4 live-verify, on owner) + SEC-001 (role-gate, PO call).

**Description markdown→HTML fix (2026-05-21, from 8.4 verification):** `Task.description` is a rich-text/HTML field (`RichTextDisplay` renders via DOMPurify'd `dangerouslySetInnerHTML`), but the agent emits markdown — so descriptions landed unstructured (collapsed newlines, literal `**`). Added `lib/markdown/markdown-to-html.ts` (`remark` + `remark-gfm` + `remark-html`) and applied it to the agent create paths: the `approvePendingAction` CREATE_TASK dispatch + the legacy `create-task.ts` `execute:true` branch (user-authored tasks already store editor HTML — untouched). Render-time DOMPurify in `RichTextDisplay` remains the XSS trust boundary. 5 new tests (`markdown-to-html.test.ts`); tsc + eslint clean. **TEST-001 now closed (8.4 verified); SEC-001 (low) remains.**

### File List

- `prisma/schema.prisma` — **modified**: `PendingAgentAction` model + 2 enums + 3 inverse relations (User/Workspace/ChatMessage).
- `prisma/migrations/20260521120000_add_pending_agent_actions/migration.sql` — **new**: additive migration (applied manually by user).
- `app/actions/pending-agent-actions.ts` — **new**: 5 server actions.
- `app/api/cron/expire-pending-actions/route.ts` — **new**: daily expiry cron.
- `vercel.json` — **modified**: cron entry added.
- `lib/agent/tools/index.ts` — **modified**: `createAgentTools` context param.
- `lib/agent/tools/create-task.ts` — **modified**: pending-row creation on `execute:false`.
- `app/api/chat/route.ts` — **modified**: stub ChatMessage + threading + onFinish update + `generateMessageId` + pending-actions block.
- `lib/hooks/use-chat-interface.ts` — **modified**: removed client assistant-message save.
- `app/actions/ai-chat.ts` — **modified**: `getChatHistory` empty-stub filter.
- `lib/agent/context-assembly.ts` — **modified**: `buildPendingActionsContext`.
- `lib/agent/system-prompt.ts` — **modified**: `pendingActionsBlock` option + injection.
- `components/features/ai-chat/agent-action-card.tsx` — **new**: card primitive.
- `components/features/ai-chat/agent-action-renderers/task-approval-renderer.tsx` — **new**: CREATE_TASK renderer.
- `components/features/ai-chat/chat-message.tsx` — **modified**: inline card render branch + grouping + flag.
- `.env.example` — **modified**: `NEXT_PUBLIC_FEATURE_INLINE_AGENT_APPROVALS`.
- `tests/unit/actions/pending-agent-actions.test.ts` — **new**: 12 tests.
- `tests/unit/components/features/ai-chat/task-approval-renderer.test.tsx` — **new**: 6 tests.
- `tests/unit/lib/agent/tools/create-task.test.ts` — **new**: 3 tests.
- `tests/unit/lib/agent/context-assembly.test.ts` — **new**: 5 tests (buildPendingActionsContext; post-QA).
- `tests/unit/components/features/ai-chat/agent-action-card.test.tsx` — **new**: 6 tests (card states + routing + optimistic approve/reject; post-QA).
- `lib/markdown/markdown-to-html.ts` — **new**: agent markdown → task HTML (8.4 fix).
- `app/actions/pending-agent-actions.ts` / `lib/agent/tools/create-task.ts` — **modified**: apply `markdownToHtml` to the agent description on create.
- `tests/unit/lib/markdown/markdown-to-html.test.ts` — **new**: 5 tests.
- `package.json` — **modified**: added `remark` + `remark-html` deps.

## QA Results

### Review Date: 2026-05-21

### Reviewed By: Quinn (Test Architect)

### Review Depth

DEEP review (auto-escalated). Triggers: touches the live chat streaming endpoint + a new persistence model; >5 ACs (31); large multi-file diff (19 files); cross-cutting FK-timing surgery governed by ADR-14.22-A. NFR + full requirements-traceability pass performed.

### Code Quality Assessment

Strong, well-disciplined implementation. The ADR-14.22-A "stub `ChatMessage` before `streamText()`" design is implemented faithfully and consistently across all six touchpoints (`route.ts` mint+stub+thread+`generateMessageId`+onFinish-update, `index.ts` context threading, `create-task.ts` pending-row insert, `use-chat-interface.ts` client-save removal, `ai-chat.ts` `content != ''` filter, schema FK). The FK invariant the ADR promises (`chat_message_id` never null/invalid) holds: the stub is written before tools can run, and `onFinish` only fills `content`/`metadata` in the same `$transaction` as the usage writes. Server-action layer is uniform — every per-row action does `withWorkspace` + an explicit `workspace_id && user_id` ownership check returning `'Forbidden'`, plus a `status === 'PENDING'` guard on the mutating paths. The approve dispatch correctly does NOT flip the row to `APPROVED` when `createTask` fails (row stays `PENDING`), matching Task 2.3. The fail-safe in `onFinish` (try/catch + Sentry, never re-thrown) and the non-fatal fallback in the tool's pending-row insert (proposal envelope still returned if the write throws) are both correctly defensive. The card's optimistic-SWR pattern (`optimisticData` + `rollbackOnError` + `sonner` toast) is idiomatic.

Two post-implementation live-test fixes are notable and sound: (1) cards now render from the union of live tool-part ids ∪ persisted `metadata.pendingActionIds`, so they survive history reload (tool parts aren't persisted); (2) cards render once at message level gated on `!isActive`, avoiding the mid-stream SWR-mount re-render that made post-tool streaming choppy. Both are documented in code comments with rationale.

### Refactoring Performed

None — this was a review-only pass (per the strict non-destructive authorization for this review). All findings are recorded as recommendations below.

### Compliance Check

- Coding Standards: ✓ — `{ success, data?, error? }` action shape, never-throw, `withWorkspace` wrapper, snake_case Prisma fields + `@@map`, shadcn primitives only (`Card`/`Button`/`Badge`/`Input`/`Textarea`/`Select`/`Skeleton`), canonical `getPriorityBadgeProps` badge-tones (no local color drift), `{ not: '' }` Prisma filter, `conversation_id: null` explicit-null query.
- Project Structure: ✓ — server actions under `app/actions/`, cron under `app/api/cron/`, components under `components/features/ai-chat/` with the new `agent-action-renderers/` subfolder mirroring the `details/` precedent, tests mirror the source tree.
- Testing Strategy: ✗ (partial) — 21 net-new unit tests at the right level (server actions, renderer, tool) all green. But the highest-risk surface (the `route.ts` streaming/persistence surgery + `chat-message.tsx` integration + feedback loop) has NO automated coverage; it relies entirely on the still-pending Task 8.4 manual playbook. See Test-Architecture note below.
- All ACs Met: ✗ (code-complete; live verification of AC 19–27 / 29–30 outstanding via Task 8.4).

### Requirements Traceability (Given-When-Then)

Persistence Model
- AC 1–4 ✓ — Given the additive migration, schema model + 2 enums + 3 cascade relations match the spec; `chat_message_id` non-null, composite + chat_message + expires_at indexes present, `@@map`. (Verified by inspection; migration applied manually by user.)

Server Actions — covered by `pending-agent-actions.test.ts` (12 tests)
- AC 5 ✓ — Given an owned PENDING CREATE_TASK row, When `approvePendingAction` runs, Then it dispatches to `createTask`, writes `result_ref={taskId}`, flips `APPROVED`, sets `decided_at`, revalidates `/laglistor`+`/tasks`. (test: "dispatches CREATE_TASK and marks APPROVED on success")
- AC 5 (failure) ✓ — When dispatch fails, Then the row stays PENDING and the error is returned. (test: "leaves the row PENDING when dispatch fails")
- AC 6 ✓ — `rejectPendingAction` sets REJECTED + decided_at, refuses non-PENDING. (2 tests)
- AC 7 ✓ — `updatePendingActionParams` updates a PENDING row, refuses APPROVED. (2 tests)
- AC 8 / 31 ✓ — `expirePendingActions` filters `status=PENDING AND expires_at < NOW()`, returns count; non-PENDING untouched via the status filter. (1 test asserts the where-clause + data)
- AC 8a ✓ — cron route + `vercel.json` `0 3 * * *` entry verified by inspection (collision-free slot; mirrors `cleanup-invitations` auth-header + `maxDuration` + job-logger). Not unit-tested (thin wrapper).
- AC 9 / 9a ✓ — `getPendingAgentAction` ownership read; cross-workspace + cross-user + missing-row all return `'Forbidden'`. (3 tests across get/approve)

AgentActionCard + TaskApprovalRenderer
- AC 10–13 ✓ (renderer states) — `task-approval-renderer.test.tsx` (6 tests) covers PENDING editable + approve/reject clicks + disabled-when-empty, and the APPROVED (task link `/tasks?task=`), REJECTED ("Avvisat"), EXPIRED ("Förslaget har gått ut") read-only variants.
- AC 11 (SWR fetch), AC 12 (unsupported-type fallback "stöds inte ännu"), AC 14 (optimistic mutate + rollback + toast) — implemented in `agent-action-card.tsx` but NOT directly tested (the planned `agent-action-card.test.tsx` from the File Locations list was not created; the renderer test exercises the body but mocks the card's handlers). GAP.
- AC 15–18, 28 ✓ — field surface parity + Swedish copy verified by inspection + renderer tests.

Tool Rewiring — covered by `create-task.test.ts` (3 tests)
- AC 22 ✓ — `execute:false` creates a PendingAgentAction with `chat_message_id=assistantMessageId`, returns `data:{pendingActionId}`. (test 1)
- AC 22 (fallbacks) ✓ — bare envelope when no `assistantMessageId`; bare envelope when the insert throws. (tests 2–3)
- AC 22a — the `route.ts` stub-write / threading / `generateMessageId` / onFinish-update is verified by `tsc` + inspection ONLY. No automated test. GAP (the core ADR surgery).
- AC 23 ✓ — `execute:true` branch unchanged (inspection).

Chat Message Integration
- AC 19–21 — `extractPendingActionId`, the `INLINE_APPROVALS_ENABLED` flag, the "keep proposal tool out of the collapsed group" rule, and the `!isActive` message-level render are implemented and read correctly, but have NO automated test. GAP.
- AC 29–30 (non-regression: streaming/reasoning/collapse/citations + flag-off sidebar parity) — relies on Task 8.4 manual playbook. GAP.

Agent Feedback Loop
- AC 24–27 — `buildPendingActionsContext` (buckets, 5-cap, Swedish summaries, since-last-turn cutoff via previous assistant message) + injection point in `buildSystemPrompt` (after company context, before subject context) are implemented correctly per inspection, but NO unit test exercises the bucketing/cutoff logic. GAP — this is pure, easily testable logic that was left untested.

### Test Architecture Assessment

Coverage is adequate for the deterministic, isolatable units (server actions, tool insert, renderer states) and the tests are at the correct level with good failure-path coverage (dispatch-fail keeps row PENDING; write-throw falls back to bare envelope; cross-tenant Forbidden). The honest gap: the three behaviors carrying the most integration risk — (a) the `route.ts` stub→onFinish persistence surgery, (b) the `chat-message.tsx` render branch + grouping, (c) the `buildPendingActionsContext` feedback loop — have zero automated coverage. (a) and (b) genuinely lack a harness (no live-LLM integration rig exists; Task 8.3 acknowledges this), so the manual 8.4 playbook is a defensible interim. But (c) is a pure async function over a mockable Prisma client and the planned `agent-action-card.test.tsx` is a pure component test — both were in scope and could have been written with the existing Vitest patterns. Their absence is the main reason this is CONCERNS rather than PASS.

### Improvements Checklist

- [ ] Add a unit test for `buildPendingActionsContext` — bucketing (pending/approved-since/rejected-since), the 5-cap, the since-last-turn cutoff (first-turn null cutoff → empty completed/rejected buckets), and the Swedish `summarizePendingAction` output (AC 24–27). Pure function, mockable Prisma — low effort, high value.
- [ ] Add `agent-action-card.test.tsx` (was in the story's File Locations but not created): SWR loading skeleton, unsupported-`action_type` fallback copy (AC 12), optimistic approve/reject + rollback-on-error + toast (AC 14).
- [ ] Complete Task 8.4 manual playbook with the flag ON and record the result in the story: propose → close/reopen (card persists from `metadata.pendingActionIds`) → approve → task created → next turn confirmation; plus a flag-OFF pass for sidebar parity (AC 29–30). This is the only validation path for AC 22a / 19–21 / 29–30.
- [ ] (Minor, non-blocking) Consider whether approve/reject should carry an explicit permission gate (e.g., `withWorkspace(cb, 'tasks:edit')`). Currently neither these actions nor the dispatched `createTask` pass a `requiredPermission`, so any workspace member can approve a task-spawning proposal. This matches the existing `createTask` posture (not a regression), but a viewer-role approval spawning a task may warrant a PO/SM decision before 14.23 widens the action types.
- [ ] (Minor) `getPendingAgentAction` collapses "row missing" and "cross-tenant" into the same `'Forbidden'`. That is the correct non-enumeration security choice; just confirm the card's SWR error copy ("Kunde inte hämta förslaget") reads acceptably for a legitimately-deleted/expired-then-cascaded row.

### Security Review

PASS. Every per-row action (`get`/`approve`/`reject`/`update`) enforces `workspace_id === ctx.workspaceId && user_id === ctx.userId` after `withWorkspace`, returning `'Forbidden'` on any mismatch or missing row — cross-workspace and cross-user access are both blocked (3 ownership tests confirm). `expirePendingActions` is intentionally global but is reachable only via the cron route, which gates on the `CRON_SECRET` Bearer header in production. The stub-write and pending-row insert are correctly skipped for the synthetic `'default'` workspace (FK to a non-existent workspace would fail). New table has RLS enabled (default-deny) per the project pattern. No secrets, no injection vectors (params are JSON columns, not interpolated SQL). One open item recorded above: there is no role-permission gate on approve (workspace-membership only), consistent with the existing `createTask` convention.

### Performance Considerations

Acceptable, with a noted TTFT cost. Per turn the route now adds: 1 indexed stub INSERT before `streamText()` (the ADR explicitly accepts this as negligible vs. model TTFT and deliberately does NOT transaction-wrap it with the stream), plus 2 reads inside `buildPendingActionsContext` (the `findFirst` for the cutoff + the `findMany take:50`). The `findMany` is bounded (take:50, then sliced to 5/bucket in memory) and hits the `pending_agent_actions_scope_idx` composite index. The onFinish `chatMessage.update` is folded into the existing usage `$transaction` (no extra round-trip). Net: ~3 added DB round-trips per turn, all indexed and small; the pre-stream INSERT is the only one on the critical TTFT path and is a single-row indexed write. No N+1, no unbounded queries. Reasonable for the value delivered.

### Reliability

PASS. The onFinish telemetry+content write is fail-safe (try/catch, Sentry capture, never re-thrown — a failed write cannot break the user-facing stream). The content/metadata update + usage writes share one `$transaction` so a turn either fully persists or fully rolls back. The tool's pending-row insert is non-fatal (proposal still returned on failure). Aborted/errored streams leave an empty stub, correctly filtered out of history by the `content != ''` clause in `getChatHistory`. The cron expiry is idempotent (status-filtered `updateMany`). Approve is not idempotent by design — the `status !== 'PENDING'` guard prevents a double-approve from creating two tasks.

### Maintainability

PASS. Clear Story/AC references throughout, per-type renderer registry (`RENDERERS` map) is cleanly extensible for 14.23+, the ADR is well-documented and the code comments explain the non-obvious choices (proposal-not-grouped, render-on-`!isActive`, sequential-vs-transaction approve update). Minor duplication: the ownership-check + try/catch boilerplate repeats across the four per-row actions (could be a small `loadOwnedAction` helper) and the citation-source extraction logic is mirrored between onFinish and the `messageMetadata` callback in `route.ts` — both are acceptable as-is and noted only for future tidy-up.

### Files Modified During Review

None (review-only — no source, migration, or non-QA story sections were modified).

### Gate Status

Gate: CONCERNS → docs/qa/gates/14.22-agent-action-card-foundation.yml
Quality Score: 80

### Recommended Status

✗ Changes Recommended (not blocking-fail) — code is production-quality and all 21 unit tests + tsc + eslint are green, but (1) Task 8.4 live verification of the ADR streaming/persistence surgery is still pending and is the only path that exercises AC 22a / 19–21 / 24–27 / 29–30, and (2) two in-scope automated tests (`buildPendingActionsContext`, `agent-action-card.tsx`) were not written. Recommend: complete 8.4 and add the two unit tests, then move to Done. The feature flag defaults to `false`, so shipping the code behind the flag while 8.4 is completed is low-risk. Final decision rests with the PO/SM.

---

### Review Date: 2026-05-21 (Re-review)

### Reviewed By: Quinn (Test Architect)

### Re-review Trigger

The dev closed two of the four prior gate issues by adding the two unit tests the previous review recommended. This is a focused re-review to verify those closures, refresh the AC-coverage trace, and recompute the gate. Scope limited to the delta — the source under review is unchanged from the first pass, so all four NFRs are carried forward at PASS (re-confirmed by inspection of the gate's prior notes; no source diff since).

### What Changed Since Last Review

- **`tests/unit/lib/agent/context-assembly.test.ts` (NEW, 5 tests)** — exercises `buildPendingActionsContext`: null-when-empty, pending-bucket emission with the Swedish summary (`Brandskydd` + `(prioritet Hög)`), first-turn null-cutoff excludes the approved/rejected buckets, since-last-turn bucketing that includes rows decided after the cutoff and excludes those before it, and the 5-entry-per-bucket cap. This is genuine logic coverage of AC 24–27, not a smoke test — it asserts the bucket labels, the cutoff arithmetic, and the cap count.
- **`tests/unit/components/features/ai-chat/agent-action-card.test.tsx` (NEW, 6 tests)** — exercises `AgentActionCard`: SWR loading (no renderer content), missing-action (empty render), CREATE_TASK renderer routing, the unsupported-`action_type` fallback copy ("Den här typen av förslag stöds inte ännu", AC 12), and optimistic approve/reject where the `mutate` mock actually invokes the optimistic-updater fn so the server action runs (AC 14 / AC 11). Genuine coverage of AC 10–14.

I re-ran the affected suites myself:
- `npx vitest run tests/unit/components/features/ai-chat tests/unit/lib/agent tests/unit/actions/pending-agent-actions.test.ts` → **121 passed (21 files)**, no failures.
- The 5 story-14.22 test files in isolation (`pending-agent-actions` + `task-approval-renderer` + `agent-action-card` + `tools/create-task` + `context-assembly`) → **32 passed (5 files)**, up from 21 across 3 files at the first review.

### Refreshed Requirements Traceability

**Moved to COVERED (were gaps):**
- **AC 11 ✓** — SWR-backed fetch path now exercised by `agent-action-card.test.tsx` (loading state, missing-action, routed render).
- **AC 12 ✓** — unsupported-`action_type` fallback copy now asserted directly.
- **AC 14 ✓** — optimistic approve/reject through the card's own handlers now tested (the `mutate` mock invokes the optimistic updater so the server action fires).
- **AC 24–27 ✓** — `buildPendingActionsContext` bucketing, since-last-turn cutoff (incl. the first-turn null-cutoff branch), 5-cap, and Swedish summaries now unit-tested.

**Still GAPS (unchanged — no live-LLM / streaming integration harness exists; covered only by the pending Task 8.4 manual playbook):**
- **AC 19–21** — `chat-message.tsx` render branch, feature-flag gating, proposal-not-grouped rule: no automated test.
- **AC 22a** — the `route.ts` stub-write / threading / `generateMessageId` / onFinish-update ADR surgery: verified by `tsc` + inspection only.
- **AC 29–30** — non-regression (streaming/reasoning/collapse/citations + flag-off sidebar parity): manual 8.4 only.

### Issues Status

- **TEST-002 (medium) — CLOSED.** `buildPendingActionsContext` is now unit-tested.
- **TEST-003 (low) — CLOSED.** `agent-action-card.test.tsx` was created and covers the SWR/fallback/optimistic surface.
- **TEST-001 (medium) — STILL OPEN.** Task 8.4 manual live-verification remains outstanding; the route.ts streaming-persistence surgery (AC 22a) + chat-message integration (AC 19–21) + non-regression (AC 29–30) are still unverified end-to-end. This is the sole reason the gate cannot go PASS.
- **SEC-001 (low) — STILL OPEN.** approve/reject carry no role-permission gate (membership + ownership only), consistent with the existing `createTask` convention — a PO decision before 14.23 widens action types.

### NFR Validation

Carried forward from the first review (source unchanged): security PASS, performance PASS, reliability PASS, maintainability PASS.

### Gate Status

Gate: CONCERNS → docs/qa/gates/14.22-agent-action-card-foundation.yml
Quality Score: 90 (was 80)

Gate stays **CONCERNS** because TEST-001 (a MEDIUM issue) is still open — the highest-integration-risk surface (ADR route.ts surgery + chat-message branch) remains end-to-end-unverified pending Task 8.4. It is now a *cleaner* CONCERNS: two issues closed, AC coverage materially improved, and the only remaining medium is the manual-verification gap that genuinely lacks a harness. Recomputed score 90 = 100 − 10 (one remaining medium TEST-001); the two low issues are not deducted under the standard 10/medium scoring, and the prior −20 reflected two mediums (TEST-001 + TEST-002) at the first pass.

### Recommended Status

✗ Changes Recommended (not blocking-fail) — complete the Task 8.4 manual playbook with the flag ON (propose → close/reopen card persists → approve → task created → next-turn confirmation) plus a flag-OFF sidebar-parity pass, record the result in the story, then move to Done. Do **not** upgrade to PASS until 8.4 is recorded. SEC-001 remains a PO/SM call, non-blocking for this story. Final decision rests with the PO/SM.
