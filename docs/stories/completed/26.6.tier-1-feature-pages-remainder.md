# Story 26.6: Tier-1 Feature Pages — Remainder (Laglista, Kravpunkter, Lagändringar, Uppgifter, Styrdokument)

## Status

InProgress — dev work (Tasks 2–4, 6) complete; open: Task 5 (people photo — Alexander), Task 7 (push → preview reviews → GSC)

## Story

**As a** Swedish SME compliance owner searching for the product category itself — a digital laglista, kravhantering, lagändringsbevakning, åtgärdsuppföljning or styrdokument kopplade till lagkrav,
**I want** a feature page per core capability that explains the lagkrav context AND proves the capability with real product surfaces,
**so that** Laglig completes Tier 1 (14 pages: 7 features + 7 industries) and captures category- and capability-stage searches with the engine and playbook that 26.4 proved and 26.5 ran at speed.

## Acceptance Criteria

### Research brief (gating — nothing ships without it)

1. A research brief exists at `docs/stories/briefs/26.6-research-keywords-images.md` covering all five features, same shape as the 26.4/26.5 briefs: primary keyword per page, secondary keywords, search intent, competitor gap, suggested H2s, FAQ candidates (cross-checked for global uniqueness against the **52 live questions**), screenshot staging tables, and people-photo decisions per page. Feature-page-specific brief requirements:
   - **Category-term winnability instead of the myndighet-term check:** the risk on funktioner pages is competitor-owned head terms (Notisum/Karnov etc. rank for `laglista`-class queries), not myndighet ownership. The brief verifies each primary is winnable (26.4 precedent: `lagefterlevnadskontroll` for kontroller; full-agent framing for ai-agent).
   - **Cannibalization guard:** `/funktioner/lagandringar` must be differentiated from the homepage sections and the future `/omraden/*` topic pages (feature/product intent vs topic/reference intent); same check for `laglista` vs the seven `/branscher/*` pages whose primaries are `laglista {bransch}`.
   - **The kravpunkt/kontroll framing rule is baked into the brief** (carried per 26.5 Completion Notes): kravpunkten definierar att en kontroll/rutin ska finnas och följas (kan peka på styrdokument/underlag); den dagliga ronden körs i verksamhetens egna rutiner; kontrollcyklerna reviderar kravpunkten; det mobila on-site-fallet är bevisdokumentation. Critical here because `/funktioner/kravpunkter` and `/funktioner/uppgifter` are about exactly these surfaces.
   - Any law lists intended for `relatedCatalogLaws` are **spot-checked against the live catalog at brief time** (documentNumber resolution; EU acts via CELEX strings — 26.5 lesson).
   **Alexander approves primary keywords, people-photo decisions and any staging-data plans before content writing starts.** [Source: docs/stories/briefs/26.5-research-keywords-images.md — structural model]

### The five pages (content)

2. Five MDX pages live at their target URLs, content-complete in Swedish, ~1,000 words each (incl. frontmatter/FAQ — the `wc -w` convention from 26.4/26.5), targeting the brief's primary keywords:
   - `/funktioner/laglista` — the category page: vad är en laglista, varför digital, hur Laglig bygger och underhåller den
   - `/funktioner/kravpunkter` — från paragraf till checklista: kravpunkter ur varje lag, koppling till styrdokument/underlag
   - `/funktioner/lagandringar` — AI-bedömda lagändringar: bevakning, bedömning, "ni behöver inte läsa SFS själva"
   - `/funktioner/uppgifter` — åtgärder med ansvar: uppgifter ur bedömningar och kontroller, uppföljning
   - `/funktioner/styrdokument` — policyer och rutiner kopplade mot lagkrav; AI-utkast
   *(Exact angles come from the brief — the lines above are scoping hints from the epic and nav descriptions, not requirements.)* [Source: docs/prd/epic-26-marketing-pages-seo-content-engine.md#story-266; lib/marketing/nav-links.ts]
3. **Bridge rule, inverted for feature pages (per the 26.4/26.5 briefs):** each capability claim sits next to the screenshot proving it, with editorial explaining the lagkrav context that makes the capability matter; every editorial H2 still carries an explicit product hand-off sentence minimum. Page anatomy: feature-surface hero → per-capability `<SplitFeature>` rows → mid-page conversion device → `<CatalogLawList>` → FAQ → CTA band. `showOrgCheck` decided per page in the brief (26.4 precedent: kontroller `true`, ai-agent `false`). [Source: docs/stories/briefs/26.5-research-keywords-images.md#the-guideproduct-bridge]
4. **Capability-accuracy audit (hard gate):** every product claim on every page is verified against shipping code on `main` at writing time, with citations in the Dev Agent Record (26.4 precedent: 12/13 SUPPORTED, recurring-cycle overstatement corrected; 26.5 precedent: 8 kravpunkt-framing spots corrected post-hoc — this story does the audit *before* publish, not after). Do not claim in-flight WIP capabilities (e.g., the document-editor diff/version work currently on `feat/tracking-events`) unless shipped by publish time. [Source: docs/stories/completed/26.4.first-validation-batch.md#tasks; docs/stories/completed/26.5.tier-1-industry-pages-remainder.md#completion-notes-list]
5. `relatedCatalogLaws` authored by **documentNumber** (CELEX strings for EU acts), 5–10 entries per feature page — 26.4 AC 4 sets only the ≥5 floor (live feature pages carry 6 and 5); the upper bound is a PO ruling at validation: feature pages argue capability, not legal coverage, and a long law list dilutes that. After build `.next/marketing-link-warnings.txt` is **empty**. [Source: docs/stories/completed/26.4.first-validation-batch.md AC 4; PO validation 2026-06-12]
6. FAQ: 4–6 page-specific Q&As per page; **no question reused across ANY marketing page** (uniqueness is global — 52 questions live across 9 pages). Legal-fact claims verified against primary sources with citations in the Dev Agent Record; Alexander does the final sign-off pass before publish (I-5 pattern).
7. `relatedPages`: each new page cross-links ≥4 sibling marketing pages. Bounded retrofit of the nine live pages: review their `relatedPages`, ADD a new feature page where it is clearly the better fit (e.g., every bransch page ↔ laglista; industri/transport ↔ uppgifter); additions only, no removals, no copy edits to live pages under this AC.

### Imagery

8. Each page carries **≥2 real in-app screenshots** (the epic's feature-page minimum), incl. a feature-surface hero, with meaningful Swedish `alt` text. People photos are **per-page decisions recorded in the brief** (26.4 I-6 precedent: kontroller got 2, ai-agent 0 — reference-y feature pages may be screenshots-only); any photos are generated by Alexander from the brief's prompts and **land last** (pages ship to preview with screenshot imagery; photo slots wired on delivery). [Source: docs/prd/epic-26-marketing-pages-seo-content-engine.md AC 10; docs/stories/completed/26.4.first-validation-batch.md I-6]
9. Screenshot staging reuses the **existing seven seeded demo workspaces** (Nordviken, Gråviken, Tärnudden, Stillviken, Vitnäset, Bruksviken, Vindåsen) — **no new workspaces expected**. What the brief's staging tables may require is *data staging* in them: uppgifter in mixed states, styrdokument-utkast, lagändringsbedömningar, kravpunkt-modal states — created via the UI from the demo login (`anna.lindqvist@nordviken.example`, ADMIN on all) or via a bounded seed-engine extension (scripts are not template surface). Capture recipe v2 (26.5 Task 4, mandated for all future shoots): **1920×880 viewport + injected CSS widening Dokument-kolumnen to 490px** so ändrings-pillerna sit inline; expanded sidebar, scroll top, hide nextjs-portal + assistant bubble via injected CSS, mixed compliance statuses (never all green), persona names only, no real PII. [Source: docs/stories/completed/26.5.tier-1-industry-pages-remainder.md Task 4]

### Technical gates (engine verifies, dev confirms)

10. Lighthouse SEO ≥90 per page on a production build (baseline: 92 on all nine live pages). Committed PNGs are fine — next/image transcodes; performance score is not this story's gate.
11. Each page auto-registers in the sitemap and `GET /og-image/funktioner/<slug>` returns `image/png` on the Vercel preview (the slug-parameterized route is proven — verify, don't rebuild).
12. JSON-LD per page: BreadcrumbList + Article + FAQPage mirroring the visible FAQ (automatic — verify).
13. CTAs emit UTM-tagged URLs + `trackEvent` (automatic — spot-verify one page).
14. Nav flip + e2e: publishing the five MDX files flips the Funktioner megamenu/footer items from **anchor-fallback** (homepage anchors — NOT "Kommer snart"; funktioner items differ from branscher here) to live routes with **zero code edits**. `tests/e2e/marketing-nav.spec.ts` currently covers Branscher/Områden only — **extend it with a content-derived Funktioner test** (published items live links with canonical hrefs; the spec's existing content-dir derivation pattern applies). Full suite passes. [Source: tests/e2e/marketing-nav.spec.ts; lib/marketing/nav-links.ts#resolveNavHref]
15. No regression: full unit suite green, `pnpm lint && pnpm typecheck` clean, build green.

### Review + Tier-1 milestone

16. PO copy review + Alexander legal-fact/capability pass on the preview for the five pages. No template sign-off needed — the 26.4 gate stays closed; only template *changes* (none expected) would reopen it.
17. **Post-publish (non-blocking):** submit the five URLs in GSC after production deploy and record the Tier-1-complete baseline — the epic's success measure ("By end of Story 26.6: 14 Tier-1 pages live; organic impressions trend up week-over-week in the following 4 weeks") starts its monitoring window here. [Source: docs/prd/epic-26-marketing-pages-seo-content-engine.md#success-criteria]

## Tasks / Subtasks

- [x] **Task 1: Research brief** (AC: 1) — *DONE 2026-06-12: brief produced by the multi-agent workflow (17 agents — 5 keyword research, 5 adversarial verify, 5 repo staging specs, 1 catalog spot-check, 1 synthesis) → `docs/stories/briefs/26.6-research-keywords-images.md`. All five primaries APPROVED by adversarial verification, zero refuted: laglista=`laglista` (head term, expect mid-page-1 ramp), kravpunkter=`lagkrav checklista`, lagandringar=`bevaka lagändringar` (harvest-driven), uppgifter=`fördela ansvar för lagefterlevnad` (conversion-support, no traffic KPI), styrdokument=`vilka policyer måste ett företag ha`. Catalog: 40/43 ACTIVE, GDPR→CELEX 32016R0679, 3 EU directives prose-only + SFS 2025:1506 as NIS2 stand-in. 29 FAQ picks, globally unique vs the 52 live. **Alexander approved the brief wholesale 2026-06-12 (in-session); brief recommendations stand as defaults on its 11 Open Questions:** uppgifter measured as conversion-support (OQ1); ai-agent MDX edit allowed for lagbevakning anchor discipline (OQ2); EU-directive ingestion registered as backlog follow-up (OQ3); OQ4's four capability verifications fold into Task 3's audit; kravpunkter checklist artifact = visible worked example, not downloadable (OQ5, anti-mall default); foto-som-bevis = uppladdat/länkat underlag only (OQ6); ONE people photo total, on laglista (OQ7); direct DB seeding approved for lagandringar ChangeEvents + styrdokument PendingAgentAction (OQ8); Nordviken repetition ×3 accepted (OQ9); secondary cessions to future /omraden + /jamfor pages confirmed (OQ10); both ranking ramps accepted (OQ11). Original task text follows.* — **not a dev-implementation task: Alexander triggers the multi-agent keyword/imagery research workflow** (same as produced the 26.4/26.5 briefs) → `docs/stories/briefs/26.6-research-keywords-images.md`; per page: primary (category-term winnability + cannibalization checks per AC 1), secondaries, intent, competitor gap, H2s, FAQ candidates (vs the 52 live questions), screenshot staging table (existing workspaces + what data must be staged), people-photo decision + prompt where warranted. Bake in the kravpunkt/kontroll framing rule. Spot-check any catalog law lists against the live catalog (documentNumber/CELEX) at brief time. **HALT for Alexander's keyword + photo-decision + staging-plan approval before Tasks 2–3.**
- [x] **Task 2: Stage demo data for feature surfaces** (AC: 9) — *depends on Task 1's staging tables.* No new workspaces; in the existing seven, stage what the briefs need: uppgifter in mixed states (incl. agent-created with ansvarig + due dates), styrdokument-utkast + published versions, lagändringsbedömningar (incl. an unread-change banner state), kravpunkt-modal states with linked underlag. Prefer UI-driven staging via the demo login; if a seed-engine extension is cleaner, scripts are fair game (run via `pnpm tsx` — the run IS the gate; engine throws on catalog-missing laws = brief defect). **Do NOT create new auth logins** without id-alignment in `create-demo-login.ts` (QA-26.4-M — chat persistence FK breaks otherwise).
- [x] **Task 3: Capability audit, then write the five MDX pages** (AC: 2–7) — run the capability audit FIRST (Explore-agent pattern from 26.4: every claim the brief's H2s imply, verified against shipping code, citations into Dev Agent Record), then write: copy `content/marketing/funktioner/_template.mdx` per page; frontmatter (title ≤60, description ≤170 — schema-enforced; `showOrgCheck` per brief); inverted-bridge body (claim ↔ screenshot pairs, product sentence per H2); FAQs; documentNumber/CELEX catalog links; `relatedPages` ≥4 + AC-7 retrofit (additions only). Restart dev server after each new file (`generateStaticParams` gotcha). Verify legal facts vs primary sources; cite in Dev Agent Record.
- [x] **Task 4: Screenshot sessions** (AC: 8, 9) — heroes ×5 + interior surfaces per the brief's staging tables, captured per recipe v2 (1920×880, 490px Dokument-kolumn, injected CSS); wire as heroMedia + SplitFeature rows. Raw shots in `.shots/` (gitignored), PNGs committed.
- [x] **Task 5: People photos (if any)** (AC: 8) — only for pages the brief designates; Alexander generates from brief prompts; intake check (compliance details flawless, 56px/edge crop if rounded corners are baked in), stage to `people/` targets, wire as SplitFeature rows. Lands last; does not block Tasks 6–7.
- [x] **Task 6: e2e extension + verification sweep** (AC: 5, 10–15) — *local e2e browser-run deferred to CI on push (0-byte output after 40 min locally, stopped on Alexander's instruction); derivation logic verified standalone per the 26.4-accepted pattern.* — extend `marketing-nav.spec.ts` with the content-derived Funktioner test (published → live links; derivation pattern already in the spec); then: `pnpm build` green; warnings file empty; sitemap contains 5 URLs; OG 200 image/png locally; Lighthouse ×5 on `next start`; JSON-LD + UTM spot-checks; `pnpm vitest run tests/unit` full; lint; typecheck; `pnpm test:e2e tests/e2e/marketing-nav.spec.ts`.
- [ ] **Task 7: Preview + review + GSC** (AC: 11, 16, 17) — push → preview OG ×5 (SSO: logged-in browser or bypass token); PO copy review + Alexander legal/capability pass; after production deploy: GSC submit ×5 + record the Tier-1-complete baseline; record outcomes here.

## Dev Notes

### Previous story insights (26.4 + 26.5 — read both Dev Agent Records + QA Results in full before starting)

- **The engine is DONE and double-proven.** If you touch `components/marketing/` or `lib/marketing/`, stop — that reopens the template gate (26.4 AC 17). Zero template edits are expected. The e2e spec extension (AC 14) is test surface, not template surface — allowed.
- **The capability-framing correction (26.5, Alexander 2026-06-11) is the single most important content constraint for THIS story:** kravpunkten definierar att en kontroll/rutin ska finnas och följas (kan peka på styrdokument/underlag); den dagliga ronden körs i verksamhetens egna rutiner; kontrollcyklerna reviderar kravpunkten (att kontrollerna körs); det mobila on-site-fallet är bevisdokumentation (foto länkat som bevis på kravpunkten). 26.5 had to rewrite 8 spots post-hoc; the kravpunkter and uppgifter pages live entirely inside this model — get it right at draft time. [Source: docs/stories/completed/26.5.tier-1-industry-pages-remainder.md#completion-notes-list]
- **Bridge rule, feature-page reading:** inverted — capability claim next to its proving screenshot, lagkrav context in the editorial; explicit product sentence per H2 still the minimum (PO ruling, 26.4).
- **Demo login id-alignment (gate QA-26.4-M):** `anna.lindqvist@nordviken.example` has aligned Prisma/Supabase ids and ADMIN on all seven workspaces. Don't create new auth logins without fixing `create-demo-login.ts` id-alignment first.
- **Capture recipe v2 (mandated, 26.5):** 1920×880 viewport + injected CSS widening Dokument-kolumnen to 490px (ändrings-pillerna inline, single-height); expanded sidebar, scroll top, hide `nextjs-portal` + assistant bubble via injected CSS, never all-green statuses, persona names only.
- **Dev-server gotchas:** new MDX file → 404 until dev-server restart (production unaffected); don't run `next build` while a dev server is up (`.next` collision); Supabase pooler P1001 is transient — retry before debugging.
- **Frontmatter schema enforces:** description ≤170 (build fails over), trimmed strings; catalog overrides frontmatter titles on matched laws — don't fight the DB's title authority.
- **FAQ uniqueness is global** — 52 questions live across 9 pages; check the new 20–30 against all of them.
- **Word-count convention:** ~1,000 words counted incl. frontmatter/FAQ (`wc -w` on the file).
- **EU acts in `relatedCatalogLaws` use CELEX strings** (e.g. `32016R0679` for GDPR) — 26.5 lesson; plain "EG"-style numbers don't resolve.

### Data models / schemas

- Frontmatter: `featurePageSchema` (`kind: 'funktioner'`) in `lib/marketing/frontmatter-schemas.ts` — every field annotated in `content/marketing/funktioner/_template.mdx`; `showOrgCheck` is a required boolean (decide per page in the brief). [Source: lib/marketing/frontmatter-schemas.ts]
- Catalog links resolve against `LegalDocument.document_number`, exact match, ACTIVE only, build-audited into `.next/marketing-link-warnings.txt`. SOSFS/HSLF-FS remain absent from the catalog (prose-only if mentioned). [Source: docs/stories/completed/26.5.tier-1-industry-pages-remainder.md#data-models--schemas]
- No schema changes expected. If one becomes necessary, that's a template-gate event — stop and flag to PO.

### File locations

- Pages: `content/marketing/funktioner/{laglista,kravpunkter,lagandringar,uppgifter,styrdokument}.mdx` — slugs MUST match `FUNKTIONER_NAV` routes in `lib/marketing/nav-links.ts` exactly (that's what flips nav live; note these five currently resolve to `anchorFallback` homepage anchors, not "Kommer snart"). [Source: lib/marketing/nav-links.ts]
- Images: `public/images/marketing/funktioner/<slug>/` (screenshots), `public/images/marketing/people/` (photos, if any). Brief: `docs/stories/briefs/`.
- e2e: `tests/e2e/marketing-nav.spec.ts` (extend with Funktioner coverage — AC 14).
- Staging scripts (only if UI staging is insufficient): extend on `scripts/lib/seed-industry-workspace.ts`; scripts are tsc-excluded — the `pnpm tsx` run is the gate. [Source: architecture/12-unified-project-structure.md#scripts]

### Testing requirements

- New test surface: the Funktioner block in `marketing-nav.spec.ts` (content-derived, same pattern as the existing Branscher tests). No MDX snapshot tests of prose. Full regression: `pnpm lint && pnpm typecheck && pnpm vitest run tests/unit && pnpm build` + the marketing-nav e2e. Content is verified by build validation + link audit + Lighthouse + review. [Source: docs/stories/completed/26.4.first-validation-batch.md#testing]

### Technical constraints

- Next.js 16 App Router, Turbopack; pages are SSG via `generateStaticParams` with `dynamicParams = false`. [Source: architecture/3-tech-stack.md]
- Titles ≤60 chars (SERP truncation; OG font handles ~45+). Lighthouse target ≥90 SEO on prod build.
- Almåsa is never referenceable in any copy (standing constraint).
- Don't market unshipped capabilities: the agent/document-editor WIP on `feat/tracking-events` (diff/version, pending-action batches) is NOT claimable unless it ships before publish — the capability audit (AC 4) is the arbiter.

### Project structure notes

No conflicts. All paths follow the structure established in 26.1–26.5; the five slugs are pre-registered in `FUNKTIONER_NAV` and footer nav, currently resolving to homepage-anchor fallbacks. This story completes Tier 1 — after it, every Funktioner and Branscher nav item is a live link, and the next epic stories (26.7–26.8) move to `/omraden/*`.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-12 | 0.5 | Tasks 2–4 + 6 DONE same-day on the 26.5 playbook: capability audit (5 agents vs main; 2 copy corrections + SFS-only-bevakning rule), staging in the existing 3 workspaces (incl. 3rd Almåsa scrub + reversible junk-doc archive; NO synthetic NEW_RULING/METADATA_UPDATE — real-workspace DB), 5 MDX pages (29 FAQs, 0/81 dupes), 16/16 screenshots (real-agent bedömning run; MCP disconnect mid-session bridged), full sweep green (5,670 unit / build / Lighthouse 92×5 / OG ×5 / sitemap / JSON-LD). Out-of-scope-but-blocking historik Redis-date bugfix applied + flagged (also fixes the public SEO surface). OPEN: Task 5 people photo (Alexander), Task 7 push/preview/reviews/GSC; local e2e deferred to CI. | James (dev) |
| 2026-06-12 | 0.4 | Task 1 DONE: research brief delivered (multi-agent workflow, 17 agents) and **approved wholesale by Alexander** — all 5 primaries adversarially verified (0 refuted), 40/43 catalog candidates ACTIVE, 29 globally-unique FAQ picks, staging specs read from shipping code (notable: styrdokument versioning/diff confirmed SHIPPED — Dev Notes caution narrowed to the laglista-modal LinkedArtifactsPanel WIP). Brief recommendations recorded as defaults on all 11 Open Questions (see Task 1 note); OQ4 capability verifications fold into Task 3's audit. EU-directive ingestion registered as backlog ask (like 26.5's SOSFS item). **Tasks 2–7 unblocked — ready for dev.** | Sarah (PO) |
| 2026-06-12 | 0.3 | Story APPROVED by Alexander (in-session, post-PO-validation). Status Draft → Approved. Next action: Task 1 — Alexander triggers the multi-agent research workflow for the 26.6 brief; Tasks 2–7 blocked behind his keyword/photo/staging approvals. | Sarah (PO) |
| 2026-06-12 | 0.2 | PO validation (validate-next-story): GO, readiness 9/10. Anti-hallucination fix applied: AC 5's "5–10 band" was attributed to 26.4 but 26.4 only sets the ≥5 floor — upper bound now correctly framed as a PO ruling (feature pages argue capability, not coverage). All other verifiable claims independently confirmed: 52 live FAQs on 9 pages (exact), 7 seeded workspaces, e2e Funktioner gap real (0 mentions in spec), template/schema/nav-slug citations accurate. | Sarah (PO) |
| 2026-06-12 | 0.1 | Initial draft from Epic 26 v0.4 + 26.4/26.5 learnings (26.5 gate PASS, quality 95, same-day). Research brief gating carried (AC 1) with feature-page-specific checks: category-term winnability replaces the myndighet check, cannibalization guard (lagandringar vs omraden; laglista vs bransch-primaries), kravpunkt/kontroll framing rule baked in per 26.5's carry-forward note. Capability-accuracy audit promoted to a hard pre-publish AC (AC 4) — feature pages are 100% capability claims. No new demo workspaces (data staging in the existing seven instead). e2e gap identified: marketing-nav.spec.ts lacks Funktioner coverage (anchor-fallback nuance) → AC 14. Capture recipe v2 mandated. Tier-1-complete GSC baseline as non-blocking AC 17. | Bob (SM) |

## Dev Agent Record

### Agent Model Used

claude-fable-5 (James / BMad dev agent)

### Debug Log References

- **Capability audit (Task 3, run FIRST per AC 4 — 2026-06-12, 5 parallel Explore agents + 2 dev reconciliations, all claims verified against `main` via `git show main:` since the working tree carries feat/tracking-events WIP):**
  - **laglista — all core claims SUPPORTED on main:** org-nr onboarding → SNI → branschanpassad grupperad lista (`lib/agent/skills/generate-law-list.ts`, `app/onboarding/_components/onboarding-wizard.tsx:125-180`); klarspråk AI-sammanfattning + `LawListItem.business_context` column (`legal-document-modal/law-header.tsx:85-135`, `compliance-detail-table.tsx:380-450`); Efterlevnad view mode + bulk-action bar + column visibility (`view-toggle.tsx`, `bulk-action-bar.tsx`, `column-settings.tsx`); filter bar status/category/ansvarig (`compliance-filters.tsx:30-80`). **OQ4(a) RESOLVED — evidence-linking UI in the laglista modal is FULLY RENDERED on main, not WIP:** paperclip count badge, EvidenceList with file/doc icons + unlink, "Länka bevis" dropdown (`kravpunkter-checklist.tsx:590-770`). Laglista interior shot 2 + H2 4 are GO. NOTE: no "Ändringar" tab *inside the law modal* (pending-change banner links out to `/laglistor?tab=changes`) — H2 5 framed against the page-level tab, which is correct in the brief's staging table.
  - **kravpunkter — SUPPORTED:** `LawListItemRequirement` model (text, is_fulfilled, bevis_required, responsible_user_id, evidence_links); bevis-required Switch + amber "Saknar bevis" badge; `/krav` cross-workspace KravTable w/ gaps filter (`app/(workspace)/krav/page.tsx`, `krav-table.tsx:60`); progress "X/Y uppfyllda" + ComplianceNarrative + LinkedArtifactsPanel + DetailsBox; cycles freeze kravpunkter via `ComplianceAuditItem.kravpunkterSnapshot` (immutable, audit framing verified); assignee inheritance w/ dashed-ring visual (`lib/requirements/helpers.ts`, `assignee-editor.tsx:89-91`). **CORRECTION REQUIRED: "färdiga kravpunkter per lag" is UNSUPPORTED** — kravpunkter are created manually (`createRequirement`) or agent-PROPOSED (`add_obligation` always creates a PendingAgentAction; tool prompt: "när ni tillsammans har brutit ner en lag i konkreta krav"). Brief FAQ #2 answer reworded to agent-assisted breakdown + approval, never "färdiga per lag".
  - **lagandringar — SUPPORTED:** Ändringar tab + Ny/Granskat badges + red-dot nav (`changes-tab.tsx`, `change-row.tsx:138-150`, `left-sidebar.tsx:257-259`); **AI prefill is REAL** (`save-assessment.ts:53-71` execute=false preview → `assessment-resolution.tsx:107-224` pre-fills status/impact/notes + AiSuggestionHint); assessment statuses/impact E2E (`schema.prisma` AssessmentStatus/ImpactLevel, `change-assessment.ts:57-146`); ChangeIndicator badges (`document-list-table.tsx:497-500`); historik + versiondiff (`browse/lagar/[id]/historik/page.tsx`, `version/[date]/page.tsx`); klarspråk `ChangeEvent.ai_summary` (`amendment-summary.ts:47-64`). **OQ4(c) RESOLVED:** detection is cron-driven — `discover-sfs-amendments` 4×daily, `enrich-amendment-summaries` every 30 min, `notify` daily 07:00, `sync-sfs-updates` daily 15:00 (`vercel.json:37-59`); no in-UI check button. NO latency claims in copy (per brief). **OQ4(d) RESOLVED — automated change detection covers SFS ONLY**; AFS + EU acts are ingested/linkable in the catalog (`ingest-afs-regulations.ts`, `ingest-eu-legislation.ts`) but have no discovery cron. Copy rule: catalog-INCLUSION claims for SFS/AFS/EU are fine ("direkt länkade i katalogen"); bevaknings-claims stay generic "lagändringar"/SFS-framed, never "bevakar AFS/EU-rätt".
  - **uppgifter — SUPPORTED:** tabs lista/sammanfattning/aktiva + stat cards + CRITICAL red-ring Kanban (`task-workspace/index.tsx:68-77`, `tab-navigation.tsx:17-29`); full task anatomy incl. overdue-red + history tab w/ TaskActivity audit trail; **kontrollfynd→uppgift verified** (`lib/compliance-audit/task-spawner.ts:1-162` ← `compliance-finding.ts`, M:N `ComplianceCycleTaskLink`, "Från avvikelse" badge in `linked-cycles-box.tsx:44-48`); **agent task approval is chat-only on main** (AgentActionCard in `chat-message.tsx`; task modal has zero PendingAgentAction surface) — H2 5/FAQ phrasing locked to "i chatten". **OQ4(b) RESOLVED — uppgift→lagkrav back-link EXISTS in UI:** LinkedLawsBox renders `task.list_item_links` (M:N TaskListItemLink) w/ document title/number → law + laglista links; LinkedCyclesBox renders cycle + source finding (`task-modal/right-panel.tsx:77-80`) — conditional H2 6 is GO. **Reconciliation: "uppgift direkt ur bedömningen" has NO dedicated button** — but `ChangeAssessmentModal` embeds ChatPanel `contextType='change'` (`change-assessment-modal.tsx:103`) and `create_task` receives CHANGE context (`chat/route.ts:310-322`), so the task is created via the agent inside the assessment modal, with approval. Copy phrased accordingly.
  - **styrdokument — ALL claims SUPPORTED on main (safest page, as the brief said):** library w/ types/statuses/review_date (`document-table.tsx:173-182`); dual-state APPROVED+DRAFT incl. DualStatusBadge two-half layout, `?view=approved` read-only + banner (`dual-status-badge.tsx:59-107`, `edit/page.tsx:106-150`); version history w/ approver + diff view (`version-history-panel.tsx:130-390`, `version-diff-view.tsx`); granskning→godkännande transitions (`documents.ts:1739-1827`, `promoteDraftToApproved` stamps approved_by); AI draft flow DRAFT_DOCUMENT/IN_EDITOR + PendingAgentApprovalBanner "Slutför godkännande"/"Avvisa" (`draft-styrdokument.ts:231-352`, `pending-agent-approval-banner.tsx:26-109`); DocumentLinksSection "Lägg till kravpunkt" → evidence (`document-links-section.tsx:58-322`); Word/PDF export (`export/route.ts`, `tiptap-to-docx.ts`/`tiptap-to-pdf.ts`). feat/tracking-events WIP on these files is styling/diff-refinement/telemetry only — zero claim deltas.
  - **Negative guards all CONFIRMED ABSENT on main:** no auto-recurring kontrollcykler (no recurrence field), no daily-rond mechanics, no native mobile app / in-app photo capture (evidence = upload/link only — OQ6 framing holds), no auto-sync of changes into laglista items, no subtasks/templates/recurring tasks/dependencies/time tracking, review_date is display-only metadata (no reminders), no form-builder in the editor.

- **Task 2 staging (2026-06-12, `scripts/seed-26-6-feature-staging.ts` + `scripts/fix-26-6-polish.ts`, verified via `scripts/verify-26-6-staging.ts`):**
  - **Almåsa re-scrub (3rd round):** 2 Nordviken WorkspaceDocument titles ("Miljöpolicy/Integritetspolicy – Almåsa Havshotell AB") + 17 document versions with Almåsa in content_json/html/extracted_text — created by agent testing AFTER the 26.4 scrubs. All replaced with "Nordviken Hotell & Konferens AB". Verified 0/0 remaining.
  - **Nordviken tasks → 12** (junk "Test" task deleted; 4 seeded tasks given assignees + due dates; 8 new hotel-flavored tasks): mix = 1 CRITICAL / 4 HIGH / 6 MEDIUM / 1 LOW, 3 overdue, 2 with comments, riskbedömning task linked → AFS 2023:1 item (LinkedLawsBox for the modal shot).
  - **Nordviken styrdokument:** brief's 4 docs created with correct dual-pointer states (Säkerhetspolicy APPROVED v1 review_date 2026-12-31; Rutiner för incidentrapportering DUAL v1-approved-by-Sofia + v2-draft-by-Anna; Instruktion för datasäkerhet IN_REVIEW; Dataskyddsbeskrivning DRAFT) + agent draft "Rutin för hantering av personuppgiftsincidenter" with ChatMessage + PendingAgentAction DRAFT_DOCUMENT/**IN_EDITOR** (action `89d2e5eb-5eef-41bf-8eb3-3597c7767df6`, doc `c76fc73a-f26d-487b-a1be-a44bbc3beb0e` — shot 2 URL recorded in script output). 11 test-junk docs ("Test 5555", "Tomt dokument", "DEMO styrdokument…", "Integritsdasdetspolicy"…) **ARCHIVED, not deleted** (delete denied by permission classifier; archive is the reversible equivalent — the aktiva tab filters ARCHIVED).
  - **Vitnäset PSL (SFS 2010:659, item `1d8166b3`):** kravpunkter 3→9 (6 uppfyllda), assignees Karin/Mikael/Eva + 1 inherited-assignee row, bevis_required on 2 (avvikelsehantering-rutinen amber "Saknar bevis"), evidence on Patientsäkerhetsberättelse-kravpunkten = WorkspaceFile "Patientsäkerhetsberättelse-2026.pdf" + APPROVED styrdokument "Rutiner för avvikelserapportering", compliance_narrative + PÅGÅENDE/MEDIUM set. MBL kravpunkter (Nordviken laglista modal) given assignees.
  - **Tärnudden:** 7 workspace-scoped ChangeAssessments on REAL pipeline events (3 REVIEWED, 2 ACTION_REQUIRED/HIGH, 1 NOT_APPLICABLE, 1 DEFERRED — personas David/Sara/Jonas/Lina, assessed_at within past week) + ONE synthetic ChangeEvent: **REPEAL of SFS 2018:1174 by SFS 2025:1506 — deliberately chosen because it is factually true** (NIS1 upphävd av cybersäkerhetslagen 15 jan 2026), so the ~2 real workspaces listing that law see correct information. **Deviation from the brief's shot spec: NO synthetic METADATA_UPDATE/NEW_RULING events** — ChangeEvents are global and this DB holds real workspaces (nshift, autoropa m.fl.); fabricated rulings would be false information outside the demo. Hero variety = REPEAL/HIGH + NEW_LAW + AMENDMENT with mixed Ny/Granskat, which satisfies AC 9. Cleanup script provided: `scripts/cleanup-26-6-synthetic-events.ts`.

- **Task 3 content (2026-06-12):** five MDX pages written per the brief's H2s/FAQs/law lists. Word counts (`wc -w` incl. frontmatter): laglista 1,386 · kravpunkter 1,316 · lagandringar 1,252 · uppgifter 1,260 · styrdokument 1,287. Titles 47–51 chars, descriptions 163–167. FAQ 6/6/5/6/6 = 29 new; **81 published questions, 0 duplicates** (only collision = unpublished `_template.mdx` vs kontroller — known false positive). `showOrgCheck: true` on ALL five (brief left lagandringar open; org-check is the standard mid-page device and the page has commercial lagbevaknings-intent — PO can flip at review). Kravpunkter page carries the OQ5 worked-example: a visible kravpunkts-checklista table (PSL för vårdgivare) above the fold-ish, NOT downloadable (anti-mall default). relatedPages 4 per new page. **AC 7 retrofit (additions only):** +`/funktioner/laglista` on all 7 bransch pages (hub rule), +`/funktioner/uppgifter` on industri+transport, +`/funktioner/kravpunkter` on kontroller, +`/funktioner/lagandringar` on ai-agent. **OQ2 edit on live ai-agent.mdx:** one sentence in "Bevakar lagändringar åt er" linking to /funktioner/lagandringar (anchor discipline; no other copy touched). All 7 funktioner routes 200 after dev-server restart; marketing unit suites 82/82.
- **Legal-fact basis (Task 3, AC 6 — for Alexander's I-5 pass):** miljöbalken 26 kap. 19 § + förordning 1998:901 (egenkontroll; ansvarsfördelnings-claim scoped to tillstånds-/anmälningspliktiga verksamheter — 4 § gäller dem), ISO 14001 6.1.3 bindande krav + 9.1.2-referenser via kontroller-sidan (verified 26.4), AFS 2023:1 SAM (arbetsmiljöpolicy, skriftlig ≥10 anställda; uppgiftsfördelning dokumenterad — fördelning på personer med kompetens/befogenheter/resurser), 67→15 AFS 1 jan 2025 (verified 26.4/26.5), ABL 8 kap. (styrelsens ansvar för organisation/förvaltning, VD löpande förvaltning), diskrimineringslagen 3 kap. aktiva åtgärder (riktlinjer/rutiner mot trakasserier), visselblåsarlagen 2021:890 (intern rapporteringskanal från 50 arbetstagare), LSO 2003:778 + allmänna råd (SBA-dokumentation), GDPR 32016R0679 (informationsskyldighet art. 13–14, registerförteckning art. 30), cybersäkerhetslagen 2025:1506 i kraft 15 jan 2026 ersätter NIS-lagen 2018:1174 + personligt ledningsansvar (verified 26.4 + brief), lönetransparensdirektivet genomförandefrist 7 juni 2026 (brief web-research), PSL 3 kap. 10 § patientsäkerhetsberättelse senast 1 mars (verified 26.5). NO latency claims, NO auto-recurring-cykler claims, NO mobile-capture claims, NO "färdiga kravpunkter"-claims — per audit.

- **Task 4 screenshots (2026-06-12, recipe v2 via chrome-devtools MCP on Anna's logged-in session): 12 of 16 captured and wired.** laglista ×4 (`hero` Arbetsrätt-expanded mixed statuses + 490px Dokument-kolumn, `filter` filter-bar chips, `efterlevnad` bulk-action bar "3 valda", `kravpunkter-modal` MBL 9 kravpunkter + bevis badges + Detaljer/Snabblänkar); uppgifter ×4 (`hero` 12-task lista w/ overdue-röda rader + Kritisk, `sammanfattning` 4 stat cards, `kanban` CRITICAL red-ring card, `modal` brandskydd task w/ 2 kommentarer + Länkade dokument → LSO SFS 2003:778); styrdokument ×4 (`hero` clean library w/ dual-badge "Godkänd v1 · Utkast v2 pågår", `links` Inställningar-sheet w/ 3 kravpunkts-länkar på diskrimineringslagen, `ai-utkast` PendingAgentApprovalBanner "Slutför godkännande"/"Avvisa", `lasvy` "Du visar Godkänd v1 i läsläge. Ett utkast pågår." + Godkänd v1 av Sofia Karlsson); kravpunkter ×4 (`hero` /krav Alla-filter 18 rader mixed, `modal` PSL 6/9 + ändringsbanner, `bevis` PSB-kravpunkt expanderad med PDF + styrdokument + "Länka bevis" + Kräver bevis-toggle, `status` ComplianceNarrative expanderad + DetailsBox). Session fixes along the way: doc authors re-assigned to personas (12 docs showed "Alexander Adstedt"), staged-doc H1-dubblering stripped, Diskrimineringspolicy-utkastet fyllt med innehåll, brandskydd-task länkad till LSO. **One accidental destructive dialog ("Ta bort kravpunkt?") opened by a stray click — CANCELLED, kravpunkt verified intact.** **REMAINING 4 (lagandringar/Tärnudden: hero changes-tab, bedomning, lista-pillar, historik): the chrome-devtools MCP disconnected mid-session (same failure as 26.4) — needs reconnect, staging is ready (workspace switched, assessments seeded).**
- **Task 4 completion (post-MCP-reconnect, 2026-06-12): lagandringar ×4 captured — 16/16 done.** `hero` (Ändringar-flik 7 rader: Upphävande/Hög på SFS 2018:1174 + Granskad-badge + Ny-rader, priofilter), `bedomning` (**real agent run** i ChangeAssessmentModal — "Förifyllt av AI — granska och justera innan du sparar", status Åtgärd krävs + påverkan + AI-anteckningar + Källor-citat; 26.4-precedentet), `lista-pillar` (Dataskydd-gruppen med röda ändringspiller "1 ändring"/"3 ändringar" inline @490px kolumn, cybersäkerhetslagen Ej uppfylld/Hög), `historik` (AML 38 ändringar, version-för-version med ändringschips + PDF; en katalog-testpost "Teständring SFS 2099:901" CSS-doldes för skottet — **catalog test-amendment flagged as cleanup backlog**). Staging dance for the hero recorded: assessed rows drop out of the tab server-side (`ca.id IS NULL`), so the REPEAL's in-UI-saved assessment was DB-deleted post-shoot to keep the row visible as Ny/Hög — final demo state = REPEAL unassessed (intentional), 4 of the 7 seeded assessments remain (REVIEWED ×2 via AML-mechanism + NOT_APPLICABLE + DEFERRED), which is what the live surface can actually show.
- **OUT-OF-SCOPE-BUT-BLOCKING BUGFIX (flag for PO, 26.4 title-suffix precedent):** both historik pages crash with `TypeError: a.effectiveDate.toISOString is not a function` whenever `getCachedAmendmentTimeline` serves from its Redis tier (dates deserialize as strings; L1 misses on every fresh process). **This 500s the public `/lagar/[id]/historik` SEO surface in production too — pre-existing on main, NOT caused by 26.6.** Minimal defensive fix applied at all 4 call sites (`new Date(...)` normalization) in `app/(workspace)/browse/lagar/[id]/historik/page.tsx` + `app/(public)/lagar/[id]/historik/page.tsx`. A root-cause fix (date revival in the cache layer) is registered as follow-up.
- **Task 6 sweep (2026-06-12, partial pending e2e):** lint 0 errors; typecheck clean; **full unit 5,670 passed / 0 failed (446 files)**; prod build green; `.next/marketing-link-warnings.txt` **empty (0 bytes)** — all 40 catalog links incl. bare-CELEX 32016R0679 + SFS 2025:1506 resolve; sitemap carries all 5 new URLs; **OG ×5 = HTTP 200 image/png locally**; JSON-LD spot-check (laglista) = BreadcrumbList + Article + FAQPage w/ 6 Questions mirroring visible FAQ; CTA hrefs UTM-tagged (`utm_campaign=funktioner-laglista`, hero + footer-strip placements; trackEvent is client-side — covered by 26.4 unit tests); **Lighthouse SEO 0.92 ×5** (= 26.4/26.5 baseline). e2e extended with the content-derived Funktioner block (published → canonical route hrefs; unpublished → anchorFallback hrefs, never "Kommer snart").

### Completion Notes List

- **Task 7 — preview OG + copy review DONE (2026-06-13):** all five funktioner OG images return **HTTP 200 image/png on the preview deployment** (`/og-image/funktioner/{laglista,kravpunkter,lagandringar,uppgifter,styrdokument}`), matching the local result. Local re-confirmed same day: 200 image/png, sizes ~31–37 KB, `laglista` validated as a real PNG at the correct **1200×630** (8-bit RGBA) from the `ImageResponse` route. **Preview copy review (PO + Alexander) — all good, no changes requested.** OG + copy portions of AC 16 satisfied; Task 7 stays OPEN only for the post-deploy step — GSC submit ×5 + record the Tier-1-complete baseline (needs production deploy).

- **Task 5 people photo DONE (2026-06-13):** Alexander generated the laglista photo (the ONE photo in 26.6 per brief OQ7). Intake PASS, zero re-roll triggers: 1200×896 (1.339 ≈ 4:3, matches the 26.5 convention exactly), Swedish-modest office (light wood, framed forest print, plant, radiator under window), woman ~45 grey-knit seated + younger man navy-shirt standing pointing at the laptop, **laptop screen content illegible**, **no brand logos**, pointing-hand + keyboard-hands anatomically clean (verified on 2× zoom crops), binder closed/blank-spine and set aside (not the hero), clear walkway + tidy desk (flawless depicted compliance), no camera awareness/smiles, warm-neutral grade sits on cream. **No baked-in rounded corners** (corner crops confirmed edge-to-edge content) → no 56px crop needed. Minor in-tolerance deviations: paper face-up not face-down (text illegible, no trigger); binder slightly bright. Processed via PIL (quality 85, progressive, EXIF stripped → 0 keys, 130 KB) to `public/images/marketing/people/laglista-genomgang-revision.jpg`. Wired as a `type:'photo'` SplitFeature ("I praktiken" / "Laglistan ni går igenom tillsammans", mediaSide=left, alternates cleanly with the four screenshot rows) at the end of the opening "Vad är en laglista" section. Renders confirmed in-browser: 200 image/jpeg, correct photo treatment (rounded corners + soft shadow, no screenshot-chrome), grade harmonizes with the cream page. Body-only change — reopens no Task 6 gate (frontmatter/catalog-links/sitemap/OG/JSON-LD untouched); dev server compiles the page 200.

- **Copy-quality pass over all 14 published marketing pages (2026-06-12, on Alexander's request, alongside his own in-session edits):** 3 parallel copyedit agents + dev author-pass. The 7 bransch pages + kontroller + ai-agent came back **clean** (one open QUESTION: fastighet uses the abbreviation "LSO" unexplained where other pages spell it out — left for Alexander, it's standard branschjargong). On the five new pages, 8 fixes applied on top of Alexander's edits: du→ni-leak in styrdokument H2 6 ("ser ni"), comma splice in lagandringar ("…tagit ställning; granskade ändringar visar…"), tense fix ("har börjat gälla"), "som"-insertions for readability ×3 (kravpunkter mappstruktur, uppgifter "den halvan som…"/"uppföljningen som…"), disambiguated "samma checklista — men som en levande del" (kravpunkter), "grupperad i områden som…" (laglista). Cross-page terminology audit clean (Laglig/Laglig.se, Ändringar-fliken, kravpunkt/kontrollcykel/bevis-underlag consistent). Post-fix: titles 47–51, descriptions 163–169 (≤170), **0 FAQ-dupes of 81**, word counts 1,257–1,386, marketing suites 82/82.

- **Capability audit verdict (AC 4): copy may proceed with TWO corrections baked in at draft time:** (1) kravpunkter are agent-proposed/manual, never "färdiga per lag" — kravpunkter-page FAQ reworded; (2) bedömning→uppgift goes via the agent chat embedded in the assessment modal (with godkännande), not a dedicated button — uppgifter H2 4 + FAQ phrased accordingly. Plus the standing rule: bevakning claims stay SFS-/lagändrings-framed (AFS/EU are catalog-included but not auto-monitored).

### File List

**New (content):**
- `content/marketing/funktioner/{laglista,kravpunkter,lagandringar,uppgifter,styrdokument}.mdx`

**New (imagery — 16 screenshots, recipe v2):**
- `public/images/marketing/funktioner/laglista/{hero,filter,efterlevnad,kravpunkter-modal}.png`
- `public/images/marketing/funktioner/kravpunkter/{hero,modal,bevis,status}.png`
- `public/images/marketing/funktioner/lagandringar/{hero,bedomning,lista-pillar,historik}.png`
- `public/images/marketing/funktioner/uppgifter/{hero,sammanfattning,kanban,modal}.png`
- `public/images/marketing/funktioner/styrdokument/{hero,links,ai-utkast,lasvy}.png`

**New (imagery — Task 5 people photo, 1 of 1):**
- `public/images/marketing/people/laglista-genomgang-revision.jpg` (1200×896, EXIF-stripped) + the `type:'photo'` SplitFeature wiring in `laglista.mdx`

**New (Task 2 staging — scripts, run via `pnpm tsx`):**
- `scripts/seed-26-6-feature-staging.ts` (Almåsa scrub + tasks + styrdokument + IN_EDITOR action + vård kravpunkter/evidence + Tärnudden assessments + factual REPEAL event)
- `scripts/fix-26-6-polish.ts` (MBL kravpunkt-assignees + junk-doc archive)
- `scripts/cleanup-26-6-synthetic-events.ts` (removes the synthetic REPEAL post-shoot)
- `scripts/verify-26-6-staging.ts` (staging assertions)

**Modified (AC 7 retrofit, additions only):**
- `content/marketing/branscher/{bygg,hotell-restaurang,it,fastighet,vard-omsorg,industri,transport}.mdx` (+`/funktioner/laglista`; industri+transport also +`/funktioner/uppgifter`)
- `content/marketing/funktioner/kontroller.mdx` (+`/funktioner/kravpunkter`)
- `content/marketing/funktioner/ai-agent.mdx` (+`/funktioner/lagandringar` in relatedPages + one OQ2 anchor-discipline sentence in "Bevakar lagändringar åt er")

**Modified (test surface — AC 14):**
- `tests/e2e/marketing-nav.spec.ts` (content-derived Funktioner block: live links + anchor-fallback assertions)

**Modified (out-of-scope-but-blocking bugfix — flagged for PO):**
- `app/(workspace)/browse/lagar/[id]/historik/page.tsx` (Redis-string effectiveDate normalization ×2)
- `app/(public)/lagar/[id]/historik/page.tsx` (same ×2)

## QA Results

### Review Date: 2026-06-14

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Excellent, disciplined execution on the proven 26.4/26.5 playbook. Five content-complete feature pages ship with zero template/engine edits (the 26.4 gate stays closed), a self-documenting Dev Agent Record, and a capability-accuracy audit run **before** publish as AC 4 mandated. The single most important constraint for this story — that every product claim is backed by shipping code on `main` (not the `feat/tracking-events` WIP) — was taken seriously by the dev and independently confirmed by me.

This was a deep review (17 ACs, large diff). I directly verified the content/SEO gates, adversarially re-verified the capability audit, and (after an initial sandbox false-alarm — see note below) directly re-ran the build to confirm the build-dependent gates.

> **Correction (same-day):** my first pass reported a build failure (`@tailwindcss/container-queries` module-not-found). That was a **sandbox artifact** — the sandbox masked `node_modules` and even returned a false negative on the package.json grep. The package is installed and declared (`package.json:171`); the unsandboxed build is **green**. The earlier "build red" concern is **withdrawn**. Thanks to Alexander for the catch.

### Refactoring Performed

None. The story deliverables required no QA refactoring. Per the review constraints I did not touch story content outside this section.

### Compliance Check

- Coding Standards: ✓ (lint 0 errors, typecheck clean; 29 lint warnings are all pre-existing in unrelated test/script files)
- Project Structure: ✓ (slugs match `FUNKTIONER_NAV`; images under the canonical paths; staging scripts are tsc-excluded as expected; zero `components/marketing`/`lib/marketing` edits)
- Testing Strategy: ✓ (build green; e2e Funktioner block present and derivation verified standalone; full local e2e run deferred to CI — accepted 26.4 precedent)
- All ACs Met: ✓ (15 verified; AC 14 e2e deferred to CI and AC 17 GSC open — both by design)

### Requirements Traceability (verified directly unless noted)

- **AC 1** Research brief ✓ — exists, approved wholesale by Alexander (Change Log 0.4); 5 primaries adversarially verified.
- **AC 2** Five MDX pages ✓ — all present; titles 46–51 (≤60), descriptions 159–164 (≤170), showOrgCheck=true ×5; word counts 1,263–1,471 (above the ~1,000 convention but consistent with 26.4/26.5 actuals).
- **AC 3** Inverted bridge ✓ — 3–5 `<SplitFeature>` rows/page, H2 structure intact; product hand-off sentences spot-confirmed.
- **AC 4** Capability audit ✓ **(independently re-verified)** — adversarial agent checked 6 sampled claims against `git show main:`: all **SUPPORTED** (`add_obligation` always proposes a PendingAgentAction; lagandringar AI prefill is real; SFS-only cron detection; styrdokument dual-state/versioning/AI-draft shipped on main; negative guards absent; uppgifter agent-approval chat-only). The dev's two corrections and negative guards hold.
- **AC 5** relatedCatalogLaws ✓ counts 8/9/7/8/9 (all 5–10) by documentNumber/CELEX; **link-warnings.txt re-verified 0 bytes** ("0 unmatched catalog links" in the build log).
- **AC 6** FAQ ✓ — 6/6/5/6/6 (within 4–6); 81 published questions, **0 duplicates** (global uniqueness confirmed).
- **AC 7** relatedPages ✓ — 4 per page (≥4); retrofit is **additions-only** (0 content lines removed from live pages, verified via diff).
- **AC 8** Imagery ✓ — 4 screenshots/page (exceeds ≥2 epic min) + 1 people photo (EXIF-stripped).
- **AC 9** Workspace reuse ✓ — staging scripts present; no new workspaces; documented.
- **AC 10** Lighthouse ◐ — 0.92×5 per dev record (matches baseline); not re-run (expensive; trusted).
- **AC 11** Sitemap + OG ✓ — **sitemap re-verified** to carry all 5 funktioner URLs; OG ×5 200 image/png confirmed local **and preview** (Completion Notes 2026-06-13).
- **AC 12** JSON-LD ◐ — automatic; dev spot-verified on laglista.
- **AC 13** UTM/trackEvent ◐ — automatic; dev spot-verified.
- **AC 14** Nav flip + e2e ◐ — Funktioner content-derived test block present in spec; full local run deferred to CI (accepted 26.4 precedent).
- **AC 15** No regression ✓ — **build green (exit 0)**, lint 0 errors, typecheck clean (all re-run at review); unit 5,670 per dev record.
- **AC 16** PO + Alexander review ✓ — DONE, no changes requested (Completion Notes 2026-06-13).
- **AC 17** Post-publish GSC ○ — open by design (non-blocking; needs production deploy).

### Concerns

None blocking. Two items close out by design:

1. **AC 14 full e2e deferred to CI** — accepted 26.4 pattern; confirm green on push.
2. **AC 17 GSC open by design** — non-blocking; do after production deploy.

### Security Review

PASS. No auth/payment surface. Demo staging reused the id-aligned `anna.lindqvist@nordviken.example` login — no new auth logins (QA-26.4-M respected). 3rd-round Almåsa scrub verified clean (0/0); persona names only, no real PII in screenshots.

### Performance Considerations

PASS for the story's gate (SEO ≥90; 0.92×5 per dev). People photo EXIF-stripped to 130 KB; committed PNGs transcoded by next/image. Performance score is explicitly out of scope (AC 10).

### Reliability

The out-of-scope historik Redis-date crash (`effectiveDate.toISOString is not a function`) was fixed defensively at all 4 call sites — also repairing the public `/lagar/[id]/historik` SEO surface 500 in production. Root-cause cache-layer date revival registered as follow-up. Good catch and correct minimal scoping.

### Improvements Checklist

- [x] Re-ran build on a clean (unsandboxed) tree — green; link-warnings empty; sitemap carries the 5 URLs
- [ ] Confirm extended `marketing-nav.spec.ts` Funktioner test passes in CI on push
- [ ] Post-deploy: GSC submit ×5 + record Tier-1-complete baseline (AC 17)
- [ ] Run `cleanup-26-6-synthetic-events.ts` + remove "Teständring SFS 2099:901" catalog test-post when no longer needed for the demo shoot

### Files Modified During Review

None. (No File List update needed.)

### Gate Status

Gate: PASS → docs/qa/gates/26.6-tier-1-feature-pages-remainder.yml
Quality score: 95

### Recommended Status

✓ **Ready for Done** — all 26.6 deliverables verified: content/SEO gates pass, capability audit independently confirmed (6/6 SUPPORTED on main), and build/link-warnings/sitemap re-verified green at review. AC 14 (CI e2e) and AC 17 (GSC post-deploy) close out by design. Story owner decides final status.
