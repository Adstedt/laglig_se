# Story 7.8: Implement Employee Filters and Sorting

## Status

Draft

## Story

**As an** HR manager,
**I want** to filter and sort employees,
**so that** I find specific groups quickly.

## Acceptance Criteria

1. Filter bar on Employee List page
2. Filters: Department, Role, Contract Type, Compliance Status, Manager
3. Multi-select filters (AND logic)
4. Sort by: Name (A-Z), Employment Date (newest/oldest), Compliance Status
5. Filters persist in URL query params
6. Clear filters button
7. Export filtered list as CSV
8. Filter/sort works in both table and card views
9. Mobile: Filters in collapsible panel

## Tasks / Subtasks

- [ ] Build filter bar component
- [ ] Implement department filter (multi-select)
- [ ] Implement role filter (multi-select)
- [ ] Implement contract type filter (multi-select)
- [ ] Implement compliance status filter (multi-select)
- [ ] Implement manager filter (dropdown)
- [ ] Add sort dropdown
- [ ] Persist filters in URL query params
- [ ] Add clear filters button
- [ ] Implement CSV export functionality
- [ ] Make filters work with both table and card views
- [ ] Create mobile-responsive filter panel
- [ ] Test all filter combinations

## Dev Notes

**Filter Bar Component:**

```typescript
// components/hr/employee-filter-bar.tsx
'use client'

import { useState, useEffect } from 'react'
import { useRouter, useSearchParams } from 'next/navigation'

interface FilterBarProps {
  departments: string[]
  roles: string[]
  managers: Array<{ id: string; name: string }>
}

export function EmployeeFilterBar({ departments, roles, managers }: FilterBarProps) {
  const router = useRouter()
  const searchParams = useSearchParams()

  const [filters, setFilters] = useState({
    departments: searchParams.get('departments')?.split(',').filter(Boolean) || [],
    roles: searchParams.get('roles')?.split(',').filter(Boolean) || [],
    contractTypes: searchParams.get('contractTypes')?.split(',').filter(Boolean) || [],
    complianceStatus: searchParams.get('complianceStatus')?.split(',').filter(Boolean) || [],
    managerId: searchParams.get('managerId') || ''
  })

  const [sortBy, setSortBy] = useState(searchParams.get('sortBy') || 'name')
  const [sortOrder, setSortOrder] = useState(searchParams.get('sortOrder') || 'asc')
  const [showMobileFilters, setShowMobileFilters] = useState(false)

  // Update URL when filters change
  useEffect(() => {
    const params = new URLSearchParams()

    if (filters.departments.length > 0) params.set('departments', filters.departments.join(','))
    if (filters.roles.length > 0) params.set('roles', filters.roles.join(','))
    if (filters.contractTypes.length > 0) params.set('contractTypes', filters.contractTypes.join(','))
    if (filters.complianceStatus.length > 0) params.set('complianceStatus', filters.complianceStatus.join(','))
    if (filters.managerId) params.set('managerId', filters.managerId)
    if (sortBy) params.set('sortBy', sortBy)
    if (sortOrder) params.set('sortOrder', sortOrder)

    router.push(`/hr/employees?${params.toString()}`, { scroll: false })
  }, [filters, sortBy, sortOrder])

  function clearFilters() {
    setFilters({
      departments: [],
      roles: [],
      contractTypes: [],
      complianceStatus: [],
      managerId: ''
    })
    setSortBy('name')
    setSortOrder('asc')
  }

  const hasActiveFilters =
    filters.departments.length > 0 ||
    filters.roles.length > 0 ||
    filters.contractTypes.length > 0 ||
    filters.complianceStatus.length > 0 ||
    filters.managerId !== ''

  return (
    <div className="bg-white rounded-lg shadow p-4 mb-6">
      {/* Mobile Filter Toggle */}
      <button
        onClick={() => setShowMobileFilters(!showMobileFilters)}
        className="md:hidden w-full flex items-center justify-between px-4 py-2 bg-gray-100 rounded-lg mb-4"
      >
        <span className="font-medium">Filters {hasActiveFilters && `(${getActiveFilterCount(filters)})`}</span>
        <svg
          className={`w-5 h-5 transform transition-transform ${showMobileFilters ? 'rotate-180' : ''}`}
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M19 9l-7 7-7-7" />
        </svg>
      </button>

      {/* Filters */}
      <div className={`space-y-4 ${showMobileFilters ? 'block' : 'hidden md:block'}`}>
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-5 gap-4">
          {/* Department Filter */}
          <MultiSelectFilter
            label="Department"
            options={departments.map((d) => ({ value: d, label: d }))}
            selected={filters.departments}
            onChange={(selected) => setFilters({ ...filters, departments: selected })}
          />

          {/* Role Filter */}
          <MultiSelectFilter
            label="Role"
            options={roles.map((r) => ({ value: r, label: formatRole(r) }))}
            selected={filters.roles}
            onChange={(selected) => setFilters({ ...filters, roles: selected })}
          />

          {/* Contract Type Filter */}
          <MultiSelectFilter
            label="Contract Type"
            options={[
              { value: 'permanent', label: 'Permanent' },
              { value: 'fixed_term', label: 'Fixed-term' },
              { value: 'consultant', label: 'Consultant' }
            ]}
            selected={filters.contractTypes}
            onChange={(selected) => setFilters({ ...filters, contractTypes: selected })}
          />

          {/* Compliance Status Filter */}
          <MultiSelectFilter
            label="Compliance"
            options={[
              { value: 'compliant', label: 'Compliant' },
              { value: 'needs_attention', label: 'Needs Attention' },
              { value: 'non_compliant', label: 'Non-Compliant' }
            ]}
            selected={filters.complianceStatus}
            onChange={(selected) => setFilters({ ...filters, complianceStatus: selected })}
          />

          {/* Manager Filter */}
          <div>
            <label className="block text-sm font-medium text-gray-700 mb-1">Manager</label>
            <select
              value={filters.managerId}
              onChange={(e) => setFilters({ ...filters, managerId: e.target.value })}
              className="w-full px-3 py-2 border border-gray-300 rounded-lg text-sm"
            >
              <option value="">All Managers</option>
              {managers.map((manager) => (
                <option key={manager.id} value={manager.id}>
                  {manager.name}
                </option>
              ))}
            </select>
          </div>
        </div>

        {/* Sort and Actions Row */}
        <div className="flex flex-wrap items-center gap-4 pt-4 border-t border-gray-200">
          {/* Sort */}
          <div className="flex items-center gap-2">
            <label className="text-sm font-medium text-gray-700">Sort by:</label>
            <select
              value={sortBy}
              onChange={(e) => setSortBy(e.target.value)}
              className="px-3 py-1.5 border border-gray-300 rounded-lg text-sm"
            >
              <option value="name">Name</option>
              <option value="employmentDate">Employment Date</option>
              <option value="complianceScore">Compliance Score</option>
            </select>
            <button
              onClick={() => setSortOrder(sortOrder === 'asc' ? 'desc' : 'asc')}
              className="p-1.5 border border-gray-300 rounded-lg hover:bg-gray-50"
              title={sortOrder === 'asc' ? 'Ascending' : 'Descending'}
            >
              {sortOrder === 'asc' ? (
                <svg className="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
                  <path d="M3 3a1 1 0 000 2h11a1 1 0 100-2H3zM3 7a1 1 0 000 2h7a1 1 0 100-2H3zM3 11a1 1 0 100 2h4a1 1 0 100-2H3zM15 8a1 1 0 10-2 0v5.586l-1.293-1.293a1 1 0 00-1.414 1.414l3 3a1 1 0 001.414 0l3-3a1 1 0 00-1.414-1.414L15 13.586V8z" />
                </svg>
              ) : (
                <svg className="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
                  <path d="M3 3a1 1 0 000 2h11a1 1 0 100-2H3zM3 7a1 1 0 000 2h5a1 1 0 000-2H3zM3 11a1 1 0 100 2h4a1 1 0 100-2H3zM13 16a1 1 0 102 0v-5.586l1.293 1.293a1 1 0 001.414-1.414l-3-3a1 1 0 00-1.414 0l-3 3a1 1 0 101.414 1.414L13 10.414V16z" />
                </svg>
              )}
            </button>
          </div>

          {/* Clear Filters */}
          {hasActiveFilters && (
            <button
              onClick={clearFilters}
              className="px-3 py-1.5 text-sm text-gray-600 hover:text-gray-900 border border-gray-300 rounded-lg hover:bg-gray-50"
            >
              Clear Filters
            </button>
          )}

          {/* Export CSV */}
          <button
            onClick={() => exportEmployeesCSV(filters, sortBy, sortOrder)}
            className="ml-auto px-3 py-1.5 text-sm bg-blue-600 text-white rounded-lg hover:bg-blue-700 flex items-center gap-2"
          >
            <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path
                strokeLinecap="round"
                strokeLinejoin="round"
                strokeWidth={2}
                d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4"
              />
            </svg>
            Export CSV
          </button>
        </div>
      </div>
    </div>
  )
}

function formatRole(role: string): string {
  return role
    .split('_')
    .map((word) => word.charAt(0).toUpperCase() + word.slice(1))
    .join(' ')
}

function getActiveFilterCount(filters: any): number {
  let count = 0
  if (filters.departments.length > 0) count += filters.departments.length
  if (filters.roles.length > 0) count += filters.roles.length
  if (filters.contractTypes.length > 0) count += filters.contractTypes.length
  if (filters.complianceStatus.length > 0) count += filters.complianceStatus.length
  if (filters.managerId) count += 1
  return count
}
```

**Multi-Select Filter Component:**

```typescript
// components/hr/multi-select-filter.tsx
'use client'

import { useState, useRef, useEffect } from 'react'

interface Option {
  value: string
  label: string
}

interface MultiSelectFilterProps {
  label: string
  options: Option[]
  selected: string[]
  onChange: (selected: string[]) => void
}

export function MultiSelectFilter({ label, options, selected, onChange }: MultiSelectFilterProps) {
  const [isOpen, setIsOpen] = useState(false)
  const dropdownRef = useRef<HTMLDivElement>(null)

  useEffect(() => {
    function handleClickOutside(event: MouseEvent) {
      if (dropdownRef.current && !dropdownRef.current.contains(event.target as Node)) {
        setIsOpen(false)
      }
    }

    document.addEventListener('mousedown', handleClickOutside)
    return () => document.removeEventListener('mousedown', handleClickOutside)
  }, [])

  function toggleOption(value: string) {
    if (selected.includes(value)) {
      onChange(selected.filter((v) => v !== value))
    } else {
      onChange([...selected, value])
    }
  }

  return (
    <div className="relative" ref={dropdownRef}>
      <label className="block text-sm font-medium text-gray-700 mb-1">{label}</label>
      <button
        onClick={() => setIsOpen(!isOpen)}
        className="w-full flex items-center justify-between px-3 py-2 border border-gray-300 rounded-lg text-sm hover:bg-gray-50"
      >
        <span className={selected.length > 0 ? 'text-gray-900' : 'text-gray-500'}>
          {selected.length > 0 ? `${selected.length} selected` : `All ${label}`}
        </span>
        <svg
          className={`w-4 h-4 text-gray-400 transform transition-transform ${isOpen ? 'rotate-180' : ''}`}
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M19 9l-7 7-7-7" />
        </svg>
      </button>

      {isOpen && (
        <div className="absolute z-10 mt-1 w-full bg-white border border-gray-300 rounded-lg shadow-lg max-h-60 overflow-y-auto">
          {options.map((option) => (
            <label
              key={option.value}
              className="flex items-center gap-2 px-3 py-2 hover:bg-gray-50 cursor-pointer"
            >
              <input
                type="checkbox"
                checked={selected.includes(option.value)}
                onChange={() => toggleOption(option.value)}
                className="w-4 h-4 text-blue-600 border-gray-300 rounded"
              />
              <span className="text-sm text-gray-900">{option.label}</span>
            </label>
          ))}
        </div>
      )}
    </div>
  )
}
```

**Get Filtered Employees:**

```typescript
// lib/hr/get-filtered-employees.ts
import { prisma } from '@/lib/prisma'
import { withWorkspace } from '@/lib/auth/with-workspace'

interface FilterOptions {
  departments?: string[]
  roles?: string[]
  contractTypes?: string[]
  complianceStatus?: string[]
  managerId?: string
  sortBy?: string
  sortOrder?: 'asc' | 'desc'
}

export async function getFilteredEmployees(filters: FilterOptions) {
  return withWorkspace(async ({ workspaceId }) => {
    const where: any = { workspaceId, status: 'active' }

    // Apply filters
    if (filters.departments && filters.departments.length > 0) {
      where.department = { in: filters.departments }
    }
    if (filters.roles && filters.roles.length > 0) {
      where.role = { in: filters.roles }
    }
    if (filters.contractTypes && filters.contractTypes.length > 0) {
      where.contractType = { in: filters.contractTypes }
    }
    if (filters.complianceStatus && filters.complianceStatus.length > 0) {
      where.complianceStatus = { in: filters.complianceStatus }
    }
    if (filters.managerId) {
      where.managerId = filters.managerId
    }

    // Build orderBy
    const orderBy: any = []
    if (filters.sortBy === 'name') {
      orderBy.push({ name: filters.sortOrder || 'asc' })
    } else if (filters.sortBy === 'employmentDate') {
      orderBy.push({ employmentDate: filters.sortOrder || 'desc' })
    } else if (filters.sortBy === 'complianceScore') {
      orderBy.push({ complianceScore: filters.sortOrder || 'desc' })
    } else {
      orderBy.push({ name: 'asc' })
    }

    const employees = await prisma.employee.findMany({
      where,
      orderBy,
      include: {
        manager: {
          select: { id: true, name: true },
        },
        kollektivavtal: {
          include: {
            kollektivavtal: {
              select: { id: true, name: true },
            },
          },
        },
      },
    })

    return employees
  })
}
```

**Export CSV Function:**

```typescript
// lib/hr/export-employees-csv.ts
export async function exportEmployeesCSV(
  filters: any,
  sortBy: string,
  sortOrder: string
) {
  try {
    const params = new URLSearchParams()
    if (filters.departments.length > 0)
      params.set('departments', filters.departments.join(','))
    if (filters.roles.length > 0) params.set('roles', filters.roles.join(','))
    if (filters.contractTypes.length > 0)
      params.set('contractTypes', filters.contractTypes.join(','))
    if (filters.complianceStatus.length > 0)
      params.set('complianceStatus', filters.complianceStatus.join(','))
    if (filters.managerId) params.set('managerId', filters.managerId)
    if (sortBy) params.set('sortBy', sortBy)
    if (sortOrder) params.set('sortOrder', sortOrder)

    const response = await fetch(`/api/employees/export?${params.toString()}`)
    const blob = await response.blob()

    // Download file
    const url = URL.createObjectURL(blob)
    const link = document.createElement('a')
    link.href = url
    link.download = `employees-${new Date().toISOString().split('T')[0]}.csv`
    link.click()
    URL.revokeObjectURL(url)
  } catch (error) {
    console.error('Export failed:', error)
    alert('Failed to export CSV')
  }
}
```

**Export API:**

```typescript
// app/api/employees/export/route.ts
import { NextResponse } from 'next/server'
import { withWorkspace } from '@/lib/auth/with-workspace'
import { getFilteredEmployees } from '@/lib/hr/get-filtered-employees'

export async function GET(request: Request) {
  return withWorkspace(async () => {
    const { searchParams } = new URL(request.url)

    const filters = {
      departments:
        searchParams.get('departments')?.split(',').filter(Boolean) || [],
      roles: searchParams.get('roles')?.split(',').filter(Boolean) || [],
      contractTypes:
        searchParams.get('contractTypes')?.split(',').filter(Boolean) || [],
      complianceStatus:
        searchParams.get('complianceStatus')?.split(',').filter(Boolean) || [],
      managerId: searchParams.get('managerId') || undefined,
      sortBy: searchParams.get('sortBy') || 'name',
      sortOrder: (searchParams.get('sortOrder') as 'asc' | 'desc') || 'asc',
    }

    const employees = await getFilteredEmployees(filters)

    // Generate CSV
    const headers = [
      'Name',
      'Role',
      'Department',
      'Employment Date',
      'Contract Type',
      'Compliance Status',
      'Compliance Score',
      'Manager',
      'Email',
      'Phone',
    ]

    const rows = employees.map((emp) => [
      emp.name,
      emp.role,
      emp.department || '',
      new Date(emp.employmentDate).toLocaleDateString('sv-SE'),
      emp.contractType,
      emp.complianceStatus,
      emp.complianceScore.toString(),
      emp.manager?.name || '',
      emp.email || '',
      emp.phone || '',
    ])

    const csv = [
      headers.join(','),
      ...rows.map((row) => row.map((cell) => `"${cell}"`).join(',')),
    ].join('\n')

    return new NextResponse(csv, {
      headers: {
        'Content-Type': 'text/csv',
        'Content-Disposition': `attachment; filename="employees-${Date.now()}.csv"`,
      },
    })
  })
}
```

**Reference:** PRD Epic 7 Story 7.8, Next.js searchParams documentation

## Testing

**Unit Tests:**

- Multi-select filter toggles options correctly
- Filters update URL query params correctly
- getActiveFilterCount returns correct count
- Export CSV includes all filtered employees

**Integration Tests:**

- Select department filter, verify only matching employees displayed
- Select multiple filters (department + role), verify AND logic
- Change sort order, verify employees reordered correctly
- Click "Clear Filters", verify all filters reset
- Export CSV, verify file downloaded with correct data
- Filters persist after page refresh (URL params)

**Manual Testing:**

- Select multiple departments, verify employees filtered correctly
- Select compliance status "Needs Attention", verify only matching employees shown
- Sort by Employment Date (newest first), verify order correct
- Click clear filters, verify all employees displayed again
- Export CSV, open in Excel, verify data correct and formatted properly
- Test on mobile, verify filters collapse into dropdown
- Apply filters in table view, switch to card view, verify filters persist
- Share URL with filters, verify recipient sees same filtered view

**Edge Cases:**

- No employees match filters (show empty state)
- All filters selected (show all employees)
- Department/role with special characters in name (CSV escaping)
- Very long employee name (CSV wrapping)
- Export with 1000+ employees (test CSV generation performance)

**Performance Testing:**

- Filtering 1000 employees completes <500ms
- Sort operation completes <300ms
- CSV export for 500 employees completes <3 seconds
- Filter dropdown opens instantly (no lag)

**Mobile Testing:**

- Filter panel collapses on mobile
- Filter panel expands when tapped
- Multi-select dropdowns work on touch devices
- Sort dropdown accessible on mobile
- Export button accessible on mobile

**Test File:** `__tests__/features/hr/employee-filters.test.tsx`

## User vs Developer Responsibilities

**User Responsibility:**

- Use filters to find specific employee groups
- Export CSV for offline analysis or reporting
- Share filtered URLs with teammates for collaboration
- Clear filters when done to see full employee list

**Developer Responsibility:**

- Implement intuitive filter UI (clear labels, visual feedback)
- Support multi-select filters with AND logic
- Persist filters in URL for shareability
- Optimize filter queries for large datasets (database indexing)
- Generate CSV correctly (escape special characters, handle encoding)
- Make filters work seamlessly with both table and card views
- Ensure mobile-responsive filter panel
- Provide clear active filter indicators (count badges, clear button)
- Optimize performance (debounce filter changes, cache results)

## Change Log

| Date       | Version | Description            | Author     |
| ---------- | ------- | ---------------------- | ---------- |
| 2025-11-12 | 1.0     | Initial story creation | Sarah (PO) |

## Dev Agent Record

_To be populated by dev agent_

## QA Results

_To be populated by QA agent_
