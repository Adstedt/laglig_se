# Story 17.17: Inline Kravpunkter Editor in Efterlevnads Table Row Accordion

## Status

Ready for Review

## Story

**As a** compliance manager or workspace member working through my law list in the Efterlevnads (compliance) view,
**I want** to view and fully edit kravpunkter (add, remove, toggle fulfilment, attach evidence) directly inline from the table row accordion,
**so that** I can triage and update compliance on 20+ laws in one pass without context-switching into the legal document modal.

## Acceptance Criteria

1. In the Efterlevnads table row-expansion accordion (`compliance-detail-table.tsx`), the right card (`Hur efterlever vi kraven?`, backed by `LawListItem.compliance_actions` free text) is **replaced** by an inline instance of the existing `KravpunkterChecklist` component bound to the row's `listItemId`.
2. The left card (`Hur påverkar denna lag oss?`, backed by `LawListItem.business_context`) is **preserved unchanged** — impact context is still valuable and independent of the checklist.
3. Inline kravpunkter supports all capabilities of the existing modal implementation (no behavioral divergence): view the list with fulfilled/unfulfilled state, toggle `is_fulfilled` (auto-saves), inline add a new kravpunkt, inline edit `text`, delete with confirmation dialog, reorder, and link/unlink evidence files and workspace documents via the existing `FilePickerModal` and `DocumentPickerModal`.
4. Empty state when a law list item has zero kravpunkter: reuses the existing empty-state UI from `KravpunkterChecklist` ("Inga kravpunkter definierade" with the "Lägg till din första kravpunkt" prompt and inline add input).
5. When a `LawListItem` has non-empty `compliance_actions` free-text (legacy content authored before the inline kravpunkter migration), surface it as a **one-time dismissable banner** at the top of the kravpunkter area inside the row accordion: *"Tidigare beskrivning: [text] — Dölj."* Dismissal is persisted in `localStorage` keyed by `listItemId`. The underlying `compliance_actions` DB value is **not** deleted or mutated — it remains accessible via the legal document modal.
6. The `businessContext` card continues to render and edit exactly as before (no changes to the left column).
7. The `KravpunkterChecklist` inside the legal document modal (unchanged entry point from Story 17.16) continues to work. This story adds a *second* mount of the same component — it does not replace the modal instance.
8. Fetching: mount `KravpunkterChecklist` only when the accordion row is expanded, so the SWR fetch stays lazy. Both surfaces (modal + table row) share the SWR cache key `list-item-requirements:${listItemId}` — mutations in one surface are reflected in the other on next revalidate without extra code.
9. Progress (`fulfilled / total`) reported by the inline component via its existing `onProgressChange` callback is surfaced in the row's status indicator region where applicable (use the same pattern already applied in `DetailsBox` in the modal; if no clean integration point exists in the compliance table row, the callback can be a no-op — note the decision in the implementation).
10. Read-only mode: when the user lacks `tasks:edit` permission, the inline component renders in read-only mode (existing `readOnly` prop on `KravpunkterChecklist`). Permission check follows the same pattern as the modal — see `complianceReadOnly` threading in `app/(workspace)/laglistor/page.tsx:62`.
11. A unit test asserts the inline mount path: rendering `compliance-detail-table.tsx` with an expanded row containing a mocked list item shows `<KravpunkterChecklist>` with the correct `listItemId`.
12. Existing tests continue to pass: `tests/unit/components/features/document-list/legal-document-modal/kravpunkter-checklist.test.tsx` (8 tests) and `tests/unit/components/features/document-list/compliance-detail-table.test.tsx` (verify no regressions from the card swap).

## Tasks / Subtasks

- [x] **Task 1: Mount `KravpunkterChecklist` in compliance detail table row accordion** (AC: 1, 2, 3, 4, 8)
  - [x] In `components/features/document-list/compliance-detail-table.tsx`, locate the row-expansion section that renders the `Hur efterlever vi kraven?` card (backed by `complianceActions`).
  - [x] Replace that card's body with `<KravpunkterChecklist listItemId={...} readOnly={complianceReadOnly} />` — mount only when the row is expanded so fetch is lazy.
  - [x] Import from `components/features/document-list/legal-document-modal/kravpunkter-checklist.tsx` (existing component from Story 17.16, no changes).
  - [x] Keep the card's heading / wrapper intact (rename label to "Kravpunkter" to match the modal's convention per Story 17.16 AC 4).
  - [x] Leave the `Hur påverkar denna lag oss?` left card untouched (AC 2).

- [x] **Task 2: Legacy `compliance_actions` banner** (AC: 5)
  - [x] Inside the row accordion's kravpunkter area, above the `KravpunkterChecklist`, render a dismissable banner when `listItem.compliance_actions` is a non-empty string AND the `localStorage` key `laglig:kravpunkter-legacy-banner-dismissed:${listItemId}` is not set.
  - [x] Banner JSX: subtle muted bg (e.g. `rounded-md border bg-muted/30 px-3 py-2 text-xs text-muted-foreground`), "Tidigare beskrivning:" prefix, the `compliance_actions` value rendered as **plain text** (see HTML-strip note below), truncated to ~160 characters with a trailing ellipsis, and a "Dölj"/X button.
  - [x] HTML stripping: `compliance_actions` is populated by the existing `RichTextEditor` (Tiptap) from Story 17.16, so it contains HTML. Convert to plain text safely with `new DOMParser().parseFromString(html, 'text/html').body.textContent ?? ''` — this also decodes HTML entities (`&amp;` → `&`). A naive `.replace(/<[^>]+>/g, '')` does not decode entities and is not recommended.
  - [x] Clicking dismiss writes `'1'` to the localStorage key and removes the banner for this session without re-fetch.
  - [x] Guard localStorage access for SSR safety (`typeof window !== 'undefined'`).
  - [x] **Do not mutate or delete** the `compliance_actions` DB value — banner is UI-only.

- [x] **Task 3: Permission wiring** (AC: 10)
  - [x] The compliance detail table already receives `complianceReadOnly` from the page (`app/(workspace)/laglistor/page.tsx:62` computes `!hasPermission(ctx.role, 'tasks:edit')`).
  - [x] Thread `complianceReadOnly` through to the row accordion's `KravpunkterChecklist` mount as the `readOnly` prop.
  - [x] Verify AUDITOR role sees the read-only variant (disabled checkbox, hidden add/delete/link affordances) — existing behavior of `KravpunkterChecklist` from 17.16. _[Unit test asserts `data-read-only='true'` on stub; in-product AUDITOR verification is user-side manual QA.]_

- [x] **Task 4: Progress callback wiring (no-op for this story)** (AC: 9)
  - [x] The compliance table row renders `compliance_status` via the existing `StatusBadge` column — that's the only visual progress surface on the row today. Wiring kravpunkter progress into that badge would conflict with the explicitly-manual `ComplianceStatus` dropdown (user retains full control of compliance status per Story 17.16 AC 12).
  - [x] For this story: pass `onProgressChange={undefined}` to the inline `<KravpunkterChecklist>`. Document the decision in the PR description. _[Implemented by omitting the prop at the `ExpandedRowContent` call site — the `KravpunkterChecklist` prop is optional.]_
  - [x] **Out of scope:** adding a distinct "kravpunkter progress" visual to the table row (e.g., small progress bar next to the kravpunkter accordion label). Draft as a follow-up if desired.

- [x] **Task 5: Responsiveness check — component reuse assumption** (AC: 3)
  - [x] **Visual reference:** no Figma mockup exists for this story. Use the existing modal instance of `KravpunkterChecklist` as the visual target — open a law in the legal document modal to see the "right shape" of the component. The inline target should feel equally usable but at the narrower column width of a table row accordion.
  - [x] Render the inline accordion with 0, 1, 5, and 20 kravpunkter and visually verify the component fits the row accordion column width without overflow or cramping in all surfaces where the compliance table renders (e.g., the laglistor page `/laglistor` in the Efterlevnads tab). _[Code is in place with as-is reuse; **visual QA is user-side** and flagged in Completion Notes as pending manual review.]_
  - [x] If the component looks cramped at the accordion width, prefer adding a `variant="inline" | "modal"` prop to `KravpunkterChecklist` over forking it. Default to `"modal"` so the existing modal call site is unchanged. Only do this if QA flags a real problem — start with as-is reuse. _[Not triggered — QA feedback will determine if needed.]_

- [x] **Task 6: Unit test for inline mount** (AC: 11)
  - [x] Add a test in `tests/unit/components/features/document-list/compliance-detail-table.test.tsx` that renders the component with an expanded row for a list item (mock data: `complianceActions: '<p>legacy text</p>'`, no kravpunkter initially) and asserts:
    - `<KravpunkterChecklist>` is rendered with the correct `listItemId` prop.
    - Legacy banner is visible on first render, then disappears after clicking the dismiss button.
    - `localStorage.setItem` is called with the expected key on dismiss.
  - [x] Mock `KravpunkterChecklist` as a stub that surfaces its `listItemId` and `readOnly` props (follow the existing component-mock pattern used in `sidebar-hint.test.tsx` — `vi.mock('...', () => ({ KravpunkterChecklist: ({ listItemId, readOnly }) => (<div data-testid='kravpunkter-stub' data-list-item-id={listItemId} data-read-only={readOnly} />) }))`).
  - [x] Wrap the render in a fresh `SWRConfig` provider per existing pattern from `kravpunkter-checklist.test.tsx` to prevent cache bleed-through. _[Not needed — the `KravpunkterChecklist` is fully mocked so no real SWR fetcher runs. Added `localStorage.clear()` in `beforeEach` instead to prevent banner-dismissal state bleed.]_

- [x] **Task 7: Regression verification** (AC: 7, 12)
  - [x] Run `pnpm test -- tests/unit/components/features/document-list/` — all existing tests pass (304/304), including the kravpunkter modal tests (10/10 standalone) and the compliance-detail-table tests (28/28, up from 23 — 5 new tests added).
  - [x] Manual QA: open the legal document modal for a law that has kravpunkter defined via the inline table row accordion — verify the mutations from inline appear in the modal. Conversely, edit from the modal and verify the inline view picks up the change on next accordion expand. Both surfaces share SWR key `list-item-requirements:${listItemId}`. _[User-side manual verification — flagged in Completion Notes.]_
  - [x] `npx tsc --noEmit` — clean.

- [x] **Task 8: Memory update**
  - [x] Append a note to `MEMORY.md` under "Key Architecture Patterns" describing that `KravpunkterChecklist` now has two mount points (modal + compliance table row accordion) and that both share the SWR cache via `list-item-requirements:${listItemId}`.

## Dev Notes

### Provenance

Epic 17's original PRD (`docs/prd/epic-17-document-management-system.md`) defined 13 stories (17.1–17.13). Both Story 17.16 (kravpunkter — Done) and this Story 17.17 are **post-epic organic additions** made after the initial epic plan — they do not appear in the PRD's "Stories" section. The kravpunkter concept was a compliance-workflow enhancement requested after the document management core landed; 17.17 extends that enhancement to the Efterlevnads table-row surface. No additional PM scope approval required — confirmed in the validation conversation preceding this draft.

### Previous Story Insights (Story 17.16 — Done)

- `KravpunkterChecklist` is a **self-contained** client component that handles its own data fetching (SWR), mutations (server actions with optimistic updates + rollback via `toast.error()`), and UI state (collapsibles, inline edit, confirmation dialogs). It exposes a minimal prop surface: `listItemId: string`, `readOnly?: boolean`, `onProgressChange?: (p: { fulfilled: number; total: number }) => void` [Source: docs/stories/completed/17.16.kravpunkter-compliance-checklist.md#Task-5].
- SWR cache key is `list-item-requirements:${listItemId}`. Two concurrent mounts of the component with the same `listItemId` share the same in-flight request and cache entry via SWR's built-in dedup — zero extra plumbing needed for modal ↔ inline sync [Source: docs/stories/completed/17.16.kravpunkter-compliance-checklist.md#Task-5, docs/architecture/21-caching-and-data-fetching-strategy.md#swr].
- Story 17.16 already renamed the modal's accordion label from "Hur efterlever vi kraven?" → "Kravpunkter" and restructured the accordion body into `[KravpunkterChecklist] + separator + [Kommentar (RichTextEditor over compliance_actions)]`. Only the **legal document modal** instance was covered. The compliance detail table row accordion was **not** touched by 17.16 — it still renders the old two-card layout [Source: docs/stories/completed/17.16.kravpunkter-compliance-checklist.md#Task-7, AC 4].
- Permission gating convention: AUDITOR role lacks `tasks:edit`; page computes `complianceReadOnly = !hasPermission(ctx.role, 'tasks:edit')` and threads it through to compliance UI [Source: app/(workspace)/laglistor/page.tsx:62].
- Optimistic update with rollback is the established pattern — see how 17.16 wires it in `KravpunkterChecklist` [Source: docs/stories/completed/17.16.kravpunkter-compliance-checklist.md#Task-5 (checkbox toggle, delete, link)]. No changes needed for this story — behavior inherited.

### Data Models (no changes in this story)

- `LawListItemRequirement` — kravpunkt: `id` (UUID), `list_item_id` (FK → LawListItem, cascade), `text` (String), `is_fulfilled` (Boolean, default false), `position` (Float, default 0), `created_by` (FK → User), timestamps. Index `[list_item_id, position]` [Source: prisma/schema.prisma#LawListItemRequirement lines 353-370].
- `RequirementEvidenceLink` — evidence junction: `id` (UUID), `requirement_id` (FK → LawListItemRequirement, cascade), `file_id` (FK → WorkspaceFile, nullable — XOR), `workspace_document_id` (FK → WorkspaceDocument, nullable — XOR), `linked_at`, `linked_by`. Unique constraints `[requirement_id, file_id]` and `[requirement_id, workspace_document_id]`. XOR enforced at application layer via Zod `.refine()` [Source: prisma/schema.prisma#RequirementEvidenceLink lines 372-392].
- `LawListItem.compliance_actions` — legacy free-text field that backed the old "Hur efterlever vi kraven?" card. Preserved in DB; surfaced as a dismissable banner per AC 5. Not dropped in this story; a follow-up story can migrate + remove [Source: prisma/schema.prisma#LawListItem; Story 17.16 AC 23 explicitly preserved this field].

### API / Server Actions (already in place from Story 17.16 — no changes)

All server actions are at `app/actions/law-list-item-requirements.ts`:

- `getRequirementsForListItem(listItemId)` — returns requirements with linked evidence (files + workspace documents), ordered by position. Used as SWR fetcher by `KravpunkterChecklist`.
- `createRequirement(listItemId, text)`
- `updateRequirement(requirementId, { text?, is_fulfilled? })`
- `deleteRequirement(requirementId)`
- `reorderRequirements(listItemId, orderedIds[])`
- `linkEvidenceToRequirement(requirementId, { fileId?, workspaceDocumentId? })`
- `unlinkEvidenceFromRequirement(requirementId, { fileId?, workspaceDocumentId? })`

All use `withWorkspace('tasks:edit')` for auth, log to `ActivityLog`, and invalidate Redis cache `list-item-requirements:${listItemId}` [Source: docs/stories/completed/17.16.kravpunkter-compliance-checklist.md#Task-2, Task-3].

### Component Specifications

- **Reuse as-is**: `components/features/document-list/legal-document-modal/kravpunkter-checklist.tsx` — exported as `KravpunkterChecklist`. Props: `{ listItemId: string, readOnly?: boolean, onProgressChange?: (_p: { fulfilled: number; total: number }) => void }`. No modifications planned in this story.
- **Evidence pickers** (also reused as-is): `FilePickerModal` from `components/features/files/file-picker-modal.tsx`, `DocumentPickerModal` from `components/features/documents/document-picker-modal.tsx`. Both open as modals over the accordion; no z-index work expected.
- **Target file** (edited in this story): `components/features/document-list/compliance-detail-table.tsx` — the Efterlevnads table with row-expansion accordion. Currently renders two side-by-side cards for `businessContext` (left) and `complianceActions` (right). This story replaces the `complianceActions` card body with `<KravpunkterChecklist>` and preserves the `businessContext` card.

### File Locations

- **Modified:** `components/features/document-list/compliance-detail-table.tsx`
- **Tests modified / added:** `tests/unit/components/features/document-list/compliance-detail-table.test.tsx`
- **Reused (no edits):** `components/features/document-list/legal-document-modal/kravpunkter-checklist.tsx`, `app/actions/law-list-item-requirements.ts`, the file/document picker modals.
- **Memory:** append to `C:\Users\audri\.claude\projects\C--Users-audri-Desktop-dev-laglig-se\memory\MEMORY.md`.

### Technical Constraints

- **No schema changes.** Per-kravpunkt notes/kommentar is deliberately deferred — if desired later, draft a follow-up Story 17.18.
- **Lazy fetch.** Mount `KravpunkterChecklist` only when the row is expanded. Pairs well with the recent default-collapsed change in commit `c7cea40` (laglistor groups now default collapsed).
- **`localStorage` key naming** for legacy banner dismissal: `laglig:kravpunkter-legacy-banner-dismissed:${listItemId}`. Namespaced with `laglig:` prefix to avoid collision with other features.
- **SSR safety:** guard all `localStorage` access with `typeof window !== 'undefined'` to avoid hydration errors — this is the standard Next.js App Router idiom for client-only browser APIs. The component using localStorage must be a Client Component (`'use client'` directive) or the access must happen inside a `useEffect`.
- **No new packages.** Everything needed is already installed (zustand, swr, shadcn/ui primitives, zod, sonner toasts).

### Testing

- **Test file location:** `tests/unit/components/features/document-list/compliance-detail-table.test.tsx` (existing file — add a new describe block; do not create a new file).
- **Framework:** Vitest 4.0.9 + `@testing-library/react` + `@testing-library/user-event`. Standard project test stack [Source: docs/architecture/3-tech-stack.md].
- **Standards:**
  - Mock `KravpunkterChecklist` as a stub surfacing its props via `data-*` attributes (pattern: `vi.mock('@/components/features/document-list/legal-document-modal/kravpunkter-checklist', () => ({ KravpunkterChecklist: ({ listItemId, readOnly }) => (<div data-testid='kravpunkter-stub' data-list-item-id={listItemId} data-read-only={readOnly ?? false} />) }))`).
  - Wrap renders in a fresh `<SWRConfig value={{ provider: () => new Map() }}>` provider to prevent cache bleed-through between tests (follows pattern from `kravpunkter-checklist.test.tsx`).
  - Mock `localStorage` via `vi.spyOn(Storage.prototype, 'getItem' | 'setItem')` or provide a fresh in-memory shim per test.
- **Specific tests required for this story:**
  1. Renders `<KravpunkterChecklist>` stub with the correct `listItemId` when a row is expanded.
  2. Passes `readOnly={true}` to the stub when the table receives `complianceReadOnly={true}`.
  3. Legacy banner appears when `complianceActions` is non-empty AND localStorage key is not set.
  4. Clicking dismiss on the legacy banner writes the expected localStorage key and hides the banner.
  5. Legacy banner does NOT appear when `complianceActions` is an empty string.
- **Existing tests that must continue to pass:** `tests/unit/components/features/document-list/legal-document-modal/kravpunkter-checklist.test.tsx` (8 tests) and all other tests under `tests/unit/components/features/document-list/`.

### Project Structure Notes

- Story 17.16 established the `components/features/document-list/legal-document-modal/kravpunkter-checklist.tsx` location. It's nested under `legal-document-modal/` because 17.16 originated in the modal context. It's **reused** from outside that directory in this story — acceptable per the codebase's existing import patterns (components are exported and freely composed).
- If component reuse across contexts becomes a sustained pattern (e.g., third mount surface arises), consider a future story to promote `kravpunkter-checklist.tsx` to `components/features/document-list/kravpunkter-checklist.tsx` (one level up). Not in scope for this story.

## Change Log

| Date       | Version | Description                                                       | Author |
|------------|---------|-------------------------------------------------------------------|--------|
| 2026-04-14 | 0.1     | Initial draft via BMAD SM create-next-story task                  | Bob (SM) |
| 2026-04-14 | 0.2     | PO validation pass: concretized SSR reference, tightened Task 4 (out-of-scope progress surface noted explicitly), replaced naive HTML strip with DOMParser approach, added Provenance section (post-epic origin of 17.16 + 17.17), added visual-reference note to Task 5 | Sarah (PO) |
| 2026-04-14 | 1.0     | Implementation complete. All 8 tasks done; 304/304 document-list tests pass; typecheck clean. Status → Ready for Review. | James (Dev) |
| 2026-04-14 | 1.1     | Visual polish pass (PM feedback during review): editorial-extension treatment — uppercase tracking-wider section labels mirroring column-header style, 2fr/3fr proportional split with hairline vertical divider, softer bg (muted/30 → muted/20) and border (border/60), tightened typography hierarchy (leading-relaxed body, muted updated-at), legacy banner restyled to "ghost" (ring instead of border, bg-background/60). No AC changes; 28/28 tests still pass. | James (Dev, via frontend-design skill) |
| 2026-04-14 | 1.2     | Structural refactor (PM feedback: "feels randomized, not structured like table columns"). Replaced single-cell colSpan + nested grid with multi-cell expansion row that reuses the table's native column grid. Business context cell now aligns exactly under "Hur påverkar denna lag oss?" column header; kravpunkter cell aligns under "Hur efterlever vi kraven?". Section labels removed (column headers serve). Robust to column visibility/reordering via `row.getVisibleCells()` index lookup, with a graceful fallback to the previous nested-grid layout when columns are hidden or non-adjacent. `workspaceMembers` prop dropped from `ComplianceRow` / `VirtualComplianceRow` (dead after removing `getUserName` usage). Test updated to use `getAllByText` (column cell + expansion cell now both render the business context text). 304/304 document-list tests pass; typecheck clean. | James (Dev, via frontend-design skill) |
| 2026-04-14 | 1.3     | Centered detail-panel treatment (PM feedback: "too much negative space" under column-aligned v1.2). Abandoned cell-per-column mirroring. Expansion now renders a single colSpan cell with a subtly darker `bg-muted/30` recessed surface; inside that, a centered `max-w-6xl` card (bordered, `shadow-sm`, `bg-card`) floats with its own identity. The card contains a `1fr/1.3fr` two-section grid with its own mini-headers (AFFÄRSKONTEXT / KRAVPUNKTER) — distinct wording from the column headers above, each underlined with a hairline `border-b` for internal structure. Sections separated by `md:border-l` hairline. Removed `canUseColumnLayout` fallback (no longer needed). No AC changes, no test changes required. 28/28 tests pass; typecheck clean. | James (Dev, via frontend-design skill) |
| 2026-04-14 | 1.4     | Multi-row expansion (PM request). Switched expansion state from `string | null` to `Set<string>` so multiple rows can stay expanded simultaneously instead of accordion-replacing. Toggle handler add/deletes from the set; all prop signatures (`ComplianceRow`, `VirtualComplianceRow`) renamed `expandedRowId` → `expandedRowIds: Set<string>` with `.has()` membership check. Existing single-row test rewritten to verify multi-row behavior (expand two → both stay open; collapse one → other unaffected). 28/28 tests pass. | James (Dev) |

## Dev Agent Record

### Agent Model Used

claude-opus-4-6[1m] (via BMAD Dev agent "James")

### Debug Log References

None — implementation proceeded without blocking errors. Single typecheck pass after completing the full prop-threading chain (page → tables → rows → expansion → `KravpunkterChecklist`); both test runs (28 compliance-table tests, 304 full document-list suite) passed on first execution.

### Completion Notes List

- **Scope delivered:** All 12 ACs satisfied; all 8 tasks closed. Inline `KravpunkterChecklist` now mounts in the Efterlevnads table row accordion, replacing the old free-text `Hur efterlever vi kraven?` card. `businessContext` card preserved. Legacy banner for existing `compliance_actions` content renders with localStorage-dismissable UX.
- **Prop-threading path:** `app/(workspace)/laglistor/page.tsx` (existing `complianceReadOnly` computation) → `DocumentListPageContent` (already received it) → `ComplianceDetailTable` / `GroupedComplianceTable` → `ComplianceGroupSection` → `ComplianceRow` / `VirtualComplianceRow` → `ExpandedRowContent` → `KravpunkterChecklist.readOnly`.
- **`ExpandedRowContent` simplification:** removed the now-unused `getUserName` helper + `complianceActionsText` logic when lifting the right card out. Retained `workspaceMembers` prop on the interface for future additions (marked with `void workspaceMembers` to silence the unused-parameter lint — a cleaner solution would be to drop the prop entirely in a follow-up once we're certain no future fields need it).
- **Legacy banner `LegacyComplianceActionsBanner`:** defined inline in `compliance-detail-table.tsx` (kept local since it's a tight helper specific to this row context, not a reusable primitive). Uses `DOMParser` for safe HTML-to-text conversion with entity decoding, truncates at 160 chars, persists dismissal in `localStorage` under `laglig:kravpunkter-legacy-banner-dismissed:${listItemId}`.
- **Test updates:** one existing test (`'expands row showing full content on chevron click'`) was updated to reflect that the expansion no longer renders "Hur efterlever vi kraven?" as a section header (now shows "Kravpunkter"). Assertion changed from `toBeGreaterThanOrEqual(2)` to `toHaveLength(1)` for that string and added a positive assertion for "Kravpunkter". 5 new tests added in a dedicated `Inline kravpunkter editor (Story 17.17)` describe block.
- **SWRConfig wrapper (Task 6)**: not added. `KravpunkterChecklist` is fully mocked with `vi.mock(...)`, so no real SWR fetch runs during tests. Instead, `beforeEach(() => localStorage.clear())` prevents banner-dismissal state bleed between tests — a more targeted fix for the actual cross-test-state risk here.
- **Pending manual QA (not blocking):**
  1. Visual responsiveness check (Task 5 AC 3): verify the inline editor reads well with 0, 1, 5, and 20 kravpunkter in the table row accordion. If cramped, introduce a `variant="inline" | "modal"` prop on `KravpunkterChecklist` (see Open Question 3 in the story).
  2. Modal ↔ inline cross-surface sync (Task 7): edit a kravpunkt from the inline view and verify it's reflected in the modal (and vice versa) via SWR's shared cache key. Code path is correct per Story 17.16's SWR design, but worth confirming with a real interaction.
  3. AUDITOR role read-only verification: assign the AUDITOR role to a test user and verify the inline editor renders read-only (disabled checkboxes, no add/delete affordances). Unit test covers the prop-threading; in-app behavioral verification is user-side.
- **Branch note:** implementation landed on `misc-fixes` alongside the other chat/layout UX work currently being batched for that branch. User confirmed earlier in the session that continuing on `misc-fixes` is acceptable for this story.
- **No schema changes, no new dependencies, no package.json edits.** Pure UI-layer story.
- **Post-implementation structural refactor (v1.2):** v1.1's visual polish was still unsatisfying — the expansion's nested grid had no relationship to the table's column grid, so content didn't line up with the column headers above and the kravpunkter checklist stretched awkwardly across a too-wide container. The fix was to stop using a single colSpan cell with a nested grid and instead render the expansion as multiple native `<TableCell>`s. The browser's native table layout then aligns each cell to its column header automatically, and the kravpunkter cell inherits its column's natural width (no more chevron detachment). Column visibility and reordering are handled at render time by finding the `businessContext` / `complianceActions` column indices in `row.getVisibleCells()`; a fallback to the previous nested-grid layout kicks in when those columns are hidden or non-adjacent. Section labels in the expansion were removed entirely — the column headers directly above already label the content, and duplicating them added visual noise. `workspaceMembers` prop dropped from `ComplianceRow`/`VirtualComplianceRow` (unused after removing `getUserName`).
- **Post-implementation UI polish (v1.1):** after initial implementation, PM flagged that the expansion felt disconnected from the table's visual language. Applied an "editorial extension" treatment via the frontend-design skill:
  - Section labels switched from `h4 text-sm font-semibold` to `text-[10px] uppercase tracking-[0.08em] font-medium text-muted-foreground` to mirror the table's column-header style — the expansion now reads as an extension of the columns, not a separate card.
  - Grid changed from 50/50 to `md:grid-cols-[2fr_3fr]` with `md:divide-x md:divide-border/50` — gives kravpunkter the room it needs for interactivity and establishes a clear editorial split with a hairline vertical rule.
  - Background softened from `bg-muted/30` to `bg-muted/20`; border tightened to `border-border/60` for a more recessive surface.
  - Padding: `px-4 py-4` → `px-6 py-5` + `md:pr-8` / `md:pl-8` on the two columns — more breathing room.
  - Body typography: added `leading-relaxed` for better reading rhythm; "Senast uppdaterad" dropped to `text-[11px] text-muted-foreground/80`.
  - Legacy banner: `border bg-muted/30 text-xs` → `bg-background/60 ring-1 ring-border/50 text-[11px] leading-relaxed` — reads as a quiet ghost of past content, not a UI widget competing for attention.
- No ACs changed by the polish pass; no behavior changed; no test updates required — the assertions are on DOM presence and data attributes, not on specific class names.

### File List

**Modified:**

- `components/features/document-list/compliance-detail-table.tsx` — added `KravpunkterChecklist` import + `X` icon; added `complianceReadOnly?` prop to `ComplianceDetailTableProps`; added `LegacyComplianceActionsBanner` inline sub-component; refactored `ExpandedRowContent` to replace the compliance-actions card with inline kravpunkter + banner; threaded `complianceReadOnly` through `ComplianceRow` and `VirtualComplianceRow` to `ExpandedRowContent`.
- `components/features/document-list/compliance-group-section.tsx` — added `complianceReadOnly?` prop; threaded through to the nested `ComplianceDetailTable`.
- `components/features/document-list/grouped-compliance-table.tsx` — added `complianceReadOnly?` prop; threaded through to both `ComplianceGroupSection` call sites (grouped + ungrouped).
- `components/features/document-list/document-list-page-content.tsx` — wired `complianceReadOnly` (already in scope from the page) to both `ComplianceDetailTable` and `GroupedComplianceTable` render call sites.
- `tests/unit/components/features/document-list/compliance-detail-table.test.tsx` — added `vi.mock` stub for `KravpunkterChecklist`; updated the `'expands row showing full content on chevron click'` test to match the new expansion structure; added `Inline kravpunkter editor (Story 17.17)` describe block with 5 new tests covering mount, read-only threading, legacy banner visibility, banner dismissal + localStorage write, and empty-banner fallback.
- `C:\Users\audri\.claude\projects\C--Users-audri-Desktop-dev-laglig-se\memory\MEMORY.md` — appended the "Kravpunkter — dual-surface inline editor (Stories 17.16 + 17.17)" architecture note (user-memory, not a project file but required by Task 8).

**No files created or deleted.**

## QA Results

_To be filled by QA agent._
