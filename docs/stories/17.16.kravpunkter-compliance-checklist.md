# Story 17.16: Kravpunkter — Structured Compliance Checklist per Law List Item

## Status

Draft

## Story

**As a** compliance manager or workspace member,
**I want** to define specific compliance requirements (kravpunkter) for each law list item and link evidence documents to each requirement,
**so that** I can clearly track which obligations apply to our organization, whether they are fulfilled, and what evidence supports each one — enabling structured audit readiness and traceable compliance.

## Acceptance Criteria

### Data Model

1. New `LawListItemRequirement` Prisma model with: `id` (UUID PK), `list_item_id` (FK → LawListItem, cascade delete), `text` (String), `is_fulfilled` (Boolean, default false), `position` (Int), `created_by` (FK → User), `created_at`, `updated_at`. Unique constraint on `[list_item_id, position]`. Table map: `law_list_item_requirements`.
2. New `RequirementEvidenceLink` junction model with: `id` (UUID PK), `requirement_id` (FK → LawListItemRequirement, cascade delete), `file_id` (FK → WorkspaceFile, nullable), `document_id` (FK → WorkspaceDocument, nullable), `linked_at`, `linked_by` (FK → User). Unique constraints: `[requirement_id, file_id]`, `[requirement_id, document_id]`. Table map: `requirement_evidence_links`. At least one of `file_id`/`document_id` must be set (validated at application level).
3. Relations added: `LawListItem` has many `LawListItemRequirement`; `WorkspaceFile` and `WorkspaceDocument` have many `RequirementEvidenceLink`.

### UI — Kravpunkter Section

4. The existing "Hur efterlever vi kraven?" accordion in the law list item modal is restructured into a single accordion item with two visual sub-sections: **top** is "Kravpunkter" (structured checklist, new), **bottom** is "Kommentar" (the existing free-text rich text editor, renamed, retaining all existing content and functionality).
5. Each kravpunkt row displays: checkbox (fulfilled/not), requirement text (inline editable on click), evidence count badge (e.g. "2 bevis") when evidence is linked, expand/collapse chevron to show linked evidence, and a delete button (X icon) with confirmation toast.
6. Expanded evidence view per kravpunkt shows: list of linked files and documents with name/title, type icon, and unlink button (X). A "+ Länka bevis" action opens a picker allowing the user to link files (via `FilePickerModal`) or workspace documents (via `DocumentPickerModal`). Supports multiple evidence items per kravpunkt.
7. "+ Lägg till kravpunkt" button below the checklist — inline text input appears on click, Enter to save, Escape to cancel. No modal. Focus auto-moves to the new input.
8. Progress indicator in the accordion trigger header: "Kravpunkter — N/M uppfyllda" showing fulfilled vs total count. Hidden when no kravpunkter exist (accordion trigger falls back to current label).
9. Empty state when no kravpunkter exist: "Inga kravpunkter definierade" with a muted prompt "Lägg till din första kravpunkt" and the add button.
10. When read-only (user lacks edit permission), checkboxes are disabled, no add/delete/edit/link/unlink actions shown. Evidence list is still viewable and expandable.
11. Error handling: if a server action fails during inline add, checkbox toggle, or evidence link, show a `toast.error()` with the error message. Revert optimistic state on failure.

### Compliance Status Suggestion

12. When kravpunkter exist for a law list item, show a small info icon (tooltip) next to the "Efterlevnad" dropdown in `DetailsBox` suggesting a status based on checklist completion: 0 fulfilled → "Ej påbörjad", some → "Delvis uppfylld", all → "Uppfylld". This is informational only — the user retains full manual control of the compliance status dropdown.

### Server Actions

13. `createRequirement(listItemId, text)` — creates kravpunkt at next position. Validates input with Zod (text: string, max 500 chars). Logs to ActivityLog. Returns created requirement.
14. `updateRequirement(requirementId, { text?, is_fulfilled? })` — updates text or fulfillment. Validates with Zod. Logs to ActivityLog with old/new values.
15. `deleteRequirement(requirementId)` — deletes requirement and its evidence links (cascade). Logs to ActivityLog.
16. `reorderRequirements(listItemId, orderedIds[])` — updates positions in batch. No activity log needed.
17. `linkEvidenceToRequirement(requirementId, { fileId?, documentId? })` — links evidence. Prevents duplicates (unique constraint). Logs to ActivityLog.
18. `unlinkEvidenceFromRequirement(requirementId, { fileId?, documentId? })` — removes evidence link. Logs to ActivityLog.
19. `getRequirementsForListItem(listItemId)` — returns all kravpunkter with their linked evidence (files + documents), ordered by position.
20. All server actions use `withWorkspace` middleware for authorization and workspace isolation, following the exact pattern in `app/actions/legal-document-modal.ts`.

### Audit & Activity

21. All create/update/delete/link/unlink operations logged to `ActivityLog` with entity_type `'requirement'`.
22. Activity tab in the law list item modal shows requirement events: "La till kravpunkt: {text}", "Markerade som uppfylld: {text}", "Tog bort kravpunkt: {text}", "Länkade bevis till: {text}".

### Integration Requirements

23. Existing `compliance_actions` field content preserved — it continues to power the "Kommentar" sub-section. No data migration needed.
24. Existing compliance status functionality unchanged — manual override always available via DetailsBox dropdown.
25. Existing evidence (Bevis) section in the right panel and evidence tab remain independent — general evidence at the law list item level is separate from requirement-specific evidence.
26. Existing `FilePickerModal` and `DocumentPickerModal` reused for evidence linking.
27. No regression in existing law list item modal functionality.

### Quality Requirements

28. All server actions covered by unit tests: CRUD, workspace isolation, cascade delete, reorder, link/unlink, duplicate prevention, Zod validation.
29. Component tests for: kravpunkter list rendering, inline add flow, checkbox toggle, evidence expand/collapse, empty state, read-only mode, progress indicator.
30. Performance: modal with 20+ kravpunkter (each with 3 evidence links) loads in under 1 second.

## Tasks / Subtasks

- [ ] **Task 1: Prisma schema + migration** (AC: 1, 2, 3)
  - [ ] Add `LawListItemRequirement` model to `prisma/schema.prisma` after `LawListItem` model (~line 332)
  - [ ] Add `RequirementEvidenceLink` model with dual nullable FKs to `WorkspaceFile` and `WorkspaceDocument`
  - [ ] Add `requirements LawListItemRequirement[]` relation to `LawListItem` model
  - [ ] Add `requirement_evidence_links RequirementEvidenceLink[]` relations to `WorkspaceFile` and `WorkspaceDocument` models
  - [ ] Run `npx prisma migrate dev` — verify migration is additive only
  - [ ] Run `npx prisma generate` — verify types compile

- [ ] **Task 2: Server actions — CRUD** (AC: 13, 14, 15, 16, 19, 20, 21, 22)
  - [ ] Create new file `app/actions/law-list-item-requirements.ts`
  - [ ] Implement `createRequirement(listItemId, text)` with Zod validation, `withWorkspace('tasks:edit')`, position auto-increment, `logActivity()` call
  - [ ] Implement `updateRequirement(requirementId, { text?, is_fulfilled? })` with Zod, workspace isolation (join through LawListItem → LawList → workspace_id), `logActivity()` with old/new values
  - [ ] Implement `deleteRequirement(requirementId)` with workspace isolation, cascade delete, `logActivity()`
  - [ ] Implement `reorderRequirements(listItemId, orderedIds[])` with position update in transaction
  - [ ] Implement `getRequirementsForListItem(listItemId)` returning requirements with evidence (include file + document relations), ordered by position
  - [ ] All actions return `ActionResult<T>` pattern, invalidate Redis cache `list-item-details:${listItemId}`

- [ ] **Task 3: Server actions — evidence linking** (AC: 17, 18, 20, 21, 22)
  - [ ] Implement `linkEvidenceToRequirement(requirementId, { fileId?, documentId? })` — validate at least one ID provided, check unique constraint for duplicate prevention, `logActivity()`
  - [ ] Implement `unlinkEvidenceFromRequirement(requirementId, { fileId?, documentId? })` — find and delete link record, `logActivity()`
  - [ ] Both actions use `withWorkspace` — verify requirement belongs to workspace via join path

- [ ] **Task 4: Server action unit tests** (AC: 28)
  - [ ] Create `app/actions/__tests__/law-list-item-requirements.test.ts`
  - [ ] Test `createRequirement` — success, invalid input (Zod), workspace isolation
  - [ ] Test `updateRequirement` — text update, fulfillment toggle, workspace isolation
  - [ ] Test `deleteRequirement` — success, cascade deletes evidence links, workspace isolation
  - [ ] Test `reorderRequirements` — position updates
  - [ ] Test `linkEvidenceToRequirement` — success (file), success (document), duplicate prevention, at-least-one validation
  - [ ] Test `unlinkEvidenceFromRequirement` — success, not-found handling
  - [ ] Follow mocking pattern from `app/actions/__tests__/legal-document-modal.test.ts` (vi.mock prisma, workspace-context)

- [ ] **Task 5: KravpunkterChecklist component** (AC: 4, 5, 7, 8, 9, 10, 11)
  - [ ] Create `components/features/document-list/legal-document-modal/kravpunkter-checklist.tsx`
  - [ ] Props: `listItemId: string`, `readOnly?: boolean`
  - [ ] Fetch requirements on mount via `getRequirementsForListItem()`
  - [ ] Render checklist rows: checkbox + text + evidence badge + expand chevron + delete (X)
  - [ ] Inline add: "+ Lägg till kravpunkt" button → text input → Enter saves via `createRequirement()` → Escape cancels
  - [ ] Checkbox toggle: call `updateRequirement(id, { is_fulfilled })` with optimistic state update, revert on failure
  - [ ] Inline text edit: click text → input field → blur/Enter saves via `updateRequirement(id, { text })` → Escape reverts
  - [ ] Delete: X button → `deleteRequirement()` → remove from list. Use confirmation toast (undo pattern) or simple confirm.
  - [ ] Progress header: compute fulfilled/total count, render "N/M uppfyllda" in parent accordion trigger
  - [ ] Empty state: "Inga kravpunkter definierade" with add prompt
  - [ ] Read-only mode: hide all mutation controls, disable checkboxes
  - [ ] Error handling: `toast.error()` on failed mutations, revert optimistic state

- [ ] **Task 6: Evidence linking per kravpunkt** (AC: 6, 11)
  - [ ] Add expand/collapse per kravpunkt row (chevron toggle)
  - [ ] Expanded view: list linked evidence items (file name or document title, type icon, unlink X button)
  - [ ] "+ Länka bevis" button in expanded view — opens a small menu/dropdown with two options: "Fil" (opens `FilePickerModal`) and "Dokument" (opens `DocumentPickerModal`)
  - [ ] On picker selection: call `linkEvidenceToRequirement()` for each selected item
  - [ ] On unlink click: call `unlinkEvidenceFromRequirement()`, remove from list optimistically
  - [ ] Evidence count badge on collapsed row: show count when > 0

- [ ] **Task 7: Refactor ComplianceActions accordion** (AC: 4, 23)
  - [ ] In `compliance-actions.tsx`: rename accordion trigger label from "Hur efterlever vi kraven?" to "Kravpunkter"
  - [ ] Restructure accordion content: render `<KravpunkterChecklist>` at top, then a separator, then the existing `RichTextEditor` content (now labeled "Kommentar")
  - [ ] Pass `readOnly` prop through to `KravpunkterChecklist`
  - [ ] Progress indicator: pass kravpunkter count up to accordion trigger via state/callback
  - [ ] Ensure existing `compliance_actions` save/edit flow untouched for the "Kommentar" section

- [ ] **Task 8: Compliance status suggestion indicator** (AC: 12)
  - [ ] In `details-box.tsx`: accept new optional prop `requirementProgress?: { fulfilled: number; total: number }`
  - [ ] When `total > 0`, render a small `Info` icon next to the Efterlevnad dropdown
  - [ ] Tooltip content: "Baserat på kravpunkter: N/M uppfyllda — föreslår {status}"
  - [ ] Derive suggested status: 0 → EJ_PABORJAD, partial → PAGAENDE, all → UPPFYLLD
  - [ ] Pass progress data from modal index.tsx (lifted from KravpunkterChecklist state)

- [ ] **Task 9: Wire up in modal index.tsx** (AC: 4, 12, 27)
  - [ ] In `left-panel.tsx`: pass `readOnly` to refactored ComplianceActions
  - [ ] Lift requirement progress state: KravpunkterChecklist reports `{ fulfilled, total }` → ComplianceActions → LeftPanel → modal index.tsx → DetailsBox
  - [ ] Add SWR cache key `list-item-requirements:${listItemId}` for kravpunkter data
  - [ ] Verify all existing modal functionality still works (business context, tasks, activity, evidence)

- [ ] **Task 10: Component tests** (AC: 29)
  - [ ] Create `tests/unit/components/features/document-list/legal-document-modal/kravpunkter-checklist.test.tsx`
  - [ ] Test: renders list of kravpunkter with checkboxes and text
  - [ ] Test: inline add flow (click add → type → Enter)
  - [ ] Test: checkbox toggle calls updateRequirement
  - [ ] Test: expand/collapse shows evidence list
  - [ ] Test: empty state rendering
  - [ ] Test: read-only mode hides mutation controls
  - [ ] Test: progress indicator shows correct count

## Dev Notes

### Relevant Source Tree

```
prisma/
  schema.prisma                          # LawListItem model (~line 261), FileListItemLink (~line 1127), ComplianceStatus enum (~line 596)

app/actions/
  legal-document-modal.ts                # Existing server actions: updateListItemComplianceActions (line 476), getListItemDetails (line 374), Zod schemas (line 150+)
  __tests__/
    legal-document-modal.test.ts         # Test pattern for workspace isolation mocking

components/features/document-list/legal-document-modal/
  index.tsx                              # Modal assembly, data flow, callback wiring (line 281+)
  left-panel.tsx                         # Renders ComplianceActions accordion (lines 91-99)
  compliance-actions.tsx                 # "Hur efterlever vi kraven?" — refactor target (lines 27-36 for props)
  details-box.tsx                        # Compliance status dropdown, STATUS_CONFIG (lines 63-94)
  evidence-tab.tsx                       # Existing evidence tab (independent, untouched)
  activity-tabs.tsx                      # Activity feed with tabs

components/features/files/
  file-picker-modal.tsx                  # Reuse for file evidence linking (props lines 33-40)

components/features/documents/
  document-picker-modal.tsx              # Reuse for document evidence linking (props lines 51-57)

components/ui/
  rich-text-editor.tsx                   # Used in ComplianceActions for free-text (props lines 91-99)

lib/services/
  activity-logger.ts                     # logActivity(workspaceId, userId, entityType, entityId, action, oldValue, newValue)

lib/hooks/
  use-list-item-details.ts              # SWR hook, cache keys: list-item:${id}, list-item-extra:${id}
```

### Key Patterns to Follow

**Server Actions:** Follow `updateListItemComplianceActions` in `legal-document-modal.ts`:
- Zod schema validation at top
- `withWorkspace(callback, 'tasks:edit')` for auth
- Prisma query with workspace isolation join
- `logActivity()` call
- Redis cache invalidation: `deleteCache(\`list-item-details:${listItemId}\`)`
- Return `ActionResult<T>` type

**SWR Optimistic Updates:** Follow ComplianceActions pattern in `compliance-actions.tsx` (lines 97-132):
- `mutate(\`list-item-extra:${listItemId}\`, updater, { revalidate: false })`
- Also update `mutate(\`list-item:${listItemId}\`, ...)`
- For kravpunkter, add new cache key: `list-item-requirements:${listItemId}`

**Activity Logging:** Use `logActivity()` from `lib/services/activity-logger.ts`:
```typescript
await logActivity(ctx.workspaceId, ctx.userId, 'requirement', requirementId, 'requirement_created', null, { text })
```

**Naming Convention:** New models use `LawListItem` prefix scope — child records inherit workspace scope through `LawListItem → LawList → workspace_id` join path. No `Workspace` prefix needed.

**Component Pattern:** KravpunkterChecklist should be a self-contained component that fetches its own data (like TasksAccordion), not rely on parent to pass requirement data. This keeps the ComplianceActions refactor minimal.

### Testing

**Framework:** Vitest + React Testing Library (per architecture doc section 3.1)

**Test File Locations:**
- Server action tests: `app/actions/__tests__/law-list-item-requirements.test.ts`
- Component tests: `tests/unit/components/features/document-list/legal-document-modal/kravpunkter-checklist.test.tsx`

**Testing Standards:**
- Mock Prisma client, workspace-context module, and activity-logger using `vi.mock()`
- Test workspace isolation: verify `withWorkspace` is called with correct permission ('tasks:edit' for mutations, 'read' for queries)
- Test Zod validation: verify invalid inputs are rejected
- Component tests: use `render()` + `screen.getByRole/getByText` + `userEvent` for interactions
- Follow existing patterns in `app/actions/__tests__/legal-document-modal.test.ts` and `tests/unit/components/features/documents/document-picker-modal.test.tsx`

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-04-01 | 0.1 | Initial draft | Sarah (PO) |
| 2026-04-01 | 0.2 | Template compliance: added Tasks, Dev Notes, Testing, Change Log | Sarah (PO) |

## Dev Agent Record

### Agent Model Used

_To be populated during implementation_

### Debug Log References

_To be populated during implementation_

### Completion Notes List

_To be populated during implementation_

### File List

_To be populated during implementation_

## QA Results

_To be populated after implementation_
