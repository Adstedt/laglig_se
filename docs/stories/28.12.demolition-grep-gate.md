# Story 28.12: Demolition + Grep-Gate

**Epic:** 28 — Unified DataTable Core (`docs/prd/epic-28-unified-datatable-core.md`)
**Status:** Done (2026-07-08)

## Demolition audit (checklist zeroed)

Verified by grep — everything on the epic's cleanup checklist was already
deleted in its owning story:

- `shared/virtual-table-body.tsx` (28.1), `document-list-card.tsx`,
  `document-list-grid.tsx`, `grouped-document-list.tsx` (28.10) — gone;
  zero imports remain.
- Local `SortableHeader` copies — zero remain; `ui/sortable-header.tsx` is
  the single shared primitive (consumed by core consumers and Tier-0).
- Clamp modules: core `column-sizing.ts` + personalregister's
  `employee-column-state.ts` (kept VERBATIM per 28.7's localStorage
  contract) are the only `COLUMN_SIZE_BOUNDS` holders.
- `_columnOrderKey` exists only in the core's fixed row comparator.

**Kept deliberately** (plan's checklist assumed they'd be orphaned; they are
not): `ui/draggable-column-header.tsx` (now used BY the core),
`ui/column-settings.tsx` shell (personalregister/laglistor settings UIs),
`ui/sortable-header.tsx` (shared header affordance, both tiers).

## Grep-gate

`scripts/check-table-conventions.sh`, wired into CI's lint-and-typecheck
job. Fails on:

1. `useReactTable` anywhere outside `components/ui/data-table/` — one
   whitelisted exception: `landing-v3/marketing-document-table.tsx`, the
   permanently frozen presentational copy from Story 28.4.
2. Imports of any Epic-28-deleted module (virtual-table-body,
   document-list-card/grid, grouped-document-list).

## Bookkeeping

- `docs/prd/epic-list.md` — Epic 28 marked Done with deviation summary.
- `docs/prd/epic-28-unified-datatable-core.md` — status header added.

## Verification

- Gate script passes on the current tree; verified it FAILS when a
  disallowed `useReactTable` usage is introduced (manual smoke).
- tsc, eslint, unit + epic-28 e2e suites green (run in 28.11, tree
  unchanged since except docs/CI).
