# Story 16.3: Contextual Onboarding Questions

## Status

Draft

## Dependencies

- **Story 16.1** (auto-login) — user must reach onboarding without verification gate
- **Story 16.2** (landing page preview, optional) — if user came from landing page with URL analysis, inferred flags are passed via `?flags=food,personalData` query param and pre-toggled

## Story

**As a** new user going through onboarding,
**I want** to answer 3-5 quick yes/no questions about my business,
**so that** the system can accurately identify which regulations apply to my company beyond what's derivable from the org number alone.

## Acceptance Criteria

### Question Step

1. New onboarding wizard step between "Company Info" and "Confirm": "Verksamhetsfragor"
2. Step shows 3-5 yes/no toggle questions, dynamically selected based on company profile
3. Questions are rendered as `Switch` toggles with Swedish labels, each with `aria-describedby` linking to its description text (consistent with settings page activity flags pattern)
4. Step is skippable — user can proceed without answering (all default to false)
5. Step header: "Hjalp oss forsta er verksamhet battre" with subtext "Ju mer vi vet, desto battre kan vi identifiera relevanta lagar."

### Question Selection Logic

6. Questions are selected from a static pool based on company profile signals:
   - **Always asked:** "Hanterar ni personuppgifter utover anstallda?" (personalData), "Har ni kollektivavtal?" (has_collective_agreement)
   - **If SNI starts with 41-43 (construction):** "Bedriver ni bygg- eller anlaggningsverksamhet?" (construction)
   - **If SNI starts with 10-12, 56 (food/restaurant):** "Hanterar ni livsmedel?" (food)
   - **If SNI starts with 20-21 (chemicals/pharma):** "Hanterar ni kemikalier eller farliga amnen?" (chemicals)
   - **If employee_count > 0 or employer tax status:** "Har ni anstallda under 18 ar?" (minorEmployees)
   - **If any international indicators:** "Bedriver ni internationell verksamhet?" (internationalOperations)
   - **If SNI in manufacturing/logistics:** "Anvander ni tunga maskiner eller fordon i verksamheten?" (heavyMachinery)
7. Maximum 5 questions shown (prioritized by relevance to SNI code)
8. If landing page URL analysis (Story 16.2) already inferred flags via `?flags=` query param, pre-toggle those questions to "yes" with note: "Baserat pa er webbplats"

### Data Persistence

9. Activity flag answers (chemicals, construction, food, personalData, publicSector, heavyMachinery, minorEmployees, internationalOperations) map to CompanyProfile `activity_flags` JSON field — serialized as `JSON.stringify({ chemicals: true, food: false, ... })` in FormData (matching existing `taxStatus`/`ongoingProcedures` pattern)
10. `has_collective_agreement` maps to its own dedicated boolean field on CompanyProfile (NOT part of activity_flags JSON) — persisted as a separate FormData entry `fd.append('hasCollectiveAgreement', String(value))`
11. Answers are included in the FormData passed to `createWorkspace()` — same pipeline as enrichment fields from Story 15.2
12. If user skips the step, no activity flags are set (all remain null/false)

### UX Details

13. Smooth transition animation between steps (CSS transition or Framer Motion consistent with landing page)
14. Each question has a one-line explanation below the label (e.g., "Paverkar vilka arbetsmiljoforeskrifter som galler")
15. Progress indicator (wizard stepper) updated to show 3 steps with labels: "Foretagsinfo" → "Verksamhet" → "Bekrafta"

## Tasks / Subtasks

- [ ] **Task 1: Question pool and selection logic** (AC: 6-8)
  - [ ] 1.1 Create `lib/onboarding/question-selector.ts` — pure function: `selectQuestions(profile: { sniCode?, employerStatus?, inferredFlags? }) → Question[]`
  - [ ] 1.2 Define question pool with: id, label, description, flagKey (activity_flags key OR 'has_collective_agreement'), SNI prefix triggers
  - [ ] 1.3 Selection logic: always-ask questions + SNI-conditional questions, max 5, prioritized by SNI relevance
  - [ ] 1.4 If inferred flags from URL analysis exist (passed via query param), include those questions pre-toggled. Reuse `lib/shared/regulatory-area-mapper.ts` from Story 16.2 for SNI-to-area knowledge if needed.

- [ ] **Task 2: Onboarding wizard step** (AC: 1-5, 13-15)
  - [ ] 2.1 Create `app/onboarding/_components/activity-questions-step.tsx` — new wizard step component
  - [ ] 2.2 Render questions as `Switch` toggles with labels and descriptions. Add `aria-describedby` on each Switch linking to the description `<p>` element for screen reader accessibility.
  - [ ] 2.3 Accept `defaultValues` prop (for pre-toggled inferred flags from query params)
  - [ ] 2.4 "Hoppa over" (skip) button + "Nasta" (next) button
  - [ ] 2.5 Update `onboarding-wizard.tsx`: add `{ id: 'activity-questions' }` to WIZARD_STEPS array between company-info and confirm. Read `flags` query param from URL and pass as defaultValues.
  - [ ] 2.6 Update `wizard-stepper.tsx`: accept optional `labels` prop (string array). Render step labels ("Foretagsinfo", "Verksamhet", "Bekrafta") below the step circles. Fall back to current numeric display if no labels provided.

- [ ] **Task 3: Data persistence** (AC: 9-12)
  - [ ] 3.1 Extend `WorkspaceOnboardingSchema` in `lib/validation/workspace.ts` with: `activityFlags: z.string().optional()` (JSON-stringified object, matching taxStatus/ongoingProcedures pattern) and `hasCollectiveAgreement: z.boolean().optional()`
  - [ ] 3.2 In wizard `handleSubmit()`, append: `fd.append('activityFlags', JSON.stringify(flagsObject))` and `fd.append('hasCollectiveAgreement', String(value))`
  - [ ] 3.3 In `createWorkspace()` in `app/actions/workspace.ts`: read `activityFlags` from FormData, `JSON.parse()` safely (try/catch), write to `CompanyProfile.activity_flags`. Read `hasCollectiveAgreement` separately and write to its own boolean field `CompanyProfile.has_collective_agreement`.

- [ ] **Task 4: Tests** (AC: all)
  - [ ] 4.1 Test question selector: correct questions for restaurant SNI (56xxx → food), construction SNI (41xxx → construction), generic company (just always-ask)
  - [ ] 4.2 Test max 5 questions enforced even when all triggers match
  - [ ] 4.3 Test pre-toggle from inferred flags
  - [ ] 4.4 Test activity flags persist to CompanyProfile.activity_flags JSON
  - [ ] 4.5 Test has_collective_agreement persists to its own boolean field (not in activity_flags)

## Dev Notes

### Current Onboarding Steps

The wizard at `app/onboarding/_components/onboarding-wizard.tsx` currently has:
```typescript
const WIZARD_STEPS = [
  { id: 'company-info' },
  // Future: { id: 'law-list' },
  { id: 'confirm' },
] as const
```

This story adds `{ id: 'activity-questions' }` between company-info and confirm. The `handleBack` from confirm step uses `setCurrentStep((prev) => Math.max(0, prev - 1))` which will correctly navigate to the new activity-questions step (step 1), not company-info (step 0).

### Dual Persistence: activity_flags vs has_collective_agreement

**Important:** These go to DIFFERENT database fields:
- Activity flags (chemicals, construction, etc.) → `CompanyProfile.activity_flags` (`Json?` type) — written as a single JSON object
- Collective agreement → `CompanyProfile.has_collective_agreement` (`Boolean`, default false) — written as its own column

The settings page (`company-profile-tab.tsx`) reads both independently:
- `getActivityFlags(profile)` reads from `activity_flags` JSON (lines 152-161)
- `profile.has_collective_agreement` reads the boolean field directly (line 232)

### Query Param for Inferred Flags

If the user came from the landing page (Story 16.2), the signup redirect URL includes `?flags=food,personalData`. The onboarding page reads this and passes it through. The activity-questions step receives these as `defaultValues` and pre-toggles matching questions.

If the user navigated directly to signup (no landing page), `flags` is absent and all questions default to false.

### SNI Code Prefix Reference

| SNI Prefix | Industry | Relevant Flags |
|-----------|----------|---------------|
| 10-12 | Food production | food |
| 20-21 | Chemicals/pharma | chemicals |
| 41-43 | Construction | construction |
| 46-47 | Wholesale/retail | (generic) |
| 49-53 | Transport/logistics | heavyMachinery |
| 55-56 | Hotels/restaurants | food |
| 62-63 | IT/software | personalData |
| 84 | Public admin | publicSector |
| 85 | Education | minorEmployees |

### Key Files

```
NEW FILES:
  lib/onboarding/question-selector.ts               — Question selection logic (pure function)
  app/onboarding/_components/activity-questions-step.tsx — New wizard step
  tests/unit/lib/onboarding/question-selector.test.ts

MODIFIED FILES:
  app/onboarding/_components/onboarding-wizard.tsx   — Add step, read flags query param
  app/onboarding/_components/wizard-stepper.tsx       — Add step labels support
  lib/validation/workspace.ts                        — Add activityFlags + hasCollectiveAgreement
  app/actions/workspace.ts                           — Persist both fields to CompanyProfile
```

### Testing

- **Location:** `tests/unit/lib/onboarding/`
- **Framework:** Vitest
- **Focus:** Question selection logic is a pure function — straightforward unit testing

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-16 | 1.0 | Initial story creation | Sarah (PO) |
| 2026-03-16 | 2.0 | Validation fixes: flags query param source clarified (from 16.2), FormData serialization specified (JSON.stringify), dual persistence documented (activity_flags JSON vs has_collective_agreement boolean), stepper labels task expanded, aria-describedby added, dependency section added | Sarah (PO) |

## Dev Agent Record

_To be populated by dev agent_

## QA Results

_To be populated by QA agent_
