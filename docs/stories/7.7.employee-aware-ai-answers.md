# Story 7.7: Employee-aware, corpus-grounded AI answers with source-distinguished citations

## Status

Done

## Story

**As a** user,
**I want** the AI to answer employee-specific questions grounded in the full law & regulation corpus plus our kollektivavtal,
**so that** I get accurate, personalized compliance guidance — for whatever law or föreskrift applies, not just LAS.

## Acceptance Criteria

1. A user can bring an employee into chat context via an **employee pill/selector** in the chat input (net-new UI — no context-picker precedent exists; model on the attachment-chip pattern). Selector visible/usable only for roles with `employees:view`; removable pill; the selected `employeeId` rides the transport body per send.
2. The agent receives a compact **`<employee_context>` block** in the system prompt (mirror `formatCompanyContext` exactly): namn, anställningsform, anställningsdatum, personaltyp, sysselsättningsgrad, tilldelat kollektivavtal (name), aktiv/inaktiv. **Explicitly omitted — asserted by test: personnummer (any form), email, phone, address.** No employee selected → no block, zero behavior change.
3. **Retrieval spans the whole corpus**: a new `search_collective_agreements` agent tool (`sourceTypes: ['COLLECTIVE_AGREEMENT']`, mirroring `search-workspace-files.ts` — one sourceType per tool, `search_laws` NOT widened) joins the always-available tools. All legal search stays untouched; the model chooses instruments freely — nothing hardcoded to LAS.
4. **Multi-avtal bias (user requirement):** retrieval gains a `sourceId?: string` filter (`RetrievalOptions` + one WHERE line, same shape as the existing sourceType filter). When an employee with an assigned agreement is in context, the CA tool **hard-filters to that agreement's chunks**; no employee (or unassigned) → all workspace agreements. Workspace isolation clause unchanged.
5. **Citations visually distinguish** Lag/Föreskrift vs **Kollektivavtal** vs Egen fil: `SourceInfo` gains a `collectiveAgreementId` discriminator; `lib/ai/citations.ts#extractSourcesFromToolResult` gets a CA branch (mirror the workspace-files branch); `citation-pill.tsx` gets a CA variant — distinct icon + "Kollektivavtal" labeling, CTA opening the agreement's PDF via the existing file QuickPreview (`workspace_file_id`). Citation names the instrument (the contextual header already carries "Avtal (Kollektivavtal) > avsnitt").
6. **Scoped grounding eval** (epic AC amended — the referenced 19.13 harness is an unbuilt Draft): `scripts/eval-ca-grounding.ts` (modeled on `scripts/benchmark-retrieval.ts`) runs the epic's reference queries against `retrieveContext` and asserts (a) CA chunks retrieved are workspace-correct and agreement-correct under bias, (b) a cross-workspace probe returns **zero** foreign CA chunks, (c) legal-corpus retrieval still returns relevant instruments across ≥2 legal areas (uppsägningstid/LAS, semester/Semesterlagen, arbetstid/ATL). Retrieval-layer eval only; the full LLM answer-grounding harness remains Story 19.13.

## Tasks / Subtasks

- [x] **Task 0 — Bind:** read 7.6's Dev Agent Record when present (agreement actions may have moved/renamed reads) + re-verify the sweep's key seams (they're cited with line numbers in Dev Notes — trust but confirm signatures).
- [x] **Task 1 — Retrieval `sourceId` filter (AC: 4):** `RetrievalOptions.sourceId?: string` + `AND (${sourceId}::text IS NULL OR cc.source_id = ${sourceId})` in the WHERE (`lib/agent/retrieval.ts` ~L169–174). Tests: filter on/off, combined with sourceTypes, null-safe.
- [x] **Task 2 — `search_collective_agreements` tool (AC: 3, 4):** new `lib/agent/tools/search-collective-agreements.ts` mirroring `search-workspace-files.ts`; factory accepts `biasAgreementId?: string` (closure) → passes `sourceId`; **plus an optional model-supplied `agreementId` input param** (from a `lookup_employee` result — see Task 2b) which overrides the closure default. Safe by construction: a foreign/hallucinated id filters to zero chunks (workspace clause underneath). Swedish tool description telling the model when to search the avtal and to pass the employee's agreement id when known; register in `lib/agent/tools/index.ts` (ALWAYS_AVAILABLE). Do NOT touch `search_laws`.
- [x] **Task 2b — `lookup_employee` tool (user amendment 2026-07-03):** new `lib/agent/tools/lookup-employee.ts` — name search over the workspace's employees (case-insensitive contains on förnamn/efternamn), returning up to 5 matches with **exactly the context-block allowlist fields** (+ `collective_agreement {id, name}`) so the agent can handle typed-name questions ("Vilken uppsägningstid har Anna?") without the pill, and disambiguate ("två anställda heter Anna"). **Registered ONLY when the session role holds `employees:view`** (conditional registration in the tool factory — role is already passed to `createAgentTools`; a role without the permission has no tool at all, not a refusing tool). Tool description: use it when the user names an employee; pass the returned agreement id to `search_collective_agreements`. PII: same allowlist-by-construction + absence tests as the context block.
- [x] **Task 3 — Employee context plumbing (AC: 1, 2, 4):**
  - [x] `app/actions/employees.ts`: `getEmployeesForChatContext()` gated `employees:view` — minimal safe fields only (`id, first_name, last_name, personel_type, employment_form, employment_date, full_time_equivalent, inactive, collective_agreement {id,name}`)
  - [x] Transport: `employeeId` in the chat body (`use-chat-interface.ts` body seam ~L141–154); route ingestion (`app/api/chat/route.ts` ~L186–207)
  - [x] Route: fetch employee server-side **re-gated `employees:view`** (never trust the client id — verify workspace + permission); `formatEmployeeContext(...)` in `lib/agent/system-prompt.ts` (mirror `formatCompanyContext`, Swedish labels via the personalregister label maps' values — do not import client components into lib; duplicate the few label strings or extract to a shared lib module); `SystemPromptOptions.employeeContext?`; wrapped `<employee_context>` in `buildSystemPrompt`
  - [x] Resolve `collective_agreement_id` → pass as `biasAgreementId` into the tool factory
  - [x] No `employeeId` → all of the above inert (hot-path regression guard: existing chat tests must pass unchanged)
- [x] **Task 4 — Citations (AC: 5):** `SourceInfo.collectiveAgreementId?`; extraction branch in `citations.ts` (citationKey = the contextual header); `citation-pill.tsx` CA variant (distinct lucide icon, "Öppna kollektivavtalet" via the file QuickPreview path using the agreement's `workspace_file_id` — the extraction branch must carry `fileId` alongside for the preview). Persistence flows through `ChatMessage.metadata.citationSources` untouched.
- [x] **Task 5 — Employee pill UI (AC: 1):** picker in `chat-input-modern.tsx` (quick-action-chip area; attachment-chip pattern for the removable pill); fetches via `getEmployeesForChatContext` on open; renders name + personaltyp; capability-gated (only when the role has `employees:view` — thread the flag via the chat panel props like existing capability props); selected employee persists in panel state until removed; body carries the id per send.
- [x] **Task 6 — Scoped grounding eval (AC: 6):** `scripts/eval-ca-grounding.ts` per AC6 (reads DATABASE_URL env like `benchmark-retrieval.ts`; NOT part of the unit suite — document the run command). Include the three epic reference queries + a foreign-workspace leakage probe.
- [x] **Task 7 — Tests:** retrieval filter unit tests; tool registration + bias pass-through; `formatEmployeeContext` **PII-absence assertions** (personnummer/email/phone/address never present, in any form); `lookup_employee` tests — role-gated registration (no `employees:view` → tool absent from the registry), name matching + 5-cap, PII absence in tool OUTPUT, workspace isolation; CA-tool `agreementId` param precedence (param > closure bias > none) + foreign-id-filters-to-zero; citations CA branch; pill RTL (gated visibility, select/remove, body payload); route inert-path regression (no employeeId → byte-identical system prompt).
- [x] **Task 8 — Validation:** targeted + `pnpm typecheck` + eslint during; **full `pnpm vitest run tests/unit` once** (known assessment-detail failure excluded). **No schema change, no migration.** No git/DB-mutating commands/dev-server (the eval script is authored, not run — no live DB in this environment).

## Dev Notes

> Grounded in a verified sweep (2026-07-03) — key file:line index below. The chat route is the app's hottest path: **the no-employee path must be provably unchanged.**

### Seam index (verified)

| Concern | Where |
|---|---|
| Transport body seam | `lib/hooks/use-chat-interface.ts:141–154` (body), `:352–362` (per-send override pattern) |
| Route body ingestion | `app/api/chat/route.ts:186–207`; system-prompt assembly `:239–287` (CompanyProfile fetch→format→inject = the exact pattern to mirror) |
| Structured block precedent | `lib/agent/system-prompt.ts:95–229` (`formatCompanyContext`), wrap at `:270`, `SystemPromptOptions` `:31` |
| Retrieval WHERE | `lib/agent/retrieval.ts:99` (signature), `:169–174` (WHERE — add the sourceId line), `RetrievalOptions` `:20–30` |
| Tool template | `lib/agent/tools/search-workspace-files.ts` (whole file); registry `lib/agent/tools/index.ts:237–314`, ALWAYS_AVAILABLE `:205–209` |
| Citation extraction | `lib/ai/citations.ts:86–413`, `SourceInfo` discriminators `:21–56` (presence-of-field branching), workspace-files branch `:211–242` as the template |
| Citation pill | `components/features/ai-chat/citation-pill.tsx:286–358` (field-presence branches; legal = no icon, file = Paperclip → CA gets its own) |
| transparency.ts CA cases | already shipped in 7.5 (`:79–81`, `:124–126`) but NOT wired to the live pipeline — the live pipeline is citations.ts + citation-pill; don't confuse the two |
| Employee data | `lib/employees/employee-repository.ts` — `EmployeeView` strips `fortnox_raw`/masks pnr (`:33–65`); `EmployeeListRow.collective_agreement.id` (`:106–109`); `getWorkspaceEmployee(id)` gated view (`:195`) |
| Eval precedent | `scripts/benchmark-retrieval.ts`; 19.13 spec (Draft, unbuilt): `docs/stories/19.13.agent-answer-grounding-eval.md` |

### Design decisions (SM/PO, pre-made)

- **No new `ChatContextType` member** — the employee is an attribute of the turn (body field), not a context type; GLOBAL/LAW mounts keep working with an employee pill added.
- **One sourceType per tool** is the codebase invariant (`search-workspace-files.ts:8` "never widen the legal search") — hence the new tool, not a widened `search_laws`.
- **Hard filter, not boost**, for the assigned agreement (per user's multi-avtal requirement): reranker offers no per-source boost hook, and "their agreement, not the other one" is a correctness property, not a preference.
- **Epic AC amendment**: "reuse 19.13 harness" → scoped retrieval-layer eval script (19.13 doesn't exist; building the full answer-eval framework here would triple the story). Cross-workspace leakage assertion = the epic's Risk-Mitigation check, now executable.
- **CA content readability in chat**: agreement text is workspace content like files — retrieval tools don't add per-permission gating (consistent with `search_workspace_files`); the `employees:view` gate protects the employee *pill/context*, not the agreement text.

### Traps

1. **Never trust the client's employeeId**: the route re-fetches under `withWorkspace`/permission check — a crafted id from another workspace must return null → no block, no bias (test it).
2. **`formatEmployeeContext` must omit PII by construction** (allowlist of fields, not blocklist) — tests assert absence of personnummer/email/phone/address.
3. **Inert-path byte-equivalence**: no employeeId → identical system prompt, identical tools except the CA tool with `sourceId: undefined` (which must behave exactly like today's absence of the tool for legal queries — the tool existing is fine; it simply searches all workspace agreements when invoked).
4. Label strings for enums in the context block: `lib/` may not import from `components/` — extract or duplicate the few Swedish labels.
5. The eval script needs a live DB — author + document, don't execute here.
6. Decimal serialization does not apply server-side (route works with Prisma types directly; `full_time_equivalent.toNumber()` or string-format at the block boundary).

### Out of scope

Full LLM answer-grounding harness (19.13), drag-employee-into-chat (epic marks drag optional — pill only), the Personalkort modal's `renderChat` panel (optional in epic; skip), any schema change.

## Change Log

| Date       | Version | Description | Author |
| ---------- | ------- | ----------- | ------ |
| 2026-07-03 | 0.1     | Drafted from epic 7.7 + verified sweep. Two SM/PO resolutions baked in: 19.13-harness AC amended to a scoped retrieval eval (harness is an unbuilt Draft); multi-avtal assigned-agreement bias implemented as a net-new `sourceId` retrieval hard-filter (user requirement 2026-07-03). | Bob (SM) |
| 2026-07-03 | 0.2     | PO pass: server-side re-gating of client-supplied employeeId (trap 1), PII-by-construction allowlist + absence tests, hot-path inert-path regression guard, lib/components import boundary for labels, tool-permission stance documented. GO. Dev held for the 7.6 checkpoint. | Sarah (PO) |
| 2026-07-03 | 0.3     | User amendment: added Task 2b `lookup_employee` tool (typed-name questions without the pill; role-gated REGISTRATION — no `employees:view` → tool absent; same PII allowlist as the block; ≤5-match disambiguation) and the CA tool's model-supplied `agreementId` param with precedence param > pill-closure > none (foreign id safely filters to zero). Register-wide HR query tools explicitly deferred to a future story. | Bob (SM) / Sarah (PO) |
| 2026-07-03 | 1.0     | Implemented: retrieval `sourceId` hard filter, `search_collective_agreements` (+ agreementId precedence), role-conditional `lookup_employee`, `<employee_context>` block + route re-gating, employee pill UI, CA citation variant, `scripts/eval-ca-grounding.ts` authored, 70+ new/updated tests. Hot path proven inert (byte-identity + route-level + transport tests; existing chat tests pass unchanged). Status → Ready for Review. | James (Dev) |

| 2026-07-03 | 1.1     | QA SEC-001 closed (orchestrator fix round): the raw body rest is no longer spread into `buildSystemPrompt` — only type-checked `ChatContextInitial` display fields (title/sfsNumber/summary) pass through, server-computed fields structurally unoverridable. New MEMBER-injection route test asserts all spoofed prompt fields dropped (JSON-level 'SPOOFED' absence) while legit display fields flow. Route+prompt+quota suites 60/60; typecheck + eslint clean. QA re-review requested. | Checkpoint fix |
| 2026-07-04 | 1.2     | User E2E checkpoint: TOOL_CONFIG display labels added for both new tools (`search_collective_agreements` → 'Söker/Sökte i kollektivavtal', `lookup_employee` → 'Slår upp/Slog upp anställd') — raw tool names no longer shown in the chat activity list. | Checkpoint fix |
## Dev Agent Record

### Agent Model Used

Claude Fable 5 (claude-fable-5)

### Debug Log References

- Targeted runs (green while developing): retrieval 17 (incl. 3 new sourceId), search-collective-agreements 11 (new), lookup-employee 10 (new), registry-role-filter 7 (counts 34→35/36 + lookup matrix), registry-narrowing 10 (FULL excludes role-conditional lookup_employee), system-prompt 47 (incl. 8 new formatEmployeeContext/<employee_context>), citations 54 (+2 CA branch), citation-pill 10 (+4 CA variant), chat-route-employee-context 5 (new), chat-quota-enforcement 7 (UNCHANGED, pass — hot-path guard), employee-context-picker 5 (new), chat-input-modern-employee-pill 4 (new), use-chat-interface-employee 4 (new), lib/employees/labels 7 (new, incl. drift guard), app/actions/employees 42 (+2 Story 7.7).
- Combined chat/agent sweep before the full run: `tests/unit/components/features/ai-chat` + right-sidebar + hem-chat + `tests/unit/agent` + `tests/unit/lib/agent` + `tests/unit/lib/ai` → 802 passed, 1 failed = the KNOWN pre-existing `assessment-detail.test.tsx` calendar time-bomb (documented by 7.2–7.6; file untouched here).
- Full suite (run exactly once at completion): `pnpm vitest run tests/unit` → **6307 passed, 5 skipped, 1 failed — pre-existing, unrelated**: `tests/unit/components/features/ai-chat/details/assessment-detail.test.tsx` calendar time-bomb (same failure documented by 7.2–7.6; file untouched here).
- `pnpm typecheck` clean (exit 0); eslint clean on all changed source + test files.
- No schema change, no migration, no DB commands, no dev server, no git operations. The eval script was authored, NOT executed (needs a live DB).

### Completion Notes List

- **Task 0 bindings honored:** 7.6's record confirmed the CA read/write actions still live in `app/actions/collective-agreements.ts` (nothing this story consumes moved); the sweep seams verified against live code — `RetrievalOptions` at `lib/agent/retrieval.ts:20`, WHERE at `:169–174`, `formatCompanyContext` at `system-prompt.ts:95`, tool registry policy/ALWAYS_AVAILABLE derivation, `use-chat-interface.ts` per-send body-override pattern (Story 19.1 attachments), CA chunk metadata (`agreement_name`/`workspace_file_id`, `chunk-workspace-document.ts:216–223`).
- **Inert-path proof (guardrail 1), three independent layers:** (1) *byte-identity test*: `buildSystemPrompt({companyContext, contextType})` === `buildSystemPrompt({..., employeeContext: undefined})` asserted with `toBe` (string identity), plus absence of `<employee_context>`; (2) *route-level test* (new `chat-route-employee-context.test.ts`): body without `employeeId` → `prisma.employee` NEVER queried, `buildSystemPrompt` receives `employeeContext: undefined`, `createAgentTools` receives no `biasAgreementId`; (3) *transport test*: `sendMessage(content)` without employee/attachments produces NO body override (pre-7.7 call shape byte-equal). Existing chat tests (chat-quota route tests, right-sidebar, hem-chat, all ai-chat component tests) pass UNTOUCHED.
- **PII by construction:** the route's employee fetch, `getEmployeesForChatContext` and `lookup_employee` all use Prisma ALLOWLIST `select`s (no personnummer/email/phone/address/fortnox_raw is ever read); `formatEmployeeContext` takes a typed allowlist shape and only reads those fields. Absence tests feed POLLUTED records (carrying pnr/email/phone/address) through both `formatEmployeeContext` and the lookup tool's mapping and assert no byte leaks; select-shape tests pin the allowlist keys.
- **`lookup_employee` role-conditional registration:** built unconditionally in the tools literal (keeps `typeof tools` NON-optional — see ripple note below), then the key is `delete`d before decision-log wrapping unless `role != null && hasPermission(role, 'employees:view')`. Fail-closed: undefined role (legacy callers/unauth fallback) → tool absent. Tested for OWNER/HR_MANAGER (present) and ADMIN/MEMBER/AUDITOR/undefined (absent).
- **CA tool bias precedence** implemented as `agreementId ?? biasAgreementId` (param > pill-closure > none) and tested including the foreign-id case (id passed through untouched; retrieval's workspace clause is the safety net — zero rows surface as the Swedish empty-result guidance steering back to unbiased search / `search_laws`).
- **Pill UX (AC 1):** the picker is a `UserRound` icon button in `chat-input-modern.tsx`'s bottom toolbar (next to Bifoga), tooltip "Fråga om en anställd". It opens a Popover+Command combobox ("Sök anställd...") that lazy-fetches `getEmployeesForChatContext` on first open; rows show namn + personaltyp label + "· Inaktiv" suffix. Selection renders a removable chip in the attachment-chip row (UserRound icon, "Anna Svensson · Tjänsteman", X with Swedish aria-label). State lives in `ChatPanelBody` → persists across sends until removed; the id rides EVERY send (incl. followup chips + suggested questions) as a per-send body override. The picker self-gates on `usePermissions().can.viewEmployees` (fail-closed), so every ChatPanel mount (sidebar GLOBAL, task/law modals) gets it with zero per-mount threading; the server re-gates regardless.
- **Citations (AC 5):** `SourceInfo.collectiveAgreementId` discriminator (checked BEFORE the fileId file-branch); extraction registers the full contextual-header citationKey PLUS two fallback keys (bare avtalsnamn, "namn (Kollektivavtal)") so shorter model citations resolve. Pill: Handshake icon, "Kollektivavtal" type label (replaces the pseudo-documentNumber line), snippet preview, "Öppna kollektivavtalet" CTA via the existing QuickPreview/`getFileById` path on the agreement's `workspace_file_id`; CA without a backing file degrades to hover-preview only (no CTA, no crash, never the legal sidebar).
- **Eval script (AC 6) — user runs it:** `npx tsx scripts/eval-ca-grounding.ts` (auto-discovers a CA-bearing workspace, its first chunked agreement, and a foreign workspace) or explicitly `npx tsx scripts/eval-ca-grounding.ts --workspace <id> [--agreement <id>] [--foreign-workspace <id>] [--top 8]`. Needs `.env.local` with DATABASE_URL + embedding/rerank keys. Exit 0 = all assertions pass; per-assertion PASS/FAIL printed, including the hostile probe (foreign workspace + the target's agreementId → must be zero rows).
- **Deviations / judgment calls:** (1) *Swedish labels duplicated* into `lib/employees/labels.ts` (personalregister is READ-ONLY this story and lib may not import components) with an equality drift-guard test pinning both maps identical. (2) *Out-of-zone ripple fix, authorized by the checkpoint message:* an optional `lookup_employee?:` property on the tools object poisoned the AI SDK's `toolCalls` union in `lib/agent/skills/generate-law-list.ts` (4× TS18048 under exactOptionalPropertyTypes). Fixed at the type-definition side (unconditional declaration + runtime `delete`) — generate-law-list.ts itself was NOT edited. (3) CA pill click fires `ca_citation_clicked` and then reuses `handleOpenFile`, which also fires `file_citation_clicked` — accepted double event rather than forking the fetch logic. (4) Followup chips + suggested questions also carry the active employeeId (pill semantics: "in context until removed"). (5) `registry-narrowing.test.ts`'s FULL constant now excludes `lookup_employee` (role-conditional, like the route-injected web_search) — counts bumped 34→35, baseline 33→34; these are registry tests, not chat-path tests.
- **Registry counts after 7.7:** MEMBER/undefined-role factory set 35 (＋search_collective_agreements); OWNER/HR_MANAGER 36 (＋lookup_employee); policy = OWNER factory set ∪ {web_search}.
- **QA should scrutinize:** see list below.
- **QA scrutiny list:**
  - The `<employee_context>` block's exact wording (system-prompt.ts) — it instructs the agent to use `search_collective_agreements` and mentions the avtals-ID; verify tone/behavior in a live chat.
  - CA tool is in ALWAYS_AVAILABLE for every role (incl. AUDITOR) per the Dev-Notes stance "agreement text is workspace content" — confirm product intent that AUDITOR can search agreement text while never seeing the employee pill/lookup.
  - `lookup_employee` name matching is contains-based per token (AND across tokens) — "Ann" matches "Anna" and "Annika"; no fuzzy/diacritic folding beyond Prisma `insensitive`. Acceptable for ≤5-disambiguation?
  - Employee pill state is per-panel React state — it does NOT persist across a full page reload (by design: turn attribute, not a context type). Confirm UX expectation.
  - The double analytics event on CA-pill click (`ca_citation_clicked` + `file_citation_clicked`) — dashboards counting file opens will include CA opens.
  - Eval script not executed here (no live DB) — run `npx tsx scripts/eval-ca-grounding.ts` against staging before sign-off; the cross-workspace probe is the epic's Risk-Mitigation check.
  - Not verified in a live browser (no dev server per story rules): picker popover layering inside the chat sidebar + the QuickPreview from a CA pill deserve a browser pass.

### File List

**New:**

- `lib/agent/tools/search-collective-agreements.ts`
- `lib/agent/tools/lookup-employee.ts`
- `lib/employees/labels.ts` (lib-side Swedish labels + formatters; drift-pinned to personalregister)
- `components/features/ai-chat/employee-context-picker.tsx`
- `scripts/eval-ca-grounding.ts` (authored, not executed — needs live DB)
- `tests/unit/agent/tools/search-collective-agreements.test.ts`
- `tests/unit/agent/tools/lookup-employee.test.ts`
- `tests/unit/app/api/chat-route-employee-context.test.ts`
- `tests/unit/components/features/ai-chat/employee-context-picker.test.tsx`
- `tests/unit/components/features/ai-chat/chat-input-modern-employee-pill.test.tsx`
- `tests/unit/lib/hooks/use-chat-interface-employee.test.tsx`
- `tests/unit/lib/employees/labels.test.ts`

**Modified:**

- `lib/agent/retrieval.ts` (RetrievalOptions.sourceId + one WHERE line)
- `lib/agent/tools/index.ts` (register CA tool + role-conditional lookup_employee; AgentToolContext.biasAgreementId; ToolName/policy entries)
- `lib/agent/system-prompt.ts` (formatEmployeeContext + EmployeeContextEmployee + SystemPromptOptions.employeeContext + `<employee_context>` injection)
- `app/api/chat/route.ts` (employeeId ingestion; re-gated allowlist fetch; employeeContext + biasAgreementId threading)
- `lib/hooks/use-chat-interface.ts` (SendMessageOptions.employeeId per-send body override)
- `app/actions/employees.ts` (+ getEmployeesForChatContext, `employees:view`-gated read)
- `lib/ai/citations.ts` (SourceInfo.collectiveAgreementId + search_collective_agreements extraction branch)
- `components/features/ai-chat/citation-pill.tsx` (CA variant: Handshake icon, Kollektivavtal label, Öppna kollektivavtalet CTA)
- `components/features/ai-chat/chat-input-modern.tsx` (picker slot + removable employee chip)
- `components/features/ai-chat/chat-panel.tsx` (panel state + threading incl. followups/suggested questions)
- `tests/unit/agent/retrieval.test.ts` (+3 sourceId tests)
- `tests/unit/agent/tools/registry-role-filter.test.ts` (counts 34→35/36; lookup_employee role matrix; policy-coverage via OWNER)
- `tests/unit/agent/tools/registry-narrowing.test.ts` (FULL excludes lookup_employee; counts; OWNER-keeps-both narrowing test)
- `tests/unit/agent/system-prompt.test.ts` (+8: formatEmployeeContext, PII absence, byte-identity inertness, block ordering)
- `tests/unit/lib/ai/citations.test.ts` (+2 CA extraction tests)
- `tests/unit/components/features/ai-chat/citation-pill.test.tsx` (+4 CA variant tests)
- `tests/unit/app/actions/employees.test.ts` (+2 getEmployeesForChatContext tests)

## QA Results

### Review Date: 2026-07-03

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Deep review (flagship story; hot-path + PII + isolation surface; >5 ACs → auto-escalated). The implementation is disciplined and pattern-faithful: every new seam mirrors its cited precedent (CA tool ← search-workspace-files, employee block ← formatCompanyContext, citation branch ← workspace-files branch, transport ← Story 19.1 body override), and the security-relevant properties are engineered *by construction* rather than by policing — typed allowlist shapes, Prisma allowlist `select`s, fail-closed gates, and the workspace clause as the invariant safety net under the new `sourceId` filter. Test quality is genuinely high: polluted-record byte-absence assertions, `toBe` string identity for the inert path, adversarial route tests, and registry matrices covering all five roles plus undefined.

**PII verdict: PASS (by construction).** I traced every path an employee record takes toward the LLM: (1) route fetch (`app/api/chat/route.ts:276–288`) — allowlist `select`, no pnr/email/phone/address/fortnox_raw ever read; (2) `formatEmployeeContext` (`lib/agent/system-prompt.ts:260–307`) — typed allowlist shape, only named fields read; polluted-record test asserts byte absence (incl. city/postcode); (3) `lookup_employee` — allowlist `select` + allowlist output mapping, polluted-row test asserts JSON-wide absence incl. bare `@`; (4) `getEmployeesForChatContext` — allowlist `select` under `withWorkspace(..., 'employees:view')`. Nothing logs the employee block (no console/telemetry of the prompt anywhere in the route or system-prompt module). `AgentDecisionLog` does persist `lookup_employee`'s full output (Story 19.5 wrapper) — reviewed and accepted: it is the allowlist mapping only, workspace-scoped, same exposure tier as chat content itself.

**Trust-boundary verdict: PASS for the employeeId path, with one adjacent finding (SEC-001, below).** The client id is never trusted: compound WHERE (`id` + `workspace_id`), permission re-gate with the *real* permission matrix (route tests don't mock `lib/auth/permissions`, so MEMBER genuinely lacks `employees:view`), `workspaceId !== 'default'` guard, and a foreign/deleted id yields 200 with no block, no bias, and no existence-revealing error. `biasAgreementId` is not client-reachable (explicit context property, not body-spread). The model-supplied `agreementId` is workspace-clamped by retrieval; the foreign-id test pins pass-through + zero-row behavior.

**Inert-path verdict: VERIFIED, all three layers real.** (1) Byte-identity: `system-prompt.test.ts` asserts `toBe` (string identity) between the pre-7.7 call shape and `employeeContext: undefined`, plus absence of the block markers; (2) route level: no `employeeId` → `prisma.employee` never called, `buildSystemPrompt` receives `employeeContext: undefined`, `createAgentTools` receives no `biasAgreementId`; (3) transport: no employee/attachments → `opts` strictly `undefined` (pre-7.7 call shape). Git diff of all pre-existing test files (registry ×2, retrieval, system-prompt, citations, citation-pill, employees) is additive/count-bump only — no assertion weakened. Chat-quota route suite untouched and green.

### Refactoring Performed

None — the story's rules for this review prohibit source modifications, and none were needed for gate determination. SEC-001 is left to dev with a precise fix sketch.

### Compliance Check

- Coding Standards: ✓ (strict TS incl. `exactOptionalPropertyTypes` handled correctly via the unconditional-declaration + runtime-`delete` mechanism; no `any` in new code; naming/doc-block house style followed)
- Project Structure: ✓ (lib/components import boundary respected via `lib/employees/labels.ts` with a drift-guard test pinning it to the personalregister maps)
- Testing Strategy: ✓ (right levels: param-shape units where SQL is mocked, honestly annotated as such, with the live-DB proof delegated to the eval script; RTL for UI; route-level integration-style tests)
- All ACs Met: ✓ (AC 6 = script authored per spec; execution requires a live DB and is explicitly the user's step)

### Requirements Traceability (AC → validating tests)

1. **Employee pill/selector** — ✓ FULL. Given a role with `employees:view`, when the picker opens, then employees list/select/remove and the id rides every send (`employee-context-picker.test.tsx` ×5, `chat-input-modern-employee-pill.test.tsx` ×4, `use-chat-interface-employee.test.tsx` ×4); fail-closed `can.viewEmployees` gate verified in component code.
2. **`<employee_context>` block** — ✓ FULL. Format/labels/Ej ifylld/block-ordering + PII byte-absence from a polluted record + injection wording (`system-prompt.test.ts` +8).
3. **Corpus-wide retrieval, CA tool** — ✓ FULL. Exactly `sourceTypes: ['COLLECTIVE_AGREEMENT']`, `LEGAL_DOCUMENT` never present, `search_laws` untouched (git-verified), ALWAYS_AVAILABLE registration pinned for every role incl. AUDITOR.
4. **Multi-avtal hard bias** — ✓ FULL (unit). `sourceId` WHERE line null-safe + combined-filter param tests; precedence param > closure > none ×4; foreign id passes through to zero rows. Live-DB semantics = eval script (user-run).
5. **Source-distinguished citations** — ✓ FULL. CA branch extraction + fallback keys (+2 tests), pill CA variant: Handshake icon, "Kollektivavtal" label, `Öppna kollektivavtalet` via QuickPreview, no-file degradation (+4 tests). First-write-wins `set()` means fallback keys never clobber primary keys.
6. **Scoped grounding eval** — ✓ authored to spec. Script reviewed line-by-line: the hostile probe (foreign workspace + target's agreementId → asserts `length === 0`) exercises the exact WHERE conjunction and **would** catch a leakage regression; the three reference queries match the epic's areas (LAS 1982:80, Semesterlagen 1977:480, ATL 1982:673); auto-discovery + documented flags runnable as described. **Not yet executed** (no live DB here).

### Security Review

- **SEC-001 (MEDIUM, the one real finding):** in `app/api/chat/route.ts`, `...initialContext` (the raw rest of the request body) is spread **last** into `buildSystemPrompt(...)` — after `employeeContext`. A crafted body field `employeeContext: "<attacker text>"` therefore injects an `<employee_context>` block for **any** authenticated role (incl. MEMBER/AUDITOR without `employees:view`), bypassing the permission gate for the block's *existence*. No PII exposure (no DB fetch occurs; content is attacker-supplied) and no cross-tenant reach, and the same channel pre-exists for `companyContext`/`pendingActionsBlock`/`thinkingEnabled` — but 7.7 widens a prompt-slot injection surface and formally breaks the "block only under permission" invariant. **Fix (small):** destructure the prompt-slot keys out of the rest (`const { employeeContext: _e, companyContext: _c, pendingActionsBlock: _p, thinkingEnabled: _t, ...initialContext }`) or allowlist `initialContext` to `{title, description, sfsNumber, summary}`.
- Dev scrutiny items judged: **AUDITOR searches CA text but has no pill/lookup** — consistent with the documented stance (agreement text = workspace content; `employees:view` protects employee data) and pinned by registry tests; flag to PO as product-intent confirmation only. **Contains-based name matching** — acceptable for ≤5 disambiguation; Prisma `insensitive` suffices for Swedish names typed correctly. **Pill not persisted across reload** — correct per the "turn attribute, not context type" design decision. **Double analytics event** (ANA-001, low) — `ca_citation_clicked` + `file_citation_clicked` on one CA-pill click will inflate file-open dashboards; accept now, fix by extracting the fetch from the tracking.
- Registry mechanism verified: `delete` runs **before** decision-log wrapping (a removed tool is never wrapped/logged); the filtered return iterates actual keys so the deleted tool can't resurrect; the non-optional type declaration is safe for the only other consumer (`generate-law-list` iterates `event.toolCalls`, never reads `tools.lookup_employee`, and gets the fail-closed runtime delete as an undefined-role caller).

### Performance Considerations

Hot path proven inert at three independent layers (see verdict above); the employee branch costs exactly one indexed `findFirst` and only when an employeeId is present under permission. Retrieval adds one null-guarded SQL predicate — same shape/cost as the existing sourceType filters.

### Files Modified During Review

None (story file QA Results + gate file only).

### Out-of-zone / hygiene notes

- ✓ `lib/agent/skills/generate-law-list.ts` NOT edited (git-verified) — the type ripple was fixed on 7.7's side as the checkpoint authorized. ENV-001 from the 7.8 gate is **discharged**: branch-wide `pnpm typecheck` is green (independently run, exit 0).
- ⚠ The shared working tree carries out-of-story edits not in 7.7's File List (`app/globals.css` dark-banding fix, `components/layout/workspace-shell.tsx` scrollbar-gutter, plus 7.8's `lib/integrations/**` and misc prototypes/briefs). Not 7.7 code — but commit 7.7 selectively so its File List stays truthful.

### Independent Verification

- `pnpm vitest run tests/unit/agent tests/unit/lib/agent tests/unit/lib/ai tests/unit/app/api tests/unit/components/features/ai-chat tests/unit/app/actions/employees.test.ts tests/unit/lib/employees tests/unit/lib/hooks` → **932 passed, 1 failed** — the failure is the documented pre-existing `assessment-detail.test.tsx` calendar time-bomb (file untouched by 7.7; same failure recorded by 7.2–7.6).
- `pnpm typecheck` → **clean, exit 0** (branch-wide, with 7.7 + 7.8 both in tree).

### Improvements Checklist

- [ ] SEC-001: stop client body keys from reaching `buildSystemPrompt`'s prompt slots (allowlist `initialContext` or destructure the slot keys out) — `app/api/chat/route.ts` (dev)
- [ ] Run `npx tsx scripts/eval-ca-grounding.ts` against staging — the cross-workspace probe is the epic's Risk-Mitigation check and cannot be proven from unit mocks (user)
- [ ] Browser pass: picker popover layering in the chat sidebar, CA-pill QuickPreview, `<employee_context>` tone in a live chat (user)
- [ ] ANA-001: dedupe the CA-pill double analytics event when convenient — `citation-pill.tsx` (dev, low)
- [ ] PO: confirm product intent that AUDITOR can search CA text while never seeing pill/lookup (documented stance; registry tests pin current behavior)

### Gate Status

Gate: CONCERNS → docs/qa/gates/7.7-employee-aware-ai-answers.yml

### Recommended Status

✗ Changes Required — SEC-001 is a ~5-line fix; with it applied (plus a green staging eval run), this is a clean PASS. (Story owner decides final status.)

---

### Re-Review Date: 2026-07-04 (SEC-001 fix round)

### Reviewed By: Quinn (Test Architect)

**Scope:** verify the orchestrator's SEC-001 fix in `app/api/chat/route.ts` and re-gate. No other code changed since the initial review.

**Fix verified (adversarial read of the diff):**

- `...initialContext` (the raw request-body rest) is **gone** from the `buildSystemPrompt` options object. Only the three legitimate `ChatContextInitial` display fields pass through — `title`, `sfsNumber`, `summary` — each individually guarded with `typeof x === 'string'`. None of these keys collides with a server-computed field, so `employeeContext`, `companyContext`, `pendingActionsBlock`, `thinkingEnabled`, `skill` (and anything else server-side) are now **structurally unoverridable** from the request body. The fix closes the whole injection class, not just the 7.7-specific field — including the pre-existing `companyContext` vector I flagged as adjacent.
- **No legit-flow regression:** `SystemPromptOptions` never consumed `description` (previously spread in and ignored), so dropping it loses nothing; LAW/TASK mounts' `title`/`sfsNumber`/`summary` still flow — pinned by the new test's positive assertions and the untouched task/law context suites.
- **New regression test is real and well-aimed** (`chat-route-employee-context.test.ts`, "SEC-001: body-injected prompt fields are dropped…"): MEMBER role smuggles `employeeContext`/`companyContext`/`pendingActionsBlock`/`thinkingEnabled`/`skill` plus legit `title`+`summary` → asserts each server field individually unspoofed, `JSON.stringify(promptOpts)` contains no `SPOOFED` anywhere (catches any future passthrough key), and the legit display fields still arrive.

**Independent verification (re-run):**

- `pnpm vitest run tests/unit/app/api/chat-route-employee-context.test.ts tests/unit/agent/system-prompt.test.ts tests/unit/usage/chat-quota-enforcement.test.ts` → **60/60 passed** (route 6 incl. the new SEC-001 test, system-prompt 47, quota 7 — hot-path guard still green).
- `pnpm vitest run tests/unit/app/api` → **11 files, 69/69 passed** (all route suites green post-fix).
- `pnpm typecheck` → **clean, exit 0** (branch-wide).

**Residuals (all low, carried):** EVAL-001 — staging run of `scripts/eval-ca-grounding.ts` is the user's step (the epic's cross-workspace Risk-Mitigation check); ANA-001 — CA-pill double analytics event (polish pass); HYG-001 — selective commit so 7.7's File List stays truthful (resolves at the commit step). Plus the PO product-intent confirmation on AUDITOR CA-text search and the manual browser pass (popover layering, QuickPreview, block tone).

### Gate Status

Gate: PASS → docs/qa/gates/7.7-employee-aware-ai-answers.yml (history: CONCERNS 2026-07-03 → PASS 2026-07-04; quality score 90 → 100)

### Recommended Status

✓ Ready for Done — pending the user-side staging eval run and browser pass before epic sign-off. (Story owner decides final status.)
