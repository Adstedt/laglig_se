# Story 21.27: Collapse ARKIVERAD into AVSLUTAD (3-state lifecycle)

## Status

Ready for Review (2026-04-28)

## Story

**As a** product owner,
**I want** the cycle lifecycle simplified from `PLANERAD → PAGAENDE → AVSLUTAD → ARKIVERAD` to `PLANERAD → PAGAENDE → AVSLUTAD`,
**so that** SMB self-audit users have one terminal active state instead of a phantom "archived" state that nothing in the UI ever transitions a cycle into.

## Context

Story 21.26 collapsed SEAL into AVSLUTAD on the grounds that the cryptographic ceremony was bureaucracy without payoff for SMB self-audit. The same logic applies one step further:

- **ARKIVERAD has no UI transition**. Nothing in the codebase moves a cycle from AVSLUTAD to ARKIVERAD — only direct DB writes can land it there. The state was scoped in the Epic 21 brief but a transition action was never implemented.
- **The only behavioral difference vs AVSLUTAD** is that ARKIVERAD locks findings (items already lock at AVSLUTAD per Phase 2). After 21.26 there were two ways findings could be locked: ARKIVERAD on the cycle, or… nothing else. So the lock semantic depended entirely on this phantom state.
- **Activity log + DB write history give defensible change history** if anyone disputes a late edit. No Nordic regulator (AML, AFS 2001:1, ISO certifying bodies, GDPR Art 30, Bokföringslagen) requires findings to be cryptographically frozen at a specific state transition.
- **List hygiene for old cycles** is solvable later via a "show only last 12 months" filter on `/laglistor/kontroller` — not via a state.

## Acceptance Criteria

1. `ComplianceCycleStatus` enum drops the `ARKIVERAD` value. New shape: `PLANERAD | PAGAENDE | AVSLUTAD`.
2. Migration `20260428120000_collapse_arkiverad_into_avslutad/migration.sql` folds any existing `ARKIVERAD` rows into `AVSLUTAD` (preserving `sealed_at` + `sealed_by_user_id`), then runs the standard Postgres enum-rewrite dance with the `DROP DEFAULT` / `SET DEFAULT` pattern from 21.26.
3. `compliance-audit-item.ts#assertCycleEditableUi`: blocks AVSLUTAD only. Swedish error drops "eller arkiverad" wording.
4. `compliance-finding.ts#assertCycleEditableUi`: deleted entirely. All 5 call sites removed. Findings have no cycle-status read-only mode.
5. `compliance-audit-cycle.ts#updateCycleMetadata`: ARKIVERAD branch dropped from the inline status check.
6. `cycle-detail-header.tsx#ReadOnlyBanner` function deleted (its only trigger was ARKIVERAD). `useEffect`/`useState`/`X` imports + `storageKey` helper deleted alongside.
7. `cycle-list-table.tsx`: `arkiverade` filter chip removed. `FilterKey` union narrows to `'aktiva' | 'slutforda' | 'alla'`. `counts.arkiverade` field dropped.
8. `cycle-status-badge.tsx#STATUS_VARIANTS`: ARKIVERAD row removed (3-row map).
9. `cycle-rapport-tab.tsx`: `shouldFetch` simplifies to `cycleStatus === 'AVSLUTAD'`.
10. `cycle-copy.ts#getCycleReadOnlyReason`: ARKIVERAD case removed; switch becomes a simple `if`.
11. `CycleDetailPage`: `findingsReadOnly` prop dropped. Pass-through to `CycleFindingsTab` removed.
12. `CycleFindingsTab`: `readOnly` + `cycleStatus` props dropped from `CycleFindingsTabProps`, `BodyProps`, `VirtualisedBodyProps`, `FindingActionsProps`. Inline read-only banner + `!readOnly` wrapper around "Lägg till anmärkning" removed. `if (readOnly) return null` in `FindingActions` removed.
13. Route shell `app/(workspace)/laglistor/kontroller/[cycleId]/page.tsx`: `itemsReadOnly()` simplifies to AVSLUTAD-only. `findingsReadOnly()` helper deleted. `findingsReadOnly` prop pass-through to `<CycleDetailPage>` deleted (both branches).
14. `revisionsrapport-renderer.ts#cycleStatusLabel`: ARKIVERAD case removed (3 cases).
15. PDF route `rapport/pdf/route.ts`: status gate unchanged (already excludes PLANERAD/PAGAENDE; the negative gate works for any post-AVSLUTAD state). No edit needed.
16. `linked-cycles-box.tsx#CYCLE_STATUS_LABELS`: ARKIVERAD label retained as legacy fallback (in case any historical cycle survives in the join table).
17. Activity log: no formatter changes — there was never a `cycle_archived` action constant in the codebase to retain.
18. Test surface: 8 test files updated with mechanical ARKIVERAD removals (fixture status changes, deleted `it()` blocks for ARKIVERAD-blocks-mutation, `describe.each` array narrowing, prop drops).
19. Story 21.26 spec gets a "Followed by Story 21.27 (2026-04-28)" header note.
20. Architecture doc top banner + §4.1 + §6.4 updated to reflect 3-state lifecycle.
21. Type-check (`npx tsc --noEmit`) clean.
22. 737/737 scoped tests passing (no SEAL/ARKIVERAD references in source code outside comments).

## Tasks / Subtasks

- [x] Schema migration `20260428120000_collapse_arkiverad_into_avslutad/migration.sql` written
- [x] `prisma/schema.prisma` updated (4 → 3 enum values)
- [x] Prisma client regenerated
- [x] Server actions: assertCycleEditableUi simplifications (item) + delete (finding) + 5 call-site removals
- [x] `cycle-detail-header.tsx`: ReadOnlyBanner function deleted
- [x] `cycle-list-table.tsx`: Arkiverade filter chip + counts removed
- [x] `cycle-status-badge.tsx`: ARKIVERAD variant removed
- [x] `cycle-rapport-tab.tsx`: shouldFetch simplified
- [x] `cycle-copy.ts`: getCycleReadOnlyReason simplified
- [x] `CycleDetailPage` + `CycleFindingsTab`: readOnly/cycleStatus props dropped
- [x] Route shell: findingsReadOnly helper + prop deleted
- [x] Renderer: cycleStatusLabel ARKIVERAD case dropped
- [x] Tests updated across 8 files
- [x] Type-check + scoped vitest + lint all clean
- [x] Apply migration to Supabase manually (user confirmed: "I ran all migrations in supabase succesfully")
- [x] Update Story 21.26 spec with "followed by 21.27" header
- [x] Update architecture doc
- [x] Update memory
- [x] QA fix-pass — close TEST-001, DOC-001..005, CSS-001 (Quinn's CONCERNS gate items)

## Dev Notes

**Migration safety**: Same pattern as 21.26's migration — wrapped in `BEGIN;…COMMIT;`, with `DROP DEFAULT` / `SET DEFAULT` around the enum surgery to avoid Postgres error 42804. Pre-flight query: `SELECT count(*) FROM compliance_audit_cycles WHERE status = 'ARKIVERAD';`. If non-zero, Step 1 of the migration folds them into AVSLUTAD with all metadata preserved; if zero, Step 1 is a no-op.

**Activity log**: no `cycle_archived` action constant existed in the codebase, so no legacy formatter to retain. The migration silently absorbs ARKIVERAD rows into AVSLUTAD; their existing `cycle_completed` activity entries (if any) continue to render unchanged.

**`linked-cycles-box.tsx` defensive map**: kept the `ARKIVERAD: 'Arkiverad'` and `SEALED: 'Fastställd'` entries as legacy fallbacks. The join table `compliance_cycle_task_links` doesn't store a denormalised status, so any task linked to a pre-collapse cycle reads the current status — but if a task is linked to a cycle that was archived before this migration, the current status will already be AVSLUTAD post-migration. The fallback is purely defensive.

**Test surface**: ~25 test references narrowed to 0 (excluding comments). Two test files lost entire `describe` blocks (the ARKIVERAD read-only banner test in items-tab; the ARKIVERAD-blocks finding tests). One test file (`cycle-detail-page-lifecycle.test.tsx`) had stale `SEALED` references from before 21.26 — those got swept too.

## Reference

- Plan: `~/.claude/plans/crispy-launching-moore.md`
- Migration: `prisma/migrations/20260428120000_collapse_arkiverad_into_avslutad/`
- Predecessor: Story 21.26 (`docs/stories/21.26.collapse-seal-into-avslutad.md`)
- PO call: continuation of 21.26's "less ceremony" thesis applied to the second redundant terminal state.

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-04-28 | 1.0 | Initial spec + implementation in flight | Alexander A. (PO) + Claude Opus 4.7 |
| 2026-04-28 | 1.1 | QA fix-pass — closed TEST-001, DOC-001..005, CSS-001 from Quinn's CONCERNS gate; status flipped to Ready for Review | James (Dev) + Claude Opus 4.7 |

## Dev Agent Record

### Agent Model Used

Claude Opus 4.7 (1M context) — `claude-opus-4-7[1m]`. Operating under the BMad `dev` persona (James).

### Debug Log References

- `npx tsc --noEmit -p tsconfig.json` — clean (0 errors)
- `npx vitest run tests/unit/lib/compliance-audit/ tests/unit/lib/activity/ app/actions/__tests__/ tests/unit/components/features/compliance-audit/ tests/unit/prisma/ tests/unit/app/workspace/` — 737/737 passing, 36 test files
- `npm run lint` — 0 errors, 27 pre-existing warnings (none in touched files; all in `scripts/test-redis-*`, `tests/performance-audit*`)
- Renderer fixture regenerated via `npx vitest run … -u` after CSS class rename (1 snapshot updated)

### Completion Notes List

QA fix-pass closes Quinn's CONCERNS gate items:

- **TEST-001 (closed)** — Dropped dangling `findingsReadOnly={overrides.readOnly ?? false}` line from `tests/unit/components/features/compliance-audit/cycle-detail/cycle-items-tab.test.tsx:241`. Test still passes (the prop was a no-op at runtime).
- **DOC-001 + DOC-002 (closed)** — `docs/uat/epic-21-uat-plan.md`: rewrote line 5 scope blurb to 3-state lifecycle and removed seal-hash + evidence-snapshot from the scope; rewrote row 4.2a to reflect findings have no cycle-status read-only mode and dropped the SEALED/ARKIVERAD banner reference. Also updated the Phase-1 advisory copy to match the in-code "bedömningarna bygger på dagens snapshot" wording (was "fastställandet bygger på...").
- **DOC-003 + DOC-004 + DOC-005 (closed)** — `docs/architecture/epic-21-lagefterlevnadskontroll.md`: line 5 status blurb now correctly says "post-21.27 only items lock at AVSLUTAD"; table row 70 reframed; §6.4 fully rewritten to cover both 21.26 and 21.27 collapses with explicit "no separate ARKIVERAD lock-everything stage anymore" and "findings have no cycle-status read-only mode at all".
- **STORY-001 (closed)** — Story status flipped from "In Progress" to "Ready for Review"; migration task + 3 doc tasks + memory task checkboxes flipped to [x] (work was already done in the original session).
- **CSS-001 (closed — carryover from 21.26 gate)** — Renamed `.seal-block` CSS class → `.completion-block` in `lib/compliance-audit/revisionsrapport-renderer.ts`; deleted the dead `.seal-hash` rule (no hash content remains in output). Also renamed the local `sealBlock` template variable → `completionBlock` for consistency. Golden HTML fixture regenerated (`__fixtures__/revisionsrapport-output.html`).

**Discovered but not in scope (deferred to follow-up):**
- `lib/compliance-audit/revisionsrapport-to-pdf.ts` still has `sealHash: string | null` on `RevisionsrapportPdfMetadata` with header/footer template branches that always pass `null` from the only production caller (`compliance-audit-report.ts:310`). Pure dead code, no runtime impact — but would warrant its own cleanup pass since the test file `revisionsrapport-to-pdf.test.ts` has 9+ `sealHash: …` references including an XSS test that's now moot.
- Several test fixtures still set `sealHash: null` on cycle-shaped fixtures (`cycle-actions-dropdown.test.tsx:34`, `cycle-detail-page-lifecycle.test.tsx:157,188`, `format-activity.test.ts:298`). Harmless — `tsconfig.json` excludes `tests/`. Not flagged in QA gate.

Validation:
- Type-check, scoped vitest (737/737), and lint (0 errors) all clean post-fixes.

### File List

**Modified:**
- `tests/unit/components/features/compliance-audit/cycle-detail/cycle-items-tab.test.tsx` — TEST-001
- `docs/uat/epic-21-uat-plan.md` — DOC-001 + DOC-002
- `docs/architecture/epic-21-lagefterlevnadskontroll.md` — DOC-003 + DOC-004 + DOC-005
- `lib/compliance-audit/revisionsrapport-renderer.ts` — CSS-001 (class rename + dead-rule deletion + variable rename)
- `lib/compliance-audit/__fixtures__/revisionsrapport-output.html` — golden fixture regenerated
- `docs/stories/21.27.collapse-arkiverad-into-avslutad.md` — STORY-001 status + checkboxes; this Dev Agent Record + Change Log entry

**No files added or deleted in this fix-pass.**

## QA Results

### Review Date: 2026-04-28

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

The collapse is well-executed and consistent with the predecessor 21.26 SEAL collapse. The diff is dominated by deletions (the `findingsReadOnly` prop chain + `ReadOnlyBanner` component + the entire `compliance-finding.ts#assertCycleEditableUi` helper with all 5 call sites), all behind clear "Story 21.27 — ..." gravestone comments. Migration mirrors the 21.26 pattern (transactional, DROP DEFAULT / SET DEFAULT around enum surgery, fold-then-rewrite). No security primitives touched. 737/737 scoped tests pass; `npx tsc --noEmit` clean; `npm run lint` 0 errors.

The one design call worth flagging: deleting `compliance-finding.ts#assertCycleEditableUi` entirely (vs simplifying to a no-op) is the right call given the story's "findings are always editable" thesis — but it does mean any future cycle-status guard for findings will be a *new* helper rather than a re-tightening of an existing one. Documented in the file-header comment block; safe to ship as-is.

### Refactoring Performed

None during this review pass — implementation was already clean. Recommendations below are for the dev to action.

### Compliance Check

- Coding Standards: ✓ TypeScript strict mode passes; deleted helpers leave clean call sites with no orphan imports/types (`EditableCheck` removed alongside; `Prisma.TransactionClient` import retained for `resolveItemResponsibleUserId` which still needs it).
- Project Structure: ✓ Migration in `prisma/migrations/20260428120000_collapse_arkiverad_into_avslutad/`; story spec in `docs/stories/21.27.*`; predecessor cross-link added to 21.26 spec header.
- Testing Strategy: ✓ Mechanical test sweep across 8 files; test count narrowed appropriately (deleted ARKIVERAD-blocks-mutation tests + readOnly-mode banner tests; positive AVSLUTAD-allows-X tests preserved). One stale test-prop drift flagged below as TEST-001.
- All ACs Met: ✓ 22/22 ACs traceable to code or tests; no functional gaps.

### Acceptance Criteria Trace

All 22 ACs verified:

- **AC 1** (enum drops ARKIVERAD): ✓ `prisma/schema.prisma:1894` + `tests/unit/prisma/compliance-audit-schema.test.ts:38` (`toHaveLength(3)`).
- **AC 2** (migration): ✓ `prisma/migrations/20260428120000_collapse_arkiverad_into_avslutad/migration.sql` — fold-then-rewrite with DROP DEFAULT / SET DEFAULT pattern. User confirmed applied to Supabase.
- **AC 3** (item guard simplifies): ✓ `app/actions/compliance-audit-item.ts:243-251`; test in `compliance-audit-item.test.ts:849-851` narrows `describe.each` to AVSLUTAD only.
- **AC 4** (finding guard deleted): ✓ `compliance-finding.ts#assertCycleEditableUi` gone; 5 call sites removed (verified by grep — only comment-only references remain).
- **AC 5** (updateCycleMetadata): ✓ `compliance-audit-cycle.ts:687-695` blocks AVSLUTAD only.
- **AC 6** (ReadOnlyBanner deleted): ✓ Function gone; `useEffect` / `useState` / `X` imports + `storageKey` helper removed from `cycle-detail-header.tsx`.
- **AC 7** (filter chip removed): ✓ `cycle-list-table.tsx:38-58` 3-entry FILTERS array; `counts` field dropped.
- **AC 8** (badge variant removed): ✓ `cycle-status-badge.tsx:11-15` 3-row STATUS_VARIANTS map.
- **AC 9** (rapport-tab shouldFetch): ✓ `cycle-rapport-tab.tsx:45` simplified to AVSLUTAD only.
- **AC 10** (cycle-copy.ts): ✓ `getCycleReadOnlyReason` is now a 1-state if-statement.
- **AC 11** (findingsReadOnly prop dropped): ✓ Removed from `CycleDetailPageProps` + page render call in `cycle-detail-page.tsx`.
- **AC 12** (CycleFindingsTab readOnly + cycleStatus): ✓ Both props dropped from `CycleFindingsTabProps`, `BodyProps`, `VirtualisedBodyProps`, `FindingActionsProps`. Imports cleaned.
- **AC 13** (route shell): ✓ `itemsReadOnly()` AVSLUTAD-only; `findingsReadOnly()` deleted; both `<CycleDetailPage>` invocations updated.
- **AC 14** (renderer label switch): ✓ `cycleStatusLabel` is 3-case.
- **AC 15** (PDF route): ✓ Gate is negative (excludes PLANERAD/PAGAENDE), works for 3-state.
- **AC 16** (linked-cycles-box defensive map): ✓ Legacy SEALED + ARKIVERAD entries documented as defensive fallbacks (line 27-32 comment).
- **AC 17** (no activity-log formatter changes): ✓ No `cycle_archived` constant existed.
- **AC 18** (test sweep): ✓ 8 files updated; tests pass — but **TEST-001 below**.
- **AC 19** (21.26 spec header): ✓ "Followed by Story 21.27" header in `docs/stories/21.26.collapse-seal-into-avslutad.md`.
- **AC 20** (architecture doc updates): ✓ Top banner + §4.1 updated — but **DOC-003 + DOC-004 + DOC-005 below**.
- **AC 21** (typecheck clean): ✓ Verified.
- **AC 22** (737/737 tests pass): ✓ Verified.

### Improvements Checklist

- [ ] **TEST-001** — `tests/unit/components/features/compliance-audit/cycle-detail/cycle-items-tab.test.tsx:241` passes `findingsReadOnly={overrides.readOnly ?? false}` to `<CycleDetailPage>`; the prop was dropped from `CycleDetailPageProps`. Test runs green because tsconfig.json excludes `tests/` so `tsc --noEmit` doesn't typecheck this file (vitest doesn't typecheck either). Dead weight that will confuse future contributors. **Drop the line.**
- [ ] **DOC-001** — `docs/uat/epic-21-uat-plan.md:5` scope blurb still describes lifecycle as 5-state (`PLANERAD → PAGAENDE → AVSLUTAD → SEALED → ARKIVERAD`). Update to 3-state.
- [ ] **DOC-002** — `docs/uat/epic-21-uat-plan.md:98` row 4.2a still says "SEALED + ARKIVERAD show their status-specific banners (and findingsReadOnly flips true so the findings tab also locks)". Both states are gone; findingsReadOnly is gone. Trim the row.
- [ ] **DOC-003** — `docs/architecture/epic-21-lagefterlevnadskontroll.md:5` says "the inline `assertCycleEditableUi` already enforces AVSLUTAD/ARKIVERAD read-only correctly" — partially stale post-21.27 (findings's editable guard was deleted entirely; only items lock at AVSLUTAD).
- [ ] **DOC-004** — `docs/architecture/epic-21-lagefterlevnadskontroll.md:70` table row "Read-only gate for SEALED/ARKIVERAD" is stale.
- [ ] **DOC-005** — Architecture doc §6.4 still says "ARKIVERAD remains the only truly read-only terminal state, locking findings as well as items." Wrong post-21.27 — findings have NO cycle-status read-only mode any more.
- [ ] **STORY-001** — Story status is "In Progress" but implementation is complete + committed (`a078dfe`) + pushed + migration applied. Tasks list checkbox "Apply migration to Supabase manually" unchecked. Flip status to "Ready for Review" and check the migration task.
- [ ] **CSS-001 (carryover from 21.26 gate)** — `.seal-block` CSS class still wraps the "Avslutad kontroll" block in `revisionsrapport-renderer.ts:217-224, 376`; `.seal-hash` rule at 225-231 is dead (no hash in output any more). Rename to `.completion-block` and drop `.seal-hash`. The 21.26 gate flagged this as "future"; 21.27 has made it more stale. Optional but easy.

### Security Review

✓ No security primitives touched. The deleted `compliance-finding.ts#assertCycleEditableUi` was a UX/business-rule guard, not an authorization boundary. Tenant isolation, permission scopes (`tasks:edit`, `activity:view`), and the route-level workspace check are all unchanged.

### Performance Considerations

✓ Net deletion of code reduces bundle/payload. No new queries, no N+1 patterns introduced. The route shell drops one helper function call per request (`findingsReadOnly()` deleted), so trivially faster RSC render.

### Reliability Considerations

✓ Migration is well-engineered: transactional, idempotent on zero-row workspaces, mirrors the proven 21.26 pattern. Fold-step preserves `sealed_at` + `sealed_by_user_id` on any pre-existing ARKIVERAD rows so no metadata loss. Rollback story: trivially recoverable from a backup since the migration is one transaction; the only data-loss vector is ARKIVERAD-only data, and it's been folded to AVSLUTAD.

### Files Modified During Review

None — all findings are recommendations for the dev to action.

### Gate Status

Gate: **CONCERNS** → `docs/qa/gates/21.27-collapse-arkiverad-into-avslutad.yml`

Quality score: **85** (100 − 10 × 1 CONCERNS-level NFR; maintainability is CONCERNS, all other NFRs PASS).

The CONCERNS gate is driven entirely by low-severity hygiene drift (1 stale test-prop + 5 doc lines + 1 carry-over CSS rename). No functional, security, or data-integrity issues. A 30-minute fix-pass closes TEST-001 + DOC-001..005 + STORY-001 and flips the gate to PASS.

### Recommended Status

**✗ Changes Required — See unchecked items above.** None block ship; all are 5-minute fixes. Suggest a fix-pass before flipping to Done.

(Story owner decides final status.)
