# Story 5.5a: Seat Enforcement

## Status

Ready for Development

## Story

**As a** workspace owner,
**I want** the platform to block adding more users than my plan allows,
**so that** I'm prompted to upgrade or buy add-on seats before paying for capacity I don't have.

## Context & Dependencies

- **Parent spec:** [Story 5.5 — Usage Limits Per Tier](./5.5.usage-limits-per-tier.md). Read it first — it owns the calibrated limits table, the `lib/usage/limits.ts` skeleton, and the trial-tier-resolution model. This story implements seat enforcement only.
- **Depends on:** Story 5.4 (Stripe billing — shipped, `subscription_tier` and Stripe Subscription Items live on `Workspace`).
- **First sub-story to need `trial_picked_tier`:** owns the Prisma migration that adds the column. 5.5b/c/d build on the same column.
- **Related code paths:**
  - Invite-create: `app/api/workspace/invitations/route.ts`
  - Invite-accept: `app/actions/invitations.ts`
  - Team tab UI: `components/features/settings/team-tab.tsx`
- **Out of scope:** Storage (5.5b), AI tokens (5.5c), Stripe metered overage (5.5d), employee enforcement (deferred — HR module not built).

## Acceptance Criteria

1. **Prisma migration adds `Workspace.trial_picked_tier SubscriptionTier?`** (nullable). Migration runs cleanly on existing data; existing rows have NULL.
2. **`lib/usage/limits.ts` exists** with the full skeleton from the parent spec — `TIER_LIMITS`, `TEAM_ADDON_PER_SEAT`, `getEffectiveLimits()`, `isUnlimited()`, AI-quota helpers (helpers used by 5.5c, but the full module lands here so the file isn't half-built across two stories).
3. **Invite-create path is gated:** `POST /api/workspace/invitations` rejects with HTTP 402 + `code: 'SEAT_LIMIT_REACHED'` when current seat count + pending invitations >= `getEffectiveLimits(workspace, addonSeats).users`. Pending invitations count toward the limit so a workspace can't fire 10 invites in parallel and break the cap on accept.
4. **Invite-accept path is gated:** `acceptInvitation()` server action re-checks the seat limit at accept time (race protection against tier downgrades or other accepts landing first). Rejects with the same shape if the workspace has filled while the invitation was pending. Stale invite is marked `EXPIRED`.
5. **Add-on seats are correctly counted:** `getEffectiveLimits(workspace, addonSeatCount)` returns the right `users` value. `addonSeatCount` is computed by counting active Stripe Subscription Items on the workspace's subscription whose `price.id !== STRIPE_TEAM_PRICE_ID` (i.e., quantity beyond the Team base Price).
6. **Trial workspaces honor `trial_picked_tier`:** a workspace with `subscription_tier='TRIAL'` and `trial_picked_tier='TEAM'` allows 3 users (Team limit), not 1 (Solo fallback).
7. **UI surfaces seat usage:** `/settings?tab=team` shows "X / N platser används". The invite modal shows "Du har använt X / N platser. Bjud in en till?" and disables the submit button when at limit, with a CTA to upgrade or buy add-on seats.
8. **Employees are NOT enforced** in any code path. Comment in `lib/usage/limits.ts` next to the `employees` field explicitly states "deferred — HR module not yet built". No `/api/employees` gate, no employee count in the team tab.
9. **Error response shape** (HTTP 402) on both create + accept paths:
   ```json
   {
     "error": "Platsgräns uppnådd",
     "message": "Din plan tillåter X platser. Uppgradera till Team eller köp en extra plats.",
     "code": "SEAT_LIMIT_REACHED",
     "currentSeats": 1,
     "limit": 1,
     "tier": "SOLO"
   }
   ```
10. **Enterprise tier bypasses all seat checks** — `getEffectiveLimits()` returns `users: null`, callers correctly interpret as unlimited.
11. **Test coverage:** unit tests for `getEffectiveLimits()` resolution; integration tests for both invite paths blocking at limit; E2E for the invite-modal CTA flow.

## Tasks / Subtasks

- [ ] Add `trial_picked_tier` column to Workspace (AC 1)
  - [ ] Create Prisma migration `prisma/migrations/<timestamp>_add_trial_picked_tier/migration.sql`
  - [ ] Update `prisma/schema.prisma` — `trial_picked_tier SubscriptionTier?` on the Workspace model
  - [ ] Run `pnpm prisma migrate dev` locally to verify (use `--create-only` then apply manually per `project_prisma_manual_migration_workflow.md` if Supabase requires)
- [ ] Create `lib/usage/limits.ts` per parent spec skeleton (AC 2)
  - [ ] Copy the `TIER_LIMITS`, `TEAM_ADDON_PER_SEAT`, `getEffectiveLimits()`, `isUnlimited()`, AI helpers, `tokensToApproxQueries()` exactly as documented in Story 5.5 Dev Notes
  - [ ] Add unit tests at `tests/unit/usage/limits.test.ts` — see Testing section
- [ ] Add `lib/usage/seats.ts` (AC 3, 4, 5)
  - [ ] `countActiveAddonSeats(workspaceId)` — queries Stripe Subscription Items via the existing Stripe SDK config, sums `quantity` of items whose `price.id !== STRIPE_TEAM_PRICE_ID`. Returns 0 for non-Team workspaces.
  - [ ] `assertSeatAvailable(workspaceId)` — async; throws `SeatLimitExceededError` if at limit. Counts current `WorkspaceMember` rows + pending `WorkspaceInvitation` rows where `status='PENDING'`.
  - [ ] Define `SeatLimitExceededError` class with `currentSeats`, `limit`, `tier` fields
- [ ] Gate invite-create (AC 3, 9)
  - [ ] Edit `app/api/workspace/invitations/route.ts` POST handler — call `assertSeatAvailable()` early
  - [ ] Catch `SeatLimitExceededError` → return JSON 402 with the exact shape in AC 9
- [ ] Gate invite-accept (AC 4, 9)
  - [ ] Edit `app/actions/invitations.ts` — `acceptInvitation()` server action calls `assertSeatAvailable()` after auth + before DB write
  - [ ] On overflow: mark the invitation `status='EXPIRED'`, return error to client with `code: 'SEAT_LIMIT_REACHED'`
- [ ] UI — team tab seat counter (AC 7)
  - [ ] Add `getSeatUsage()` server action at `app/actions/seats.ts` (or extend an existing actions file). Return shape:
    ```typescript
    export interface SeatUsage {
      used: number          // count of WorkspaceMember + pending WorkspaceInvitation
      limit: number | null  // null = unlimited (Enterprise)
      tier: SubscriptionTier
      addonSeatCount: number  // 0 unless TEAM with add-ons
    }
    export async function getSeatUsage(): Promise<SeatUsage>
    ```
  - [ ] Edit `components/features/settings/team-tab.tsx` — fetch `getSeatUsage()`
  - [ ] Render "X / N platser används" subtitle
  - [ ] Pass `seatsAvailable: limit === null ? Infinity : Math.max(0, limit - used)` to invite modal
- [ ] UI — invite modal (AC 7)
  - [ ] Show "Du har använt X / N platser" copy
  - [ ] Disable submit button when `seatsAvailable === 0`
  - [ ] Replace disabled button with upgrade CTA: "Uppgradera plan" linking to `/settings?tab=billing`, OR "Köp extra plats" if Team (links to Stripe Customer Portal for add-on)
- [ ] Tests (AC 11)
  - [ ] Unit tests in `tests/unit/usage/limits.test.ts` — see Testing
  - [ ] Integration tests in `tests/unit/usage/seat-enforcement.test.ts` — see Testing
  - [ ] E2E test in `tests/e2e/seat-enforcement.spec.ts` — see Testing

## Dev Notes

### File map (which files to touch)

| Concern | File |
|---|---|
| Schema | `prisma/schema.prisma`, `prisma/migrations/<ts>_add_trial_picked_tier/` |
| Limits constants + helpers | **NEW** `lib/usage/limits.ts` |
| Seat-specific check + Stripe seat counter | **NEW** `lib/usage/seats.ts` |
| Invite create gate | `app/api/workspace/invitations/route.ts` |
| Invite accept gate | `app/actions/invitations.ts` |
| Team tab UI | `components/features/settings/team-tab.tsx` |
| Invite modal UI | `components/features/settings/invite-member-modal.tsx` (confirmed, edit existing) |

### Add-on seat counting

Team add-on seats live on the Stripe subscription as additional `SubscriptionItem` rows. The base Team Price ID is `env.STRIPE_TEAM_PRICE_ID`. Any other Price ID on the subscription with `recurring.interval='month'` is an add-on seat (each `quantity: 1`). Story 5.6 (Add-On Purchase System, backlog) will formalize this — for v1 just count items where `price.id !== STRIPE_TEAM_PRICE_ID` and sum quantities.

> **⚠️ Cross-reference with Story 5.6:** This counting logic assumes Story 5.6 will model add-on seats as **additional `SubscriptionItem` rows on the same subscription, with distinct Price IDs**. If 5.6 ships first with a different shape (e.g., a single `quantity` increment on the base Price, or an `addon_seats` column on `Workspace`), revise `countActiveAddonSeats()` to match. Validate against the 5.6 design doc before implementing this function.

```typescript
// lib/usage/seats.ts (sketch — not full implementation)
import { stripe } from '@/lib/stripe/config'
import { env } from '@/lib/env'

export async function countActiveAddonSeats(workspaceId: string): Promise<number> {
  const workspace = await prisma.workspace.findUnique({
    where: { id: workspaceId },
    select: { stripe_subscription_id: true, subscription_tier: true },
  })
  if (!workspace?.stripe_subscription_id) return 0
  if (workspace.subscription_tier !== 'TEAM') return 0

  const sub = await stripe.subscriptions.retrieve(workspace.stripe_subscription_id, {
    expand: ['items.data.price'],
  })

  return sub.items.data
    .filter((item) => item.price.id !== env.STRIPE_TEAM_PRICE_ID)
    .reduce((sum, item) => sum + (item.quantity ?? 0), 0)
}
```

### Pending-invitation race

Counting pending invitations toward the seat limit prevents this attack/oversight:

1. Solo workspace (1 user, owner) fires 5 invites in 5 seconds (each invite-create currently passes the 1-user check)
2. All 5 invitees accept simultaneously
3. Workspace ends up with 6 users despite paying for 1 seat

Counting `WorkspaceInvitation.status='PENDING'` toward the seat tally fixes this at create time. The accept-time re-check (AC 4) handles the rare case where the invitation was created before a downgrade flipped the tier limit.

### Trial-picked-tier resolution (parent-spec recap)

`getEffectiveLimits()` already does this — uses `trial_picked_tier ?? subscription_tier`. The signup flow (Story 4.x — onboarding) is responsible for writing `trial_picked_tier` when the user picks a tier on the signup tier-picker. **5.5a does NOT modify the signup flow** — it just reads the column. Cross-story coordination: Story 4.x's signup work needs to update to set this column. If the signup flow already exists without a tier-picker, this story should NOT add one — that's Epic 4 territory. For now, expect `trial_picked_tier` to be NULL on existing TRIAL workspaces; they'll fall back to the TIER_LIMITS.TRIAL row (Solo equivalent).

### UI copy (Swedish, B2B tone)

| Surface | Copy |
|---|---|
| Team tab subtitle | `{N - used > 0 ? "{used} av {N} platser används" : "Alla {N} platser används — uppgradera för fler"}` |
| Invite modal helper text (under email field) | `"{used} / {N} platser används"` (muted color) |
| Invite modal disabled state | Replace submit button with `<Button asChild><Link href="/settings?tab=billing">Uppgradera plan</Link></Button>` |
| Error toast on accept-overflow | `"Arbetsplatsen har inga lediga platser kvar. Be ägaren uppgradera planen."` |

### Migration order (important)

If `pnpm prisma migrate dev` is run against the dev/sandbox DB:
1. Migration adds `trial_picked_tier` as nullable
2. No backfill needed — existing TRIAL workspaces will fall back to Solo limits via `getEffectiveLimits()`'s null-coalesce
3. **Production rollout:** apply via the manual migration workflow per `project_prisma_manual_migration_workflow.md` memory note — Supabase shared DB needs careful handling.

### Testing

**Test file locations:**
- Unit: `tests/unit/usage/limits.test.ts`
- Integration: `tests/unit/usage/seat-enforcement.test.ts`
- E2E: `tests/e2e/seat-enforcement.spec.ts`

**Frameworks:** Vitest for unit + integration (matches existing `tests/unit/billing/stripe-integration.test.ts` pattern), Playwright for E2E.

**Required test cases:**

*Unit (`limits.test.ts`):*
- `getEffectiveLimits` resolves `trial_picked_tier='TEAM'` → returns Team limits when `subscription_tier='TRIAL'`
- `getEffectiveLimits` resolves `trial_picked_tier=null` → returns subscription_tier-based limits
- `getEffectiveLimits` adds Team add-on seats correctly: `getEffectiveLimits({tier:'TEAM'}, 2).users === 5`
- `getEffectiveLimits` does NOT add add-ons for Solo (returns base 1 even with addonSeats=99)
- `getEffectiveLimits` returns `null` for Enterprise unlimited dimensions
- `isUnlimited(null)` true; `isUnlimited(0)` false; `isUnlimited(1)` false

*Integration (`seat-enforcement.test.ts`):*
- POST `/api/workspace/invitations` returns 402 + `SEAT_LIMIT_REACHED` when Solo workspace already has 1 user (mock `prisma.workspaceMember.count` and `prisma.workspaceInvitation.count`)
- POST `/api/workspace/invitations` returns 402 when 0 members but 1 pending invitation (Solo)
- POST `/api/workspace/invitations` succeeds for Team workspace at 2 users / 3 limit
- POST `/api/workspace/invitations` succeeds for Team workspace at 3 users with 2 add-on seats (5 cap)
- `acceptInvitation` returns error + marks invite EXPIRED when seats are full at accept time
- Enterprise workspace bypasses all checks (mock subscription_tier='ENTERPRISE' → invite always succeeds)
- Mock `stripe.subscriptions.retrieve` to return varying SubscriptionItem counts; verify `countActiveAddonSeats` math

*E2E (`seat-enforcement.spec.ts`):*
- Solo workspace owner navigates to `/settings?tab=team`, sees "1 / 1 platser används"
- Clicks "Bjud in" → modal shows "Uppgradera plan" instead of submit button
- Clicks "Uppgradera plan" → lands on `/settings?tab=billing`

**Latency budget:** seat check is O(1) DB query (`COUNT` on `WorkspaceMember` + `WorkspaceInvitation`) plus a Stripe API call for add-on seats. Stripe Subscription retrieval is ~100-200ms — acceptable on the invite path which isn't a hot path.

**Caching scope (important):** if multiple seat checks fire in one HTTP request (rare but possible — e.g., team tab page + concurrent modal data fetch), use a **request-scoped memo** (e.g., a `Map` declared inside the route handler / server action call, NOT module-scope). Do NOT import 5.5c's process-wide `seat-cache.ts` here — that cache is tuned for the chat hot path's repeated reads of the same workspace's seat count over many minutes. The invite-create path's seat count needs to reflect any concurrent invitation-create in the same request, so per-request memoization is the right scope.

## Change Log

| Date       | Version | Description                                                                | Author   |
| ---------- | ------- | -------------------------------------------------------------------------- | -------- |
| 2026-05-05 | 1.0     | Initial story draft — sub-story of refreshed Story 5.5 v3.0                | Bob (SM) |
| 2026-05-05 | 1.1     | Validation fixes: confirmed invite-modal filename; added `getSeatUsage()` return-shape sketch; clarified request-scoped vs process-wide cache scope (do not import 5.5c's seat-cache here). | Sarah (PO) |

## Dev Agent Record

_To be populated by dev agent_

### Agent Model Used

_To be populated_

### Debug Log References

_To be populated_

### Completion Notes List

_To be populated_

### File List

_To be populated_

## QA Results

_To be populated by QA agent_
