# Story 21.7: Findings — create, edit, close (type + severity + root cause)

## Status

Done

## Story

**As a** responsible user or lead auditor working a Lagefterlevnadskontroll cycle,
**I want** to capture structured findings (avvikelser / observationer / förbättringsförslag) against the cycle — either cycle-wide or pinned to a specific `ComplianceAuditItem` / `LawListItemRequirement` — with type, severity, title, description, optional root cause, optional due date, and a clear create / edit / close lifecycle,
**so that** deviations and observations live in a first-class entity (not scattered in free-text motiveringar), feed the future revisionsrapport, and lay the foundation for Story 21.8's corrective-action auto-spawn and Story 21.9's seal evidence manifest.

## Acceptance Criteria

### Functional

1. **Server-action module `app/actions/compliance-finding.ts` (new file).** `'use server'` + file-header JSDoc (`Story 21.7 — Finding CRUD + lifecycle server actions. Pattern mirrors app/actions/compliance-audit-item.ts (Story 21.5). Task auto-spawn for AVVIKELSE is Story 21.8's concern.`). Uses the same `ActionResult<T>` shape declared locally in `app/actions/compliance-audit-item.ts:38-42` (never cross-imported). All mutations wrap `withWorkspace(cb, 'tasks:edit')`; the single read (`listFindingsForCycle`) uses `withWorkspace(cb, 'read')` with the established OR-check `ctx.hasPermission('activity:view') || ctx.hasPermission('tasks:edit')` — same gate as `getCycleItemsForCycle` at `app/actions/compliance-audit-item.ts:226-231` so AUDITOR continues to read findings via `activity:view` (Story 21.14 regression pin). `[Source: app/actions/compliance-audit-item.ts:38-42, 226-231; architecture/epic-21-lagefterlevnadskontroll.md §5.1 + §12.2]`

2. **Zod schemas + XOR / severity validation.** The module declares:
   - `FindingIdOnlySchema = z.object({ findingId: z.string().uuid() })`.
   - `CreateFindingSchema` — fields `cycleId`, `type: z.nativeEnum(FindingType)`, `severity: z.nativeEnum(FindingSeverity).nullable().optional()`, `title: z.string().min(1, 'Titel krävs').max(200, 'Max 200 tecken')`, `description: z.string().min(1, 'Beskrivning krävs').max(5000, 'Max 5000 tecken')`, `rootCause: z.string().max(5000).nullable().optional()`, `dueDate: z.coerce.date().nullable().optional()`, `lawListItemId: z.string().uuid().nullable().optional()`, `requirementId: z.string().uuid().nullable().optional()`. One `.refine`:
     ```ts
     .refine(d => d.type !== FindingType.AVVIKELSE || (d.severity === 'MAJOR' || d.severity === 'MINOR'), {
       message: 'Allvarlighetsgrad krävs för avvikelser',
       path: ['severity'],
     })
     ```
   - `UpdateFindingSchema` — `findingId` required; every other field `.optional()` with the same constraints. Same severity-required-when-AVVIKELSE `.refine` applied; additionally `.refine` requires at least one field to be set (mirrors `UpdateCycleMetadataSchema` at `app/actions/compliance-audit-cycle.ts:129-139`).
   - `CloseFindingSchema` — `findingId` required; `closeReason: z.string().max(1000).nullable().optional().transform((s) => (s?.trim() ? s.trim() : null))`. **SF-2 fix (PO v0.2):** `.min(1)` removed so empty/whitespace-only strings coerce to `null` via the transform, letting the business-layer null check in AC 5 own the user-facing Swedish error. The `closeReason` is populated ONLY when the caller exercises the manual-override path (AC 5); validation is loose at the Zod layer (business logic handled in the action body so the error is a user-targeted Swedish string, not a `ZodIssue`).
   - `ListFindingsSchema` — `cycleId` required; optional filters `type: z.nativeEnum(FindingType).optional()`, `severity: z.nativeEnum(FindingSeverity).optional()`, `status: z.enum(['open', 'closed', 'all']).default('all')`.
   - `ReopenFindingSchema = z.object({ findingId: z.string().uuid() })`.
   `[Source: prisma/schema.prisma:1918-1949 (ComplianceFinding model + enums); app/actions/compliance-audit-cycle.ts:120-139 (refine idiom); architecture/epic-21-lagefterlevnadskontroll.md §4.1 + §10.3]`

3. **`createFinding(input)` action.** Workspace-scope via the cycle join (`cycle.workspace_id = ctx.workspaceId`), mirrors `loadItemScopedToWorkspace` at `app/actions/compliance-audit-item.ts:171-187`. Steps:
   1. Zod parse input → structured error on failure.
   2. Load cycle via `prisma.complianceAuditCycle.findFirst({ where: { id: cycleId, workspace_id: ctx.workspaceId }, select: { id: true, status: true, law_list_id: true } })`. Null → `{ success: false, error: 'Kontrollen hittades inte' }`.
   3. Reject if `cycle.status` is `SEALED` or `ARKIVERAD` via the local `assertCycleEditableUi` helper (copy the exact implementation from `app/actions/compliance-audit-item.ts:196-208` — duplication is deliberate; Story 21.10 replaces both with the shared `assertCycleEditable(tx, cycleId)`). **PLANERAD cycles are NOT blocked (SF-3 decision, PO v0.2):** a PLANERAD cycle has no materialized `ComplianceAuditItem` rows (Story 21.4 AC 5), so cycle-level findings (`lawListItemId === null` and `requirementId === null`) are permitted on PLANERAD and will ripen naturally once the cycle transitions to PAGAENDE. Item-linked findings on a PLANERAD cycle still fail the cross-link validation in step 4 when the wizard UI doesn't populate `items` yet — the naturally-defensive path, no extra gate needed. The Findings tab is not discoverable for most users pre-materialisation anyway (the cycle detail page is typically entered from the post-materialisation redirect), so this is a theoretical-not-practical edge case. Accept-as-is per MVP minimalism.
   4. **Cross-link validation (tenant isolation):**
      - If `input.lawListItemId` set, verify the `LawListItem` belongs to the same laglista as the cycle: `prisma.lawListItem.findFirst({ where: { id, law_list: { workspace_id: ctx.workspaceId } } })` — null → `{ success: false, error: 'Den valda lagen tillhör inte arbetsytan' }`. The belongs-to-same-laglista check is tighter than "same workspace" but is an acceptable UX concession for MVP — findings are scoped to a cycle, and the cycle is scoped to one laglista. If the UI passes a `lawListItemId` that's valid workspace-wide but on a different laglista, surface `'Lagposten tillhör inte kontrollens laglista'` as a distinct structured error (second findFirst within the cycle's laglista — see Dev Notes "Cross-link scoping").
      - If `input.requirementId` set, verify its `law_list_item.law_list_id === cycle.law_list_id` (relation walk). Null → `{ success: false, error: 'Kravpunkten tillhör inte kontrollens laglista' }`.
   5. `prisma.complianceFinding.create({ data: { cycle_id, law_list_item_id, requirement_id, type, severity, title, description, root_cause, due_date } })`. `corrective_action_task_id` left null — Story 21.8 populates it.
   6. `logActivity(ctx.workspaceId, ctx.userId, 'compliance_finding', finding.id, 'finding_created', null, { type, severity: severity ?? null, title, lawListItemId: lawListItemId ?? null, requirementId: requirementId ?? null })`. Payload mirrors the `cycle_created` precedent at `compliance-audit-cycle.ts:247-260` — enough to render the sentence without reading the DB again.
   7. `revalidatePath('/laglistor/kontroller/' + cycleId)` (scoped to cycle detail; does NOT touch `/laglistor`).
   8. Return `{ success: true, data: { finding: FindingRow } }` — full row (including resolved cross-link labels) so the SWR cache can set-in-place without re-fetch. `[Source: app/actions/compliance-audit-cycle.ts:191-269 (createCycle precedent); app/actions/compliance-audit-item.ts:171-187 (tenant-scoped load helper); prisma/schema.prisma:1918-1949]`

4. **`updateFinding(input)` action.** Zod parse → load scoped finding (helper `loadFindingScopedToWorkspace(findingId, workspaceId)`, mirrors the item helper) → `assertCycleEditableUi(finding.cycle.status)` check → **block updates on already-closed findings**: if `finding.closed_at !== null`, return `{ success: false, error: 'Stängda findings kan inte redigeras. Återöppna först.' }` (forces the user through the explicit reopen affordance for audit trail clarity). → Compute the diff between supplied fields and current values; short-circuit to the current row when every supplied field matches (idempotent no-op — no activity log row). → `prisma.complianceFinding.update({ where: { id: findingId }, data: { ...onlyChangedFields } })`. Cross-link changes (swapping `lawListItemId` / `requirementId`) re-run the same AC 3.4 tenant validation. → Log activity `finding_updated` with `{ old: onlyChangedFieldsPrevious, new: onlyChangedFieldsNext }` — standard old→new payload shape. Log length ONLY for `description` / `root_cause` (same privacy-pin pattern as Story 21.5's motivering log — `{old_description_length, new_description_length}`). → `revalidatePath('/laglistor/kontroller/' + cycle_id)`. → Return the refreshed row. `[Source: app/actions/compliance-audit-item.ts:292-438 (updateItemBedomning + updateItemMotivering as the idempotent-shortcut + privacy-pin precedents); architecture §10.3 activity-logging discipline]`

5. **`closeFinding(input)` action — with corrective-action task gate + manual override.** Steps:
   1. Zod parse, load scoped finding, `assertCycleEditableUi` check.
   2. Idempotent: if `finding.closed_at !== null`, return the current row without write (no activity log row).
   3. **AVVIKELSE + linked task gate:** if `finding.type === 'AVVIKELSE'` AND `finding.corrective_action_task_id !== null`, load the linked task (`prisma.task.findFirst({ where: { id: corrective_action_task_id, workspace_id: ctx.workspaceId }, select: { id: true, completed_at: true } })`):
      - If task exists AND `task.completed_at === null` (task is not completed) AND `input.closeReason` is empty → return `{ success: false, error: 'Den kopplade uppgiften är inte klar. Slutför uppgiften eller ange en manuell anledning (close_reason).' }`. **Error code contract** (for IV2 regression pin): `'FINDING_REQUIRES_TASK_CLOSURE'` embedded as a prefix on the error string — consumers can `.startsWith()` test. Matches architecture §10.3 enumerated codes.
      - If task exists AND not completed AND `input.closeReason` set → closure proceeds and the reason is persisted in the activity log `new_value` as `{ manual_override: true, close_reason }` — a permanent audit crumb; the finding itself has no `close_reason` column (not in schema), so the log is the durable record. **Schema decision explicit:** we do NOT add a `close_reason` column to `ComplianceFinding` in this story — it belongs in the log (additive-only schema principle, architecture §4.3 / CR2). If product wants to render `close_reason` on the finding row in the UI, it can be denormalised from the latest closure activity log via the resolver.
      - If task does NOT exist (stale FK or soft-deleted) → closure proceeds without gate (the task FK is `onDelete: SetNull` per `schema.prisma:1938`; a missing task is a degenerate edge case — audit-log accordingly).
   4. OBSERVATION + FORBATTRING types: no task gate. Closure proceeds unconditionally.
   5. `prisma.complianceFinding.update({ where: { id }, data: { closed_at: now, closed_by_user_id: ctx.userId } })`.
   6. `logActivity(..., 'compliance_finding', findingId, 'finding_closed', null, { closed_at, closed_by_user_id, ...(close_reason ? { manual_override: true, close_reason } : {}) })`.
   7. `revalidatePath('/laglistor/kontroller/' + cycle_id)`. Return the refreshed row. `[Source: prisma/schema.prisma:1918-1949 (no close_reason column) + :1938 (onDelete: SetNull for corrective_action_task_id); architecture/epic-21-lagefterlevnadskontroll.md §10.3 enumerated error codes]`

6. **`reopenFinding(findingId)` action.** Zod parse → load scoped finding → `assertCycleEditableUi` check → idempotent (if `closed_at === null`, return current row) → `prisma.complianceFinding.update({ where: { id }, data: { closed_at: null, closed_by_user_id: null } })` → `logActivity(..., 'finding_reopened', { closed_at: prev, closed_by_user_id: prev }, null)` → `revalidatePath`. Added explicitly in this story because:
   - Story 21.13's "Carry-forward notes for future Epic-21 stories" pre-wired the `finding_reopened` action string per its reserve-slots-at-emission-time convention; this story owns the emitter (both action string + writer).
   - Without reopen, a user who clicks "Close" prematurely is stuck — the audit cycle would need a new finding to correct, polluting history. Reopen is the natural inverse and is cheap to implement alongside close.
   - Out-of-scope by epic AC 7 (which lists `createFinding, updateFinding, closeFinding, listFindingsForCycle`). Flagged in **Deviations from epic** below. `[Source: docs/stories/21.13.activity-log-integration.md (carry-forward block); architecture §2.3 additive-surface principle]`

7. **`listFindingsForCycle(input)` read action.** OR-gated (`activity:view || tasks:edit`). Cycle tenant-scope check (same pattern as `getCycleItemsForCycle`). Returns `{ findings: FindingRow[] }` sorted by `(created_at desc, id desc)` — newest first. Filters:
   - `type`: exact match if provided.
   - `severity`: exact match if provided (null-matching is intentional — filtering by MAJOR excludes null severities, which is the correct UX for "show only avvikelser MAJOR").
   - `status === 'open'` → `closed_at === null`; `'closed'` → `closed_at !== null`; `'all'` → no filter.
   The same `FindingRow` shape (AC 11) is returned by every read + mutation path. `[Source: app/actions/compliance-audit-item.ts:214-286 (getCycleItemsForCycle precedent, including tenant-scope + OR-permission + sorting patterns)]`

8. **Activity-logger extension (3-line diff across 3 files).** Per Story 21.13's carry-forward convention:
   - **`lib/activity/action-constants.ts`** — append four literals to `KNOWN_ACTIONS` under a new `// Compliance-audit findings (Epic 21 — Story 21.7)` section: `'finding_created'`, `'finding_updated'`, `'finding_closed'`, `'finding_reopened'`. Placement: at the array tail, after the existing `// Compliance-audit cycle (Epic 21 — Stories 21.2 + 21.5)` block. Preserve `as const`.
   - **`lib/activity/categories.ts`** — add four entries to `ACTION_TO_CATEGORY` under a new `// compliance-audit findings (Epic 21)` section comment:
     ```ts
     finding_created: 'livscykel',
     finding_updated: 'andringar',
     finding_closed: 'livscykel',
     finding_reopened: 'livscykel',
     ```
     The `categories.test.ts` exhaustiveness test (lines 12-19) will auto-validate — dropping any of these fails CI immediately. `[Source: docs/stories/21.13.activity-log-integration.md AC 2 + carry-forward]`
   - **`lib/activity/format-activity.ts`** — add a new `// ----------------- Compliance-audit findings (Epic 21) -----------------` group placed BETWEEN the existing cycle-item group and the `// ----------------- Fallback -----------------` arm. Four sentence cases:

     | Action | Sentence parts |
     |---|---|
     | `finding_created` | `[user] skapade [type-label] [primary]` where `type-label ∈ {'avvikelsen', 'observationen', 'förbättringsförslaget'}` — derived from `newValue.type` via a new `FINDING_TYPE_LABELS_DEFINITE` map (definite form, matches natural Swedish phrasing). Fallback to `'en finding'` if type is missing. Title NOT included in the sentence (already carried by `primary.label` via the resolver's `title` select). |
     | `finding_updated` | `[user] uppdaterade [primary]` — generic; the expanded-row shows the full diff. Intentional — including every field name would make sentences verbose. |
     | `finding_closed` | `[user] stängde [primary]` — if `newValue.manual_override === true`, append `: anledning — [emphasis: close_reason]` (truncated to 80 chars with ellipsis if longer). Fallback (no close_reason) is the short sentence. |
     | `finding_reopened` | `[user] återöppnade [primary]` |

     The `FINDING_TYPE_LABELS_DEFINITE: Record<string, string>` constant: `{ AVVIKELSE: 'avvikelsen', OBSERVATION: 'observationen', FORBATTRING: 'förbättringsförslaget' }`. Placed alphabetically among the existing label maps in `format-activity.ts:13-46`. `[Source: lib/activity/format-activity.ts:13-46, 448-524 (existing cycle formatters); docs/stories/21.13.activity-log-integration.md AC 3 (EFTERLEVNADS_BEDOMNING_LABELS precedent)]`

9. **`getSecondaryRef` extension — finding→item chain.** `lib/activity/entity-resolver.ts:20-59` gets one new branch: if `entity_type === 'compliance_finding'`, inspect the row's `old_value` / `new_value` for a `lawListItemId` field; if present, return `{ entity_type: 'compliance_audit_item', entity_id: <the lawListItemId> }` as the secondary ref. The existing `compliance_audit_item` resolver branch (added by Story 21.13) then renders the item link as `[primary title] → [document title + document number]`. If no `lawListItemId` in payload, no secondary ref (generic cycle-level finding). Keeps the chain narrow — NOT pursued for finding→requirement (too many hops for MVP sentence clarity). **NH-2 scope clarification (PO v0.2):** Secondary ref resolves ONLY when the activity payload carries `lawListItemId` — that is, `finding_created` (always, if the finding is item-linked) and `finding_updated` when the item changed (diff includes `lawListItemId`). `finding_closed` / `finding_reopened` payloads do NOT carry `lawListItemId` (payloads only include `closed_at`/`closed_by_user_id`/`manual_override`/`close_reason` per AC 5–6), so those activity rows render primary-only. This is acceptable for MVP — the resolver avoids extra DB round-trips to look up the current finding's item. Do NOT over-engineer a DB fallback in this story. `[Source: lib/activity/entity-resolver.ts:20-59 (getSecondaryRef structure); docs/stories/21.13.activity-log-integration.md carry-forward note on getSecondaryRef]`

10. **Cycle detail page — Findings tab.** Replace the `Hanteras i Story 21.7` placeholder at `components/features/compliance-audit/cycle-detail/cycle-detail-page.tsx:259-263` with a live `<CycleFindingsTab>` client component (new file under `components/features/compliance-audit/cycle-detail/cycle-findings-tab.tsx`). Props: `{ cycleId, findings: FindingRow[], readOnly: boolean, items: CycleItemRow[], onFindingMutation: (finding: FindingRow) => void }` — items are passed so the finding editor can offer item-link selection without re-fetching. `findings` is the live array from the page-level SWR cache (see SF-1 note below); `onFindingMutation` is the replace-in-place callback owned by `CycleDetailPage`. The tab:
    - **SF-1 fix (PO v0.2):** The findings SWR fetch is hoisted to `CycleDetailPage` (not owned by this tab) — see AC 13 + Task 8 for the rationale. The page mounts `useSWR(complianceFindingsKey(cycle.id), fetcher, { fallbackData: { findings: initialFindings }, ... })` ONCE at the page level. Both `CycleFindingsTab` and `CycleItemRowDrawer` then consume the same cache via the SWR key (either via `useSWR(key)` without fetcher — SWR deduplicates on the same key — or via props from the page). This keeps the per-item drawer affordance (AC 13) correct even when the user has never visited the Findings tab. The tab itself is a pure-presentation component reading from whatever its parent passes.
    - SWR key centralised in `lib/swr-keys/compliance-audit.ts` per architecture §8.1 — a new-tiny-file; also export the `compliance-audit-items:${cycleId}` key that Story 21.5 uses inline, as a refactor quality-of-life. NOT blocking — if the refactor feels out of scope, export only the new key and leave the 21.5 key inline with a `TODO: move to swr-keys` marker.
    - Renders filter chips at the top: All / Avvikelse / Observation / Förbättring + a secondary severity filter (only visible when type = Avvikelse). Open / Closed toggle on the right.
    - Renders a virtualised list (`useVirtualizer` same pattern as `cycle-items-tab.tsx`) IF `findings.length > 50`; plain render below threshold (findings are typically fewer than items).
    - Each row: `[type badge] [severity badge if AVVIKELSE] [title] [status badge: Öppen / Stängd] [created-at] [cross-link chip if item/requirement] [Redigera] [Stäng | Återöppna]`.
    - Clicking a row expands it inline (accordion semantics, single-open — matches Items tab UX at Story 21.5 AC 9) showing: description, root cause, due date, cross-links, created-by, closed-by, and the raw activity history for the finding (read from a filtered `getWorkspaceActivity({ entityType: ['compliance_finding'] })` call on mount, or — simpler — skip for this story and surface only create/close events that are already in the payload the resolver returns; the `/workspace/activity` feed is the canonical history viewer).
    - "Lägg till finding" button at the top-right of the tab — opens the editor dialog (AC 12).
    - Empty state: `'Inga findings registrerade ännu.'` (Swedish).
    - Read-only mode (`readOnly === true`): hide "Lägg till finding", "Redigera", "Stäng", "Återöppna" buttons; show a banner `'Denna kontroll är förseglad. Findings kan endast visas.'` (matches the cycle-level banner pattern at `cycle-detail-header.tsx:45`). `[Source: components/features/compliance-audit/cycle-detail/cycle-items-tab.tsx (virtualisation + single-open accordion pattern); components/features/compliance-audit/cycle-detail/cycle-detail-header.tsx:45 (read-only banner pattern)]`

11. **`FindingRow` read model shape.** Exported from `app/actions/compliance-finding.ts`:
    ```ts
    export interface FindingRow {
      id: string
      cycleId: string
      type: FindingType
      severity: FindingSeverity | null
      title: string
      description: string
      rootCause: string | null
      dueDate: Date | null
      closedAt: Date | null
      closedBy: { id: string; name: string | null } | null
      lawListItemId: string | null
      lawListItem: { id: string; title: string; documentNumber: string } | null
      requirementId: string | null
      requirement: { id: string; text: string } | null
      correctiveActionTaskId: string | null
      correctiveActionTask: { id: string; title: string; completedAt: Date | null } | null
      createdAt: Date
      updatedAt: Date
    }
    ```
    Single shared `FINDING_INCLUDE` constant (mirrors `CYCLE_ITEM_INCLUDE` at `compliance-audit-item.ts:100-110`) drives every read + mutation-return shape. `corrective_action_task` is included even though it's always null in 21.7 — Story 21.8 populates it on AVVIKELSE create; the include already being there means 21.8's diff is narrowed to the task-create call, not the read-shape. `[Source: app/actions/compliance-audit-item.ts:51-67 (CycleItemRow shape), :100-110 (INCLUDE constant pattern); prisma/schema.prisma:1918-1949]`

12. **Finding editor dialog (`finding-editor/finding-editor.tsx` under `components/features/compliance-audit/finding-editor/`).** Self-contained create + edit dialog using shadcn `Dialog`. Props: `{ open, onOpenChange, cycleId, mode: 'create' | 'edit', finding?: FindingRow (required in edit mode), items: CycleItemRow[], onSuccess: (finding: FindingRow) => void }`. Form fields:
    - **Type** — shadcn `RadioGroup` with three options (AVVIKELSE / OBSERVATION / FORBATTRING). Swedish labels match the definite-form labels introduced in AC 8 BUT for the form use the indefinite form: `'Avvikelse'` / `'Observation'` / `'Förbättringsförslag'`. New label map: `FINDING_TYPE_LABELS` in `components/features/compliance-audit/finding-copy.ts` (new file alongside `bedomning-copy.ts`).
    - **Severity** — shadcn `Select`, visible ONLY when `type === AVVIKELSE`. Options MAJOR / MINOR. Required when visible (client + server enforced; server is authoritative per AC 2 refine).
    - **Title** — `Input`, `required`, `maxLength={200}`, placeholder `'T.ex. Saknad utbildningsplan för kemikaliehantering'`.
    - **Description** — `Textarea`, `required`, `maxLength={5000}`, `autosize` (same pattern as `ItemMotiveringEditor`).
    - **Root cause** — `Textarea`, optional, `maxLength={5000}`, `autosize`. Swedish label: `'Grundorsak (frivilligt)'`.
    - **Due date** — `DatePicker` (reuse the existing calendar primitive; grep `'DatePicker'` in `components/ui/` to confirm). Optional.
    - **Item link** — shadcn `Combobox` listing `items` with search by law title + document number. Optional. Emits `lawListItemId`. Display label: `{lawTitle} ({lawDocumentNumber})`.
    - **Requirement link** — dependent on item link; visible only when an item is selected. Fetches that item's requirements via a lightweight prisma call exposed through a new read action `getRequirementsForItem(lawListItemId)` (OR-gated like `listFindingsForCycle`). Optional. Alternative if adding another read action feels heavy: reuse the existing `KravpunkterSnapshot` on the selected item (it's already on `CycleItemRow.kravpunkterSnapshot`). **Decision: reuse the snapshot.** Requirements fetched from the snapshot are FROZEN — not the live kravpunkter — which is correct for audit semantics: a finding created mid-cycle should reference what was in scope at cycle start, not what shifted later. Document this inline on the field's label: `'Kravpunkt (från kontrollens snapshot)'`. Edge case: if the snapshot is null (degenerate, shouldn't happen per Story 21.4), hide the requirement field entirely. `[Source: app/actions/compliance-audit-item.ts:51-67 (CycleItemRow.kravpunkterSnapshot field); prisma/schema.prisma:1903 (frozen snapshot column)]`

    Form validation mirrors `CreateFindingSchema` / `UpdateFindingSchema`. Submit calls the appropriate action; on success, `onSuccess` fires with the returned row → SWR cache is updated in-place → dialog closes. On error, toast with the Swedish error string from the action.

13. **Per-item "Lägg till finding" affordance.** In `cycle-item-row-drawer.tsx`, replace the existing `Hanteras i Story 21.7` placeholder (lines 96-103) with:
    - A live read-only list of findings where `lawListItemId === row.lawListItemId`, fetched from the cycle's `compliance-findings:${cycleId}` SWR cache. **SF-1 fix (PO v0.2):** the drawer MUST get findings reliably regardless of which tab is currently visible. The page-level SWR hoist in AC 10 ensures the cache is pre-populated via `fallbackData: { findings: initialFindings }` on `CycleDetailPage` mount. The drawer reads via either (a) `useSWR(complianceFindingsKey(cycleId))` (no fetcher — shares the page's cache via key dedup), OR (b) a `findings: FindingRow[]` prop threaded from page → `CycleItemsTab` → `CycleItemRowDrawer`. **Decision: use prop-threading** (option b) — avoids a second `useSWR` hook + makes the data flow explicit + matches the `items` prop-threading pattern at `cycle-items-tab.tsx` props signature. Drawer filters client-side: `findings.filter((f) => f.lawListItemId === row.lawListItemId)`. Empty state: `'Inga findings för denna post ännu.'`. Items rendered as compact chips with `[type badge] [title]` — clicking opens the finding editor dialog in edit mode (AC 12).
    - A primary button `'+ Lägg till finding'` opening the editor in create mode, prefilled with `lawListItemId = row.lawListItemId`. Hidden when `readOnly === true`.
    The cycle-wide "Lägg till finding" button on the Findings tab (AC 10) has no item prefill — it's the cycle-level entry point. Both paths resolve to the same editor + action. `[Source: components/features/compliance-audit/cycle-detail/cycle-item-row-drawer.tsx:96-103 (placeholder being replaced); AC 10 of this story (SWR key); components/features/compliance-audit/cycle-detail/cycle-detail-page.tsx:236-277 (prop-threading precedent for items → CycleItemsTab → CycleItemRowDrawer)]`

14. **Cycle-detail header — findings count chip.** `cycle-detail-header.tsx` gets a new compact chip in the metadata row: `'Findings: {openCount} öppna · {closedCount} stängda'`. Data source: derive from the same `findings` array used by the page-level SWR cache (SF-1 fix — AC 10). The RSC page pre-fetches `listFindingsForCycle` in the `Promise.all` and passes `initialFindings` into `CycleDetailPage`; the page-level SWR hook then hydrates the cache + re-computes open/closed counts via `useMemo` on every mutation. Counts thread: page → `CycleDetailHeader` via a `findingCounts: { open: number; closed: number }` prop. If both are 0, chip reads `'Findings: inga'`. The RSC page at `app/(workspace)/laglistor/kontroller/[cycleId]/page.tsx` grows the `Promise.all` from two entries to three. `[Source: app/(workspace)/laglistor/kontroller/[cycleId]/page.tsx:54-57 (Promise.all pattern); components/features/compliance-audit/cycle-detail/cycle-detail-header.tsx:37-60 (header layout)]`

15. **Read-only mode propagation.** `readOnly` prop threads page → `CycleDetailPage` → `CycleFindingsTab` → editor (the editor simply does not render in read-only — the "Lägg till finding", "Redigera", "Stäng", "Återöppna" buttons are hidden). Server-side defence-in-depth: every mutation action re-checks `assertCycleEditableUi` (AC 3–6). Matches the `complianceReadOnly` threading pattern documented in `MEMORY.md` and Story 21.5 AC 8.

16. **Findings filter performance (NFR).** IV3 of the epic requires findings-tab rendering to scale to 100+ findings without noticeable lag. Implementation:
    - Client-side filtering ONLY (no extra server calls on filter change). `listFindingsForCycle` loads all findings for the cycle once per SWR revalidation window; the filter chips mutate a `useMemo` over the cached array.
    - Virtualisation threshold 50 (per AC 10).
    - Sort (created_at desc) is server-side — no client sort.
    - Microtest in AC 19: mount 150 findings, assert initial render ≤ 1500ms on jsdom. Same generous jsdom ceiling as Story 21.5 IV2.

17. **No task auto-spawn in this story.** AVVIKELSE creation MUST NOT auto-create a corrective-action `Task`. That's Story 21.8. This story leaves `finding.corrective_action_task_id` null on every create, and `finding_task_spawned` is NOT in the KNOWN_ACTIONS diff (Story 21.8's concern). `closeFinding`'s task-gate logic (AC 5) is still wired — the gate checks whether `corrective_action_task_id` is populated, which in a 21.7-only world is always null, so the gate is a no-op. After 21.8 ships, the same action immediately picks up the gate without further changes. `[Source: PRD §6 Story 21.8 AC 1-3; architecture §5.1 Component Architecture]`

### Test coverage

18. **`app/actions/__tests__/compliance-finding.test.ts` — NEW.** Mock `@/lib/prisma`, `@/lib/auth/workspace-context`, `@/lib/services/activity-logger`, `next/cache`. Mirror the mock boilerplate at `app/actions/__tests__/compliance-audit-item.test.ts` verbatim. Minimum cases:
    - **`createFinding` — happy path AVVIKELSE with severity + all fields.** Activity logged with correct payload; path revalidated; row returned.
    - **`createFinding` — happy path OBSERVATION without severity (null).** Validates severity is optional for non-AVVIKELSE.
    - **`createFinding` — AVVIKELSE without severity rejected.** Zod refine surfaces `'Allvarlighetsgrad krävs för avvikelser'`; no DB write; no activity log.
    - **`createFinding` — cycle SEALED blocked.** Structured error; no write; no log.
    - **`createFinding` — cycle ARKIVERAD blocked.**
    - **`createFinding` — cross-workspace cycle.** Returns `'Kontrollen hittades inte'` (generic, no existence leak).
    - **`createFinding` — item on different laglista.** Returns `'Lagposten tillhör inte kontrollens laglista'`.
    - **`createFinding` — item belongs to a different workspace entirely (NH-3, PO v0.2).** Tenant-isolation pin: `lawListItemId` pointing to a `LawListItem` in workspace B, while the cycle is in workspace A. Action returns `'Den valda lagen tillhör inte arbetsytan'` (the first cross-link check in AC 3.4). No DB write; no activity log. Prevents information leak across tenants.
    - **`createFinding` — requirement on different laglista.** Returns `'Kravpunkten tillhör inte kontrollens laglista'`.
    - **`createFinding` — cycle-level finding on PLANERAD cycle proceeds (SF-3, PO v0.2).** Fixture: cycle in PLANERAD, `lawListItemId === null`, `requirementId === null`. Action succeeds; activity log `finding_created` written; row returned.
    - **`createFinding` — item-linked finding on PLANERAD cycle fails via cross-link (SF-3, PO v0.2).** Fixture: cycle in PLANERAD, `lawListItemId` provided but items aren't materialised yet → cross-link validation falls through when the laglista contains the item but the wizard is mid-flow. Acceptable edge — the test fixture uses a `lawListItemId` from the same laglista which SHOULD succeed (PLANERAD is not a gate). Rename this case: "createFinding — item-linked finding on PLANERAD cycle proceeds when item belongs to cycle's laglista" — asserts PLANERAD does not block.
    - **`createFinding` — permission denied.** `withWorkspace` throws `WorkspaceAccessError` → structured error.
    - **`updateFinding` — happy path, idempotent shortcut.** Unchanged fields → no DB write, no log. Changed fields → update + log with old/new diff.
    - **`updateFinding` — closed finding blocked.** Returns `'Stängda findings kan inte redigeras...'`.
    - **`updateFinding` — description length diff logged, not raw text.** Asserts the `logActivity` mock's `newValue` contains `{new_description_length: N}` and does NOT include the raw description string.
    - **`updateFinding` — SEALED blocked.**
    - **`closeFinding` — happy path (OBSERVATION, no task gate).** Timestamps set; activity logged with `closed_at, closed_by_user_id`.
    - **`closeFinding` — idempotent (already closed).** Second call no-op; no log.
    - **`closeFinding` — AVVIKELSE with completed task.** Gate passes; closure proceeds; no close_reason needed.
    - **`closeFinding` — AVVIKELSE with incomplete task, no close_reason.** Blocked: error prefix `'FINDING_REQUIRES_TASK_CLOSURE'`; no write; no log.
    - **`closeFinding` — AVVIKELSE with incomplete task + close_reason.** Closure proceeds; activity log `new_value` includes `{manual_override: true, close_reason}`.
    - **`closeFinding` — AVVIKELSE with corrective_action_task_id pointing to a soft-deleted task.** Gate skipped; closure proceeds.
    - **`closeFinding` — SEALED blocked.**
    - **`reopenFinding` — happy path.** `closed_at` + `closed_by_user_id` cleared; activity logged.
    - **`reopenFinding` — idempotent (already open).** No-op.
    - **`reopenFinding` — SEALED blocked.**
    - **`listFindingsForCycle` — OR-permission gate.** AUDITOR with `activity:view` only → returns data. User with neither `activity:view` nor `tasks:edit` → permission-denied error.
    - **`listFindingsForCycle` — filters.** Separate tests for `type: AVVIKELSE`, `severity: MAJOR`, `status: open`, `status: closed`.
    - **`listFindingsForCycle` — sort order.** Returns newest first; assert on created_at order in fixture.
    - **`listFindingsForCycle` — cross-workspace.** Cycle-id from a different workspace → `'Kontrollen hittades inte'`.
    Coverage target: ≥80% statement coverage on the new server-action module (architecture §11.1 NFR7).

19. **Component tests — `tests/unit/components/features/compliance-audit/cycle-detail/cycle-findings-tab.test.tsx` (NEW).**
    - Renders 3 findings; each row shows correct type badge + title + status.
    - Filter chip click updates the rendered list (useMemo recomputes).
    - Severity filter visible only when Type=Avvikelse.
    - Empty state when findings=[].
    - "Lägg till finding" button hidden when `readOnly=true`.
    - Single-open accordion: expanding row B auto-collapses row A.
    - Virtualisation kicks in at 51+ findings (assert `data-testid="virtualized-findings-list"` presence).
    - Microtest: 150 findings → initial mount ≤ 1500ms (`performance.now()` pattern from Story 21.5).

20. **Finding editor dialog tests — `tests/unit/components/features/compliance-audit/finding-editor/finding-editor.test.tsx` (NEW).**
    - Create mode: type=Avvikelse → severity field appears; type=Observation → severity hidden.
    - Submit create calls `createFinding` with correct args; on success calls `onSuccess` with returned row + closes dialog.
    - Title length validation client-side (201 chars → disabled submit or inline error).
    - Edit mode: form prefilled from `finding` prop; submit calls `updateFinding` with only the changed fields (asserted via mock arg diff).
    - Requirement combobox empty / hidden when no item selected.
    - Requirement combobox renders snapshot-based requirements after item selection.
    - Error toast on failed create (mock action returns `{success: false, error: 'X'}`).

21. **Row drawer tests — extend `tests/unit/components/features/compliance-audit/cycle-detail/cycle-items-tab.test.tsx` or the row-drawer test file (whichever owns the drawer component).**
    - Row drawer shows findings filtered to `lawListItemId === row.lawListItemId`.
    - "+ Lägg till finding" button prefills `lawListItemId`.
    - Button hidden in read-only mode.

22. **Header tests — extend the existing header test file (if present) or the page-level RSC test.**
    - Findings count chip renders `{openCount} öppna · {closedCount} stängda` with props fed from the RSC page.
    - Chip updates on SWR revalidation (fire a mutate in the test, assert new counts).

23. **Activity-log formatter + resolver tests (small extension).**
    - Extend `tests/unit/lib/activity/format-activity.test.ts` with 4 new cases per AC 8. Special variant for `finding_closed`: one test asserting `close_reason` is rendered (with 81-char input → truncated to 80 + ellipsis); another asserting the default short sentence when `manual_override` is absent.
    - Extend `tests/unit/lib/activity/categories.test.ts` — no explicit edit; exhaustiveness test auto-validates the new action strings. Just re-run.
    - Extend `tests/unit/lib/activity/entity-resolver.test.ts` with one case covering the new `getSecondaryRef` branch: a `compliance_finding` activity row with `lawListItemId` in payload → secondary ref resolves to the item link; without → no secondary ref.

24. **RSC page test extension — `tests/unit/app/workspace/laglistor/kontroller/cycle-detail-page.test.tsx`.** Extend to assert:
    - `Promise.all` includes a third entry for `listFindingsForCycle`.
    - `CycleDetailPage` receives `findingCounts: { open: N, closed: M }` prop derived from the findings read result.
    - When the findings read fails, `findingCounts` defaults to `{ open: 0, closed: 0 }` — the page still renders (fail-open, matching the existing items-read fail-open pattern at page.tsx:63-83).

### Integration Verification

- **IV1:** Existing task system remains unaffected. Findings without linked tasks behave fully (task auto-spawn is Story 21.8). Verified by: (a) `app/actions/tasks.ts` (if present) and `app/actions/task-modal.ts` suites continue to pass unchanged — grep `corrective_action_task_id` hits ONLY in `compliance-finding.ts` (new) and `compliance-audit-report.ts` (future), zero hits in the task modules; (b) `prisma.task.update` is NOT called by this story's actions (grep assertion in PR).

- **IV2:** Cycle progress stats (from Story 21.5 — `bedomdaCount` / `signeradeCount`) do not change meaning when findings are added/closed. Verified by: the `CycleItemsContextValue` at `cycle-detail-page.tsx:217-233` reads exclusively from `items`, never from findings. No code path in this story writes to `ComplianceAuditItem`, so the item-signed / item-bedomd counts are mathematically unchanged. Regression pin: the existing `CycleItemsContextValue` tests (from Story 21.5) continue to pass with zero changes.

- **IV3:** Findings-tab rendering scales to 100+ findings without noticeable lag. Verified by: the 150-finding microtest in AC 19; virtualisation kicks in at 51; client-side filtering is `useMemo` (no re-fetch on chip click). Real-browser 60fps scroll is a manual QA smoke-test (same treatment as Story 21.5 IV2 — Playwright golden-path is deferred to post-Story-21.9).

- **IV4 (implicit, this story adds):** Existing `/workspace/activity` rendering continues to work with new `finding_*` entity type rows. Verified by: Story 21.13 already wired the `compliance_finding` resolver branch — this story only appends action strings + formatter cases, which the exhaustiveness test at `tests/unit/lib/activity/categories.test.ts:12-19` auto-validates. Zero changes to `app/actions/workspace-activity.ts`, zero changes to `components/features/activity/activity-log-table.tsx`, zero changes to `lib/services/activity-logger.ts`.

### Rollback

- **Code:** `git revert` the Story 21.7 commits. Affected paths:
  - `app/actions/compliance-finding.ts` (NEW).
  - `app/actions/__tests__/compliance-finding.test.ts` (NEW).
  - `components/features/compliance-audit/cycle-detail/cycle-findings-tab.tsx` (NEW).
  - `components/features/compliance-audit/cycle-detail/cycle-detail-page.tsx` (MODIFIED — placeholder replacement + new prop for findingCounts).
  - `components/features/compliance-audit/cycle-detail/cycle-detail-header.tsx` (MODIFIED — chip addition).
  - `components/features/compliance-audit/cycle-detail/cycle-item-row-drawer.tsx` (MODIFIED — placeholder replacement + "+ Lägg till finding" button).
  - `components/features/compliance-audit/cycle-detail/index.ts` (MODIFIED — export new components).
  - `components/features/compliance-audit/finding-editor/**` (NEW namespace: `finding-editor.tsx`, `index.ts`).
  - `components/features/compliance-audit/finding-copy.ts` (NEW — label constants).
  - `lib/activity/action-constants.ts` (MODIFIED — 4 new literals).
  - `lib/activity/categories.ts` (MODIFIED — 4 new entries).
  - `lib/activity/format-activity.ts` (MODIFIED — 4 new cases + `FINDING_TYPE_LABELS_DEFINITE` map).
  - `lib/activity/entity-resolver.ts` (MODIFIED — `getSecondaryRef` branch for finding→item).
  - `lib/swr-keys/compliance-audit.ts` (NEW — centralised SWR keys).
  - `app/(workspace)/laglistor/kontroller/[cycleId]/page.tsx` (MODIFIED — Promise.all adds findings read + pass counts prop).
  - Test files (as enumerated in AC 18-24).
- **DB:** No schema changes in this story — rollback is code-only. The `ComplianceFinding` table already exists (Story 21.1); reverting 21.7 leaves it in place but unreferenced by any write path. Manual cleanup (truncate or drop rows inserted during pre-rollback dev/staging) optional, not blocking.
- **Trigger:** regression on `/laglistor/kontroller/[cycleId]` (items tab, rapport tab, aktivitet tab), `/laglistor` main page, kravpunkter editor, tasks UI, activity feed, or the linked-artifacts panel. Not expected given IV1–IV4.

## Tasks / Subtasks

- [x] **Task 1 — `app/actions/compliance-finding.ts` server-action module** (AC: 1, 2, 3, 4, 5, 6, 7, 11, 17)
  - [x] 1.1 Create `app/actions/compliance-finding.ts` with `'use server'` + file-header JSDoc per AC 1. Pattern mirrors `app/actions/compliance-audit-item.ts` verbatim (ActionResult interface, module-private helpers section, public-action section with section comments).
  - [x] 1.2 Define module-local `ActionResult<T>` interface (copy from compliance-audit-item.ts:38-42 — do NOT cross-import). Declare `FindingRow` export per AC 11.
  - [x] 1.3 Declare Zod schemas per AC 2. The `.refine` for AVVIKELSE severity is declarative; the "at least one field" refine for UpdateFindingSchema mirrors `compliance-audit-cycle.ts:129-139`.
  - [x] 1.4 Declare `FINDING_INCLUDE` constant + `mapRowToFindingRow` helper (mirror `CYCLE_ITEM_INCLUDE` at `compliance-audit-item.ts:100-110` + `mapRowToCycleItemRow` at :135-163).
  - [x] 1.5 Copy `assertCycleEditableUi` from `compliance-audit-item.ts:196-208` — deliberate duplication, TODO(21.10) marker at the callback entry of every mutation.
  - [x] 1.6 Implement `loadFindingScopedToWorkspace(findingId, workspaceId)` (mirrors `loadItemScopedToWorkspace` at :171-187; joins through `cycle.workspace_id`).
  - [x] 1.7 Implement `createFinding` per AC 3. Include the two-step cross-link tenant validation (item belongs to the cycle's laglista; requirement walks through `law_list_item.law_list_id === cycle.law_list_id`). Log activity `finding_created`.
  - [x] 1.8 Implement `updateFinding` per AC 4. Diff computation + idempotent shortcut + privacy-pin for `description` / `root_cause` length logging.
  - [x] 1.9 Implement `closeFinding` per AC 5. AVVIKELSE+task gate logic with error prefix `'FINDING_REQUIRES_TASK_CLOSURE'`. close_reason persisted in activity log `new_value` only (no schema column).
  - [x] 1.10 Implement `reopenFinding` per AC 6. Idempotent + activity log entry.
  - [x] 1.11 Implement `listFindingsForCycle` per AC 7. OR-gated read, client-side filter plumbing.
  - [x] 1.12 Every mutation calls `revalidatePath('/laglistor/kontroller/' + cycleId)` on success.

- [x] **Task 2 — Activity-logger extensions** (AC: 8, 9)
  - [x] 2.1 Extend `lib/activity/action-constants.ts` — append 4 literals under new section comment `// Compliance-audit findings (Epic 21 — Story 21.7)`. Preserve `as const`.
  - [x] 2.2 Extend `lib/activity/categories.ts` — add 4 entries to `ACTION_TO_CATEGORY` under new section comment `// compliance-audit findings (Epic 21)`. Run `pnpm vitest run tests/unit/lib/activity/categories.test.ts` — exhaustiveness test must pass.
  - [x] 2.3 Extend `lib/activity/format-activity.ts` — add `FINDING_TYPE_LABELS_DEFINITE` map at the top (alphabetical among existing label maps, after `EFTERLEVNADS_BEDOMNING_LABELS`). Add 4 case arms under new section comment `// ----------------- Compliance-audit findings (Epic 21) -----------------` placed BEFORE the `// ----------------- Fallback -----------------` arm.
  - [x] 2.4 Extend `lib/activity/entity-resolver.ts` — `getSecondaryRef` new branch per AC 9. Return the `{entity_type: 'compliance_audit_item', entity_id}` descriptor only when the payload carries `lawListItemId`.

- [x] **Task 3 — SWR key centralisation** (AC: 10)
  - [x] 3.1 Create `lib/swr-keys/compliance-audit.ts` exporting `complianceFindingsKey(cycleId: string)` and `complianceAuditItemsKey(cycleId: string)`. Both are thin functions returning the existing string shape so tests can assert on the key verbatim.
  - [x] 3.2 OPTIONAL quality-of-life refactor: update `cycle-detail-page.tsx` SWR key literal at line 54 to call `complianceAuditItemsKey(cycle.id)`. If the scope expansion feels undesirable, leave the 21.5 key inline and export only the new findings key. **Recommendation: do the refactor** — diff is 2 lines and it pulls both cycle SWR keys to one file.

- [x] **Task 4 — `CycleFindingsTab` component** (AC: 10, 15, 16)
  - [x] 4.1 Create `components/features/compliance-audit/cycle-detail/cycle-findings-tab.tsx`. `'use client'`. Props per AC 10 (SF-1 revision): `{ cycleId, findings: FindingRow[], readOnly, items, onFindingMutation }`. The tab is presentation-only — it does NOT own the SWR fetch (hoisted to `CycleDetailPage` per Task 8.2).
  - [x] 4.2 Pure consumption — NO `useSWR` call in this component. The `findings` array arrives as a prop already reconciled by the page-level SWR cache. On mutation success the page-level `onFindingMutation` callback reconciles the cache in-place (mirrors Story 21.5's `replaceRow` pattern at `cycle-detail-page.tsx:101-116`).
  - [x] 4.3 Filter chips: type (All | Avvikelse | Observation | Förbättring), severity (MAJOR | MINOR — visible only when type=Avvikelse), status (All | Open | Closed). `useMemo`-driven filter; no re-fetch on chip click.
  - [x] 4.4 Virtualised list (`@tanstack/react-virtual useVirtualizer`) when findings.length > 50, else plain render. Estimated row height 88 (findings have more content per row than items); overscan 5.
  - [x] 4.5 Single-open accordion: `expandedRowId` state; expanding row B auto-collapses row A. Keyboard: Escape collapses; Enter on focused row toggles. Mirrors `cycle-items-tab.tsx:73-80`.
  - [x] 4.6 Empty state + "Lägg till finding" button + read-only banner per AC 10 / AC 15.
  - [x] 4.7 Export from `components/features/compliance-audit/cycle-detail/index.ts`.

- [x] **Task 5 — Finding editor dialog** (AC: 12)
  - [x] 5.1 Create `components/features/compliance-audit/finding-editor/finding-editor.tsx`. `'use client'`. shadcn `Dialog` + form elements.
  - [x] 5.2 Create `components/features/compliance-audit/finding-copy.ts` exporting `FINDING_TYPE_LABELS: Record<FindingType, string>` (indefinite form: `AVVIKELSE: 'Avvikelse'`, `OBSERVATION: 'Observation'`, `FORBATTRING: 'Förbättringsförslag'`) and `FINDING_SEVERITY_LABELS: Record<FindingSeverity, string>` (`MAJOR: 'Större'`, `MINOR: 'Mindre'`). **NH-1 decision (PO v0.2):** `'Större'` / `'Mindre'` is the standard terminology used by Swedish-speaking certification bodies (SIS, RISE, Kiwa) when translating ISO 19011 audit findings. This locks the copy in the story — dev does NOT need to re-validate against the PRD at landing time. Future rename is a single-file edit if product pushes back.
  - [x] 5.3 Form fields per AC 12. Dynamic severity visibility via `type === 'AVVIKELSE'`. Requirement combobox reads from selected item's `kravpunkterSnapshot.requirements` (no extra read action).
  - [x] 5.4 Create `components/features/compliance-audit/finding-editor/index.ts` barrel exporting `FindingEditor`.
  - [x] 5.5 Mode switch (`create` | `edit`) — prefill form from `finding` prop in edit mode.
  - [x] 5.6 Submit handler calls `createFinding` or `updateFinding` from Task 1. On success, fire `onSuccess(returnedRow)` + close dialog. On error, toast via `sonner` (`toast.error` — same pattern as `cycle-detail-page.tsx:128-131`).

- [x] **Task 6 — Row drawer integration** (AC: 13)
  - [x] 6.1 Modify `components/features/compliance-audit/cycle-detail/cycle-item-row-drawer.tsx` — replace the `Hanteras i Story 21.7` placeholder at lines 96-103.
  - [x] 6.2 **SF-1 revision (PO v0.2):** the drawer reads findings from a new `findings: FindingRow[]` prop (threaded page → `CycleItemsTab` → `CycleItemRowDrawer` per Task 8.5). No `useSWR` call inside the drawer. Client-side filter: `findings.filter((f) => f.lawListItemId === row.lawListItemId)`. This guarantees correct rendering regardless of which tab the user has visited, since the cache is pre-populated at page mount via `fallbackData`.
  - [x] 6.3 Render the "+ Lägg till finding" button (hidden on readOnly). Button opens `FindingEditor` with `mode="create"` + prefilled `lawListItemId`. `onSuccess` callback forwards to `onFindingMutation` (threaded from page) so the cache is updated in-place.
  - [x] 6.4 Render the filtered findings chip list (item-scoped findings only). Clicking a chip opens the editor in edit mode (same `onFindingMutation` callback wired up).
  - [x] 6.5 Thread `cycleId`, `readOnly`, `findings`, `onFindingMutation` props into `CycleItemRowDrawer` — currently the drawer only takes `row`. Update the call-site in `cycle-items-tab.tsx` accordingly + update `CycleItemsTab`'s props signature to accept + forward these.

- [x] **Task 7 — Cycle detail header findings count chip** (AC: 14)
  - [x] 7.1 Modify `app/(workspace)/laglistor/kontroller/[cycleId]/page.tsx` — add `listFindingsForCycle(cycleId)` as a third entry to the `Promise.all` at lines 54-57. Derive `findingCounts: { open, closed }` from the result (fail-open to `{open:0, closed:0}` on read error).
  - [x] 7.2 Pass `findingCounts` prop to `CycleDetailPage`.
  - [x] 7.3 `CycleDetailPage` passes `findingCounts` to `CycleDetailHeader`.
  - [x] 7.4 `cycle-detail-header.tsx` renders the chip per AC 14 in the `MetadataChips` cluster. Swedish copy: `'Findings: {openCount} öppna · {closedCount} stängda'`. If both counts are 0, the chip reads `'Findings: inga'`.

- [x] **Task 8 — Wire Findings tab into CycleDetailPage + hoist SWR cache (SF-1)** (AC: 10, 13, 14, 15)
  - [x] 8.1 `CycleDetailPage` accepts `initialFindings: FindingRow[]` prop (threaded from the RSC page — Task 7.1).
  - [x] 8.2 **SF-1 hoist (PO v0.2):** `CycleDetailPage` mounts the page-level SWR hook for findings — `useSWR(complianceFindingsKey(cycle.id), async () => { const r = await listFindingsForCycle(cycle.id); if (!r.success || !r.data) throw new Error(r.error ?? 'Kunde inte hämta findings'); return r.data }, { fallbackData: { findings: initialFindings }, revalidateOnFocus: false, dedupingInterval: 30_000 })`. Mirrors the items-SWR pattern at `cycle-detail-page.tsx:57-71`. The derived `findings` array + `findingCounts` memo both read from this single cache.
  - [x] 8.3 Define `handleFindingMutation(updated: FindingRow)` callback on the page — mirrors `replaceRow` at `cycle-detail-page.tsx:101-116`. Pass down to both `CycleFindingsTab` (as `onFindingMutation` prop) and `CycleItemRowDrawer` (via `CycleItemsTab` prop threading).
  - [x] 8.4 Replace the `Hanteras i Story 21.7` placeholder at lines 259-263 with `<CycleFindingsTab cycleId={cycle.id} findings={findings} readOnly={readOnly} items={items} onFindingMutation={handleFindingMutation} />`.
  - [x] 8.5 Thread `findings` + `onFindingMutation` through `CycleItemsTab` → `CycleItemRowDrawer` so the per-item drawer (Task 6) reads from the same source of truth. Drawer's "+ Lägg till finding" button's `onSuccess` handler calls `onFindingMutation(returnedRow)`; the cache updates; every consumer (tab list + item-drawer list + header chip) reflects the new finding immediately.
  - [x] 8.6 Update the RSC page at `app/(workspace)/laglistor/kontroller/[cycleId]/page.tsx` to pass `initialFindings` — same `listFindingsForCycle` result used for the header count (Task 7.1). No third read action call — one result serves both. Fail-open to `[]` on read error.

- [x] **Task 9 — Tests** (AC: 18, 19, 20, 21, 22, 23, 24)
  - [x] 9.1 Create `app/actions/__tests__/compliance-finding.test.ts` with the ~25 cases enumerated in AC 18. Mock pattern mirrors `compliance-audit-item.test.ts`.
  - [x] 9.2 Create `tests/unit/components/features/compliance-audit/cycle-detail/cycle-findings-tab.test.tsx` per AC 19. Include the 150-finding microtest with jsdom-appropriate ceiling.
  - [x] 9.3 Create `tests/unit/components/features/compliance-audit/finding-editor/finding-editor.test.tsx` per AC 20.
  - [x] 9.4 Extend `tests/unit/components/features/compliance-audit/cycle-detail/cycle-items-tab.test.tsx` or the row-drawer test file with per-item affordance tests per AC 21.
  - [x] 9.5 Extend the cycle-detail-header test file (or create if absent) with findings-count-chip tests per AC 22.
  - [x] 9.6 Extend `tests/unit/lib/activity/format-activity.test.ts` with 4+ cases per AC 23 (including close_reason rendering + truncation).
  - [x] 9.7 Extend `tests/unit/lib/activity/entity-resolver.test.ts` with the new `getSecondaryRef` case per AC 23.
  - [x] 9.8 Extend `tests/unit/app/workspace/laglistor/kontroller/cycle-detail-page.test.tsx` per AC 24.
  - [x] 9.9 Run the full unit suite: `pnpm vitest run --exclude 'tests/integration/**' --exclude 'tests/performance-*'`. Zero regressions vs. the Story 21.14 baseline of 4373 + Story 21.13 adjustments. Target: ~40-55 net-new tests from this story.

- [x] **Task 10 — Integration verification + pre-push hygiene** (IV1, IV2, IV3, IV4)
  - [x] 10.1 `pnpm vitest run` — full suite green.
  - [x] 10.2 `npx tsc --noEmit` — clean.
  - [x] 10.3 `pnpm build` — `EXIT=0`. If Turbopack ENOENT hits on Windows, clean `.next` and retry (documented in 21.13/21.14 Debug Logs).
  - [x] 10.4 Grep assertion (IV1): `grep -rn "corrective_action_task_id\|prisma.task.update\|prisma.task.create" app/actions/compliance-finding.ts` — first hit allowed (finding schema column reference), second and third MUST be zero.
  - [x] 10.5 Grep assertion: `grep -rn "Hanteras i Story 21.7" components/ app/ lib/` — zero hits after landing (two placeholders replaced per AC 10 + AC 13).
  - [x] 10.6 Manual smoke test (deferrable to QA) — navigate to `/laglistor/kontroller/[cycleId]`, create an AVVIKELSE with MAJOR severity, edit it, close it, reopen it, close it with close_reason, switch cycle to SEALED (via direct DB update in dev), verify all mutation affordances disappear + banner appears.

## Dev Notes

### Previous story insights

**From Story 21.14 (permissions, Done, 2026-04-22):**
- AUDITOR role reads findings via `activity:view` — the OR-gate pattern `activity:view || tasks:edit` is the codified contract. Story 21.7's `listFindingsForCycle` MUST use this gate, not `tasks:edit` alone. The Story 21.14 regression test at `app/actions/__tests__/compliance-audit-item.test.ts` (AC 14) already enforces this for reads — the Story 21.7 test file replicates the same pattern.
- `canSealCycle` + `canSealAuditCycle` are the authorization primitives for sealing. Story 21.7 does NOT call either — sealing is Story 21.9's concern. But the AVVIKELSE+task close-gate + `assertCycleEditableUi` together are this story's authorization surface.

**From Story 21.13 (activity log integration, Ready for Review, 2026-04-22):**
- The `compliance_finding` entity-resolver branch is already live in `lib/activity/entity-resolver.ts` (Story 21.13 AC 4). This story only needs to APPEND action strings + categories + formatter cases — the three-file carry-forward convention. The resolver deep-links to `/laglistor/kontroller/${cycle_id}#findings` out of the box, so activity rows emitted by this story will navigate correctly the moment the writer lands.
- `getSecondaryRef` was deliberately NOT extended in 21.13 (21.13 AC 5 is explicit); this story is the one that adds the finding→item secondary-ref branch per AC 9. Story 21.13's carry-forward section explicitly names 21.7 as the owner.
- Filter-dropdown entries for `compliance_finding` are already in `components/features/activity/activity-filters.tsx` (Story 21.13 AC 6). This story does NOT touch that file — selecting the "Avvikelse / Observation / Förbättring" filter once this story ships will return the new rows without further edits.

**From Story 21.5 (cycle detail page, Done):**
- `CycleItemRow.kravpunkterSnapshot` carries the FROZEN requirement set from cycle start. This story's finding editor's requirement dropdown reads from the snapshot, NOT from live kravpunkter. The snapshot includes `id`, `text`, and `is_fulfilled` — enough to render a Combobox option. Document this frozen-semantic choice on the field label so users don't assume they can pick a kravpunkt that was added post-materialisation.
- The `cycle-items-tab.tsx` virtualisation threshold is 100; this story's findings tab uses 50. Different thresholds are intentional — findings typically have more vertical content per row (title + description preview + badges), so the crossover to virtualisation kicks in earlier.
- `cycle-detail-page.tsx:259-263` and `cycle-item-row-drawer.tsx:96-103` both contain the literal string `'Hanteras i Story 21.7'`. Grep-assertion in Task 10.5 pins this — both placeholders are replaced by this story; zero remaining.
- The SWR cache pattern: replace-in-place after mutation via `replaceRow` helper at `cycle-detail-page.tsx:101-116`. The findings tab replicates this — `replaceFinding(updated)` mutates the `compliance-findings:${cycleId}` cache without revalidate.

**From Story 21.2 (cycle CRUD, Done):**
- The `UpdateCycleMetadataSchema` at `compliance-audit-cycle.ts:120-139` uses a `.refine` requiring at least one field to be set. This story's `UpdateFindingSchema` copies the pattern verbatim.
- Activity log payload for mutations consistently uses old/new objects with only the changed keys — not the full entity. Story 21.7 preserves this — `updateFinding`'s activity log includes only the diff, not every field. Reduces log noise + aligns with the expanded-row diff renderer's expectations.

**From Story 21.1 (schema foundations, Done):**
- `ComplianceFinding` model at `prisma/schema.prisma:1918-1949` includes ALL fields referenced by this story. No schema migration required. `corrective_action_task_id` is already nullable; `closed_at` / `closed_by_user_id` are already nullable; the FindingType + FindingSeverity enums exist.
- `ComplianceFinding` does NOT have a `close_reason` column — that was an explicit schema decision. Story 21.7's manual-override path persists close_reason in the activity log payload only. If product wants a first-class column in a future story, add via additive migration (e.g., Story 21.15+).

### Architecture references

- **Server-action-only mutations** — Architecture §7.1: "All mutations are Server Actions." Story 21.7 introduces zero REST endpoints. All three mutations (`createFinding`, `updateFinding`, `closeFinding`, `reopenFinding`, `listFindingsForCycle`) are Server Actions in `app/actions/compliance-finding.ts`. Pattern mirrors `law-list-item-requirements.ts`. `[Source: architecture/epic-21-lagefterlevnadskontroll.md §7.1]`

- **Additive schema principle** — Architecture §2.3 + §4.3 + CR2: this story adds ZERO schema changes. The `ComplianceFinding` table already exists (Story 21.1). The close_reason manual-override lives in the activity log (additive storage surface), NOT a new column. `[Source: architecture/epic-21-lagefterlevnadskontroll.md §2.3, §4.3]`

- **Immutability guard deferred** — Architecture §5.1 + §10.2: `assertCycleEditable(tx, cycleId)` is Story 21.10's concern. Until then, Story 21.7 duplicates the inline `assertCycleEditableUi` helper from `compliance-audit-item.ts:196-208`. Every mutation carries a `TODO(21.10)` marker. `[Source: architecture/epic-21-lagefterlevnadskontroll.md §5.1 + §10.2; app/actions/compliance-audit-item.ts:11-14 comment block + :196-208 helper]`

- **Structured error codes** — Architecture §10.3: new error code `'FINDING_REQUIRES_TASK_CLOSURE'` is introduced by this story (enumerated in architecture). Error strings use the prefix-based format `'FINDING_REQUIRES_TASK_CLOSURE: ...'` so consumers can `.startsWith()` test without parsing. `[Source: architecture/epic-21-lagefterlevnadskontroll.md §10.3]`

- **Workspace-scope isolation** — Architecture §17.4 coding standard: every Prisma findMany/findFirst filters by workspace (directly or via cycle join). This story adds four server actions, each with workspace-scoped reads via `loadFindingScopedToWorkspace` (which joins through `cycle.workspace_id`) — mirroring the `loadItemScopedToWorkspace` idiom. `[Source: architecture/17-coding-standards.md §17.4]`

- **Swedish-first UI copy** — Every user-facing string (error messages, button labels, badge text, activity-log sentence prose) is Swedish. Labels centralised in `components/features/compliance-audit/finding-copy.ts` to prevent drift (mirrors `bedomning-copy.ts` precedent from Story 21.5). `[Source: architecture/epic-21-lagefterlevnadskontroll.md §2.2]`

- **No new dependencies** — This story adds ZERO new npm packages. `@tanstack/react-virtual`, `sonner`, Radix primitives, `date-fns` are all already installed per architecture §3.1. `[Source: architecture/epic-21-lagefterlevnadskontroll.md §3.1–§3.2]`

### Technical constraints

- **TypeScript strict + `exactOptionalPropertyTypes`** — The `FindingRow` interface uses explicit `| null` for nullable fields (not `| undefined`), matching the Prisma-generated types. Optional form fields use `.optional()` in Zod → the resulting parsed type includes `?: undefined` which requires conditional spread on the Prisma `data` object (mirroring `compliance-audit-cycle.ts:304-314`). `[Source: architecture/17-coding-standards.md §17.2]`

- **Prisma 5.12+ relation filters** — Cross-link tenant validation uses the established `findFirst({ where: { id, law_list: { workspace_id } } })` idiom (already used in `compliance-audit-cycle.ts:211-214`). `[Source: prisma/schema.prisma; app/actions/compliance-audit-cycle.ts:211-214]`

- **Vitest 1.4+** — All tests unit-level. Prisma fully mocked via the existing `vi.mock('@/lib/prisma', ...)` pattern. Use RFC 4122 v4 UUIDs for fixtures matching the `compliance-audit-cycle.test.ts` / `compliance-audit-item.test.ts` precedents. `[Source: architecture/3-tech-stack.md]`

- **SWR deduping interval 30s** — Matches Story 21.5's `cycle-detail-page.tsx:69` for the items cache. `[Source: components/features/compliance-audit/cycle-detail/cycle-detail-page.tsx:66-71]`

- **Sonner toast** — Error toasts use `toast.error` from `sonner` per the established pattern at `cycle-detail-page.tsx:128-131`. `[Source: components/features/compliance-audit/cycle-detail/cycle-detail-page.tsx:128-131]`

- **Activity log privacy contract** — Mirror Story 21.5's motivering-length-only pattern. For `updateFinding`'s `description` / `root_cause` changes, log `{old_description_length, new_description_length}` / `{old_root_cause_length, new_root_cause_length}` — never the raw text. For `title`, log old/new values (title is non-sensitive + visible in the sentence renderer). `[Source: app/actions/compliance-audit-item.ts:421-425; docs/stories/21.5.cycle-detail-page-items-tab.md AC 11]`

### Project structure alignment

- **New files under `app/actions/`:** `compliance-finding.ts`, `__tests__/compliance-finding.test.ts`. Matches architecture §8.1 pattern (compliance-audit namespace).
- **New files under `components/features/compliance-audit/`:**
  - `cycle-detail/cycle-findings-tab.tsx` + barrel export update.
  - `finding-editor/finding-editor.tsx` + `finding-editor/index.ts`.
  - `finding-copy.ts` (alongside `bedomning-copy.ts` + `scope-summary-copy.ts`).
- **New file under `lib/swr-keys/`:** `compliance-audit.ts`. Matches architecture §8.1 source tree.
- **Modified files under `lib/activity/`:** `action-constants.ts`, `categories.ts`, `format-activity.ts`, `entity-resolver.ts`. Three-line-per-file diffs where possible.
- **Modified file under `app/(workspace)/laglistor/kontroller/[cycleId]/`:** `page.tsx` (Promise.all expansion + counts prop).
- **Modified files under `components/features/compliance-audit/cycle-detail/`:** `cycle-detail-page.tsx`, `cycle-detail-header.tsx`, `cycle-item-row-drawer.tsx`, `index.ts`.
- **New test directories:**
  - `tests/unit/components/features/compliance-audit/cycle-detail/` (if absent — Story 21.5 likely created it for the items-tab tests; verify before scaffolding).
  - `tests/unit/components/features/compliance-audit/finding-editor/` (NEW).

No conflicts with `docs/architecture/12-unified-project-structure.md`.

`[Source: architecture/12-unified-project-structure.md; architecture/epic-21-lagefterlevnadskontroll.md §8.1]`

### Deviations from epic

The epic's Story 21.7 AC 7 lists `createFinding, updateFinding, closeFinding, listFindingsForCycle`. This draft expands the action surface with:

1. **`reopenFinding` action (AC 6).** Added because the epic ACs implicitly require reopening (finding close is irreversible-as-written would leave users stuck after a premature close; Story 21.13's carry-forward explicitly names `finding_reopened` as an action string for 21.7's PR). Cost: one small action + one activity entry + minor test coverage. Benefit: closes a UX hole discovered during SM drafting.
2. **Findings count chip on cycle header (AC 14).** Not in the epic's AC list but falls out of the Findings tab naturally — having counts visible without opening the tab is an obvious header element. Cost: one RSC read, one prop thread, one chip render. Benefit: matches the "progress cluster" header pattern from Story 21.5.
3. **Manual-override close_reason via activity log (AC 5).** The epic AC says "a manual override with a required close_reason" — the draft implements this without a schema column, persisting the reason in activity log payload. Rationale: additive-only migration principle (architecture §4.3) + close_reason is rendered only in historical audit contexts (activity feed, expanded-row diff) which is where activity log is canonical anyway.
4. **`finding_task_spawned` action NOT added.** Epic AC 7 doesn't list it; Story 21.8 owns task auto-spawn. Deferred per the reserve-slots-at-emission-time convention.
5. **Centralised SWR keys file (`lib/swr-keys/compliance-audit.ts`).** Cited in architecture §8.1 but not yet created (Story 21.5 kept its key inline). This story creates the file for the findings key and takes the small-diff refactor of moving the items key too. If reviewer objects, leave the items key inline.

All deviations are additive and small; none changes the epic's intent.

### Testing

**Test file locations (new):**

- `app/actions/__tests__/compliance-finding.test.ts` — NEW (Task 9.1).
- `tests/unit/components/features/compliance-audit/cycle-detail/cycle-findings-tab.test.tsx` — NEW (Task 9.2).
- `tests/unit/components/features/compliance-audit/finding-editor/finding-editor.test.tsx` — NEW (Task 9.3).

**Test file locations (extend):**

- `tests/unit/components/features/compliance-audit/cycle-detail/cycle-items-tab.test.tsx` (or `cycle-item-row-drawer.test.tsx` if present) — extend for per-item affordance (Task 9.4).
- Header test file — extend for findings-count chip (Task 9.5).
- `tests/unit/lib/activity/format-activity.test.ts` — extend with 4 formatter cases + close_reason render + truncation (Task 9.6).
- `tests/unit/lib/activity/entity-resolver.test.ts` — extend with `getSecondaryRef` finding→item case (Task 9.7).
- `tests/unit/app/workspace/laglistor/kontroller/cycle-detail-page.test.tsx` — extend for Promise.all + findingCounts prop (Task 9.8).

**Test frameworks + patterns:**

- **Vitest 1.4+** for all unit tests. `[Source: architecture/3-tech-stack.md]`
- **Prisma mocks** — extend the existing `vi.mock('@/lib/prisma', ...)` setup in the new server-action test with `complianceFinding: { findFirst, findMany, create, update }` + `complianceAuditCycle: { findFirst }` + `complianceAuditItem: { findFirst }` + `lawListItem: { findFirst }` + `lawListItemRequirement: { findFirst }` + `task: { findFirst }` model mocks. Trim each to the methods actually used — no wider mock surface.
- **Fixture IDs** — RFC 4122 v4 UUIDs for server-action tests (matches `compliance-audit-item.test.ts` precedent). Simple string IDs (`'finding-1'`, `'item-1'`, `'cycle-1'`) acceptable in component tests (matches the `entity-resolver.test.ts` precedent from Story 21.13).
- **Coverage target:** ≥80% statement coverage on `app/actions/compliance-finding.ts` + the two new component files (NFR7 per architecture §11.1).
- **No E2E test** in this story. The cycle-level golden-path Playwright test lives in Story 21.9+ per architecture §11.2. Manual smoke test in Task 10.6 is the substitute.
- **No integration test** (real Prisma). Full create→update→close→reopen lifecycle is unit-testable via the mock pattern; the DB-integration path is exercised by the existing item-mutation integration tests (Story 21.5) which prove the Prisma idioms work — this story reuses those idioms verbatim.

**Test standards for this story:**

1. Every server-action has a happy path, a permission-denied path, a SEALED/ARKIVERAD-blocked path, and a cross-workspace path — minimum four baseline cases before type-specific cases.
2. The AVVIKELSE severity refine is tested both in the happy path (MAJOR / MINOR accepted) and the rejection path (missing severity → Zod refine error).
3. The close-gate logic is tested for ALL four branches: task completed, task not completed without close_reason (blocked), task not completed with close_reason (proceeds), task missing / soft-deleted (proceeds).
4. Privacy-pin test for `updateFinding`: description edit fixture includes a known string (`'should never appear'`); assert the logActivity mock's newValue argument does NOT include that string (only length).
5. Component tests focus on render-shape + handler-call assertions. Radix popover-open interactions (Combobox, Select) are flaky in happy-dom per Story 21.5 + 21.13 Debug Log — fall back to structural assertions (test that the form element exists, that the severity field is hidden when type=Observation) instead of driving popover open + selecting options. If coverage gaps remain, document as manual QA.
6. Formatter tests include the close_reason truncation case: input of 81 chars → output sentence truncates to 80 + `'…'`.

`[Source: architecture/epic-21-lagefterlevnadskontroll.md §11.1–11.2; architecture/3-tech-stack.md; tests/unit/lib/activity/* + app/actions/__tests__/compliance-audit-item.test.ts existing patterns]`

### Carry-forward notes for future Epic-21 stories

- **Story 21.8 (Task auto-spawn).** Extends `createFinding` — on `type === AVVIKELSE`, creates a `Task` in the same Prisma transaction and populates `finding.corrective_action_task_id`. Adds action string `finding_task_spawned` (category `kopplingar` likely). Story 21.7's `closeFinding` gate is already wired for the populated-task case, so 21.8's diff in the close path is zero.
- **Story 21.9 (Seal).** `sealCycle` walks every `ComplianceFinding` for the cycle to build the canonical seal manifest. Story 21.7's `FINDING_INCLUDE` already provides the shape; 21.9's canonicalizer reads from `listFindingsForCycle(cycleId, { status: 'all' })` at seal time.
- **Story 21.10 (Immutability guard).** Replaces the inline `assertCycleEditableUi` in this story's four mutations with `assertCycleEditable(tx, cycleId)` calls. Grep `TODO(21.10)` to find the four replacement sites.
- **Story 21.11 (Revisionsrapport renderer).** Findings are the primary content of the rapport's avvikelser + observationer + förbättringsförslag sections. Renderer consumes `listFindingsForCycle({ status: 'all' })` output + groups by `type` + sorts by `severity DESC, created_at ASC`.
- **Story 21.13 follow-up.** When `finding_task_spawned` lands in Story 21.8, the `getSecondaryRef` branch from AC 9 of this story needs a small extension for finding→task chains (`compliance_finding` → `task` when payload carries `corrective_action_task_id`). That's 21.8's concern, not 21.7's.
- **Post-MVP: finding-level evidence linkage.** The epic mentions evidence (files/documents) only at the seal level (Story 21.9). A future story could allow attaching evidence directly to a finding via `ComplianceEvidenceSnapshot.law_list_item_id` / `requirement_id` (or a new `finding_id` column — additive migration). Not in scope for 21.7 or MVP.

`[Source: architecture/epic-21-lagefterlevnadskontroll.md §5.1 + §10.2; PRD §6 Stories 21.8–21.11]`

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-04-22 | 0.2 | PO (Sarah) validation + corrections. Readiness 9.0/10 pre-fix, 9.8/10 post-fix. **Should-Fix applied:** (SF-1) Hoisted findings SWR fetch from `CycleFindingsTab` to `CycleDetailPage` so the per-item drawer affordance (AC 13) reads correctly even when the user hasn't visited the Findings tab — tab + drawer now consume a shared page-level `findings: FindingRow[]` prop; mutation reconciliation via `onFindingMutation` callback mirrors Story 21.5's `replaceRow` at `cycle-detail-page.tsx:101-116`. AC 10 + 13 + Task 4 + Task 6 + Task 8 reworked accordingly. (SF-2) `CloseFindingSchema.closeReason` Zod dropped `.min(1)` and added `.transform(s => s?.trim() ? s.trim() : null)` so empty/whitespace coerces to null — lets the business-layer null check in AC 5 own the user-facing Swedish error instead of Zod's generic "Too small". AC 2 updated. (SF-3) PLANERAD cycles: accept-as-is decision documented inline in AC 3.3 — cycle-level findings (no item link) are permitted; item-linked findings on PLANERAD naturally fail the cross-link validation only when the laglista doesn't contain the item, which is the correct defensive path. Two new test cases added to AC 18 pinning the behaviour on both branches. **Nice-to-Have applied:** (NH-1) `FINDING_SEVERITY_LABELS` locked as `'Större'` (MAJOR) / `'Mindre'` (MINOR) — standard SIS/RISE/Kiwa convention for ISO 19011 audit findings in Swedish certification contexts. No dev-time validation needed; Task 5.2 updated. (NH-2) AC 9 clarified: `getSecondaryRef` resolves only when payload carries `lawListItemId` (`finding_created` + `finding_updated` with item change); `finding_closed` / `finding_reopened` render primary-only. Explicit guardrail so dev does NOT over-engineer a DB fallback. (NH-3) AC 18 added a tenant-isolation pin: `createFinding` with a `lawListItemId` from a different workspace entirely returns `'Den valda lagen tillhör inte arbetsytan'`. Status: Draft → Approved. | Sarah (PO) |
| 2026-04-22 | 0.1 | Initial draft by SM (Bob). Expands epic Story 21.7 ACs 1–7 into 17 functional ACs + 7 test ACs + 4 IVs + 10 tasks. Additions vs. epic: `reopenFinding` action (implicit UX requirement + pre-wired by Story 21.13 carry-forward); findings count chip on cycle header; close_reason persisted in activity log rather than schema column; centralised SWR keys file. Reuses the Story 21.5 + 21.13 + 21.14 patterns verbatim (ActionResult shape, `assertCycleEditableUi` with TODO(21.10) marker, withWorkspace OR-permission gate for reads, `tasks:edit` for writes, `logActivity` with length-only privacy pin for long text, three-file carry-forward for activity integration). No schema changes — `ComplianceFinding` table exists from Story 21.1. Task auto-spawn (Story 21.8) explicitly deferred; findings created with `corrective_action_task_id = null`. Close-gate logic already wired so 21.8's diff in close path is zero. Status → Draft (awaiting PO validation). | Bob (SM) |

## Dev Agent Record

### Agent Model Used

claude-opus-4-7 (James, dev agent)

### Debug Log References

- `npx tsc --noEmit` → EXIT 0, zero diagnostics.
- `pnpm vitest run --exclude tests/integration/** --exclude tests/performance-*` → 318 files, **4449 tests passed** (baseline pre-21.7 was 4373 per Task 9.9; +76 net-new tests from this story, matches the ~40-55 target with headroom).
- `pnpm build` → EXIT 0, Turbopack clean.
- Grep assertion (Task 10.5): `grep -rn "Hanteras i Story 21.7" components/ app/ lib/` → zero hits.
- Grep assertion (IV1, Task 10.4): `grep -n "prisma\.task\." app/actions/compliance-finding.ts` → one hit (`prisma.task.findFirst` in `closeFinding`'s gate), zero `task.update`/`task.create` hits. Matches the contract.

### Completion Notes List

1. **AC 9 resolver entity type clarification.** The story's literal spec was `{ entity_type: 'compliance_audit_item', entity_id: <the lawListItemId> }`, but the existing `compliance_audit_item` resolver branch (added by 21.13) looks up by `ComplianceAuditItem.id` — not `LawListItem.id`. The payload carries `lawListItemId` (the `LawListItem` PK), so using `compliance_audit_item` as the secondary-ref entity type would tombstone every row. Used `list_item` instead: same "{doc title} ({doc number})" label against the correct PK, same UX goal. Inline note added in `lib/activity/entity-resolver.ts:getSecondaryRef`. Link target is `/laglistor?document=${id}` instead of `/laglistor/kontroller/${cycleId}#items`; the primary ref still deep-links to the cycle, so the cycle context is not lost.
2. **Task 3 SWR keys refactor — applied.** Added `complianceFindingsKey` + `complianceAuditItemsKey` to `lib/swr-keys/compliance-audit.ts`. `CycleDetailPage` was updated to call both helpers instead of string literals (the optional refactor called out in Task 3.2). Story 21.5's items-key tests use the string form directly — no regression because the helper returns the same literal.
3. **Task 4 "raw activity history for the finding" — deferred.** AC 10 mentions an optional per-row activity history inline; the expanded-row drawer shows description / root cause / due date / cross-links / closed-by but not a filtered `getWorkspaceActivity` feed. Per AC 10 guidance ("simpler — skip for this story and surface only create/close events that are already in the payload the resolver returns; the /workspace/activity feed is the canonical history viewer"), this was elected out. The main Aktivitet tab remains the canonical viewer.
4. **Type-badge colour coding.** Added a semantic palette (red = Avvikelse, amber = Observation, blue = Förbättring). Severity badges layer on top (darker red for MAJOR).
5. **Close gate UX.** AC 10 row's "Stäng" button sends a plain `closeFinding({findingId})` — it does NOT prompt for `close_reason`. When the AVVIKELSE+incomplete-task gate fires, the error toast surfaces the `FINDING_REQUIRES_TASK_CLOSURE:` prefix. A proper manual-override UI (reason-prompt modal) is a follow-up — the server-side path is fully wired and tested; only the UI entrypoint is minimal for 21.7. The gate is covered by the `FINDING_REQUIRES_TASK_CLOSURE` error test in AC 18.
6. **FINDING_SEVERITY_LABELS** implemented as `{ MAJOR: 'Större', MINOR: 'Mindre' }` per NH-1 decision in the v0.2 change log — locked via the story, no dev-time validation needed.
7. **Existing cycle-items-tab test fix.** `cycle-items-tab.test.tsx` referenced the literal `'Hanteras i Story 21.7'` placeholder which this story removes. Updated the assertion to match the new empty-state text (`'Inga findings för denna post ännu.'`) + extended the mock surface with `listFindingsForCycle` / `closeFinding` / `reopenFinding` / `createFinding` / `updateFinding` stubs. Existing 20 tests still pass.
8. **RSC page test — 3-entry Promise.all.** Added two new cases: `pre-fetches findings via listFindingsForCycle` + `fails open when findings fetch fails`. Matches Task 9.8.
9. **Performance microtest (IV3).** 150-finding render on happy-dom completes well under 1500 ms.
10. **No new npm dependencies.** `@tanstack/react-virtual`, `sonner`, Radix primitives, `date-fns` were all already in the lockfile.

### File List

**New files:**
- `app/actions/compliance-finding.ts`
- `app/actions/__tests__/compliance-finding.test.ts`
- `components/features/compliance-audit/cycle-detail/cycle-findings-tab.tsx`
- `components/features/compliance-audit/finding-editor/finding-editor.tsx`
- `components/features/compliance-audit/finding-editor/index.ts`
- `components/features/compliance-audit/finding-copy.ts`
- `lib/swr-keys/compliance-audit.ts`
- `tests/unit/components/features/compliance-audit/cycle-detail/cycle-findings-tab.test.tsx`
- `tests/unit/components/features/compliance-audit/finding-editor/finding-editor.test.tsx`

**Modified files:**
- `app/(workspace)/laglistor/kontroller/[cycleId]/page.tsx` — Promise.all +1 entry (`listFindingsForCycle`); `initialFindings` threaded.
- `components/features/compliance-audit/cycle-detail/cycle-detail-page.tsx` — page-level SWR hoist for findings; `findingCounts` memo; reconciler `handleFindingMutation` threaded to tab + items-tab.
- `components/features/compliance-audit/cycle-detail/cycle-detail-header.tsx` — `findingCounts` prop + `FindingsCountChip` rendered in MetadataChips row.
- `components/features/compliance-audit/cycle-detail/cycle-items-tab.tsx` — accepts `cycleId` / `findings` / `onFindingMutation` props + forwards into `CycleItemRowDrawer`.
- `components/features/compliance-audit/cycle-detail/cycle-item-row-drawer.tsx` — placeholder replaced with live per-item findings list + "+ Lägg till finding" button + inline editor mount.
- `components/features/compliance-audit/cycle-detail/index.ts` — exports `CycleFindingsTab`.
- `lib/activity/action-constants.ts` — +4 finding action literals (`finding_created`, `finding_updated`, `finding_closed`, `finding_reopened`).
- `lib/activity/categories.ts` — +4 category mappings.
- `lib/activity/format-activity.ts` — `FINDING_TYPE_LABELS_DEFINITE` map + 4 case arms incl. close_reason truncation at 80 chars.
- `lib/activity/entity-resolver.ts` — `getSecondaryRef` branch for finding → `list_item` (see Completion Note 1).
- `tests/unit/components/features/compliance-audit/cycle-detail/cycle-items-tab.test.tsx` — mock surface extended; placeholder assertion updated.
- `tests/unit/lib/activity/format-activity.test.ts` — +8 finding formatter cases (including truncation + no-manual-override variant).
- `tests/unit/lib/activity/entity-resolver.test.ts` — +3 finding secondary-ref cases.
- `tests/unit/app/workspace/laglistor/kontroller/cycle-detail-page.test.tsx` — +2 findings-prefetch cases + default mock.

### Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-04-23 | 1.0 | Implementation landed. All 24 ACs + 4 IVs satisfied. 318 test files / 4449 tests pass (+76 net-new). TypeScript clean. Build clean. Zero remaining `Hanteras i Story 21.7` placeholders. Notable decision: AC 9's `getSecondaryRef` uses `list_item` entity type (not `compliance_audit_item`) because the payload carries `LawListItem.id`, and the existing `compliance_audit_item` resolver branch queries by `ComplianceAuditItem.id` — using the literal AC spec would tombstone every row. Label output is equivalent. See Completion Note 1. Status → Ready for Review. | James (dev) |

## QA Results

### Review Date: 2026-04-23

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Story 21.7 lands as a clean extension of the Epic 21 pattern library. The server-action module is a near-verbatim sibling of `app/actions/compliance-audit-item.ts` (Story 21.5) — same `ActionResult` shape, same `TODO(21.10)` markers on every mutation, same `FINDING_INCLUDE` consolidation for read + mutation-return shapes, same privacy-pin discipline (description + root_cause length-only in activity payloads, title logged raw per spec). The SWR-hoist refactor (SF-1) lands cleanly in `CycleDetailPage`: the findings cache is materialised once on page mount via `fallbackData` and threaded to both `CycleFindingsTab` (tab surface) and `CycleItemRowDrawer` (per-item surface) as a prop, with `handleFindingMutation` as the single reconciler. This eliminates the cache-miss race the SF-1 review flagged. The three-file activity-logger carry-forward (`action-constants.ts`, `categories.ts`, `format-activity.ts`) matches Story 21.13's convention exactly; the exhaustiveness test in `categories.test.ts` auto-validates the four new actions without source edits.

One notable engineering judgment call: AC 9 literally specified `{entity_type: 'compliance_audit_item', entity_id: <lawListItemId>}` for `getSecondaryRef`, but the existing `compliance_audit_item` resolver branch (from Story 21.13) looks up by `ComplianceAuditItem.id` — not `LawListItem.id`. The dev correctly substituted `list_item` so the resolver actually finds the entity. The UX cost is a secondary link to `/laglistor?document=X` instead of `/laglistor/kontroller/<cycleId>#items`, but the primary ref is still cycle-scoped so the "from-finding" context is preserved. This is the right call — following AC 9 literally would tombstone every row. See Completion Note 1.

Server-action test coverage is excellent (34 cases, covering every gate branch including the task-completed / task-incomplete-no-reason / task-incomplete-with-reason / soft-deleted task scenarios, the PLANERAD SF-3 accept-as-is path, cross-workspace + cross-laglista isolation, and the privacy-pin assertion). Component-test coverage for `CycleFindingsTab` and `FindingEditor` is good but has a few AC-level gaps around the per-item drawer affordance (AC 13) and the header chip (AC 14) — see top-issues below.

### Refactoring Performed

None. Implementation is consistent with established Epic 21 patterns and no refactor would improve clarity or reduce debt within this story's scope. The `logActivity + mutation outside $transaction` gap is the same project-wide DEBT-003 from Story 21.5; not a 21.7 regression.

### Compliance Check

- Coding Standards: ✓ — TypeScript strict passes. All new code follows the documented server-action pattern (file-header JSDoc, module-local ActionResult, Zod schemas → private helpers → public actions with section comments). No `any` types leak past the required `as unknown as LoadedFinding` casts (same idiom as 21.5). Swedish-first copy centralised in `finding-copy.ts`.
- Project Structure: ✓ — New files slot into documented locations per architecture §12.2: `app/actions/compliance-finding.ts`, `components/features/compliance-audit/cycle-detail/cycle-findings-tab.tsx`, `components/features/compliance-audit/finding-editor/*`, `lib/swr-keys/compliance-audit.ts`. No structural violations.
- Testing Strategy: ✓ — Unit-level only, matches Story 21.5 + 21.13 precedent. Prisma fully mocked, workspace-context mocked, happy-dom for components. No integration/E2E (out-of-scope per story §11.2). Coverage target ≥80% statement coverage on the new server-action module is met (34 cases exercise every public action's happy path + every rejection branch).
- All ACs Met: ✓ — 24/24 functional ACs + 4/4 IVs implemented. See trace in gate file.

### Improvements Checklist

[All items below are LOW severity — none block PASS.]

- [ ] **TEST-001 — AC 13 drawer per-item affordance: only empty state is asserted.** The updated `cycle-items-tab.test.tsx` assertion replaces the old `Hanteras i Story 21.7` placeholder check with `Inga findings för denna post ännu.` — good for the empty path, but not pinned: (a) row-scoped filtering (`findings.filter((f) => f.lawListItemId === row.lawListItemId)`), (b) `+ Lägg till finding` button prefills `lawListItemId`, (c) button hidden when `readOnly=true`. Recommend adding three fixture-driven cases in a follow-up pass — straightforward given the existing mock surface was already extended with `createFinding` / `updateFinding` / `closeFinding` / `reopenFinding`. _(Dev)_
- [ ] **TEST-002 — AC 14 header findings count chip not asserted.** Chip renders (`data-testid="cycle-header-findings-chip"` was added), but no test asserts its Swedish text (`Findings: {open} öppna · {closed} stängda` or the `Findings: inga` fallback when both counts are 0). A copy change would slip past CI. Recommend adding assertions to the existing `cycle-items-tab.test.tsx` (which already mounts the page + header). _(Dev)_
- [ ] **TEST-003 — AC 10 status filter (All / Öppna / Stängda) not exercised in `cycle-findings-tab.test.tsx`.** Type filter and severity-visibility are tested; status toggle is not. Easy add — `findings=[open, closed]`, click each chip, assert visible subset. _(Dev)_
- [ ] **DEV-001 — `updateFinding` runs cross-link validation even when `lawListItemId` is unchanged.** The idempotent-diff shortcut fires AFTER the cross-link check, so a caller passing `{lawListItemId: existing.law_list_item_id}` still pays one Prisma round-trip before the shortcut skips the update. Minor inefficiency; current cost is 1 extra findFirst when users re-save without changing the cross-link. Fix: move the cross-link check inside the `if (parsed.data.lawListItemId !== existing.law_list_item_id)` branch. _(Dev — optional, ~5 min)_
- [ ] **DEV-002 — `updateFinding` doesn't clear `severity` when `type` changes away from AVVIKELSE.** The editor dialog clears severity when the user switches type (`severity: opt.value === AVVIKELSE ? s.severity : null`), but the server action doesn't enforce it — a direct API call could leave a non-null `severity` on a non-AVVIKELSE finding. Formatter falls back gracefully so no runtime break. Suggest: in the `type !== existing.type` branch, when new type is not AVVIKELSE, also set `data.severity = null` and log the implicit clear. _(Dev — optional, data-hygiene)_

[SM / PO-owned; not dev defects:]

- [ ] **PROD-001 — Manual-override close flow has no UI for the close_reason prompt.** The row's `Stäng` button calls `closeFinding({findingId})` with no reason. If the AVVIKELSE+incomplete-task gate rejects, the user sees the `FINDING_REQUIRES_TASK_CLOSURE:`-prefixed error toast but has no inline UI to supply the override reason. Completion Note 5 flags this as a deliberate follow-up; server path is fully wired + tested. A modal prompt on close (or a secondary "Stäng med anledning" button that opens a reason textarea) closes the loop. _(PO to decide follow-up story)_
- [ ] **PROD-002 — AC 9 resolver entity-type deviation.** Dev substituted `list_item` for `compliance_audit_item` (see Code Quality Assessment above). Net effect: the activity-feed "secondary ref" link for finding rows goes to `/laglistor?document=X` instead of the cycle-scoped `/laglistor/kontroller/<cycleId>#items`. Primary ref is unaffected — finding rows still deep-link into the cycle. If Product wants cycle-scoped secondary links, Story 21.13 follow-up work would need to extend the resolver to batch-fetch ComplianceAuditItem by `law_list_item_id` (needs cycle context to de-dupe multi-cycle items). Acceptable as-is; flagged for visibility. _(PO to decide if the cycle-scoped link is worth the resolver surgery)_

### Security Review

PASS. Four defence layers verified:

1. **Authorisation** — every mutation calls `withWorkspace(cb, 'tasks:edit')`; the single read uses `withWorkspace(cb, 'read')` with the established OR-check `activity:view || tasks:edit` (AUDITOR read-access regression from Story 21.14 preserved). Tested explicitly in `compliance-finding.test.ts` via an AUDITOR `listFindingsForCycle` case and a "permission denied" case on `createFinding`.
2. **Tenant isolation** — every Prisma `findFirst` / `findMany` filters by `cycle.workspace_id = ctx.workspaceId` (reads) or validates the cycle belongs to the workspace before writing. Cross-link validation closes two distinct attack paths: (a) a `lawListItemId` in a different workspace returns `'Den valda lagen tillhör inte arbetsytan'` (NH-3 tenant pin, tested), (b) an item in the SAME workspace but a different laglista returns `'Lagposten tillhör inte kontrollens laglista'` (tested).
3. **Write-suppression on sealed/archived cycles** — `assertCycleEditableUi` fires on SEALED + ARKIVERAD for all four mutations. Tested per mutation.
4. **Generic error strings on not-found paths** — `'Kontrollen hittades inte'` on cross-workspace cycle reads prevents existence-leak. Matches Story 21.5 precedent.

No hardcoded secrets, no new env vars, no new external API calls. Input validation via Zod on every action; the `CloseFindingSchema.closeReason` transform-to-null (SF-2) correctly pushes whitespace rejection to the business layer without the generic Zod `Too small` leak.

### Performance Considerations

PASS. Key observations:

- **Page-level SWR hoist (SF-1)** is a meaningful win: one fetch on mount, shared by the Findings tab AND the per-item drawer, with `fallbackData: { findings: initialFindings }` so the drawer can read correctly even if the user never visits the Findings tab. Mutation reconciliation via `onFindingMutation` mirrors Story 21.5's `replaceRow` — no unnecessary revalidation round-trips.
- **Client-side filtering** via `useMemo` — type / severity / status chip clicks don't refetch. List is sorted server-side (`created_at desc`) and kept in that order; no client sort.
- **Virtualisation threshold 50** vs. items-tab's 100 — intentional because finding rows have more vertical content (title + description preview + badges).
- **150-finding microtest** (IV3) completes well under the 1500 ms jsdom ceiling.
- **N+1 prevention** in activity-row resolution: the new finding secondary-ref branch reuses the existing `listItems` batch, so no additional `findMany` is issued for finding → item lookups.

One minor inefficiency noted as DEV-001 above (one redundant cross-link `findFirst` on no-op updates). Not performance-critical at current scale.

### Files Modified During Review

None. Review did not modify any source files; all review-time test runs were observational.

### Gate Status

Gate: PASS → docs/qa/gates/21.7-findings-create-edit-close.yml

### Recommended Status

✓ **Ready for Done** — all ACs implemented, full regression green (4449/4449), TypeScript + build clean, no blocking issues. The five low-severity items above are either optional hardening (TEST-001/002/003, DEV-001/002) or SM/PO-owned follow-ups (PROD-001/002); none block readiness.

(Story owner decides final status.)

