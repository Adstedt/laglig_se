# Story 17.11: Agent Tool — Update Existing Documents (Section Edits via Approval)

## Status

Approved

> 🔁 **Re-scoped 2026-05-22 (v2.0, correct-course — Sarah, PO).** Original scope was `create_document` **+** `update_document` agent tools with markdown↔Tiptap converters on the legacy two-phase (`execute=false/true`) pattern. That scope is **superseded**:
> - **`create_document` dropped — redundant.** Story **14.24 (Done, live-verified)** ships `draft_styrdokument`, the product-aligned draft-and-approve path for agent-created documents. 14.24 explicitly superseded the atomic-create approach.
> - **Converters dropped.** 14.24 has the agent emit **Tiptap JSON directly** — no markdown round-trip — so `markdownToTiptap`/`tiptapToMarkdown` are unnecessary.
> - **Two-phase `execute` pattern dropped.** 14.23 migrated agent document mutations to the **`PendingAgentAction` approval-card** pattern; 14.24 removed `execute:true` entirely.
> - **Stale model refs corrected.** Epic 17 uses the **`WorkspaceDocument*`** prefix (bare `Document` = the SFS *legal* model — naming collision) and version `source: 'TIPTAP'` (no `'AGENT'` enum value).
>
> **This story now covers only the genuine remaining gap: the agent proposing section-level edits to an *existing* `WorkspaceDocument`, gated by an approval card** (consistent with 14.22/14.23/14.24). PO validation v2.1 (2026-05-22) pinned the server-action targets and corrected the ActivityLog AC.
>
> 🔄 **SM refresh 2026-06-02 (v2.2 — Bob, SM)** brings all code citations in line with shipped state: line-numbers refreshed against current `app/actions/documents.ts`, the renderer template swapped from 14.24's `draft-document-renderer` to **14.28's `update-requirement-renderer`** (closer pattern for an overwrite-via-diff card), the 17.10b auto-reindex finding folded into AC 8 (no dispatch-side reindex call needed — `saveDocumentVersion` already triggers it), the **new `AGENT` enum value** on `WorkspaceDocumentVersionSource` flagged (we still pick `TIPTAP` to match 14.24's actual dispatch — agent authorship is stamped in the activity log per AC 10), 19.7c registry-narrowing harness count-bump locations added, the 197B-S1 sidebar-hint default change noted, and **CP-001** added to AC 6 (no internal IDs in renderer copy). No AC scope, design, or AC numbering changes.

## Story

**As an** AI compliance agent,
**I want** to propose a section-level edit to an existing workspace document (subject to user approval),
**so that** I can help users keep a policy/procedure current without re-drafting the whole document — while never silently mutating an approved document.

## Story Context

**Existing System Integration:**

- **Create is already shipped (14.24).** Out of scope here. This story is **update-only**.
- **Approval pattern (14.22/14.23/14.24/14.28/14.30):** new edits flow through a `PendingAgentAction` (`action_type: UPDATE_DOCUMENT`) → approval-card renderer → `approvePendingAction` dispatch. **No `execute:true` path** (removed per 14.23). The closest in-flight precedent is **14.28 `UPDATE_REQUIREMENT`** — same shape (overwrite-an-existing-field via a diff card), now Done with the renderer + dispatch branch live.
- **Models:** `WorkspaceDocument` / `WorkspaceDocumentVersion` (Epic 17, `WorkspaceDocument*` prefix). New content saved as a new `WorkspaceDocumentVersion`. **Source enum (`WorkspaceDocumentVersionSource`) now includes `TIPTAP`, `IMPORT`, `AGENT`** (`schema.prisma:2007`) — `AGENT` was added but is currently unused in the codebase; **this story uses `source: 'TIPTAP'` to match 14.24's actual dispatch** (which calls `createDocument`, which hardcodes `'TIPTAP'`) — agent authorship is tracked via `created_by` + the activity-log `new_value` stamp (AC 10), not via the enum.
- **Reuse:** `ActionRendererFrame` + `useDebouncedParamsChange` (14.23 "spine & whisper" card chrome, 4 states); `createPendingActionRow` helper; the `approvePendingAction` dispatch switch + its `tasks:edit` gate (SEC-001); the read-only **chat detail panel** canvas seam (`document-draft`-style `ChatDetailItem`, 14.24 AC 11a) for the before/after preview.
- **Content format:** agent emits the replacement section as **Tiptap JSON** (matches `draft_styrdokument`).
- **17.10b indexing contract (NEW since v2.0):** DRAFT and IN_REVIEW workspace documents are now indexed and queryable by the agent (with `[Utkast:]` citations). **`saveDocumentVersion` already calls `indexWorkspaceDocument` via `after()`** (`app/actions/documents.ts:~668`), so an `update_document` dispatch auto-triggers a re-embed — no explicit `markWorkspaceDocumentDirty` call needed in the dispatch branch.

**Out of scope:** creating new documents (14.24's `draft_styrdokument`); whole-document rewrites; markdown conversion; the read/list tools (Story 17.10); the legal `Document`/`DocumentVersion` (SFS) models.

## Acceptance Criteria

### Schema

1. `PendingAgentActionType` extended with **`UPDATE_DOCUMENT`**, appended after `TRANSITION_DOCUMENT_STATUS` (the most recent addition, Story 14.30 — `schema.prisma:1618`). Pattern is **additive append** (`ALTER TYPE ... ADD VALUE`; generate `--create-only` and hand the SQL to the user — never auto-apply, per the project's manual-migration workflow). No new tables.
2. `params` JSON shape for `UPDATE_DOCUMENT`: `{ documentId, sectionHeading, oldSectionContentJson (Tiptap), newSectionContentJson (Tiptap), changeSummary, entity_version: string }`. Two captured-at-propose-time snapshots:
   - **`oldSectionContentJson`** is captured at propose time by extracting the current section's contents via the same `updateSection`/section-locator helper used by the dispatch (AC 9) — locate the heading, capture the body up to the next same-or-higher-level heading. This lets the renderer show the diff without re-fetching the document AND preserves an honest "what the agent was comparing to" view even if the document drifts before approval. **Mirrors 14.28's `oldSnapshot` pattern** (see `components/features/ai-chat/agent-action-renderers/update-requirement-renderer.tsx:37, 56, 73, 88`).
   - **`entity_version: string`** is the document version snapshot (ISO-8601 UTC, from `document.updated_at.toISOString()` — mirrors 14.28's tool at `lib/agent/tools/update-requirement.ts:188`). Populated by the tool, consumed by 14.31's staleness guard at approval time. 17.11 itself does not implement the staleness check; it only ensures the snapshot is captured so 14.31's retrofit picks it up cleanly (14.31's `STALENESS_STRATEGY` enumerates `UPDATE_DOCUMENT` as a guarded type).

### Tool — `update_document`

3. New tool `lib/agent/tools/update-document.ts` (`update_document`). Zod schema: `document_id` (string), `section_heading` (string — heading text to locate), `updated_content` (**Tiptap JSON** for the replacement section), `change_summary` (string). **No `execute` param** — the tool only ever proposes a pending row. Registered with policy `'write'` in `lib/agent/tools/index.ts` (slot in alphabetically; the existing write neighbours are `update_compliance_status`, `update_requirement`, `transition_document_status`).
4. **Guards (at tool time):** target document is in the caller's workspace; status is **DRAFT or IN_REVIEW** (reject APPROVED/SUPERSEDED/ARCHIVED via `wrapToolError` — guide the user to branch a new editable version first via the existing **`createDraftFromApproved`** action (`app/actions/documents.ts:823`)); `section_heading` exists in the document; and `newSectionContentJson` is **not deep-equal** to the resolved current section body (no-op edit rejected at propose time — surfaces a `"Inga ändringar att föreslå"` error via `wrapToolError`, no pending row created). On any failure → `ToolError`, no pending row.
5. On success: create a `PendingAgentAction(UPDATE_DOCUMENT)` via `createPendingActionRow` and return a `WriteToolResponse` whose preview shows the **section heading + a short before/after excerpt**. `wrapWriteToolResponse` is used as-is; its `_meta.sidebarHint` is already `'none'` (post-197B-S1, 2026-05-27) — the inline card is the surface, no detail-sidebar autopops.

### Renderer

6. New renderer `components/features/ai-chat/agent-action-renderers/update-document-renderer.tsx`, **built on `ActionRendererFrame`** (do not hand-roll chrome/states). **Primary template: `update-requirement-renderer.tsx`** (14.28, 225 lines) — closer pattern for "edit an existing entity's field via a diff card" than 14.24's `draft-document-renderer.tsx` (which is a create card). Borrow the chat-detail-panel before/after preview pattern from 14.24. **Diff display reads from `params.oldSectionContentJson` vs `params.newSectionContentJson` — no re-fetch** (AC 2 captures both snapshots at propose time):
   - **PENDING:** document title + target section heading + a compact before→after excerpt (from the two `params` snapshots); "Visa mer" opens the full read-only before/after in the **chat detail panel** (canvas seam, per 14.24 AC 11a) — not a modal. Controls: "Godkänn" / "Avvisa".
   - **APPROVED:** title + link to the document at its new version.
   - **REJECTED / EXPIRED:** standard muted variants.
   - **CP-001 (formatting guardrail):** renderer copy NEVER exposes internal identifiers — no raw `documentId`, no `pendingActionId`, no `section_heading` rendered as a code-style/quoted identifier. Use the document **title** and section **heading text** as natural Swedish language.
7. Register the renderer in **both** exported maps in `components/features/ai-chat/agent-action-card.tsx` — `RENDERERS[UPDATE_DOCUMENT]` (line ~56) and `ACTION_TYPE_LABELS[UPDATE_DOCUMENT]` (line ~39, suggested label: `'Uppdatera dokument'` — mirrors 14.28's `'Ändra kravpunkt'` / 14.30's `'Ändra dokumentstatus'` pattern).

### Approval dispatch

8. `approvePendingAction` (`app/actions/pending-agent-actions.ts`) gains an `UPDATE_DOCUMENT` branch (inherits the existing `tasks:edit` gate). Flow: re-read the current `content_json` → **re-assert the DRAFT/IN_REVIEW status guard** (state may be stale since proposal) → `updateSection()` (AC 9) to produce the **full** updated document → call the existing **`saveDocumentVersion(documentId, contentJson, changeSummary?, title?, contentHtml?)`** (`app/actions/documents.ts:590`), which appends a new `WorkspaceDocumentVersion` (`source: 'TIPTAP'` by enum default), bumps `current_version_id`/`current_version_number`, writes a `document_version_saved` ActivityLog row, **AND triggers `indexWorkspaceDocument` via `after()`** so 17.10b chunks stay current — all in one `$transaction`. ⚠️ **`saveDocumentVersion` takes the WHOLE document `contentJson`, not a section** — pass the full `updateSection` output. It also expects `contentHtml`; generate it from the Tiptap JSON (e.g. `generateHTML(json, extensions)`) so `extracted_text` derives correctly. **Note:** `saveDocumentVersion` does NOT itself enforce the DRAFT/IN_REVIEW guard — the dispatch branch must (AC 11). **Reindex is automatic** — no explicit `markWorkspaceDocumentDirty` call needed. **The downstream `syncWorkspaceChunks` is content-hash-gated** — a repeated update with identical content is a no-op at the chunk level. Tests should assert `indexWorkspaceDocument` was invoked but should NOT assert chunk-level mutation on no-content-change paths (the AC 4 no-op-edit guard already blocks identical re-proposes at tool time; the hash-gate is the belt-and-suspenders backstop).
9. Pure section-replacement utility `lib/documents/update-document-section.ts` → `updateSection(contentJson, sectionHeading, newContentNodes)`: locate the heading (case-insensitive), replace from that heading to the next heading of **same-or-higher level** (preserving the heading, replacing the body), return the full updated doc. Heading-not-found → informative error.
10. `ActivityLog` on update uses `entity_type: 'workspace_document'` (the live convention — **not** `'document'`, which is the SFS legal entity). `saveDocumentVersion` (AC 8) **already writes** a `document_version_saved` row, so an agent update is auditable for free. To distinguish *agent* edits from human ones, stamp agent authorship in the row's `new_value` (e.g. `{ ..., by: 'agent', pendingActionId }`) rather than minting a redundant action label. Only add a distinct `document_updated_by_agent` action label (in `lib/activity/action-constants.ts`) if product wants it surfaced separately in the feed.

### Security / non-regression

11. Workspace scoping and the DRAFT/IN_REVIEW guard are enforced **both** at tool time (AC 4) **and** at dispatch (AC 8) — never trust the proposal-time snapshot. A role lacking `tasks:edit` cannot approve (inherited gate).
12. `draft_styrdokument` and all existing agent tools/renderers are behaviourally unchanged (additive).

### Prompt

13. `lib/agent/system-prompt.md` updated (restart dev server to load): when to **update** an existing document (`update_document`) vs **draft a new one** (`draft_styrdokument`); provide the replacement section as Tiptap JSON; cannot edit Approved documents.
14. `TOOL_CONFIG` (`chat-message.tsx`) gains an `update_document` entry (`label`/`doneLabel`/`icon`, e.g. "Uppdaterar dokument" / "Föreslog ändring" / `FileEdit`). Follow the 14.28/14.29/14.30 precedent — chip labels were caught in their owner smokes (1428-001 + 14.30 process finding) and must include both PENDING + DONE labels.

## Tasks / Subtasks

- [ ] **Task 1: Schema** (AC 1–2) — add `UPDATE_DOCUMENT` to `PendingAgentActionType` (append after `TRANSITION_DOCUMENT_STATUS`, `schema.prisma:1618`); migration `--create-only` + manual handoff per the project workflow; `prisma generate`.
- [ ] **Task 2: Section-replace utility** (AC 9) — `lib/documents/update-document-section.ts` + unit tests (heading found / replaced / boundaries / not-found error / case-insensitivity / same-or-higher-level termination).
- [ ] **Task 3: `update_document` tool** (AC 3–5, 11) — propose-only pending row; tool-time guards (incl. workspace, status, heading-exists, AND no-op-edit per AC 4); preview. **No `execute` path.** Captures BOTH `entity_version = document.updated_at.toISOString()` AND `oldSectionContentJson` (the current section body, extracted via the section-locator helper from AC 9) in `params` — for 14.31's staleness guard and the renderer's diff display, respectively. Registered in `lib/agent/tools/index.ts` with policy `'write'`.
- [ ] **Task 4: Approval dispatch** (AC 8, 10, 11) — `UPDATE_DOCUMENT` branch in `approvePendingAction` (`app/actions/pending-agent-actions.ts`): re-assert DRAFT/IN_REVIEW guard → `updateSection()` to full doc → generate `contentHtml` → `saveDocumentVersion(documentId, fullContentJson, changeSummary, undefined, generatedHtml)` (`documents.ts:590`); stamp agent authorship in the activity `new_value`. **Note:** reindex is automatic via `saveDocumentVersion`'s built-in `after(indexWorkspaceDocument(...))`.
- [ ] **Task 5: Renderer** (AC 6–7) — on `ActionRendererFrame`, modeled on `update-requirement-renderer.tsx` (14.28); PENDING/APPROVED/REJECTED/EXPIRED; detail-panel before/after (borrow 14.24's pattern); register in both maps of `agent-action-card.tsx`; suggested label `'Uppdatera dokument'`.
- [ ] **Task 6: Prompt + label** (AC 13–14) — `system-prompt.md` + `TOOL_CONFIG` (both PENDING and DONE labels per 1428-001 lesson).
- [ ] **Task 7: Testing** — section-replace unit; tool guards + pending-row creation (incl. `entity_version` snapshot); dispatch happy-path + stale-status re-guard + `tasks:edit` denial + reindex side-effect; renderer states (incl. CP-001 — no raw IDs in copy); **registry-narrowing test count bumps** in `tests/unit/agent/tools/registry-role-filter.test.ts` and `tests/unit/agent/tools/registry-narrowing.test.ts` (19.7c harness — same pattern as 17.10's bumps); typecheck + lint clean.

## Dev Notes

- **Naming:** use `WorkspaceDocument*` everywhere; bare `Document`/`DocumentVersion` are the SFS *legal* models (collision). Version `source: 'TIPTAP'` (see below). [Confirmed in-code by 14.24.]
- **Reuse 14.28's diff-card pattern for the renderer**, not 14.24's create card: `components/features/ai-chat/agent-action-renderers/update-requirement-renderer.tsx` (225 lines) is the closer template — overwrite-an-existing-field-via-a-diff is the same shape as overwrite-a-section. Borrow 14.24's `chat-detail-context.tsx` + `chat-detail-sidebar.tsx` pattern for the read-only before/after detail-panel preview (AC 6, AC 11a from 14.24). See `docs/stories/completed/14.28.update-requirement-approval.md` and `docs/stories/completed/14.24.agent-draft-document-approval.md`.
- **Version-append action (pinned by PO validation v2.1, line-refreshed v2.2):** **`saveDocumentVersion(documentId, contentJson, changeSummary?, title?, contentHtml?)`** at `app/actions/documents.ts:590` (was `:537` in v2.0; the file grew). Appends a new `WorkspaceDocumentVersion` (default `source: TIPTAP` per the column default), bumps `current_version_id` + `current_version_number`, writes a `document_version_saved` `ActivityLog` row, **AND triggers `indexWorkspaceDocument` via Next.js `after()`** (lines ~665-680 — 17.10b reindex path) — all in one `$transaction`, workspace-scoped via `withWorkspace`. Takes the **whole** document `contentJson` (not a section) and expects `contentHtml` for `extracted_text` derivation. It does **not** enforce the document-status guard — the dispatch branch must (AC 8/11). `createDocument` (`documents.ts:62`) is new-doc only and is **not** used here.
- **17.10b auto-reindex (NEW finding for v2.2):** Since `saveDocumentVersion` already wraps `indexWorkspaceDocument` in `after()` (fire-and-forget, hash-gated inside `syncWorkspaceChunks` so a no-op content change is free), the dispatch does **not** need an explicit `markWorkspaceDocumentDirty(documentId, workspaceId)` call. The cron-debounce path (`markWorkspaceDocumentDirty` + `/api/cron/sweep-draft-reindex`) is for the **autosave** path; the **explicit-version-save** path here goes straight through `indexWorkspaceDocument`. The DRAFT/IN_REVIEW chunks reindex automatically — `[Utkast:]` citations stay accurate post-edit.
- **`WorkspaceDocumentVersionSource` enum (NEW value flagged for v2.2):** `schema.prisma:2007` (was `:1997` in v2.0). Values: `TIPTAP`, `IMPORT`, **`AGENT`** (the `AGENT` value was added after v2.0 but **is currently unused** by 14.24's actual dispatch — `createDocument` at `documents.ts:62` hardcodes `'TIPTAP'`). For consistency with 14.24, **17.11's dispatch also uses `'TIPTAP'`** (or accepts the column default, which is `TIPTAP`). Agent authorship is tracked via `created_by` + the activity `new_value` stamp (AC 10), mirroring 14.24's choice. If a future story consolidates on `AGENT` for agent-written versions, both 14.24 and 17.11 should flip together — outside this story's scope.
- **`wrapWriteToolResponse` 197B-S1 fix (2026-05-27):** the helper's `_meta.sidebarHint` default is now `'none'` (was `'open'`). 17.11's tool inherits this — no detail-sidebar autopops on the write-tool response envelope; the inline approval card is the surface. (`lib/agent/tools/utils.ts:49-66`.)
- **14.31 staleness coupling:** 14.31 (Approved, blockers cleared 2026-05-28) retrofits an `entity_version` snapshot guard across all overwrite types — `UPDATE_DOCUMENT` is on its guarded list. 17.11 just needs to populate `params.entity_version` (a `WorkspaceDocument.updated_at` snapshot) at propose time per AC 2 — 14.31's dispatch-side check will pick it up cleanly when 14.31 ships.
- **Why approval-gated (not atomic):** document mutations go through `PendingAgentAction` since 14.23; an agent editing a live document must be reviewable. The `execute=false/true` two-phase pattern from the original 17.11 draft is deprecated.

### Testing

Vitest + mocked Prisma + RTL for the renderer. Cover:
- `updateSection` boundary logic (heading found / not found / case-insensitivity / same-or-higher-level termination)
- Tool guards (foreign workspace; APPROVED / SUPERSEDED / ARCHIVED doc; missing heading) + `entity_version` snapshot capture
- Dispatch happy-path + stale-status re-guard (status changed APPROVED between propose and approve → rejected at dispatch) + `tasks:edit` denial
- Reindex side-effect verification (assert `indexWorkspaceDocument` invoked; hash-gated free-path acceptable)
- Renderer states + **CP-001 assertion** (no `documentId` / `pendingActionId` / raw `section_heading` in the rendered DOM as a quoted identifier)
- **Registry-narrowing harness count bumps** in `tests/unit/agent/tools/registry-role-filter.test.ts` and `tests/unit/agent/tools/registry-narrowing.test.ts` — the 19.7c harness counts the tools per role/skill; adding `update_document` (policy `'write'`) bumps the EDITOR count and the workspace-doc skill subset. Follow the 17.10 precedent (its refresh hit two count bumps + ~22 new automated tests across the two files).

Mirror 14.28's test layout (`tests/unit/lib/agent/tools/update-requirement.test.ts`, `tests/unit/actions/pending-agent-actions/update-requirement-dispatch.test.ts`, renderer test under `tests/unit/components/features/ai-chat/`).

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-30 | 1.0 | Initial story creation from Epic 17 | Sarah (PO) |
| 2026-03-30 | 1.1 | Fix: two-phase confirmation pattern, tool API (inputSchema/zodSchema/zod-v4), TOOL_CONFIG shape, prerequisite note, ActivityLog AC | Sarah (PO) |
| 2026-05-22 | 2.0 | **Re-scoped via correct-course (Sarah, PO).** `create_document` dropped (redundant with shipped 14.24 `draft_styrdokument`); markdown↔Tiptap converters dropped (14.24 emits Tiptap JSON directly); legacy `execute=false/true` two-phase pattern replaced with the `PendingAgentAction` **`UPDATE_DOCUMENT` approval-card** pattern (14.22/14.23/14.24); stale model refs corrected (`WorkspaceDocument*`, `source:'TIPTAP'`). Scope now = **agent section-edit of an existing document, approval-gated**. Status held Draft pending SM rigor pass (pin version-append action signature + line refs). | Sarah (PO) |
| 2026-05-22 | 2.1 | **PO validation GO (8→9/10) + should-fixes/nice-to-haves applied (Sarah, PO).** Verified all infra against live source. Pinned the version-append target **`saveDocumentVersion` (`documents.ts:537`)** into AC 8 + Task 4 + Dev Notes (incl. the read→`updateSection`→full-doc + `contentHtml` flow; status guard stays in the dispatch branch). Corrected AC 10 ActivityLog (`entity_type: 'workspace_document'`; reuse `document_version_saved` + agent-authorship `new_value` stamp; no invented label). AC 4 APPROVED-doc guidance now points at **`createDraftFromApproved` (`:755`)**. Named the `WorkspaceDocumentVersionSource` enum (`schema.prisma:1997`). Remaining SM pass reduced to renderer/preview detailing. | Sarah (PO) |
| 2026-06-02 | 2.2 | **SM refresh against shipped code (Bob, SM).** Mechanical updates only — no scope/design/AC-numbering change. (a) Line numbers refreshed: `saveDocumentVersion` `:537 → :590`, `createDraftFromApproved` `:755 → :823`, `createDocument` `:54 → :62`, `WorkspaceDocumentVersionSource` `:1997 → :2007`. (b) **New `AGENT` enum value** on `WorkspaceDocumentVersionSource` flagged (Dev Notes) — currently unused by 14.24's dispatch (`createDocument` hardcodes `'TIPTAP'`), so 17.11 stays on `'TIPTAP'` for consistency. (c) **17.10b auto-reindex finding:** `saveDocumentVersion` already wraps `indexWorkspaceDocument` in `after()` — dispatch does NOT need to call `markWorkspaceDocumentDirty` (AC 8 + Dev Notes updated). (d) Renderer template swapped from `draft-document-renderer.tsx` (14.24) → `update-requirement-renderer.tsx` (14.28) — overwrite-via-diff is closer than create card. (e) `params.entity_version` snapshot added to AC 2 + Task 3 to couple cleanly with 14.31 staleness guard (Approved, blockers cleared). (f) **CP-001** added to AC 6 — renderer copy never exposes raw `documentId` / `pendingActionId` / `section_heading` identifiers. (g) 197B-S1 sidebar-hint default change noted (`'open' → 'none'`, 2026-05-27). (h) 19.7c registry-narrowing harness count-bump locations added to Task 7 + Testing (`tests/unit/agent/tools/registry-role-filter.test.ts` + `tests/unit/agent/tools/registry-narrowing.test.ts`). (i) AC 7 label suggestion `'Uppdatera dokument'` mirrors 14.28/14.30 precedent. Status stays **Draft** pending PO re-validation. | Bob (SM) |
| 2026-06-02 | 2.3 | **SM should-fix pass post PO validate-story-draft (GO 9/10 conditional — Bob, SM).** Applied PO Sarah's two should-fixes + two nice-to-haves: **SF-1** — `oldSectionContentJson` added to AC 2 params shape (mirrors 14.28's `oldSnapshot` at `update-requirement-renderer.tsx:37, 56, 73, 88`); captured at propose time via the same section-locator helper; cascaded to Task 3 (capture step) and AC 6 (renderer reads both snapshots from `params` — no re-fetch). **SF-2** — `entity_version` pinned to ISO-8601 UTC string format in AC 2 (`document.updated_at.toISOString()`) — mirrors 14.28's tool at `lib/agent/tools/update-requirement.ts:188`; cascaded to Task 3. **NTH-1** — hash-gate surfaced in AC 8 main text (`syncWorkspaceChunks` is content-hash-gated; test author guidance: assert `indexWorkspaceDocument` invocation, NOT chunk-level mutation on no-content-change paths). **NTH-2** — no-op-edit guard added to AC 4 (`newSectionContentJson` deep-equal to current section body → reject at propose time with `"Inga ändringar att föreslå"` via `wrapToolError`, no pending row). Status held **Draft** — PO to flip to Approved. | Bob (SM) |
| 2026-06-02 | 2.4 | **PO sign-off — Status flipped Draft → Approved (Sarah, PO).** Verified all v2.3 should-fix/nice-to-have edits landed cleanly: AC 2 params shape now carries both `oldSectionContentJson` and ISO-string `entity_version` (mirrors 14.28 verbatim); AC 4 no-op-edit guard added; AC 6 renderer cascade ("Diff display reads from `params.oldSectionContentJson` vs `params.newSectionContentJson` — no re-fetch") landed; AC 8 hash-gate guidance landed; Task 3 capture-step cascade landed. Internal cross-references consistent (AC 2 ↔ AC 6 ↔ AC 8 ↔ AC 9 ↔ Task 3). **No further PO cycle needed — ready for `*develop-story`.** Implementation Readiness Score: 9.5/10. Confidence: High. | Sarah (PO) |

## Dev Agent Record

_To be populated by dev agent_

## QA Results

_To be populated by QA agent_
