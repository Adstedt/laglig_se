# Story 8.23: Email Deep-Link Opens Amendments List

## Status

Draft

## Story

**As a** user who clicks "Granska ändringen" in an amendment notification email,
**I want** to land on Hem with the full list of unacknowledged amendments visible,
**so that** I can see all pending changes at once and triage them — not just the single amendment from the email.

## Context & Dependencies

**Problem with current flow:**

The email links to `/dashboard?changeId=<id>`, which immediately enters `ChangeAssessmentView` for that one specific change. If the user has 5 unacknowledged amendments that day, the email shows all of them as separate cards, but clicking any CTA takes them straight into a single assessment with no visibility into the others. They have to finish or back out, then manually click the amendments context card to see what else is pending.

**Desired flow:**

Email CTA → Hem with `ChangePicker` dialog auto-opened → user sees full list of unacknowledged amendments → picks one to assess. The clicked amendment could be highlighted or sorted first, but the user has full context.

**Builds on:**

- Story 14.11: Hem — chat-first dashboard with context cards
- Story 8.4: `notify-amendment-changes` cron — generates email with `assessUrl`
- `ChangePicker` component (`components/features/dashboard/change-picker.tsx`) — already exists, fetches `getUnacknowledgedChanges()`, renders scrollable list
- `ChangeAssessmentView` — full-screen assessment entered after picking a change

**Current deep-link flow:**
```
Email CTA → /dashboard?changeId=abc123
  → server: getUnacknowledgedChangeById(changeId)
  → HemPage: initialChange → immediately renders ChangeAssessmentView
```

**Proposed deep-link flow:**
```
Email CTA → /dashboard?view=amendments (optionally &highlight=abc123)
  → HemPage: auto-opens ChangePicker dialog on mount
  → User sees all pending amendments, picks one → ChangeAssessmentView
```

## Acceptance Criteria

### Email Link Update

1. `notify-amendment-changes` cron changes `assessUrl` from `/dashboard?changeId=${ev.id}` to `/dashboard?view=amendments`
2. Email template CTA label remains "Granska ändringen" (singular) per card, but all CTAs link to the same amendments list view

### Dashboard Deep-Link Handling

3. When `/dashboard?view=amendments` is loaded, `HemPage` auto-opens the `ChangePicker` dialog on mount
4. URL is cleaned after consuming the param (existing `replaceState` pattern)
5. If there are no unacknowledged amendments when the user arrives, show the empty state in the picker ("Inga väntande ändringar")

### Backwards Compatibility

6. The existing `?changeId=` deep-link continues to work (direct-to-assessment) for any bookmarked or cached links
7. No breaking change to `ChangePicker`, `ChangeAssessmentView`, or `getUnacknowledgedChanges()`

## Tasks / Subtasks

### Phase 1: Email Link Change

- [ ] Task 1.1 (AC: 1, 2): Update `assessUrl` construction in `notify-amendment-changes` cron
  - Change from `/dashboard?changeId=${ev.id}` to `/dashboard?view=amendments`
  - All amendment cards in the same digest email now link to the same URL

### Phase 2: Dashboard Auto-Open

- [ ] Task 2.1 (AC: 3, 4, 5): Handle `?view=amendments` in `HemPage`
  - Read `view` from `searchParams`
  - If `view === 'amendments'`, set `showChangePicker(true)` on mount
  - Clean URL with `replaceState`
  - Works alongside existing `changeId` handling (not a replacement)

### Phase 3: Testing

- [ ] Task 3.1: Update unit tests for cron route (new assessUrl format)
- [ ] Task 3.2: Test deep-link auto-opens picker, empty state, and backwards compat with `?changeId=`

## Dev Notes

### Scope

This is a small, focused change — ~10 lines in the cron route, ~10 lines in HemPage. No new components needed.

### Why Not Highlight the Specific Amendment?

We could pass `&highlight=abc123` to scroll-to or visually highlight the clicked amendment in the picker. But since the picker is already sorted by priority + recency, and the list is typically short (< 10 items), the added complexity isn't justified for v1. Can add later if users request it.

### Email UX Consideration

Each amendment card in the digest email still has its own "Granska ändringen" button — they just all link to the same `/dashboard?view=amendments` URL. This is intentional: the email gives the overview, the app gives the interaction. An alternative would be a single CTA at the bottom of the email ("Granska alla ändringar") but that's a larger email template change for a separate story.

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-04-01 | 1.0 | Initial story creation | Alexander |
