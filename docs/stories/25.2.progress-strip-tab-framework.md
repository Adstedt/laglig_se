# Story 25.2: Progress strip + tab framework (B.2 shell)

## Status

Draft

## Story

**As a** new user who just clicked "Generera ny lista" (or in B.4+, "Importera"),
**I want** the first-run modal to stay open with a live progress strip at the top + a tab bar below where I can read tutorial content while my list is being built,
**so that** the 2–5 minute LLM wait stops feeling like a dead spinner and turns into a productive onboarding moment — and I can minimise the modal whenever I'm done reading without cancelling the background job.

This is the **B.2 wedge** of Epic 25: it ships the **TutorialStep shell** (progress strip + tab bar + empty tab-body container) without any tab content. Tab content is B.3. Done states are B.4. The FAB + tutorial-only mode is B.6. B.2's sole job is to land the chrome and the data wiring so B.3/B.4/B.6 can plug into a known surface.

Concretely: B.2 adds a new `<TutorialStep>` mode that the modal transitions into after a successful Generera click (and, later in B.4, after a successful Importera commit). The step renders (a) a `<ProgressStrip>` driven by the same SWR key as the dashboard's existing `<LawListGenerationProgress>` banner (single source of truth — confirmed in arch §4 and §6.3); (b) a tab bar with six placeholder tabs (no content, just labels + active-tab underline + "1 av 6" counter + horizontal scroll on narrow viewports); (c) an empty tab-body container; (d) a "Minimera" affordance that calls the existing `minimiseFirstRunModal()` action and closes the modal — the dashboard banner takes over after close (same handoff B.1 already wired via `router.refresh() + router.push('/dashboard')`).

This unlocks the "tutorial-while-waiting" UX the architecture has been pointing at since the brief. The user finally sees something happen during generation instead of staring at a spinner.

## Acceptance Criteria

### State machine + mounting

1. **`<FirstRunModal>` gains a fourth `step` value: `'tutorial'`.** The existing union type `Step = 'path-choice' | 'template-pick' | 'import-upload'` extends to `Step = 'path-choice' | 'template-pick' | 'import-upload' | 'tutorial'`. Adding a new value is a payload-shape extension only — no schema change, no migration.
2. **Generera success transitions the modal to `step='tutorial'` instead of closing it.** Currently in B.1, `handleGenerate` in `path-choice-step.tsx` calls `runDismissAction(minimiseFirstRunModal)` → `onClose()` → `router.refresh() + router.push('/dashboard')` (closes the modal and lets the dashboard banner take over). B.2 changes this: on `res.ok` (200) the modal transitions to the new `'tutorial'` step instead. On 409 (already-in-progress soft success) it also transitions to `'tutorial'` (the user lands in the right place regardless of whether they kicked off the run or just observed an existing one). On error: existing behavior preserved (toast + modal stays on path-choice). **`minimiseFirstRunModal()` is no longer called on Generera success in B.2** — it gets called later when the user hits Minimera in the tutorial step (AC 14) or when generation completes and done state ships in B.4.
3. **Importera success also transitions to `step='tutorial'` if the user wants to stay (B.2 deferral).** In B.1, `handleImportSuccess` in `first-run-modal.tsx` minimises + closes + routes to `/laglistor/skapa/{importId}/granska`. B.2 keeps this B.1 behaviour as-is — the import path does NOT transition to the tutorial step in B.2. (Rationale: import already has a substantive post-success surface — the review page. Adding tutorial-while-waiting there would conflict with the review flow. B.4's `done-import-step` will explicitly land the import path's "done" surface.) Document this scope decision in Completion Notes.
4. **The path-choice step's `data-onboarding-focus-target` moves from the Generera card to the new tutorial-step's first focusable element on transition.** When the user clicks Generera and the modal transitions to `'tutorial'`, the existing `useEffect` in `<FirstRunModal>` (the one that queries `[data-onboarding-focus-target="true"]` on step change, see `first-run-modal.tsx:90-106`) should find the active tab button. Default focus target: the first tab button (matches the AC 32 pattern from Story 25.1 where each step focuses its first interactive element).

### Per-step header copy (extends Story 25.1's `headerForStep`)

5. **`headerForStep` in `first-run-modal.tsx:57-79` gains a `'tutorial'` case.** Copy:
    - **Title:** "Vi skapar er personliga laglista" (mirrors the prototype's progress-strip headline at `_prototypes/onboarding-tutorial-modal.html:791`)
    - **Description:** "Du kan stänga rutan — vi fortsätter i bakgrunden och notifierar när det är klart." (mirrors the prototype's subhead at `:795-798`)
    - **Progress:** `'KOM IGÅNG · STEG 2 AV 2'` (same as the existing template-pick + import-upload sub-steps — generation is conceptually step 2 of a 2-step flow)
6. **The new `<TutorialStep>` body renders the progress strip + tab bar + tab body container in the modal's existing 8-padding region** (no change to the shell's px-8 pt-6 layout from Story 25.1). The header (logo + wordmark + progress indicator + title + description) and footer (separator + reassurance + skip) are owned by `<FirstRunModal>` and unchanged — but per AC 14 the footer hides in the tutorial step (the Minimera affordance lives inside the step body instead).

### Progress strip — `<ProgressStrip>` component

7. **New file: `components/features/onboarding-modal/progress-strip.tsx`.** Client component. Mirrors the existing `<LawListGenerationProgress>` data wiring (SWR key `'/api/workspace/generation-status'`, fetcher returns `GenerationStatusResponse` — see `components/features/dashboard/law-list-generation-progress.tsx:42-58`) but renders a different visual: prototype frame ③ fidelity. **Same SWR key as the banner so both surfaces share one cache** (per arch §4: "The modal's progress strip reuses the same SWR key").
8. **`<ProgressStrip>` props:**
    - `initialStatus?: GenerationStatus | null` — same prop the banner takes; lets the modal pass server-derived initial status to avoid an empty-state flash.
    - No other props in B.2. Tab list + active tab are owned by the parent `<TutorialStep>`, not the strip.
9. **Visual structure (prototype frame ③ — see `_prototypes/onboarding-tutorial-modal.html:783-869`):**
    - **Top row:** `<Loader2 className="h-4 w-4 animate-spin" />` icon + "Vi skapar er personliga laglista" title in `font-safiro text-[15px]`
    - **Subhead:** "Du kan stänga rutan — vi fortsätter i bakgrunden och notifierar när det är klart." in `text-[12.5px] text-muted-foreground mb-3`
    - **Step trail (horizontal chevron-separated chips):** for each `step` in the SWR-fetched `progress` array, render a pill. Steps with `status='done'` get a `<Check className="h-3 w-3" />` + label in `text-foreground`. Step with `status='active'` gets a `bg-primary text-primary-foreground` rounded-md pill with a `h-1.5 w-1.5 rounded-full bg-primary-foreground` pulse-dot (animated, see AC 10). Steps with `status='pending'` get just the label in `text-muted-foreground`. Chevrons (`<ChevronRight className="h-3 w-3" />`) between every pair.
    - **Shimmer progress bar:** `h-1 w-full rounded-full` with an indeterminate-progress shimmer animation (see AC 11). Track is `bg-muted`; fill is a gradient that translates left-to-right continuously.
    - **Bottom row (font-mono):** `"Steg X av Y"` on left, `"N / M rader"` on right (Y = progress.length; N/M from `GenerationStatusResponse.data.itemCount` and the expected total — see AC 12 for the rader-count source).
10. **Pulse-dot animation:** add a `.pulse-dot` utility under the existing `@layer utilities` block in `app/globals.css` (next to `.font-safiro`). Animation: `@keyframes pulseDot { 0%, 100% { opacity: 1 } 50% { opacity: 0.4 } }`, `.pulse-dot { animation: pulseDot 1.4s ease-in-out infinite }`. Reuses the same pattern the prototype defines at `:213-228`. Apply only to the active step's dot.
11. **Shimmer animation:** add a `.shimmer-track` / `.shimmer-track .fill` utility under the same `@layer utilities`. Animation: `@keyframes shimmer { 0% { transform: translateX(-100%) } 100% { transform: translateX(200%) } }`, `.shimmer-track { @apply bg-muted overflow-hidden relative }`, `.shimmer-track .fill { @apply absolute inset-y-0 left-0 w-1/3 bg-gradient-to-r from-transparent via-foreground/20 to-transparent; animation: shimmer 2.4s ease-in-out infinite }`. Reuses the prototype pattern at `:184-211`. The shimmer is intentionally indeterminate (no progress percentage) — the actual progress is shown via the step-trail chips, not the bar.
12. **`rader` counts come from the SWR response.** `GenerationStatusResponse.data.itemCount` is the current count; the expected total is... not currently in the response. **B.2 scope decision:** if `itemCount` is null/undefined OR an "expected total" field doesn't exist on the response, fall back to showing just `"Steg X av Y"` and omit the `"N / M rader"` row. Do NOT block B.2 on adding a total-count field to the API (that's a Story 16.4 follow-up). Document the gap in Completion Notes. The prototype's "47 / 120 rader" is illustrative, not a contract.
13. **`<ProgressStrip>` hides itself when status is non-progress.** If SWR returns `status === 'completed'` or `'failed'` or `null`, the strip returns `null`. The done state is B.4's concern (`done-generate-step.tsx` will own the celebration UI). For B.2: if generation finishes while the user is reading tutorial tabs, the strip just disappears — the modal stays open with the tabs intact until the user clicks Minimera. (B.4 will replace this disappear-quietly behavior with an explicit transition to `done_generate` mode.)

### Tutorial step shell — `<TutorialStep>` component

14. **New file: `components/features/onboarding-modal/tutorial-step.tsx`.** Client component. Owns the tab navigation state via `useState<TutorialTabId>('vad-ar-en-laglista')`. Renders top-to-bottom: `<ProgressStrip />` → tab bar → tab body container → Minimera affordance (right-aligned, separated by a subtle border-t).
15. **`<TutorialStep>` props:**
    - `initialStatus?: GenerationStatus | null` — forwarded to `<ProgressStrip>`.
    - `onMinimise: () => Promise<void>` — called when user clicks Minimera. Parent (`<FirstRunModal>`) wires this to `runDismissAction(minimiseFirstRunModal, ...)` + `handleClose()` + `router.refresh() + router.push('/dashboard')` (same pattern Story 25.1 uses for Generera handoff to dashboard banner).
16. **Tab bar:**
    - **Six tab buttons** in DOM order, matching the architecture's `tutorial-tabs/` folder spec (arch §6.1):
       1. `vad-ar-en-laglista` → "Vad är en laglista?"
       2. `kravpunkter` → "Kravpunkter & bevis"
       3. `uppgifter` → "Uppgifter"
       4. `kontroller` → "Kontroller"
       5. `lagandringar` → "Lagändringar"
       6. `ai-agenten` → "AI-agenten"
    - **Active tab styling:** `text-foreground` + a `h-0.5 bg-foreground rounded-full` underline pinned to `absolute left-3 right-3 -bottom-px` (matches prototype `_prototypes/onboarding-tutorial-modal.html:877-884`). Inactive tabs: `text-muted-foreground hover:text-foreground`.
    - **Horizontal scroll on narrow viewports:** wrapper uses `overflow-x-auto flex items-center gap-0.5`. Each tab is `shrink-0`. The active tab's underline does NOT scroll-into-view automatically — leaves the user's scroll position untouched on tab change.
    - **AI-agenten tab carries a "Ny" chip** (`inline-flex items-center rounded-full px-1.5 py-0 text-[9px] font-semibold uppercase tracking-wider bg-[var(--tone-info-soft-bg)] text-[var(--tone-info-soft-fg)]`) — same chip the prototype shows at `:905-913`. The other five tabs have no chip in B.2.
    - **"X av 6" counter on the far right:** `ml-auto shrink-0 text-[11px] uppercase tracking-[0.08em] text-muted-foreground pl-3` (mirrors the modal's existing progress-indicator typography from Story 25.1 + the prototype's eyebrow style). Updates as the active tab changes: shows `"{activeTabIndex + 1} av 6"`.
17. **Tab body container:** for B.2, an empty `<div className="px-1 pt-7 pb-7 min-h-[280px]">` placeholder per tab. Each tab's body just renders a `<p className="text-sm text-muted-foreground">Innehåll kommer snart</p>` so the layout has SOMETHING visible while B.3 fills in real content. The `min-h-[280px]` is to prevent jarring height shifts as B.3 lands actual content. Document the placeholder choice in Completion Notes — it's a known temporary state.
18. **Tab change records a `tab_viewed` event.** On every `setActiveTab(newTabId)` call, fire `void recordTabViewed(newTabId)` (the existing server action from Story 25.0 — see `app/actions/onboarding-modal.ts`). Fire-and-forget per the established telemetry pattern. **Do NOT fire on initial mount** — the first tab is auto-selected by default, not an explicit user view. Only fire when the user actively switches tabs.
19. **Minimera affordance:** a single button at the bottom-right of `<TutorialStep>`, separated from the tab body by a `<Separator />` (shadcn). Button: `<Button variant="outline" size="sm">` with `<Minimize2 className="mr-1.5 h-3.5 w-3.5" /> Minimera`. Matches prototype `:864-869`. On click: call `props.onMinimise()`. The button is NOT inside the progress strip (the prototype puts it there, but moving to bottom-right gives it more click affordance + decouples it from the progress strip's visibility — if the strip hides on completion, Minimera still needs to be reachable).

### Modal-shell footer behaviour

20. **The path-choice footer (separator + lock icon + "Allt sparas i ert workspace…" + "Hoppa över guiden") hides on the tutorial step.** Currently in Story 25.1, the footer renders only on `step === 'path-choice'` (`first-run-modal.tsx:215-235`). The tutorial step gets its own bottom affordance (Minimera, AC 19) and the footer would visually conflict. The existing `{step === 'path-choice' && <footer>}` guard already handles this — no code change needed beyond the new step being a falsy match.
21. **Tutorial step gets the same `pb-7` bottom-padding stub as the other sub-steps** (template-pick + import-upload already render `<div className="pb-7" />` per `first-run-modal.tsx:237`). Make sure the Minimera affordance + tab body + ProgressStrip layout adds up to a coherent vertical rhythm that matches the existing 8-padding shell.

### Sub-step transitions back to path-choice

22. **No "← Tillbaka" affordance on the tutorial step.** Unlike template-pick + import-upload (which render a Tillbaka button per Story 25.1 AC 13-14), the tutorial step does NOT let the user go back to path-choice. Once a generation is in flight, going back would be misleading — the work is already committed. The only ways out are: (a) Minimera (modal closes, banner shows progress on dashboard, generation continues), or (b) waiting for completion (B.4 will land the auto-transition to `done_generate`). Document this design decision in Completion Notes — it's a deliberate UX choice that diverges from the path-choice sub-steps' reversible model.

### Telemetry

23. **`tab_viewed` events use the tab `id` as the payload's `tab_id` field.** Tab IDs match the architecture's planned filenames (e.g., `'vad-ar-en-laglista'`, `'kravpunkter'`, etc.) — kebab-case, stable across B.2 → B.3 → B.6. The existing `recordTabViewed(tabId: string)` server action signature (Story 25.0) takes the tab id directly; no payload-shape changes needed.
24. **No new `OnboardingEvent.event_type` values introduced in B.2.** All telemetry rides existing events from Story 25.0 (`modal_opened`, `path_chosen`, `tab_viewed`, `modal_dismissed`, `fab_dismissed`). Sub-step transitions inside the modal still do NOT emit a separate `substep_viewed` event (same constraint as Story 25.1 AC 30).
25. **The `modal_dismissed` event still fires when the user clicks Minimera.** That's the existing `minimiseFirstRunModal()` server action's job (it writes `{from_state: 'path_choice'}` per `app/actions/onboarding-modal.ts:72-74`). **B.2 scope decision:** leave the hardcoded `from_state: 'path_choice'` as-is. Changing it to `from_state: 'tutorial'` per actual step would require a server-action signature change to accept the from-state as a param, which is out of scope. Document the inaccuracy in Completion Notes — the funnel will show all dismisses as `from_state: 'path_choice'` until B.6's tutorial-only mode lands and forces the param.

### Visual / a11y

26. **The active tab is focusable + activatable via keyboard.** `<button>` element (not a `<div role="button">`), Tab key navigates between tabs, Enter/Space activates. Active-tab styling uses `focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2` so keyboard users can see focus position. The "Ny" chip on the AI-agenten tab is inside the button, so it's part of the focusable target.
27. **Tab bar is `role="tablist"`, tabs are `role="tab"`, tab body is `role="tabpanel"` with `aria-labelledby` pointing at the active tab's id.** Standard ARIA tabs pattern (https://www.w3.org/WAI/ARIA/apg/patterns/tabs/). The active tab has `aria-selected="true"` and `tabIndex={0}`; inactive tabs have `aria-selected="false"` and `tabIndex={-1}`. Roving tabindex (left/right arrows move focus between tabs without changing selection — that's an "ARIA-recommended" interaction). **B.2 minimum:** ARIA attributes correct, keyboard nav via Tab works; roving tabindex with arrow keys is nice-to-have, can defer to B.3 if it adds too much code.
28. **`<ProgressStrip>` uses `aria-live="polite"` on the wrapper** so screen reader users hear progress updates as steps complete. The shimmer bar gets `aria-hidden="true"` (purely decorative).
29. **The Minimera button has an accessible label "Minimera"** (visible text, no additional aria-label needed). The Minimize2 icon is `aria-hidden="true"`.

### Feature flag + rollback

30. **No new feature flag introduced.** The B.0 emergency-disable flag `FIRST_RUN_MODAL_V0` (per `lib/onboarding/first-run-modal-flag.ts`) covers B.2 — setting it to false continues to disable the entire modal including the new tutorial step. If a partial rollback is needed (e.g., revert to B.1's "Generera closes the modal" behaviour while keeping the rest of B.2), add a flag in a follow-up — B.2 doesn't speculate.

### Regression coverage

31. **All Story 25.1 tests still pass unchanged.** The `path-choice-step.test.tsx` Generera 200-happy-path test (currently asserts `mockMinimise` + `mockPush` are called after a successful fetch) needs **one assertion update**: assert the parent's `onPickTutorial` callback (new prop, see AC 32) is called instead of `mockMinimise` / `onClose` / `mockPush`. The 409-as-success test gets the same update. The Generera-failure test is unchanged (failure path still toasts + stays on path-choice). The 7 other path-choice tests (Mall, Importera, Tom lista, Hoppa över, etc.) are unchanged.
32. **`<PathChoiceStep>` gains a new `onPickGenerate: () => void` callback prop.** Mirrors `onPickTemplate` and `onPickImport` (currently used for Mall + Importera sub-step transitions). The Generera handler in `path-choice-step.tsx:70` no longer awaits `minimiseFirstRunModal` or closes the modal directly — it just calls `onPickGenerate()` after the API succeeds (or returns 409). The parent shell (`<FirstRunModal>`) wires `onPickGenerate={() => setStep('tutorial')}`. **The fetch + 409 handling stays inside `<PathChoiceStep>`** — only the post-success transition behaviour moves to the callback. **Removed from `<PathChoiceStep>`**: the `router.refresh() + router.push('/dashboard')` calls (no longer applicable — modal stays open). The `runDismissAction(minimiseFirstRunModal)` call. The `onClose()` call. **Kept**: the loading state (isGenerating + ref guard), the fetch, the 409 → soft-success branch, the toast on hard error, the path_chosen telemetry write.
33. **New `<FirstRunModal>` tests:**
    - Clicking the stubbed PathChoiceStep's Generera-success callback transitions header copy to "Vi skapar er personliga laglista".
    - The "STEG 2 AV 2" indicator shows on the tutorial step.
    - The path-choice footer (Hoppa över guiden) is NOT rendered on the tutorial step.
    - The tutorial step renders the `<TutorialStep>` mock.
34. **New `<TutorialStep>` tests** (file: `tests/unit/components/features/onboarding-modal/tutorial-step.test.tsx`):
    - Renders 6 tab buttons with correct labels.
    - First tab is active on mount (no `tab_viewed` event fires on initial mount).
    - Clicking a different tab updates the active styling AND fires `recordTabViewed` with the correct tab id.
    - "X av 6" counter updates as user navigates.
    - Clicking Minimera calls the `onMinimise` callback prop.
    - The progress strip renders inside the step body (mocked via `vi.mock` of `./progress-strip`).
35. **New `<ProgressStrip>` tests** (file: `tests/unit/components/features/onboarding-modal/progress-strip.test.tsx`):
    - Renders the step trail with correct done/active/pending visual states based on mocked SWR response.
    - Active step gets the pulse-dot.
    - Shows "Steg X av Y" + "N / M rader" when itemCount is present.
    - Shows "Steg X av Y" only (no rader row) when itemCount is null/undefined.
    - Returns null when status is `'completed'`, `'failed'`, or null.
    - Uses the same SWR key as `<LawListGenerationProgress>` (assert via mock or by spying on `useSWR`).

### Smoke / verification (manual, deferred to staging)

36. **Manual smoke procedure for AC validation:**
    1. Fresh signup → land on `/dashboard` → modal opens at path-choice step.
    2. Click Generera → modal DOES NOT close (B.2 behaviour change) → header copy changes to "Vi skapar er personliga laglista" + "STEG 2 AV 2".
    3. Progress strip renders at the top of the body: spinner + title + step trail with the active step pulsing + shimmer bar + "Steg X av Y" row.
    4. Below the strip: 6 tab buttons. First tab active. "1 av 6" on the right.
    5. Click tab 3 (Uppgifter). Active styling moves. Counter shows "3 av 6". DB: new `OnboardingEvent` row with `event_type='tab_viewed'` and `payload->>'tab_id'='uppgifter'`. No event row for the initial mount of tab 1.
    6. Tab body shows "Innehåll kommer snart" placeholder (B.3 will fill).
    7. Click Minimera → modal closes → dashboard banner (`<LawListGenerationProgress>`) appears + continues showing the same progress. The SWR cache is shared, so the banner's first render is instant (no empty-state flash).
    8. Reload `/dashboard` → modal does NOT re-open (the `first_run_dismissed_at` write from `minimiseFirstRunModal` keeps it closed). Banner still shows progress.
    9. Wait for generation to complete → banner shows CompletedCard. Modal does not auto-re-open (B.4 will land that flow).
37. **Post-merge production check (within 24h):**
    - `onboarding_events` aggregated by `event_type='tab_viewed'` + `payload->>'tab_id'` — confirm tab views are landing per tab. If any tab gets zero views over 24h of first-runs, double-check that the tab id matches the button id wiring.
    - Sentry scan for errors originating from `components/features/onboarding-modal/tutorial-step.tsx` or `progress-strip.tsx`.
    - Verify the modal-to-banner handoff: in a real production session, the SWR cache should warm-share between the strip + banner so the banner's first render is instant. If users report a flash of "loading…" on the banner after closing the modal, the SWR key isn't matching.
    - If any check fails: set `FIRST_RUN_MODAL_V0=false` in Vercel env, open a follow-up ticket, redeploy.

## Tasks / Subtasks

- [ ] **Task 1 — Extend `<FirstRunModal>` with the `'tutorial'` step** (AC: 1, 2, 5, 6, 21)
  - [ ] Extend the `Step` union to include `'tutorial'`.
  - [ ] Extend `headerForStep` (`first-run-modal.tsx:57-79`) with the `'tutorial'` case (title + description + progress copy per AC 5).
  - [ ] Add the new `'tutorial'` branch in the step body switch (`first-run-modal.tsx:182-225`). Mount `<TutorialStep initialStatus={?} onMinimise={handleMinimiseFromTutorial} />`. (`initialStatus` source: pass through from a new prop on `<FirstRunModal>` that the dashboard derives — see Task 5.)
  - [ ] Add a new shell-level handler `handleMinimiseFromTutorial`: `await runDismissAction(minimiseFirstRunModal, 'minimiseFirstRunModal')` → on success: `handleClose()` + `router.refresh()` + `router.push('/dashboard')` (same Story 25.1 pattern). On failure: early return — modal stays open.
  - [ ] Verify the existing `pb-7` stub at `first-run-modal.tsx:237` covers the tutorial step too (it already does — the condition is `step !== 'path-choice'`).

- [ ] **Task 2 — Move Generera's post-success transition to a callback prop** (AC: 2, 32, 33)
  - [ ] In `components/features/onboarding-modal/path-choice-step.tsx`:
    - Add a new prop `onPickGenerate: () => void` to `PathChoiceStepProps`.
    - In `handleGenerate`: after `recordOnboardingEvent('path_chosen', { path: 'generate' })` (only on non-409 success per Story 25.1 ROBUST-001) AND on the 409-as-success branch, call `onPickGenerate()` instead of `runDismissAction(minimiseFirstRunModal, ...)` + `onClose()` + `router.refresh() + router.push('/dashboard')`. Remove those four lines.
    - The failure path is unchanged (toast + early return + reset `isGeneratingRef`).
    - The `useRouter()` import + `router` declaration can be removed if `router.push('/laglistor')` from Tom lista (`handleManual`) is the only remaining use — keep them if so.
  - [ ] In `components/features/onboarding-modal/first-run-modal.tsx`:
    - Wire the new prop on the `<PathChoiceStep>` mount (`first-run-modal.tsx:185-189`): `onPickGenerate={() => setStep('tutorial')}`.
  - [ ] In `tests/unit/components/features/onboarding-modal/path-choice-step.test.tsx`:
    - Update the Generera happy-path test ("Generera card → fires POST, records path_chosen=generate, minimises, closes") to assert `onPickGenerate` was called instead of `mockMinimise` / `onClose` / `mockPush`.
    - Update the 409-as-success test similarly.
    - Remove the `mockMinimise` and `mockPush` assertions from Generera tests (those become the parent's responsibility).
    - Add the `onPickGenerate` field to the `renderStep` helper's overrides type.

- [ ] **Task 3 — Build `<ProgressStrip>` component** (AC: 7, 8, 9, 10, 11, 12, 13, 28)
  - [ ] Create `components/features/onboarding-modal/progress-strip.tsx`. `'use client'`. Use the same SWR pattern as `components/features/dashboard/law-list-generation-progress.tsx:42-58`: `useSWR<GenerationStatusResponse>('/api/workspace/generation-status', fetcher, { refreshInterval: 3000, fallbackData: initialStatus ? { status: initialStatus, ... } : undefined })`. Reuse the `GenerationStatusResponse` type if it's exported; otherwise define a local equivalent matching the same shape (avoid coupling for now — refactor to a shared lib in a follow-up).
  - [ ] Render the visual structure per AC 9 (loader+title row, subhead, step trail, shimmer bar, font-mono rader row).
  - [ ] Wrap the whole strip in `aria-live="polite"` (AC 28). Add `aria-hidden="true"` on the shimmer bar.
  - [ ] Early-return `null` if `data?.status` is `'completed'`, `'failed'`, or null/undefined (AC 13).
  - [ ] Handle the missing `itemCount` case (AC 12) — if undefined, skip the rader row entirely.

- [ ] **Task 4 — Add pulse-dot + shimmer utilities to `app/globals.css`** (AC: 10, 11)
  - [ ] Add `.pulse-dot` + `@keyframes pulseDot` under the existing `@layer utilities` block (next to `.font-safiro` at `app/globals.css:122-125`).
  - [ ] Add `.shimmer-track` + `.shimmer-track .fill` + `@keyframes shimmer` under the same block.
  - [ ] Verify both render correctly in light AND dark mode (the prototype uses warm-tinted shadow + bg-primary which inverts in dark mode — the active step's `bg-primary text-primary-foreground` pill needs to read correctly in dark too; the `pulse-dot bg-primary-foreground` should still be visible against the inverted background).

- [ ] **Task 5 — Build `<TutorialStep>` component** (AC: 14, 15, 16, 17, 18, 19, 22, 26, 27, 29)
  - [ ] Create `components/features/onboarding-modal/tutorial-step.tsx`. `'use client'`. Props per AC 15.
  - [ ] State: `const [activeTab, setActiveTab] = useState<TutorialTabId>('vad-ar-en-laglista')`. Define `type TutorialTabId = 'vad-ar-en-laglista' | 'kravpunkter' | 'uppgifter' | 'kontroller' | 'lagandringar' | 'ai-agenten'`.
  - [ ] Define `const TABS: Array<{ id: TutorialTabId; label: string; hasNyChip?: boolean }>` per AC 16 — 6 entries, last one (`ai-agenten`) carries `hasNyChip: true`.
  - [ ] Render top-to-bottom:
    1. `<ProgressStrip initialStatus={initialStatus} />`
    2. Tab bar (`role="tablist"`): map `TABS` to buttons (`role="tab"`, `aria-selected`, active-tab underline, "Ny" chip on AI-agenten per AC 16). Right-aligned "X av 6" counter.
    3. Tab body (`role="tabpanel"`, `aria-labelledby` set to active tab's id): placeholder `<p>Innehåll kommer snart</p>` per AC 17.
    4. Separator + right-aligned Minimera button (`<Button variant="outline" size="sm">` with `<Minimize2>` icon) per AC 19.
  - [ ] Tab click handler: `setActiveTab(newId); if (newId !== activeTab) void recordTabViewed(newId)` — guards against double-firing on the same tab + skips the initial mount per AC 18.
  - [ ] No back affordance per AC 22.
  - [ ] Focus management: first tab button gets `data-onboarding-focus-target="true"` per AC 4 so the shell's existing focus useEffect (`first-run-modal.tsx:92-106`) lands focus there on step transition. NOT mounted via `autoFocus` — Radix's `onOpenAutoFocus` is already prevented at the dialog level per Story 25.1's polish pass.

- [ ] **Task 6 — Plumb `initialStatus` from the server to the modal** (AC: 14, 15)
  - [ ] In `app/(workspace)/dashboard/page.tsx`: the existing `generationStatus` variable (already read from `prisma.workspace.findUnique({ select: { law_list_generation_status: true } })` at lines ~94-97) needs to also be passed to `<HemPage>` as a prop available to the modal. It already IS passed (`generationStatus={generationStatus}`) — verify and reuse.
  - [ ] In `components/features/dashboard/hem-page.tsx`: pass `initialStatus={generationStatus ?? null}` to `<FirstRunModal>` (next to the existing `templates={firstRunTemplates}` + `userFirstName={userName}` props).
  - [ ] In `components/features/onboarding-modal/first-run-modal.tsx`: accept the new `initialStatus?: GenerationStatus | null | undefined` prop. Forward to `<TutorialStep initialStatus={initialStatus} />` in the tutorial branch.
  - [ ] Type `GenerationStatus` may not be exported from the banner. Either import from there + export it, or define a local type alias `type GenerationStatus = 'pending' | 'in_progress' | 'completed' | 'failed'`. **Recommend defining locally in `first-run-modal.tsx`** to avoid a cross-component dependency — the banner stays the source of truth for its own typing.

- [ ] **Task 7 — Wire `<TutorialStep>` Minimera back to the existing minimise action** (AC: 14, 19, 25)
  - [ ] `<FirstRunModal>` defines `handleMinimiseFromTutorial = async () => { if (await runDismissAction(minimiseFirstRunModal, 'minimiseFirstRunModal')) { handleClose(); router.refresh(); router.push('/dashboard'); } }` — same shape as `handleSkip` per Story 25.1's polish.
  - [ ] Pass `onMinimise={handleMinimiseFromTutorial}` to `<TutorialStep>`.
  - [ ] Note in Completion Notes that the `modal_dismissed` event still records `from_state: 'path_choice'` (hardcoded in the server action per `app/actions/onboarding-modal.ts:72-74`) — funnel reports will need to know this until a future story changes the signature to accept a from_state param.

- [ ] **Task 8 — Tests** (AC: 31, 33, 34, 35)
  - [ ] Update `tests/unit/components/features/onboarding-modal/path-choice-step.test.tsx` per Task 2 (Generera tests assert `onPickGenerate` callback instead of minimise+close+push).
  - [ ] Extend `tests/unit/components/features/onboarding-modal/first-run-modal.test.tsx` per AC 33: mock `./tutorial-step` like the other sub-step mocks, add 4 new tests (Generera→tutorial header, STEG 2 AV 2 indicator, no footer on tutorial step, TutorialStep renders).
  - [ ] Create `tests/unit/components/features/onboarding-modal/tutorial-step.test.tsx` per AC 34: 6 tests (renders 6 tabs, first tab active, tab change updates + fires event, "X av 6" counter, Minimera calls callback, ProgressStrip mounted).
  - [ ] Create `tests/unit/components/features/onboarding-modal/progress-strip.test.tsx` per AC 35: 6 tests (visual states from SWR mock, pulse-dot on active, rader row with/without itemCount, hides on completed/failed/null, SWR key assertion).
  - [ ] Mock pattern: same as Story 25.1's existing mocks. SWR mock: `vi.mock('swr', () => ({ default: vi.fn() }))` then `vi.mocked(useSWR).mockReturnValue({ data: { status: 'in_progress', progress: [...], itemCount: 47 }, mutate: vi.fn() })`.

- [ ] **Task 9 — Verification** (AC: 36, 37)
  - [ ] Run the full `tests/unit` suite. All 117 Story 25.0+25.1 tests + the new B.2 tests pass. Zero regressions.
  - [ ] Run `tsc --noEmit` and `pnpm eslint components/features/onboarding-modal/` — both clean.
  - [ ] Document the staging-smoke procedure (AC 36) and post-merge verification (AC 37) in Completion Notes for the next operator.
  - [ ] Smoke check in dev: reset Almåsa workspace (the `.reset-onboarding-tmp.ts` pattern from Story 25.1), click Generera, verify the modal transitions to tutorial (does NOT close) and the strip + tabs render. The actual end-to-end LLM generation isn't required to validate B.2 — the strip just needs to show SOMETHING during in_progress.

## Dev Notes

### Source artefacts

- **Epic PRD:** `docs/prd/epic-25-first-run-onboarding-modal.md` — B.2 scope at lines 152–168.
- **Architecture:** `docs/architecture/first-run-onboarding-modal.md` — §4 (SWR-sharing constraint at line 49 + 92), §6.1 (component file list including `tutorial-step.tsx` + `progress-strip.tsx`), §6.2 (state machine — `tutorial` mode + transitions), §9 sequencing table (B.2 scope at line 548).
- **Prototype:** `_prototypes/onboarding-tutorial-modal.html` frame ③ at lines 770–1000+. Source of truth for the progress-strip visual + tab-bar layout. The shimmer + pulse-dot CSS at lines 184–228 of the prototype is the reference for `app/globals.css` additions in Task 4.
- **Story 25.0:** `docs/stories/completed/25.0.path-choice-gate.md` — server actions in place (`recordTabViewed`, `minimiseFirstRunModal`, `recordOnboardingEvent`); workspace columns (`first_run_dismissed_at`, `first_run_tabs_viewed`); `OnboardingEvent` table; `FIRST_RUN_MODAL_V0` flag.
- **Story 25.1:** `docs/stories/completed/25.1.modal-shell-three-card-upgrade.md` — modal shell with the sub-step state machine, `headerForStep` lookup, `runDismissAction` helper, `data-onboarding-focus-target` focus pattern, dashboard banner-mount fallback (`router.refresh() + router.push('/dashboard')`), Generera card with loading state + ref-based double-click guard, 409-as-soft-success treatment.

### Critical reuse — `LawListGenerationProgress`

- **File:** `components/features/dashboard/law-list-generation-progress.tsx`. SWR key at line 53: `'/api/workspace/generation-status'`. Fetcher pattern at lines 42–43. Type `GenerationStatusResponse` at line 34.
- **Do NOT refactor the banner.** B.2 builds a SEPARATE `<ProgressStrip>` component that uses the SAME SWR key — SWR's request deduplication means both surfaces share one network request + one cache entry, but neither has to know about the other. This is the architecture's explicit instruction (§4 line 49: "the modal's progress strip reuses the same SWR key so both surfaces render from one source of truth"). The banner stays load-bearing on the dashboard for the post-minimise + post-completion experience.
- **The banner has retry logic** at line 67-75 of `law-list-generation-progress.tsx` (auto-fires `POST /api/workspace/generate-law-list` when status === 'pending'). `<ProgressStrip>` should NOT include this retry logic — the banner already covers it, and duplicating it would cause double-fire when both surfaces are mounted (which never happens, but defense in depth). Document this in Completion Notes.

### Existing patterns to follow

- **Sub-step state machine** — already in place in `<FirstRunModal>` via `const [step, setStep] = useState<Step>('path-choice')`. Adding `'tutorial'` is a one-line union extension + a new branch in the render switch + a new case in `headerForStep`. Same pattern as Story 25.1 template-pick + import-upload.
- **`runDismissAction(action, label)` helper** at `components/features/onboarding-modal/run-dismiss-action.ts`. Use it for the Minimera handler. Returns boolean; on false, early-return without closing.
- **`recordTabViewed(tabId: string)` server action** at `app/actions/onboarding-modal.ts:137-166` — exists since Story 25.0, signature is `(tabId: string) => Promise<{ok: boolean, ...}>`. Fire-and-forget per established telemetry pattern (`void recordTabViewed(...)`).
- **Sonner toast pattern** — established. Use for unexpected error paths (e.g., if the modal-shell minimise fails on Generera success → toast + stay in tutorial step).

### Key decisions baked into this story

- **Tutorial step is NOT reversible.** No "← Tillbaka" affordance back to path-choice. Once generation is in flight, going back is misleading because the path is already committed. Only ways out: Minimera (close to banner) or wait for done state (B.4). This diverges from template-pick + import-upload which ARE reversible. See AC 22.
- **Import path doesn't get B.2 tutorial-step treatment.** Currently Importera success routes to `/laglistor/skapa/{importId}/granska` (the review surface). Adding a tutorial-while-waiting step there would conflict with the review flow. B.4 will land `done-import-step.tsx` as the import path's explicit done surface. See AC 3.
- **Tab body is a placeholder ("Innehåll kommer snart") in B.2.** Real content is B.3's job — 6 separate tab files under `tutorial-tabs/` with mini-previews of actual product surfaces. B.2 just needs the navigation chrome to land so B.3 has somewhere to plug content in. The `min-h-[280px]` on the body container prevents height shifts when B.3 lands. See AC 17.
- **Minimera lives at the bottom-right of the step body, NOT inside the progress strip.** Prototype puts it inside the strip (frame ③), but that couples Minimera to strip visibility (strip hides on completion per AC 13, but Minimera must still be reachable). Bottom-right is the cleanest decoupling. See AC 19.
- **`modal_dismissed` event still records `from_state: 'path_choice'` even when fired from the tutorial step.** Changing this requires a server-action signature change (accept a from_state param). Out of scope for B.2 — funnel analysts will need to know the limitation until B.6 reworks this. See AC 25.
- **Tab IDs are kebab-case + stable across B.2 → B.3 → B.6.** B.3 will create files matching these IDs (`tutorial-tabs/tab-vad-ar-en-laglista.tsx`, etc.). Using stable IDs now means the `tab_viewed` telemetry has consistent values across the epic. See AC 23.
- **Roving tabindex (arrow keys to move between tabs) is nice-to-have, not required.** Standard ARIA tabs pattern recommends it but Tab key navigation + Enter activation covers the keyboard a11y minimum. B.2 ships the ARIA attributes correctly + Tab nav; arrow-key roving can land in B.3 if it doesn't add too much code. See AC 27.
- **No FAB in B.2.** The corner FAB ships in B.6. Until then, "Minimera" just means "close + dashboard banner takes over" — exactly what B.1's Generera handoff already does. The Minimera button visual signals to the user "you can dismiss this without losing the work" — even without the FAB, the dashboard banner provides the persistent progress indicator.

### Out of scope explicitly

- Tab content for the 6 tutorial tabs (B.3)
- The `tutorial_only` mode (B.6 — strips the progress strip and renders just the tabs for post-completion learning)
- Done states for generate / import (B.4)
- Feedback form 7th tab (B.5)
- Corner FAB + Hjälp sidebar entry + URL deep-link (B.6)
- A "back to path-choice" affordance from the tutorial step
- Importera success transitioning to tutorial-step (kept at B.1 behaviour — routes to `/laglistor/skapa/{importId}/granska`; B.4's `done-import-step` will land the explicit import-done surface)
- Refactoring `<LawListGenerationProgress>` to extract a shared progress-strip primitive (deliberate — the two surfaces share SWR data only, not visual code)
- Adding an "expected total" field to `GenerationStatusResponse` so the rader row can show "N / M" (Story 16.4 follow-up)
- Roving tabindex with arrow-key navigation between tabs (defer to B.3 if it adds bloat)
- Changing the `modal_dismissed` server action signature to accept a `from_state` param (defer to B.6 when tutorial-only mode formalises the multi-state dismiss surface)

### Project structure notes

All new + modified file paths align with `docs/architecture/12-unified-project-structure.md` + the architecture's `§8 Source Tree Additions`:
- `components/features/onboarding-modal/tutorial-step.tsx` — new, sits alongside `path-choice-step.tsx` / `template-pick-step.tsx` etc.
- `components/features/onboarding-modal/progress-strip.tsx` — new, same folder.
- `components/features/onboarding-modal/first-run-modal.tsx` — modified (new step branch + handler).
- `components/features/onboarding-modal/path-choice-step.tsx` — modified (new callback prop + remove now-parent's responsibilities).
- `app/globals.css` — modified (new pulse-dot + shimmer utilities under existing `@layer utilities`).
- `components/features/dashboard/hem-page.tsx` — modified (forward `initialStatus` prop).
- `tests/unit/components/features/onboarding-modal/tutorial-step.test.tsx` — new.
- `tests/unit/components/features/onboarding-modal/progress-strip.test.tsx` — new.
- `tests/unit/components/features/onboarding-modal/first-run-modal.test.tsx` — modified (sub-step mocks + new transition tests).
- `tests/unit/components/features/onboarding-modal/path-choice-step.test.tsx` — modified (Generera tests assert callback instead of close+push).

Kebab-case for new component files (consistent with the rest of `components/features/onboarding-modal/`).

### Testing

**Test locations (this story):**
- `tests/unit/components/features/onboarding-modal/tutorial-step.test.tsx` — new component test (6 tests per AC 34)
- `tests/unit/components/features/onboarding-modal/progress-strip.test.tsx` — new component test (6 tests per AC 35)
- `tests/unit/components/features/onboarding-modal/first-run-modal.test.tsx` — extend with 4 new sub-step transition tests (AC 33)
- `tests/unit/components/features/onboarding-modal/path-choice-step.test.tsx` — update 2 Generera assertions (AC 31, 32)

**Frameworks:** Vitest (unit), Vitest + React Testing Library (component). Mock pattern: same as Story 25.1's existing mocks:
- Mock `@/app/actions/onboarding-modal` (`recordTabViewed`, `minimiseFirstRunModal`, `recordOnboardingEvent`)
- Mock `next/navigation` (`useRouter` → `{ push: vi.fn(), refresh: vi.fn() }`)
- Mock `swr` for `<ProgressStrip>` tests: `vi.mock('swr', () => ({ default: vi.fn() }))` + `vi.mocked(useSWR).mockReturnValue(...)`
- For `<FirstRunModal>` tests, mock the new `./tutorial-step` like the other sub-step mocks: `vi.mock('@/components/features/onboarding-modal/tutorial-step', () => ({ TutorialStep: ({ onMinimise }) => <div data-testid="tutorial-step"><button onClick={onMinimise}>stub-minimise</button></div> }))`

**Coverage targets:**
- `<TutorialStep>` component: 90%+ on the 6 cases in AC 34
- `<ProgressStrip>` component: 90%+ on the 6 cases in AC 35
- `<FirstRunModal>` sub-step transitions: 4 new tests for the tutorial branch
- `<PathChoiceStep>` regression updates: same assertion count as Story 25.1 (9 tests), with 2 updated (Generera happy + 409) and 7 unchanged

**Existing tests to maintain (no edits):**
- All Story 25.0 tests (`get-onboarding-state`, `first-run-modal-flag`, `onboarding-modal` actions, `workspace.test.ts` regression block) — pass unchanged
- All Story 25.1 tests for `<TemplatePickStep>` + `<ImportUploadStep>` mount + import-upload sub-step — pass unchanged
- Existing `<LawListGenerationProgress>` tests from Story 16.4 — pass unchanged (no banner code modified in B.2)

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-05-16 | 0.1 | Initial draft from Epic 25 PRD (Story B.2 scope at lines 152–168) + architecture doc (§4 SWR-sharing, §6.1 component spec, §6.2 state machine `tutorial` mode + transitions, §9 sequencing table). 37 ACs across 6 subsystems (state machine + mounting, header copy, progress strip, tutorial step shell, modal-shell footer, sub-step transitions back, telemetry, a11y, regression+smoke). 9 implementation tasks. Defers tab content (B.3), done states (B.4), feedback tab (B.5), FAB + tutorial-only mode (B.6) explicitly. Key design decisions: tutorial step is NOT reversible (no Tillbaka), import path doesn't transition to tutorial in B.2 (kept at B.1 routes-to-granska), Minimera lives at bottom-right of step body NOT inside progress strip (decouples from strip's hide-on-completion behavior), `modal_dismissed.from_state` stays hardcoded to `'path_choice'` (server-action signature change deferred), tab IDs are kebab-case + stable across B.2→B.3→B.6 for telemetry consistency. Prototype source: `_prototypes/onboarding-tutorial-modal.html` frame ③ at lines 770–1000+, including the shimmer + pulse-dot CSS at lines 184–228 (ported to `app/globals.css` under existing `@layer utilities`). SWR-sharing is the load-bearing architectural constraint — `<ProgressStrip>` uses the same `/api/workspace/generation-status` key as the existing `<LawListGenerationProgress>` banner, so both surfaces share one network request + cache entry. Banner stays unchanged on the dashboard for the post-minimise + post-completion experience. | Bob (SM) |

## Dev Agent Record

### Agent Model Used

_To be populated by the Dev agent._

### Debug Log References

_To be populated by the Dev agent._

### Completion Notes List

_To be populated by the Dev agent._

### File List

_To be populated by the Dev agent._

## QA Results

_To be populated by the QA agent._
