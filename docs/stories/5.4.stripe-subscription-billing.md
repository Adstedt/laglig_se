# Story 5.4: Integrate Stripe for Subscription Billing

## Status

Ready for Review

## Story

**As a** product owner,
**I want** to collect payments via Stripe,
**so that** users can subscribe to paid tiers.

## Context & Dependencies

- **Builds on:** Story 5.1 (completed) — `SubscriptionTier` enum (`TRIAL`, `SOLO`, `TEAM`, `ENTERPRISE`) and `subscription_tier`, `trial_ends_at`, `status`, `paused_at`, `deleted_at` inlined on the `Workspace` model.
- **Builds on:** Story 5.7 (completed) — workspace settings page at `app/(workspace)/settings/` (billing tab will live here).
- **Architectural pattern:** Story 5.1 chose to inline subscription fields directly on `Workspace` (snake_case) rather than use the separate `Subscription` model described in `docs/architecture/9-database-schema.md §9.11`. The standalone `Subscription` model was never added to `prisma/schema.prisma`. **Story 5.4 follows the inline-on-Workspace pattern.** Architecture doc §9.11 should be updated post-implementation to reflect this decision.
- **Depended on by:** Stories 5.5 (usage limits), 5.6 (add-ons), 5.8 (pause/delete), 5.10 (unit economics).
- **Note:** Pricing tiers are subject to validation — EUR 399/899/2000 are initial estimates. Update Stripe Price IDs once confirmed.

## Acceptance Criteria

1. Stripe account created; secret + webhook + price-ID env vars wired through the type-safe validator at `lib/env.ts` (test + production keys).
2. Stripe Customer created for each workspace on first billing interaction; `stripe_customer_id` persisted on `Workspace`.
3. Three Stripe Products + monthly recurring Prices created in EUR: Solo (EUR 399/mo), Team (EUR 899/mo), Enterprise (EUR 2,000+/mo custom). Price IDs stored as env vars.
4. Stripe Checkout integration for trial-to-paid conversion — Checkout Session is created with `subscription_data.metadata.workspaceId` so the metadata propagates to the resulting Subscription object.
5. Billing page (`/settings/billing`) shows: current plan, next billing date, payment method (managed via Stripe Customer Portal), invoice history (live `stripe.invoices.list` for the workspace's customer, with hosted-invoice links).
6. "Upgrade Plan" flow: select tier → Stripe Checkout → Subscription created. "Manage Payment Method" button opens a Stripe Customer Portal session.
7. Webhook endpoint `/api/webhooks/stripe` handles, at minimum: `checkout.session.completed`, `customer.subscription.updated`, `customer.subscription.deleted`, `invoice.payment_failed`, `invoice.payment_succeeded`.
8. Subscription state synced from webhooks to `workspaces`: `subscription_tier`, `subscription_status`, `current_period_end`, `stripe_subscription_id`, and `status` (e.g., `PAUSED` on subscription cancellation).
9. Failed payments → email notification via Resend + 3-day grace period (`payment_grace_period_ends_at`); after expiry, workspace access is blocked at the `workspace-context` boundary (HTTP 402-equivalent redirect to billing page).
10. Webhook idempotency: each Stripe `event.id` is processed at most once (deduped via a `StripeWebhookEvent` row with `stripe_event_id @unique`).
11. Stripe Tax enabled on Checkout (`automatic_tax: { enabled: true }`) for EU VAT compliance.
12. Test subscription lifecycle: Trial → Paid → Upgrade → Downgrade → Cancel → Resume (via Portal).

## Tasks / Subtasks

- [x] Add `stripe` SDK dependency (AC: 1)
  - [x] `pnpm add stripe@^14.21` (resolved to ^14.25.0)
- [x] Extend `lib/env.ts` validator with Stripe envs (AC: 1)
  - [x] Add to `server`: `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_SOLO_PRICE_ID`, `STRIPE_TEAM_PRICE_ID`, `STRIPE_ENTERPRISE_PRICE_ID`
  - [x] Add to `client`: `NEXT_PUBLIC_APP_URL`
  - [x] Wire entries into `runtimeEnv`
- [ ] Provision Stripe (AC: 1, 3) — **operator-applied; outside dev agent scope**
  - [ ] Create Stripe account (test + production)
  - [ ] Create Products + monthly Prices in EUR (Solo, Team, Enterprise)
  - [ ] Enable Stripe Tax for EU
  - [ ] Note IDs in `.env.local` / Vercel env
- [x] Update `prisma/schema.prisma` Workspace model with new fields (AC: 2, 8, 9) — **migration generated with `--create-only`; user must apply manually**
  - [x] `stripe_customer_id String? @unique`
  - [x] `stripe_subscription_id String?`
  - [x] `subscription_status String?` (Stripe status: `active | past_due | canceled | unpaid`)
  - [x] `current_period_end DateTime?`
  - [x] `payment_grace_period_ends_at DateTime?`
- [x] Add `StripeWebhookEvent` model for idempotency (AC: 10) — **migration generated; bundled with workspace fields in `20260504120000_add_stripe_billing_fields/migration.sql`**
- [x] Implement Stripe client + price map: `lib/stripe/config.ts` (AC: 1)
- [x] Implement `lib/stripe/customer.ts` → `getOrCreateStripeCustomer` (AC: 2)
- [x] Implement Checkout endpoint `app/api/billing/checkout/route.ts` (AC: 4, 6, 11)
  - [x] Permission gate: `workspace:billing`
  - [x] Pass `subscription_data: { metadata: { workspaceId, tier } }` so updated/deleted webhooks can resolve the workspace
  - [x] Pass `automatic_tax: { enabled: true }` and `customer_update: { address: 'auto', name: 'auto' }`
  - [x] Omit `payment_method_types` to let Stripe auto-enable Card / Klarna / Swish per Stripe Dashboard config
- [x] Implement Customer Portal endpoint `app/api/billing/portal/route.ts` (AC: 5, 6)
- [x] Implement Invoices endpoint `app/api/billing/invoices/route.ts` — proxies `stripe.invoices.list({ customer })` to UI (AC: 5)
- [x] Build billing page UI at `app/(workspace)/settings/billing/page.tsx` (AC: 5, 6)
  - [x] Current plan card, next billing date, payment-method link (opens Portal)
  - [x] Invoice history table with `hosted_invoice_url` / `invoice_pdf` links
  - [x] Upgrade/downgrade tier buttons (Checkout for upgrade, Portal for downgrade/cancel)
- [x] Implement webhook endpoint `app/api/webhooks/stripe/route.ts` (AC: 7, 8, 10)
  - [x] Use raw `request.text()` and `request.headers.get('stripe-signature')` (note: changed from `next/headers` to make handler unit-testable without a request scope)
  - [x] `stripe.webhooks.constructEvent(body, signature, env.STRIPE_WEBHOOK_SECRET)`
  - [x] Idempotency check: if `StripeWebhookEvent.stripe_event_id` exists, return 200 immediately
  - [x] Persist `StripeWebhookEvent` row before mutating Workspace
  - [x] Handle each event type per Dev Notes
  - [x] Resolve `current_period_end` via `subscription.items.data[0].current_period_end` (newer Stripe API versions; fall back to `subscription.current_period_end` if SDK version exposes it)
- [x] Enforce grace-period block in `lib/auth/workspace-context.ts` (AC: 9)
  - [x] If `workspace.payment_grace_period_ends_at && workspace.payment_grace_period_ends_at < new Date()`, treat workspace as not accessible. New `WorkspaceAccessError` code `PAYMENT_PAST_DUE` (no existing code covered this); `requirePermission` maps it to HTTP 402 with a `redirectTo: '/settings/billing?reason=past_due'` body. Cached context payload extended with `paymentGracePeriodEndsAt` so cache-hit branch re-checks expiry.
- [x] Create React Email template `emails/payment-failed.tsx` (AC: 9)
  - [x] Props: `companyName`, `amount` (EUR), `gracePeriodEndsAt`, `manageBillingUrl`
  - [x] Swedish copy + brand styling (mirror `emails/task-overdue.tsx` pattern)
- [x] Wire payment-failed email send via existing `lib/email/email-service.ts` from inside `invoice.payment_failed` webhook handler (AC: 9) — `from: 'no-reply'` (auth-critical/billing transactional, per FROM_ADDRESSES doc)
- [x] Tests at `tests/unit/billing/stripe-integration.test.ts` (AC: 12)
  - [x] ~~Use MSW to mock Stripe HTTP calls~~ — MSW is in tech stack §3.1 but isn't a project dep yet; adopting MSW is a deps decision the dev agent shouldn't make unilaterally. Used `vi.mock` for the Stripe SDK methods we touch (matches existing codebase pattern at `tests/unit/agent/retrieval.test.ts`). Real `stripe.webhooks.constructEvent` retained so signature verification is exercised end-to-end.
  - [x] Webhook signature verification — valid + tampered
  - [x] Idempotent replay of same `event.id`
  - [x] Lifecycle: checkout completed → subscription.updated (tier change) → subscription.deleted (cancel) → invoice.payment_failed (grace period) → invoice.payment_succeeded (grace clearance)
  - [x] `getOrCreateStripeCustomer` — returns existing, creates new, persists
  - [x] Idempotency rollback when handler throws (verifies retry path)
- [x] Local dev workflow (AC: 7)
  - [x] Add `stripe listen --forward-to localhost:3000/api/webhooks/stripe` snippet to `README.md` Development section
- [ ] Production wiring (AC: 1, 7) — **operator-applied; outside dev agent scope**
  - [ ] Configure Stripe webhook URL in Stripe Dashboard
  - [ ] Add `STRIPE_WEBHOOK_SECRET` (and price IDs, secret key) to Vercel project env

## Dev Notes

### Schema changes — extend Workspace inline (snake_case)

```prisma
// prisma/schema.prisma — extend the existing model Workspace { ... }
  // Story 5.4: Stripe billing
  stripe_customer_id           String?   @unique
  stripe_subscription_id       String?
  subscription_status          String?   // active | past_due | canceled | unpaid
  current_period_end           DateTime?
  payment_grace_period_ends_at DateTime?
```

### New model — webhook idempotency

```prisma
model StripeWebhookEvent {
  id               String   @id @default(uuid())
  stripe_event_id  String   @unique
  event_type       String
  processed_at     DateTime @default(now())

  @@index([processed_at])
  @@map("stripe_webhook_events")
}
```

### Env validator (`lib/env.ts`)

Extend the existing `createEnv` call:

```typescript
server: {
  // ...existing
  STRIPE_SECRET_KEY: z.string().min(1),
  STRIPE_WEBHOOK_SECRET: z.string().min(1),
  STRIPE_SOLO_PRICE_ID: z.string().min(1),
  STRIPE_TEAM_PRICE_ID: z.string().min(1),
  STRIPE_ENTERPRISE_PRICE_ID: z.string().min(1),
},
client: {
  // ...existing
  NEXT_PUBLIC_APP_URL: z.string().url(),
},
runtimeEnv: {
  // ...existing
  STRIPE_SECRET_KEY: process.env.STRIPE_SECRET_KEY,
  STRIPE_WEBHOOK_SECRET: process.env.STRIPE_WEBHOOK_SECRET,
  STRIPE_SOLO_PRICE_ID: process.env.STRIPE_SOLO_PRICE_ID,
  STRIPE_TEAM_PRICE_ID: process.env.STRIPE_TEAM_PRICE_ID,
  STRIPE_ENTERPRISE_PRICE_ID: process.env.STRIPE_ENTERPRISE_PRICE_ID,
  NEXT_PUBLIC_APP_URL: process.env.NEXT_PUBLIC_APP_URL,
},
```

### Stripe client (`lib/stripe/config.ts`)

```typescript
import Stripe from 'stripe'
import { env } from '@/lib/env'

// Omit apiVersion to use the SDK's pinned default — keeps us aligned with stripe@14.21+.
export const stripe = new Stripe(env.STRIPE_SECRET_KEY)

export const STRIPE_PRICE_IDS = {
  SOLO: env.STRIPE_SOLO_PRICE_ID,
  TEAM: env.STRIPE_TEAM_PRICE_ID,
  ENTERPRISE: env.STRIPE_ENTERPRISE_PRICE_ID,
} as const

export type StripePaidTier = keyof typeof STRIPE_PRICE_IDS // 'SOLO' | 'TEAM' | 'ENTERPRISE'
```

### Customer creation (`lib/stripe/customer.ts`)

```typescript
import { stripe } from './config'
import { prisma } from '@/lib/prisma'

export async function getOrCreateStripeCustomer(
  workspaceId: string,
  email: string,
  companyName: string
) {
  const workspace = await prisma.workspace.findUnique({
    where: { id: workspaceId },
    select: { stripe_customer_id: true },
  })
  if (workspace?.stripe_customer_id) return workspace.stripe_customer_id

  const customer = await stripe.customers.create({
    email,
    name: companyName,
    metadata: { workspaceId },
  })

  await prisma.workspace.update({
    where: { id: workspaceId },
    data: { stripe_customer_id: customer.id },
  })

  return customer.id
}
```

### Checkout endpoint (`app/api/billing/checkout/route.ts`)

```typescript
import { NextResponse } from 'next/server'
import { getWorkspaceContext } from '@/lib/auth/workspace-context'
import { requirePermission } from '@/lib/api/require-permission'
import { stripe, STRIPE_PRICE_IDS, type StripePaidTier } from '@/lib/stripe/config'
import { getOrCreateStripeCustomer } from '@/lib/stripe/customer'
import { prisma } from '@/lib/prisma'
import { env } from '@/lib/env'

export async function POST(request: Request) {
  const denied = await requirePermission('workspace:billing')
  if (denied) return denied

  const context = await getWorkspaceContext()
  const { tier } = (await request.json()) as { tier: StripePaidTier }

  if (!STRIPE_PRICE_IDS[tier]) {
    return NextResponse.json({ error: 'Invalid tier' }, { status: 400 })
  }

  const workspace = await prisma.workspace.findUniqueOrThrow({
    where: { id: context.workspaceId },
    include: { owner: { select: { email: true } } },
  })

  const customerId = await getOrCreateStripeCustomer(
    workspace.id,
    workspace.owner.email,
    workspace.name
  )

  const session = await stripe.checkout.sessions.create({
    customer: customerId,
    mode: 'subscription',
    line_items: [{ price: STRIPE_PRICE_IDS[tier], quantity: 1 }],
    automatic_tax: { enabled: true },
    customer_update: { address: 'auto', name: 'auto' },
    // CRITICAL: propagate metadata to the Subscription so customer.subscription.* webhooks resolve workspace
    subscription_data: { metadata: { workspaceId: workspace.id, tier } },
    metadata: { workspaceId: workspace.id, tier },
    success_url: `${env.NEXT_PUBLIC_APP_URL}/settings/billing?success=true&session_id={CHECKOUT_SESSION_ID}`,
    cancel_url: `${env.NEXT_PUBLIC_APP_URL}/settings/billing`,
  })

  return NextResponse.json({ url: session.url })
}
```

### Customer Portal endpoint (`app/api/billing/portal/route.ts`)

```typescript
import { NextResponse } from 'next/server'
import { getWorkspaceContext } from '@/lib/auth/workspace-context'
import { requirePermission } from '@/lib/api/require-permission'
import { stripe } from '@/lib/stripe/config'
import { prisma } from '@/lib/prisma'
import { env } from '@/lib/env'

export async function POST() {
  const denied = await requirePermission('workspace:billing')
  if (denied) return denied

  const context = await getWorkspaceContext()
  const workspace = await prisma.workspace.findUniqueOrThrow({
    where: { id: context.workspaceId },
    select: { stripe_customer_id: true },
  })
  if (!workspace.stripe_customer_id) {
    return NextResponse.json({ error: 'No Stripe customer' }, { status: 400 })
  }

  const session = await stripe.billingPortal.sessions.create({
    customer: workspace.stripe_customer_id,
    return_url: `${env.NEXT_PUBLIC_APP_URL}/settings/billing`,
  })

  return NextResponse.json({ url: session.url })
}
```

### Invoice list endpoint (`app/api/billing/invoices/route.ts`)

```typescript
import { NextResponse } from 'next/server'
import { getWorkspaceContext } from '@/lib/auth/workspace-context'
import { requirePermission } from '@/lib/api/require-permission'
import { stripe } from '@/lib/stripe/config'
import { prisma } from '@/lib/prisma'

export async function GET() {
  const denied = await requirePermission('workspace:billing')
  if (denied) return denied

  const context = await getWorkspaceContext()
  const workspace = await prisma.workspace.findUniqueOrThrow({
    where: { id: context.workspaceId },
    select: { stripe_customer_id: true },
  })
  if (!workspace.stripe_customer_id) {
    return NextResponse.json({ invoices: [] })
  }

  const invoices = await stripe.invoices.list({
    customer: workspace.stripe_customer_id,
    limit: 24,
  })

  return NextResponse.json({
    invoices: invoices.data.map((inv) => ({
      id: inv.id,
      number: inv.number,
      amount_due: inv.amount_due,
      currency: inv.currency,
      status: inv.status,
      created: inv.created,
      hosted_invoice_url: inv.hosted_invoice_url,
      invoice_pdf: inv.invoice_pdf,
    })),
  })
}
```

### Webhook handler (`app/api/webhooks/stripe/route.ts`)

> **Auth bypass:** This route is public. It MUST NOT call `requirePermission` or any session helper. Identity is verified solely by Stripe signature.

```typescript
import { NextResponse } from 'next/server'
import { headers } from 'next/headers'
import type Stripe from 'stripe'
import { stripe, STRIPE_PRICE_IDS } from '@/lib/stripe/config'
import { prisma } from '@/lib/prisma'
import { env } from '@/lib/env'
import { sendEmail } from '@/lib/email/email-service'
import { PaymentFailedEmail } from '@/emails/payment-failed'

export async function POST(request: Request) {
  const body = await request.text()
  const signature = (await headers()).get('stripe-signature')
  if (!signature) {
    return NextResponse.json({ error: 'Missing signature' }, { status: 400 })
  }

  let event: Stripe.Event
  try {
    event = stripe.webhooks.constructEvent(body, signature, env.STRIPE_WEBHOOK_SECRET)
  } catch (err) {
    const message = err instanceof Error ? err.message : 'Signature verification failed'
    console.error('[STRIPE-WEBHOOK] signature failed:', message)
    return NextResponse.json({ error: message }, { status: 400 })
  }

  // Idempotency — skip if we've already handled this event
  const existing = await prisma.stripeWebhookEvent.findUnique({
    where: { stripe_event_id: event.id },
  })
  if (existing) return NextResponse.json({ received: true, duplicate: true })

  try {
    await prisma.stripeWebhookEvent.create({
      data: { stripe_event_id: event.id, event_type: event.type },
    })

    switch (event.type) {
      case 'checkout.session.completed': {
        const session = event.data.object as Stripe.Checkout.Session
        const workspaceId = session.metadata?.workspaceId
        const tier = session.metadata?.tier
        if (!workspaceId || !tier) break

        await prisma.workspace.update({
          where: { id: workspaceId },
          data: {
            subscription_tier: tier as 'SOLO' | 'TEAM' | 'ENTERPRISE',
            stripe_subscription_id: session.subscription as string,
            subscription_status: 'active',
            trial_ends_at: null,
          },
        })
        break
      }

      case 'customer.subscription.updated': {
        const subscription = event.data.object as Stripe.Subscription
        const workspaceId = subscription.metadata?.workspaceId
        if (!workspaceId) break

        const priceId = subscription.items.data[0]?.price.id
        const tier =
          (Object.entries(STRIPE_PRICE_IDS).find(([, id]) => id === priceId)?.[0] as
            | 'SOLO'
            | 'TEAM'
            | 'ENTERPRISE'
            | undefined) ?? undefined

        // Stripe API 2024-04-10+ moved current_period_end onto the item; older versions keep it on subscription.
        const periodEnd =
          subscription.items.data[0]?.current_period_end ??
          // @ts-expect-error — fallback for older API surface
          subscription.current_period_end

        await prisma.workspace.update({
          where: { id: workspaceId },
          data: {
            ...(tier ? { subscription_tier: tier } : {}),
            subscription_status: subscription.status,
            current_period_end: periodEnd ? new Date(periodEnd * 1000) : null,
          },
        })
        break
      }

      case 'customer.subscription.deleted': {
        const subscription = event.data.object as Stripe.Subscription
        const workspaceId = subscription.metadata?.workspaceId
        if (!workspaceId) break

        await prisma.workspace.update({
          where: { id: workspaceId },
          data: {
            status: 'PAUSED',
            paused_at: new Date(),
            subscription_status: 'canceled',
          },
        })
        break
      }

      case 'invoice.payment_failed': {
        const invoice = event.data.object as Stripe.Invoice
        const customerId = invoice.customer as string

        const workspace = await prisma.workspace.findFirst({
          where: { stripe_customer_id: customerId },
          include: { owner: { select: { email: true } } },
        })
        if (!workspace) break

        const gracePeriodEndsAt = new Date(Date.now() + 3 * 24 * 60 * 60 * 1000)

        await prisma.workspace.update({
          where: { id: workspace.id },
          data: {
            payment_grace_period_ends_at: gracePeriodEndsAt,
            subscription_status: 'past_due',
          },
        })

        await sendEmail({
          to: workspace.owner.email,
          subject: 'Betalning misslyckades - Laglig.se',
          react: PaymentFailedEmail({
            companyName: workspace.name,
            amount: (invoice.amount_due ?? 0) / 100,
            gracePeriodEndsAt,
            manageBillingUrl: `${env.NEXT_PUBLIC_APP_URL}/settings/billing`,
          }),
          from: 'notifications',
        })
        break
      }

      case 'invoice.payment_succeeded': {
        const invoice = event.data.object as Stripe.Invoice
        const customerId = invoice.customer as string
        const workspace = await prisma.workspace.findFirst({
          where: { stripe_customer_id: customerId },
        })
        if (!workspace) break

        await prisma.workspace.update({
          where: { id: workspace.id },
          data: {
            payment_grace_period_ends_at: null,
            subscription_status: 'active',
          },
        })
        break
      }
    }

    return NextResponse.json({ received: true })
  } catch (error) {
    console.error('[STRIPE-WEBHOOK] handler error:', error)
    // Roll back the idempotency row so we can retry on next delivery
    await prisma.stripeWebhookEvent
      .delete({ where: { stripe_event_id: event.id } })
      .catch(() => {})
    return NextResponse.json({ error: 'Webhook handler failed' }, { status: 500 })
  }
}
```

### Grace-period enforcement (`lib/auth/workspace-context.ts`)

Inside `getWorkspaceContext()` (or its helper), after loading the workspace:

```typescript
if (
  workspace.payment_grace_period_ends_at &&
  workspace.payment_grace_period_ends_at < new Date()
) {
  // Block server-side access; client UI surfaces a "past due" banner via the billing page redirect.
  throw new WorkspaceAccessBlockedError({
    reason: 'past_due',
    redirectTo: '/settings/billing?reason=past_due',
  })
}
```

The dev agent should mirror existing patterns in `lib/auth/workspace-context.ts` (e.g. how paused/deleted workspaces are blocked) rather than introducing a new error type if one already covers this case.

### Architecture references

- `docs/architecture/3-tech-stack.md §3.1` — Stripe SDK (`stripe@14.21+`), React Email, Resend, MSW for testing, `@t3-oss/env-nextjs`.
- `docs/architecture/9-database-schema.md §9.4.1` — Workspace model (extended inline).
- `docs/architecture/9-database-schema.md §9.11` — Standalone `Subscription` model documented but **not implemented**; this story extends inline-on-Workspace per Story 5.1 precedent. Update §9.11 to reflect inline pattern post-implementation.
- `docs/architecture/13-api-strategy-decision.md` — REST routes used for external integrations (webhooks).
- `docs/architecture/15-security-and-performance.md` — webhook signature verification, env validation, no card data stored locally.
- `docs/architecture/17-coding-standards.md` — snake_case for DB fields, camelCase for TS variables.

### Source tree (new files)

```
lib/stripe/
  config.ts
  customer.ts
app/api/billing/
  checkout/route.ts
  portal/route.ts
  invoices/route.ts
app/api/webhooks/stripe/
  route.ts
app/(workspace)/settings/billing/
  page.tsx
emails/
  payment-failed.tsx
tests/unit/billing/
  stripe-integration.test.ts
```

### Existing helpers to reuse (verified to exist)

- `lib/auth/workspace-context.ts` — `getWorkspaceContext()`
- `lib/api/require-permission.ts` — `requirePermission('workspace:billing')`
- `lib/auth/permissions.ts` — `'workspace:billing'` permission already mapped to OWNER role
- `lib/email/email-service.ts` — `sendEmail({ to, subject, react, from })`
- `lib/prisma.ts` — `prisma` client (`@/lib/prisma`)
- `lib/env.ts` — type-safe env access (`@t3-oss/env-nextjs`)
- React Email templates live at `emails/*.tsx` (imported as `@/emails/<name>`)

### Stripe SDK / API version notes

- Pin via `stripe@^14.21`. Omit explicit `apiVersion` to use the SDK's locked default; this avoids drift between SDK types and runtime payloads.
- `subscription.current_period_end` was relocated to `subscription.items.data[0].current_period_end` in newer Stripe API versions. The webhook code reads from the item first and falls back to the legacy field for safety.
- Stripe does **not** automatically copy Checkout Session metadata onto the resulting Subscription. The Checkout creator passes `subscription_data.metadata` explicitly so `customer.subscription.updated`/`.deleted` can resolve a `workspaceId`.

### Swedish payment methods

Leave `payment_method_types` unset on Checkout. Stripe will auto-enable **Card**, **Klarna**, and **Swish** based on the workspace's billing country and what is enabled in the Stripe Dashboard (per tech stack §3.1). Configuring methods centrally in the Dashboard is preferred over hard-coding here.

## Testing

**Test framework:** Vitest (`tests/unit/billing/stripe-integration.test.ts`), mocking Stripe HTTP via MSW (per tech stack §3.1).

**Unit tests:**

- `getOrCreateStripeCustomer` returns existing ID when `stripe_customer_id` is set
- `getOrCreateStripeCustomer` creates customer + persists ID when missing
- Checkout endpoint rejects invalid tier
- Webhook signature verification — valid event accepted, tampered body rejected (400)
- Webhook idempotency — duplicate `event.id` returns 200 without re-mutating Workspace
- Grace-period rollback on `invoice.payment_succeeded` (clears `payment_grace_period_ends_at`)

**Integration tests (lifecycle):**

- Trial → `checkout.session.completed` → `subscription_tier=SOLO`, `subscription_status=active`, `trial_ends_at=null`
- Upgrade: `customer.subscription.updated` (Solo → Team) → `subscription_tier=TEAM`, `current_period_end` updated
- Downgrade: `customer.subscription.updated` (Team → Solo) → `subscription_tier=SOLO`
- Cancel: `customer.subscription.deleted` → `status=PAUSED`, `paused_at` set, `subscription_status=canceled`
- Failure: `invoice.payment_failed` → `payment_grace_period_ends_at` set 3 days out, email queued via Resend (mocked)
- Recovery: `invoice.payment_succeeded` → `payment_grace_period_ends_at=null`, `subscription_status=active`
- Grace-period enforcement: workspace-context throws/redirects when `payment_grace_period_ends_at < now`

**Stripe test mode:**

- Test cards: `4242 4242 4242 4242` (success), `4000 0000 0000 0341` (decline at first invoice → triggers `invoice.payment_failed`)
- Local webhook forwarding: `stripe listen --forward-to localhost:3000/api/webhooks/stripe` (CLI-printed signing secret goes into `.env.local` as `STRIPE_WEBHOOK_SECRET` for dev)
- Verify events in Stripe Dashboard (Test mode → Developers → Events)

## Change Log

| Date       | Version | Description                                                                                              | Author     |
| ---------- | ------- | -------------------------------------------------------------------------------------------------------- | ---------- |
| 2025-11-12 | 1.0     | Initial story creation                                                                                   | Sarah (PO) |
| 2026-02-19 | 2.0     | Updated auth patterns, email via Resend, added context                                                   | Sarah (PO) |
| 2026-05-04 | 3.0     | PO validation pass: aligned schema with inline-on-Workspace pattern (snake_case), fixed email import path, added subscription metadata propagation, idempotency, Customer Portal, invoice list, grace-period enforcement, env validator, automatic_tax, payment_succeeded handling. AC reorganized; tasks now reference AC numbers. | Sarah (PO) |

## Dev Agent Record

### Agent Model Used

`claude-opus-4-7[1m]` (Anthropic Opus 4.7, 1M context) acting as BMad dev agent (James).

### Debug Log References

- Migration generated via `prisma migrate diff` fallback (shadow DB has a pre-existing FK type mismatch on `document_visits.document_id` that blocks `prisma migrate dev`; same workaround used by Story 2.32). The stray `DROP INDEX content_chunks_embedding_idx` produced by `migrate diff` is unrelated drift and was hand-stripped from the SQL before commit.
- `tsc --noEmit` clean. ESLint clean on all touched files.
- Vitest: 4,208 unit tests pass (including 11 new Stripe tests). 14 integration test files fail with `column "workspaces.stripe_customer_id" does not exist` — expected, will resolve once the operator applies the migration. No unit-level regressions from this story.

### Completion Notes List

- **Migration is operator-applied.** SQL lives at `prisma/migrations/20260504120000_add_stripe_billing_fields/migration.sql`. Until it's applied to the live DB, integration tests that hit the real DB will fail because the Prisma client now knows about columns the DB doesn't have. Story is **not** "Ready for Review" until the migration runs.
- **Webhook handler reads from `request.headers.get` rather than `next/headers`** — story spec called out `headers().get('stripe-signature')` but `next/headers` requires a request scope and can't be unit-tested without one. `request.headers.get` is functionally identical for route handlers and removes the unit-test obstacle.
- **MSW deferred.** Story spec recommended MSW for HTTP mocking. MSW is in tech stack §3.1 but isn't a project dependency. Following the dev-agent blocking rule on unapproved deps, I used `vi.mock` for the Stripe SDK methods we call (matches the existing pattern in `tests/unit/agent/retrieval.test.ts`). Webhook signature verification is exercised against the real `stripe.webhooks.constructEvent` so the cryptographic surface IS tested end-to-end.
- **`PAYMENT_PAST_DUE` is a new `WorkspaceAccessError` code.** None of the existing codes (UNAUTHORIZED / NO_WORKSPACE / WORKSPACE_DELETED / ACCESS_DENIED) captured "past-due", so I added it per the story note allowing new codes when no existing one covers the case. `requirePermission` and `requirePermissionWithContext` map it to HTTP 402 with a `redirectTo: '/settings/billing?reason=past_due'` body.
- **Cached workspace context now includes `paymentGracePeriodEndsAt`** (ISO string) so the Redis cache-hit branch re-checks expiry without an extra DB round-trip. Cache TTL (5 min) is well inside the 3-day grace window so a freshly-set deadline can't be missed.
- **Tier-from-priceId resolver** factored into `lib/stripe/config.ts` as `tierForPriceId(priceId)` — used by the `customer.subscription.updated` handler to translate Stripe price IDs back to our `SubscriptionTier` enum.
- **Existing `BillingTab` placeholder** at `components/features/settings/billing-tab.tsx` was left untouched. The new standalone `/settings/billing` page is the canonical surface; the tab can be redirected/removed in a follow-up.
- **Operator follow-ups before this can ship:**
  1. ~~Apply the migration~~ — done by operator (with RLS on the new tables; zero CREATE POLICY = service-role-only, which is the safest config).
  2. Provision Stripe (account, products, prices, Stripe Tax) and populate the 5 STRIPE_* env vars + NEXT_PUBLIC_APP_URL in `.env.local` and Vercel.
  3. Configure the Stripe webhook URL (`{NEXT_PUBLIC_APP_URL}/api/webhooks/stripe`) in the Stripe Dashboard and copy the signing secret into env vars.
  4. Run a manual lifecycle test in Stripe test mode (Trial → Checkout → Upgrade → Downgrade → Cancel → Resume) per AC 12.

- **QA-fix pass (gate 5.4 → CONCERNS resolution):**
  - **BILLING-001 (HIGH)** — closed two ways. UI: `billing-dashboard.tsx` now uses a `TIER_RANK` map to detect downgrades and routes them to Stripe Customer Portal instead of Checkout. Server: `app/api/billing/checkout/route.ts` returns 409 `SUBSCRIPTION_EXISTS` if the workspace already has an active/trialing/past_due subscription, with `portalUrl` in the response body. Defense-in-depth — even if a future caller bypasses the UI, the API rejects the request. Verified by new test "rejects with 409 when workspace already has an active subscription" which also asserts `stripeMock.checkout.sessions.create` was NOT called.
  - **BILLING-002 (medium)** — `lib/stripe/customer.ts` now passes `idempotencyKey: \`customer:${workspaceId}\`` to `stripe.customers.create`. Stripe replays the same idempotency key within 24h and returns the same customer, so concurrent first-time clicks no longer create orphan customers in Stripe. Existing test updated to assert the new 2nd-arg signature.
  - **TEST-001** — Added "Checkout endpoint rejects an invalid tier with 400".
  - **TEST-002** — Added "workspace-context throws PAYMENT_PAST_DUE when payment_grace_period_ends_at is past" + a happy-path counterpart "returns context normally when payment_grace_period_ends_at is in the future". Required extending the test mock surface with `prisma.user`, `prisma.workspaceMember`, `next/headers#cookies`, and `getServerSession`.
  - **OBS-001 + OBS-002** — Deferred per user instruction.

### File List

**Added:**
- `prisma/migrations/20260504120000_add_stripe_billing_fields/migration.sql`
- `lib/stripe/config.ts`
- `lib/stripe/customer.ts`
- `app/api/billing/checkout/route.ts`
- `app/api/billing/portal/route.ts`
- `app/api/billing/invoices/route.ts`
- `app/api/webhooks/stripe/route.ts`
- `app/(workspace)/settings/billing/page.tsx`
- `components/features/billing/billing-dashboard.tsx`
- `emails/payment-failed.tsx`
- `tests/unit/billing/stripe-integration.test.ts`

**Modified:**
- `prisma/schema.prisma` (Workspace model + new StripeWebhookEvent model)
- `lib/env.ts` (added 6 Stripe env vars + NEXT_PUBLIC_APP_URL)
- `lib/auth/workspace-context.ts` (PAYMENT_PAST_DUE error code, grace-period gate, cached payload extended)
- `lib/api/require-permission.ts` (PAYMENT_PAST_DUE → 402 mapping in all 3 handlers)
- `README.md` (Stripe webhook dev workflow snippet)
- `package.json` (+ stripe@^14.25.0)
- `pnpm-lock.yaml`

**QA-fix pass (gate 5.4 CONCERNS → addressed):**
- `app/api/billing/checkout/route.ts` (added 409 server-side guard for active-subscription case — BILLING-001)
- `components/features/billing/billing-dashboard.tsx` (added TIER_RANK, route downgrades to Portal instead of Checkout — BILLING-001)
- `lib/stripe/customer.ts` (added `idempotencyKey: 'customer:${workspaceId}'` — BILLING-002)
- `tests/unit/billing/stripe-integration.test.ts` (added Checkout invalid-tier + 409 tests, workspace-context past_due + grace-active tests — TEST-001, TEST-002)

**QA re-review fix pass (gate 5.4 CONCERNS-2 → addressed):**
- `components/features/billing/billing-dashboard.tsx` (BILLING-003: when workspace has an active sub, route ALL non-current tier tiles through Portal regardless of upgrade vs downgrade — UI now mirrors the server guard. Trial-to-first-paid retains Checkout. Footer copy updated from "Nedgradering, paus och uppsägning…" to "Planbyten, paus och uppsägning…")
- `tests/unit/components/features/billing/billing-dashboard.test.tsx` (NEW — 5 regression-locking tests: TRIAL workspace shows 3 Checkout buttons, TEAM workspace shows 1 downgrade-to-Portal + 1 upgrade-to-Portal + 1 Aktiv, SOLO workspace shows 2 upgrade-to-Portal, past_due treated as active, canceled is NOT treated as active)

### Change Log

| Date       | Version | Description                                                                                          | Author       |
| ---------- | ------- | ---------------------------------------------------------------------------------------------------- | ------------ |
| 2026-05-04 | 4.0     | Implemented all dev-agent-scope tasks. Migration file generated (operator-applied). 11 new unit tests pass; 4,208 unit tests pass total. Operator must apply migration + provision Stripe before integration tests + production deploy. | James (Dev) |
| 2026-05-04 | 4.1     | QA-fix pass after gate CONCERNS. Closed BILLING-001 (downgrade UI routes to Portal + server-side 409 guard against existing active subscription), BILLING-002 (idempotencyKey on customers.create), TEST-001 (Checkout invalid-tier + existing-sub tests), TEST-002 (workspace-context past_due block + grace-active happy-path tests). 15/15 Stripe tests pass; 4,212/4,212 unit tests pass total. OBS-001 + OBS-002 deferred per user direction. Status → Ready for Review. | James (Dev) |
| 2026-05-04 | 4.2     | QA re-review fix pass. Closed BILLING-003 (UI now symmetric with server guard — when active sub exists, ALL non-current tier tiles route through Portal, not just downgrades). Added 5 regression-locking UI tests in tests/unit/components/features/billing/billing-dashboard.test.tsx. 4,217/4,217 unit tests pass total. OBS-001 + OBS-002 still deferred. Status remains Ready for Review pending QA re-gate. | James (Dev) |

## QA Results

### Review Date: 2026-05-04

### Reviewed By: Quinn (Test Architect)

### Gate: **CONCERNS** — `docs/qa/gates/5.4-stripe-subscription-billing.yml`

### Summary

Implementation is sound and well-tested at the unit level: 11/11 Stripe tests pass; signature verification exercised end-to-end against the real `stripe.webhooks.constructEvent`; idempotency rollback path verified. Migration applied successfully (operator-confirmed); the previously-blocked workspace integration tests (`workspace/isolation`, `workspace/switcher`) now pass. Remaining 9 integration test failures are pre-existing infra issues (Redis env, `next/cache` mock exhaustiveness, test seed cleanup) — none caused by Story 5.4.

**However**, one **HIGH-severity UX gap (BILLING-001)** must be fixed before production: the BillingDashboard's "Uppgradera" button creates a new Stripe Checkout session for *any* non-current tier — including downgrades. A TEAM customer clicking SOLO→Uppgradera would create a SECOND Stripe subscription (Checkout in `mode: 'subscription'` does not replace existing subs), resulting in **double billing**. The story Dev Notes explicitly say downgrades go through Customer Portal, but neither the UI nor the server endpoint enforces this.

### Top issues

| ID | Severity | Finding | Suggested action |
|---|---|---|---|
| BILLING-001 | **HIGH** | "Uppgradera" creates new Checkout for downgrade tiers → double billing | Hide the button for tiers ≤ current; add server-side guard in `/api/billing/checkout` rejecting requests when an active subscription exists |
| BILLING-002 | medium | `getOrCreateStripeCustomer` race: concurrent first-time clicks could create two Stripe customers | Pass `idempotencyKey: \`customer:${workspaceId}\`` to `stripe.customers.create`; optionally wrap in `$transaction` |
| TEST-001 | low | Missing test called out in story spec line 569 (Checkout invalid-tier rejection) | Add a test posting `{ tier: 'INVALID' }` and asserting 400 |
| TEST-002 | low | Missing test called out in story spec line 583 (workspace-context grace-period block) | Add a test for `getWorkspaceContext()` throwing `PAYMENT_PAST_DUE` when the grace deadline is past |
| OBS-001 | low | Webhook doesn't bust Redis auth-context cache; up to 5 min lag on past-due gate state changes | Either document the lag (acceptable trade-off vs. 3-day grace window) or call `invalidateUserCache(ownerId, 'context')` from the relevant webhook handlers |
| OBS-002 | low | Existing `BillingTab` placeholder unchanged; users see "kommer snart" tab + new full UI at `/settings/billing` | Replace placeholder with a button linking to `/settings/billing`, or remove the tab |

### NFR validation

- **Security: PASS** — Webhook signature verification uses real `constructEvent`. Public webhook route correctly omits session helpers. RLS enabled on `stripe_webhook_events` with zero `CREATE POLICY` → service-role-only (safest config). `workspaces` table also has zero user-write policies, so the new `stripe_*` columns can't be tampered with from the client. Permission gate `workspace:billing` correctly maps to OWNER only.
- **Performance: PASS** — Webhook handler is serial and short. Cached workspace context extended with `paymentGracePeriodEndsAt` so the cache-hit branch re-checks expiry without an extra DB query. Invoice list capped at `limit: 24`.
- **Reliability: CONCERNS** — Idempotency rollback (catch-and-delete on handler failure) is tested and works. BUT if the rollback delete itself fails, the `.catch(() => {})` silently swallows it and the event becomes a permanent duplicate. Acceptable trade-off vs over-engineering. BILLING-002 is a low-probability reliability issue (race window is milliseconds).
- **Maintainability: PASS** — Files well-scoped, comments explain the *why* (metadata propagation, raw-body signature path, cache-invariant for past-due). `tierForPriceId` resolver factored out cleanly. Tests follow existing codebase pattern.

### Requirements traceability

| AC | Status | Notes |
|---|---|---|
| 1 (envs wired) | ✅ Covered | `lib/env.ts` extended; verified via test mock |
| 2 (customer per workspace) | ✅ Covered | `getOrCreateStripeCustomer` tested both branches |
| 3 (Stripe products + EUR prices) | ⏸ Operator | Awaiting Stripe Dashboard provisioning |
| 4 (Checkout + metadata propagation) | ✅ Covered | `subscription_data.metadata` set in code, asserted via test |
| 5 (billing page UI) | ✅ Covered | `/settings/billing` renders plan, period end, payment-method link, invoice table |
| 6 (Upgrade + Manage Payment Method) | ⚠ See BILLING-001 | Upgrade flow works for trial→paid; downgrade from paid creates duplicate sub |
| 7 (5 webhook event types) | ✅ Covered | Each handler has a dedicated test |
| 8 (sub state synced) | ✅ Covered | All handlers update `Workspace` correctly |
| 9 (failed payment + 3-day grace + access block) | ✅ Covered | Email send + grace timestamp + workspace-context gate all wired; **see TEST-002 for missing direct test of the gate** |
| 10 (webhook idempotency) | ✅ Covered | Duplicate-event test + rollback-on-throw test |
| 11 (Stripe Tax on Checkout) | ✅ Covered | `automatic_tax: { enabled: true }` in code |
| 12 (full lifecycle) | ⏸ Partial — operator-validated | Unit tests cover ~80%; full Trial→Cancel→Resume in Stripe Test Mode pending operator |

### Recommendations

**Immediate (before production):**

1. Fix BILLING-001 — prevent multi-subscription creation. UI: `components/features/billing/billing-dashboard.tsx:240-272`. Server guard: `app/api/billing/checkout/route.ts:38-44`.
2. Fix BILLING-002 — pass `idempotencyKey` to `stripe.customers.create`. `lib/stripe/customer.ts:25-33`.

**Future (can ship without):**

- Add the two missing tests (TEST-001, TEST-002).
- Decide on OBS-001 cache invalidation (document or wire `invalidateUserCache`).
- Resolve OBS-002 — redirect/remove the placeholder BillingTab.
- Owner-email handling on ownership transfer is unhandled (out of scope for 5.4; track as follow-up).

### Status recommendation

**Hold "Ready for Review" until BILLING-001 + BILLING-002 are fixed and the operator completes the Stripe-Test-mode lifecycle pass (AC 12).** Story owner can also choose to WAIVE BILLING-001 if downgrade volume is expected to be near-zero in MVP, but the fix is small enough (~15 LOC) that I recommend just fixing it.

---

### Re-Review Date: 2026-05-04

### Reviewed By: Quinn (Test Architect) — 2nd pass

### Gate: **CONCERNS** (carried; quality_score 70 → 85)

### Summary

The dev fix pass closed all four issues from the original gate cleanly:

- **BILLING-001 (HIGH)** ✅ — server guard at `app/api/billing/checkout/route.ts:48-64` returns `409 SUBSCRIPTION_EXISTS` when an active subscription exists; UI uses `TIER_RANK` to detect downgrades and routes them to Customer Portal via `Nedgradera via portal` button. Defense-in-depth verified by the new test asserting `stripeMock.checkout.sessions.create` is NOT called.
- **BILLING-002 (medium)** ✅ — `lib/stripe/customer.ts:31-40` now passes `idempotencyKey: 'customer:${workspaceId}'` to `stripe.customers.create`. Existing test updated to assert the 2-arg signature.
- **TEST-001** ✅ — Added "Checkout endpoint rejects an invalid tier with 400" + bonus "rejects with 409 when workspace already has an active subscription".
- **TEST-002** ✅ — Added "workspace-context throws PAYMENT_PAST_DUE when grace deadline is past" + happy-path counterpart.

15/15 Stripe tests pass. 4,212/4,212 unit tests pass overall.

### One regression introduced — BILLING-003

The BILLING-001 server guard correctly rejects ANY Checkout attempt when an active subscription exists (because Checkout in `mode: 'subscription'` always creates a NEW subscription, regardless of whether the new price is higher or lower than the existing one). But the UI still uses `handleUpgrade → /api/billing/checkout` for paid-tier *upgrades* — only downgrades get routed to the Portal. So a TEAM customer clicking ENTERPRISE "Uppgradera":

1. UI calls POST `/api/billing/checkout` `{tier: 'ENTERPRISE'}`
2. Server returns `409 SUBSCRIPTION_EXISTS` with a `portalUrl` hint
3. UI's error path: `alert(data.error)` → user sees "Workspace already has an active subscription"
4. The `portalUrl` hint is not used

The user *can* still upgrade — by clicking "Hantera betalsätt" (Portal) — but the contextual upgrade flow is now broken. Severity **MEDIUM** (revenue path affected, no security/data risk, fix is small).

### Top issues

| ID | Severity | Status | Finding |
|---|---|---|---|
| BILLING-001 | high | ✅ Closed | Verified — server guard + UI both in place |
| BILLING-002 | medium | ✅ Closed | Verified — idempotencyKey passed |
| TEST-001 | low | ✅ Closed | Test exists and passes |
| TEST-002 | low | ✅ Closed | Tests exist and pass (block + happy path) |
| **BILLING-003** | **medium** | **NEW** | Paid-tier upgrades rejected by server guard, UI still tries Checkout → 409 alert |
| OBS-001 | low | Deferred | Cache invalidation lag — per user direction |
| OBS-002 | low | Deferred | BillingTab placeholder duplication — per user direction |

### Suggested fix for BILLING-003

Two paths, both small:

1. **Symmetry fix (preferred)** — make UI mirror the server. Replace the `isUpgrade` vs `isDowngrade` branching in `billing-dashboard.tsx:288-300` with a single `hasActiveSub` check; route ALL non-current tier tiles through `handleManagePortal` when an active subscription exists. Result: every plan change for an existing subscriber goes through Portal (consistent with server guard); only the trial-to-first-paid case uses Checkout.
2. **Quick patch** — in `handleUpgrade`, if the response has `portalUrl`, call `fetch(portalUrl, {method: 'POST'})` to get the Portal URL and redirect. Keeps the current UI labels but rescues the user when 409 fires.

### NFR validation

- Security **PASS** — no new attack surface; webhook signature still real-validated; RLS still service-role-only.
- Performance **PASS** — server guard is O(1); new tests run <2s.
- Reliability **PASS** — race fixed; double-billing data corruption prevented even on UI bypass.
- Maintainability **PASS** — `TIER_RANK` is a clean abstraction; both fixes carry inline `WHY` comments referencing the gate ID.

### Status recommendation

**Hold at "Ready for Review" until BILLING-003 is addressed** (5-15 LOC UI tweak). Quality is meaningfully better than the previous gate — the HIGH issue is closed, all original concerns resolved — but the introduced regression should be polished before launch. Operator-pending items (Stripe provisioning + Test-mode lifecycle for AC 12) remain orthogonal blockers.

---

### Re-Review Date: 2026-05-04 (3rd pass)

### Reviewed By: Quinn (Test Architect)

### Gate: **PASS** (quality_score 70 → 85 → 92)

### Summary

BILLING-003 closed via UI symmetry. `components/features/billing/billing-dashboard.tsx` now exposes a `hasActiveSub` flag mirroring the server-side guard exactly (`stripeSubscriptionId` set AND `subscription_status` ∈ `{active, trialing, past_due}`). Per-tile rendering branch becomes a clean three-way: `isCurrent` → "Aktiv" disabled · `hasActiveSub` → Portal button (label by direction) · else → Checkout button. Footer copy updated from "Nedgradering, paus och uppsägning…" to "Planbyten, paus och uppsägning…" since both directions now flow through Portal.

Critically, a new test file `tests/unit/components/features/billing/billing-dashboard.test.tsx` locks the regression in with 5 scenarios. The TEAM-active scenario explicitly asserts both the positive (Portal buttons appear with the right labels) and the negative (`expect(screen.queryByRole('button', { name: /^Uppgradera$/ })).toBeNull()`) — so any future revert that re-introduces the bug will fail loudly.

### All issues now closed

| ID | Sev | Status | Closure verified |
|---|---|---|---|
| BILLING-001 | high | ✅ Closed (2nd pass) | Server guard + UI Nedgradera + test asserting `stripeMock.checkout.sessions.create` is NOT called |
| BILLING-002 | medium | ✅ Closed (2nd pass) | `idempotencyKey: customer:${workspaceId}` in place; signature test updated |
| TEST-001 | low | ✅ Closed (2nd pass) | Invalid-tier rejection test exists + bonus 409 test |
| TEST-002 | low | ✅ Closed (2nd pass) | Past-due block + grace-active happy-path tests exist |
| **BILLING-003** | **medium** | ✅ **Closed (3rd pass)** | UI symmetric with server; 5 regression-locking tests |
| OBS-001 | low | Deferred per user | Cache invalidation lag (5 min vs. 3-day grace) |
| OBS-002 | low | Deferred per user | BillingTab placeholder duplication |

### NFR validation

- **Security PASS** — webhook signature verification real-validated, RLS service-role-only, PAYMENT_PAST_DUE → 402 wired in all 3 require-permission handlers, no new attack surface across all 3 dev passes.
- **Performance PASS** — server guard O(1), cached context avoids extra DB query, invoice list capped at 24, UI test runs in 75ms.
- **Reliability PASS** — three orthogonal layers protect against the original double-billing risk: (1) BILLING-001 server guard prevents data corruption even if UI bypassed; (2) BILLING-002 idempotencyKey closes the create-create race; (3) BILLING-003 UI symmetry means user-facing flow can't trigger the server's 409. Webhook idempotency rollback also tested.
- **Maintainability PASS** — `TIER_RANK` + `hasActiveSub` are small, named abstractions. Three rounds of fix comments inline reference gate IDs (BILLING-001/002/003) so future readers can trace the *why*. New regression-locking test asserts both positive and negative — robust against accidental reverts.

### Test totals

20/20 Stripe + billing tests pass · 4,217/4,217 unit tests pass · `pnpm tsc --noEmit` clean · `pnpm eslint` clean on touched files.

### Remaining items (none blocking dev gate)

| Item | Owner | Type |
|---|---|---|
| OBS-001 cache invalidation lag | Future story | Deferred per user |
| OBS-002 BillingTab placeholder | Future story | Deferred per user |
| Owner-email handling on workspace transfer | Future story | Out of scope |
| AC 3 — Stripe products + EUR prices | Operator | Operator-pending |
| AC 12 — Stripe Test Mode lifecycle | Operator | Operator-pending |
| Stripe webhook URL in Dashboard | Operator | Operator-pending |
| STRIPE_* env vars in Vercel | Operator | Operator-pending |

### Status recommendation

**PASS — gate cleared. Story is dev-complete.** The Status field can move to `Done` once the operator finishes Stripe provisioning and runs the AC 12 lifecycle pass in Stripe Test Mode. Until then, `Ready for Review` is appropriate. The dev work is ready to ship as soon as the operator items land — there are no remaining code-level blockers.
