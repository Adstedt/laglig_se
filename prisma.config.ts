/**
 * Prisma configuration file
 * Migrated from package.json#prisma to avoid deprecation warning
 */

const prismaConfig = {
  seed: 'tsx prisma/seed-multi-content.ts',
}

export default prismaConfig
